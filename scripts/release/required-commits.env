# 发布门禁清单：键 = 受管的 tag 前缀（v4.1 → GATE_v4_1），值 = 必须已经是 HEAD 祖先的提交。
# 值留 pending ⇒ check-release-gate.sh 退 3（fail-closed，不许空过）。
# 4.1.x（含 rc）：bui-c 的「同账号重复节点不自动合并，只提示或经菜单确认」那条改动
# （spec §7.6 事实 3，主会话与 bui-c 会话的共同结论）。
#
# 下面这条 sha 现在还**不是** v4 的祖先，这是预期的：合入路径已定为「先切 v4.0.x 发布分支发
# 4.0.2、再 merge forward 进 v4」，所以在 4.0.2 发完并 merge forward 之前，门禁就该判不过
# （fail-closed 的本意）。判据是 `git merge-base --is-ancestor`，所以那条发布分支必须**普通
# merge**、不许 squash —— squash 会造出一个新提交、原 sha 永远不会成为 HEAD 的祖先，门禁从此
# 无法通过（真要 squash 就得把这里的值换成 squash 后的那个 sha）。
GATE_v4_1=080a3d3e2b96c0ddce338a8acffe9dede0bcb2dc
