# b-ui v4 实施总纲（子项目拆分、接口契约、并行编排）

> **For agentic workers:** 本文不是可直接执行的任务计划，而是 5 份子项目计划的索引与契约。执行请进入各子项目计划（`2026-09-11-v4-p0-foundation.md` 等），每份都要求 `superpowers:subagent-driven-development`。

**Goal:** 按 `docs/superpowers/specs/2026-09-11-v4-architecture-design.md` 交付 v4.0.0：单二进制 `bui` 控制器 + `bui-schema` 共享渲染 crate + Rust 客户端 `bui-c`，在 bwg-rick 通过 M1–M5 验收。

**Spec:** `docs/superpowers/specs/2026-09-11-v4-architecture-design.md`

## 全局约束（每个子项目隐含）

- Rust stable ≥ 1.85（本机 1.93），edition 2021，`cargo clippy -- -D warnings` 零告警，`cargo fmt --check` 通过。
- 发布目标 `x86_64-unknown-linux-musl`（本机已装）与 `aarch64-unknown-linux-musl`（CI）；依赖优先纯 Rust；TLS 用 `rustls` + `ring`，禁止 openssl。
- 协议集、端口、标签、UUID、密码与 v3 一致；订阅输出必须与 v3 golden 样本逐项相等（spec §4.4、§8）。
- sing-box 配置兼容 1.12–1.14：typed DNS servers、TUN `address` 数组、rule action（`sniff`/`hijack-dns`/`reject`）、`route.default_domain_resolver`、**不用** `rule_set` / `download_detour`。
- 凭据、密钥、密码不进 argv、不进日志（`tracing` 字段一律脱敏）；state 与快照文件 0600。
- 生产主机只用别名 bwg-rick / bwg-tizi / baiyi；公开仓库里不出现真实 IP、端口、域名。
- 分支 `v4`；每个任务一个 commit，格式 `feat(scope): …` / `test(scope): …` / `fix(scope): …`，尾部附署名；不 `git commit -a`；不裸 `git stash`；不推送（推送由主理人决定）。
- v3 的 `server/`、`web/server.js`、`b-ui-client.sh` 在开发期**保留**作为移植参照与 fixture 生成器，P5 最后一个任务才删除。
- 每个任务先写失败测试再实现；集成测试用本机真实内核（`sing-box` 1.13.19 `/usr/bin/sing-box`、`xray` 26.3.27 `/usr/local/bin/xray`、`hysteria` 2.12.2 `/usr/local/bin/hysteria`），二进制不存在时 `skip` 而不是失败。

## 子项目与依赖

| 子项目 | 计划文件 | 交付 | 依赖 | 对应验收 |
|---|---|---|---|---|
| **P0 基础** | `2026-09-11-v4-p0-foundation.md` | workspace、`bui-schema`（模型、节点集合、四内核 + 三订阅 + 客户端渲染、解析、v3 导入、golden fixtures） | — | M1 的机器化部分 |
| **P1 守护进程核心** | `2026-09-11-v4-p1-daemon-core.md`（P0 完成后由规划 agent 依据真实接口起草，Fable 审） | `bui` crate：state 存储、对账器（文件/单元/sysctl/防火墙/DNS/二进制）、CLI、unix socket API 框架、`install` / `import-v3` / `upgrade` 骨架、证书监听、watchdog | P0 | M1 |
| **P2 面板与用户** | `2026-09-11-v4-p2-panel-users.md` | axum 管理员 API（契约同 v3）+ 前端嵌入 + 采样/限额 + `auth-hook` + Xray gRPC + 用户域 501 桩 + 前端 4 处改动 | P0、P1 的 state/API 框架 | M1、M3 |
| **P3 住宅模块** | `2026-09-11-v4-p3-residential.md` | 上游管理、体检、健康切换、黑名单（候选/确认/生效/复核）、relay 重放选择、面板住宅端点 | P0、P1 的 state/API 框架 | M2 |
| **P4 客户端** | `2026-09-11-v4-p4-client.md` | `bui-c`：profiles、渲染（复用 P0）、单元与 timer、check、update、import-v3、数字菜单 | P0 | M4 |
| **P5 发布与硬化** | `2026-09-11-v4-p5-release.md` | GitHub Actions、manifest、`install.sh` 引导、soak/压测脚本、CHANGELOG、删除 v3 文件 | P1–P4 | M5 |

依赖图：P0 → {P1, P4} 并行；P1 的「state 存储 + API 框架」两个任务完成后 → {P2, P3} 并行（与 P1 其余任务并行）；全部 → P5。

## 接口契约（跨子项目共用，改动需 Fable 批准）

### C1 `bui-schema` 公共 API（P0 交付，P1–P4 消费）

```rust
bui_schema::model::{State, NodeParams, Ports, Reality, Obfs, Admin, User, Credentials, Protocol, Entitlements,
                    ResidentialEntitlement, TrafficLimit, Usage, PortalAuth, Billing, Order, OrderStatus,
                    Residential, ResidentialGroup, ResiMode, Upstream, UpstreamKind, Verified,
                    Blacklist, Rule, Pin, AutoEntry, SystemSettings, Versions, CatalogItem, CatalogKind}
bui_schema::nodes::{Node, NodeKind, Transport, nodes_for(&User, &NodeParams, &Residential) -> Vec<Node>}
bui_schema::parse::{upstream_url(&str) -> Result<UpstreamInput, ParseError>, node_uri(&str) -> Result<Node, ParseError>}
bui_schema::keywords::DEFAULT_KEYWORDS: &[&str]   // 67 条，与 v3 residential-helper.sh DEFAULT_DOMAINS 相同
bui_schema::render::hysteria::{direct_yaml(&NodeParams, &Paths) -> String, residential_yaml(&NodeParams, &Paths) -> String}
bui_schema::render::xray::{config(&NodeParams, &[User], &Paths) -> serde_json::Value, structural_hash(&Value) -> String}
bui_schema::render::relay::config(&ResidentialGroup, &RelayOpts) -> serde_json::Value
bui_schema::render::subscription::{uri_list(&[Node]) -> String /*base64*/, singbox(&[Node], &SplitRules) -> Value, clash(&[Node], &SplitRules) -> String}
bui_schema::render::client::{tun_config(&Node, &ClientOpts) -> Value, mixed_config(&Node, &ClientOpts) -> Value}
bui_schema::v3::import(dir: &Path) -> Result<State, ImportError>
```

### C2 `bui` 内部模块边界（P1 交付框架，P2/P3 各自填模块）

```rust
// crates/bui/src/state/store.rs
pub struct Store { … }                       // 单进程唯一持有 State；写 = 临时文件 + rename + 备份 10 份
impl Store { pub async fn read(&self) -> Arc<State>; pub async fn update(&self, f: impl FnOnce(&mut State)) -> Result<Arc<State>>; }
// crates/bui/src/reconcile/mod.rs
pub trait Module: Send + Sync {              // P2/P3 各实现一个，注册在 main.rs
    fn name(&self) -> &'static str;
    fn render(&self, s: &State, ctx: &RenderCtx) -> Vec<Artifact>;          // 文件/单元/sysctl 等期望项
    fn routes(&self) -> axum::Router<AppState>;                              // 该模块的 HTTP 路由
    fn spawn(&self, ctx: DaemonCtx) -> Vec<tokio::task::JoinHandle<()>>;     // 后台任务
}
pub enum Artifact { File { path, content: Vec<u8>, mode: u32, restart: Option<Unit> }, Unit { name, content }, Sysctl { key, value }, Binary { name, version, sha256 } }
// crates/bui/src/api/mod.rs
pub struct AppState { pub store: Store, pub bus: EventBus /* 变更后触发 reconcile */, pub runtime: Runtime }
```

### C3 文件与路径（P1 定义，全体遵守）

`/opt/b-ui/{state.json, runtime.json, auth-snapshot.json, state.backups/, bin/, certs/, config.yaml, config-residential.yaml, xray-config.json, singbox-relay.json, Caddyfile}`；`/run/b-ui.sock`；单元 `b-ui / hysteria-server / hysteria-residential / xray / b-ui-relay / caddy`。客户端 `/opt/bui-c/{bin/sing-box, profiles.json, config.json}`，`bui-c.service`、`bui-c.timer`。

## 并行编排（Workflow）

1. **P0 工作流**：Task 1–2 串行（scaffold + 模型契约）→ Task 3/4/5/11/12 并行 → Task 6–10 并行（渲染器，依赖 5 的 fixtures 与 12 的关键字表）→ 集成 agent 跑全量 `cargo test`/clippy/fmt 并合并。每个任务：Opus 实现（独立 worktree，分支 `v4-p0-tN`）→ Fable 派生的审查 agent 两段审查（spec 符合性、代码质量）→ 合并到 `v4`。
2. **P0 完成后**：三个规划 agent（Opus，effort high）并行起草 P1、P4 与 P2/P3 的接口任务，Fable 审后各自开工作流；P2/P3 在 P1 前两个任务合并后启动。
3. 每个子项目结束：Fable 在 bwg-rick 做该里程碑验收（spec §9），主理人确认后进入下一个。
4. 监控：Haiku 一次性核对合并结果与测试报告；长跑（soak/压测）用 `nohup` + 标记文件 + `Monitor`。

## 里程碑验收对照

| 里程碑 | 子项目 | 主理人可见的检查点 |
|---|---|---|
| M1 | P0 + P1 + P2（API 部分） | bwg-rick `bui install --import-v3` 后 v2rayN 四节点可连、面板可用、订阅逐项等价 |
| M2 | P3 | 住宅池 / 黑名单 / 体检卡 |
| M3 | P2（采样/限额/auth-hook） | 加用户不掉线、计数准确、到期被踢 |
| M4 | P4 | baiyi 上 `bui-c` 全功能 |
| M5 | P5 | soak / 压测 / 升级回滚 / v4.0.0 |
