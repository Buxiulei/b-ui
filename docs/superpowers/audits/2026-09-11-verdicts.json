[
 {
  "id": "client-C1",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json, id client-C1): \"b-ui-client.sh:44-57 print_banner() is defined but has zero call sites anywhere in the 5810-line file.\" No counter-claim, no also_raised_as.\n\n1. File size and definition: `wc -l b-ui-client.sh` -> 5810. `sed -n '40,60p'` shows `print_banner() {` at /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh:44 with closing `}` at line 57 (14 lines, matching the claim's 44-57 range).\n\n2. Call sites in the file: `grep -n 'print_banner' b-ui-client.sh` -> exactly one hit, line 44 (the definition). No call.\n\n3. Indirect/dynamic invocation: `grep -n -i 'banner\\|eval \\|declare -f\\|\"\\$fn\"\\|\"\\$func\"\\|\\${fn}\\|\\${func}' b-ui-client.sh` -> only line 44. No eval, no function-name variables, no `type`/`declare -f` introspection that could reach it.\n\n4. External callers: repo-wide `grep -rn print_banner` shows other files define and call their OWN print_banner (install.sh:51 def / install.sh:765 call; server/b-ui-cli.sh:181 def; b-ui-server.sh:41 def / 1310 call) — these are separate functions in separate scripts, not calls into b-ui-client.sh. Nothing sources b-ui-client.sh: server/b-ui-cli.sh:559-561 only curl-downloads it, web/server.js:1530-1550 only serves the file bytes (with SCRIPT_VERSION substitution, lines 29-30). The doc plan docs/superpowers/plans/2026-05-07-cli-redesign.md:900/1012/1547 references `print_banner` in the old design only.\n\n5. History: `git log --oneline -S'print_banner' -- b-ui-client.sh` -> c76ba3a (2026-05-08 \"refactor(ui): replace gum arrow menus with numeric two-column layout\") and 1be5ed8 (rename). `git show c76ba3a -- b-ui-client.sh | grep -B3 -A3 print_banner` shows the sole call was removed in the main loop diff:\n```\n     while true; do\n         clear\n-        print_banner\n-\n-        show_status_bar\n```\nSince then the definition was only touched by v3.4.29 (version.json:64 changelog: added `2>/dev/null || true` to the `clear` at line 45, i.e. a hardening applied to an already-dead function).\n\n6. Runtime angles: b-ui-client.sh is a client-side script (installed as bui-c on client hosts, e.g. baiyi), not run by cron/systemd on the servers; its own systemd units (hysteria-client/xray-client/bui-tun) exec binaries, not the script's functions. No live-host state is involved in the claim, so no SSH was performed. Upgrade path: the client self-updater replaces the whole file, so no older version depends on this function being present in the new file.\n\nshellcheck is not installed locally, so the SC2329 unused-function warning could not be reproduced; the grep evidence is conclusive on its own.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "client-C2",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (from docs/superpowers/audits/2026-09-11-claims.json, id client-C2): \"b-ui-client.sh:300-319 tui_write() is defined but has zero call sites anywhere in the file.\" No counter-claim, no also_raised_as.\n\nStatic: `grep -rn tui_write .` (excluding .git) in /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5 -> the only code hit is b-ui-client.sh:300 (`tui_write() {`, definition). Other hits are docs only (docs/superpowers/plans/2026-05-07-cli-redesign.md:245, docs/superpowers/specs/2026-05-07-cli-redesign.md:252, audit JSONs). `grep -nw tui_write b-ui-client.sh | grep -v '^300:'` -> NONE. Identifier histogram of tui_* in the file: tui_write appears exactly 1 time (vs tui_info 37, tui_error 24, tui_success 15, tui_confirm 4, tui_menu 3, tui_filter 2, ...). No dynamic dispatch that could reach it: `grep -nE 'eval |declare -f|export -f|\"\\$\\{?fn|\\$\\{?func|tui_\"?\\$|\"tui_|compgen -A function|\\$\\(type -t' b-ui-client.sh` -> no output. No other repo file sources b-ui-client.sh (server.js only serves it as a download; version.json lists it as a file). Main checkout /Users/woo/Desktop/b-ui/b-ui-client.sh also has exactly 1 occurrence.\n\nFunction body actually spans b-ui-client.sh:300-314 (closing `}` at 314; 315 blank; 316-319 are the comment header of check_client_update() at 320) — the claim's \"300-319\" is off by ~5 lines but the identity of the function is unambiguous.\n\nWhy it is dead: the only multi-line gum input in the file is inline in tui_import_node() at b-ui-client.sh:5416 (`raw_input=$(gum write --placeholder ... --char-limit 0 --width 70 --height 8) || return 0`) with its own non-gum fallback loop at 5420-5425 — it duplicates tui_write's logic instead of calling it (the 2026-05-07 plan intended tui_write as the wrapper).\n\ngit: `git log --oneline -S'tui_write' -- b-ui-client.sh` -> single commit ee03b99 (2026-05-07, \"feat(bui-c): add TUI helpers and global flag parsing\"); `git show ee03b99:b-ui-client.sh | grep -n tui_write` -> only the definition (line 230) even at introduction time, i.e. it has never had a caller. The inline gum write in tui_import_node was added the same day in a2e1fe3 (\"feat(bui-c): TUI toggle TUN, import node, and service control menus\").\n\nNo SSH used: the claim is about repository file contents, not live host state.",
  "deletable_under_v4": true,
  "correction": "Minor: the function occupies lines 300-314, not 300-319 (315 is blank, 316-319 are check_client_update's comment header). Substance of the claim is correct.",
  "high_impact": false
 },
 {
  "id": "client-C3",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "Repo: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5, file b-ui-client.sh (5810 lines).\n\nEntry points enumerated (static): BASH_SOURCE guard at b-ui-client.sh:5798-5810 -> `auto` => auto_update_all() (5632); otherwise first_run_setup() (5752) then main() (5259). main() -> check_root, parse_global_flags, check_os (537), dispatch_subcommand() (5217, case: cmd_switch/cmd_tun/cmd_reload_tun/cmd_import/cmd_start/cmd_stop/cmd_restart/cmd_status/cmd_list/help), or tui_main_loop() (5287). No `eval`, `${!var}`, `declare -f`, `type -t` or `$fn`-style indirect function invocation exists in the file (`grep -n 'eval\\|\\$fn\\|\"\\$func\\|\\${fn' b-ui-client.sh` -> only comment line 2217 \"替代 eval\"; `grep -n 'declare -f\\|\\${!\\|type -t'` -> no hits).\n\nDefinitions: check_and_install_dep() b-ui-client.sh:812-836; check_dependencies() b-ui-client.sh:839-958 (closing brace at 958, not 963 as the claim states).\n\nCallers: `grep -n 'check_dependencies\\|check_and_install_dep' b-ui-client.sh` -> 812 (def), 839 (def), and call sites 861, 866-869, 883-886, 894-897, 916, 917, 921 — ALL of which are check_and_install_dep calls inside check_dependencies' body (839-958). That is 16 call sites, not the 12 the claim states. There is NO line anywhere in the file (including heredocs) that invokes check_dependencies: `grep -n 'check_dep\\|install_dep' b-ui-client.sh | grep -v '^8[1-9][0-9]:\\|^9[0-2][0-9]:'` -> empty.\n\nCross-repo: `grep -rn 'check_dependencies\\|check_and_install_dep' --include='*.sh' --include='*.js' --include='*.json' --include='*.md' .` outside b-ui-client.sh hits only install.sh:94 (its OWN separate definition) and install.sh:769 (calling install.sh's own function; install.sh never sources b-ui-client.sh — install.sh:246 only lists it as a download target). No file sources bui-c/b-ui-client.sh to call this function (grep for `source .*bui-c|\\. /usr/local/bin/bui-c|source .*b-ui-client` -> none).\n\nGit history: `git log --oneline -S'check_dependencies' -- b-ui-client.sh` -> 071d391 (added, 2 occurrences: def+call) and 596335b \"feat(bui-c): TUI status bar and gum choose main menu\" whose diff shows `-        check_dependencies` (the sole call removed). Since then only the definition remains.\n\nTransitive note: detect_package_manager() (784) is called only from check_dependencies:846, so it is dead too; PKG_MANAGER/PKG_INSTALL are therefore never set at runtime, and the `[[ \"$PKG_MANAGER\" == \"apt\" ]]` test in the uninstall path at b-ui-client.sh:4628 already always evaluates false today (pre-existing latent behaviour, unaffected by deletion).\n\nNo SSH used: the claim is purely about static code reachability, not live host state.",
    "deletable_under_v4": true,
    "correction": "Core claim holds; two numeric details are off: check_dependencies() ends at line 958 (not 963), and there are 16 check_and_install_dep call sites (861, 866-869, 883-886, 894-897, 916, 917, 921), not 12 — all still inside the dead body. Deleting lines 812-958 (and optionally detect_package_manager 784-809, also unreachable) is zero behaviour change on a fresh install; the only residual reference to PKG_MANAGER at line 4628 is already never set today."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "Claim text (docs/superpowers/audits/2026-09-11-claims.json id=client-C3): check_and_install_dep()+check_dependencies() at b-ui-client.sh:812-963 are unreachable. No counter-claim, no also_raised_as.\n\nStatic call graph (repo at v3.6.3, /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -n 'check_dependencies\\|check_and_install_dep' b-ui-client.sh` -> definitions at 812 (check_and_install_dep) and 839 (check_dependencies); the ONLY other hits are lines 861,866-869,883-886,894-897,916,917,921 = exactly 12 call sites of check_and_install_dep, all inside check_dependencies (body 839-958, verified via awk for the closing `}`; 959-963 are a blank + section-banner comment, so \"812-963\" is off by 5 harmless lines). check_dependencies has zero callers anywhere in b-ui-client.sh.\n- `grep -rn 'check_dependencies\\|check_and_install_dep' --include='*.sh' --include='*.js' .` -> other hits only in install.sh:94/769 and b-ui-server.sh:87/1308, which are separate same-named server-side functions, not references into the client script.\n- `grep -nE 'eval |\"\\$func\"|\\$\\{?fn\\}?'` -> no dynamic/indirect function invocation in b-ui-client.sh. dispatch_subcommand() (5217-5257) is a fixed case statement (switch/tun/reload-tun/import/start/stop/restart/status/list/help); main() at 5259, tui_main_loop() at 5287, first_run_setup() at 5752 contain no call.\n\nHistory:\n- `git log -S'check_and_install_dep' --oneline -- b-ui-client.sh` -> only 071d391 (2025-12-10 \"feat(client): 添加端口检测、依赖自动安装、SSH端口保护\"): introduced both functions and the sole caller (`+    check_dependencies` in tui_main_loop).\n- `git log -S'check_dependencies' --oneline -- b-ui-client.sh` -> 071d391 and 596335b (2026-05-07 \"feat(bui-c): TUI status bar and gum choose main menu\"). `git show 596335b -- b-ui-client.sh` hunk `@@ -4454,36 +4492,73 @@ main()` removed from tui_main_loop:\n    -    # 只在首次运行时检测依赖 (核心未安装时)\n    -    if ! command -v hysteria ... ; then\n    -        check_dependencies\n    -    fi\n  Since 2026-05-07 (v3.4.x TUI rewrite) the function has had no caller; ~4 months dead.\n\nCollateral (not in claim but strengthens it): detect_package_manager() (784-810) is called only at line 846 inside dead check_dependencies; PKG_MANAGER is assigned only at 786-806. Hence in uninstall() (line 4628) `[[ \"$PKG_MANAGER\" == \"apt\" ]]` is always false on every live path (variable unset), so that apt-remove branch is also dead; the else branch `rm -f /usr/local/bin/sing-box /usr/bin/sing-box` always runs. Live sing-box install path (line 1177-1181) already removes the sagernet apt source on its own.\n\nNo SSH performed: the claim is purely about script reachability, not host state.",
    "deletable_under_v4": true,
    "correction": "Minor: check_dependencies' body ends at line 958, not 963 (959-963 are blank + a `# 端口检测工具` section comment). Additionally, detect_package_manager() (784-810) is reachable only through the dead check_dependencies, so it and the `PKG_MANAGER == \"apt\"` branch in uninstall() (4628-4631) are dead as well; deleting the cluster should include detect_package_manager and simplify uninstall() to the unconditional `rm -f` branch (zero behaviour change, since PKG_MANAGER is never set at runtime today)."
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "Static: `grep -rn \"check_dependencies\\|check_and_install_dep\" .` (excluding .git/audits) → in b-ui-client.sh only definitions at :812 (check_and_install_dep, closes :836) and :839 (check_dependencies, closes :958) plus call sites :861,:866-869,:883-886,:894-897,:916,:917,:921 — all inside check_dependencies' body. Other hits are install.sh:94/:769 and b-ui-server.sh:87/:1308, which are different same-named functions in different scripts (never sourced into the client). Additionally detect_package_manager (:784-809) has its only caller at :846, inside the same dead body, so it is dead too.\n\nRuntime-invocation lens (all negative):\n- Dynamic dispatch: `grep -nE 'eval |\"\\$@\"|declare -f|type -t'` → only run_with_spinner :161, gum wrappers :226/:229/:251, and dispatch_subcommand :5217-5253 which is a fixed `case \"$cmd\"` (switch/tun/reload-tun/import/start/stop/restart/status/list) — no function-name-from-argv path.\n- Entry :5798-5811: `case \"${1:-}\" in auto) auto_update_all ;; *) first_run_setup; main \"$@\"`. first_run_setup (:5752) only checks `command -v hysteria/xray/sing-box` and calls install_all_cores; main (:5259) → check_root/parse_global_flags/check_os → dispatch_subcommand or tui_main_loop. None reach check_dependencies.\n- History: `git log -S'    check_dependencies' -- b-ui-client.sh` → 596335b removed the only caller (diff hunk: tui_main_loop previously had `if ! command -v hysteria ... ; then check_dependencies; fi`), originally added in 071d391.\n- Delivery/sourcing: version.json files[] ships b-ui-client.sh unmodified; web/server.js /install-client wrapper (:140-195) installs curl'd binaries itself and just tells the user to run `sudo bui-c` — never invokes a bui-c function; `grep -rn \"source.*b-ui-client\\|\\. .*b-ui-client\\|b-ui-client.sh [a-z]\"` → only the fallback download URL at b-ui-client.sh:453. The BASH_SOURCE guard (:5796) permits `. /usr/local/bin/bui-c` but no repo script does so. Client cron line is `bui-c auto` (:5738) → auto_update_all only.\n- Live host: `ssh bwg-rick` grep of /opt/b-ui/b-ui-client.sh and /opt/b-ui/packages/b-ui-client.sh (SCRIPT_VERSION 3.4.15) shows identical dead layout (defs :784/:812/:839, calls only :846/:861/:916/:917/:921 inside the body); `grep -rn \"bui-c\\|b-ui-client\" /etc/cron* /var/spool/cron /etc/systemd/system` → nothing.\n\nMinor corrections to the claim's numbers: check_dependencies ends at :958 not :963, and there are 16 check_and_install_dep call sites (not 12), all still inside the dead body. One side effect to note: uninstall() :4628 tests `$PKG_MANAGER`, which only detect_package_manager sets; since that is never called, the variable is always empty at runtime and uninstall already always takes the `else` (rm -f /usr/local/bin/sing-box /usr/bin/sing-box) branch — deleting the dead trio changes nothing there.",
    "deletable_under_v4": true
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "client-C4",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json, id client-C4): \"b-ui-client.sh:1053-1072 get_server_ip() and get_current_ssh_ip() are defined but have zero call sites in b-ui-client.sh ...\". No counter_claim, no also_raised_as.\n\nDefinitions verified at /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh:1053-1063 (get_server_ip: greps \"^server:\" from $CONFIG_FILE, dig A) and :1064-1067 (get_current_ssh_ip: `echo \"$SSH_CONNECTION\" | awk '{print $3}'`). Note: the claim's range \"1053-1072\" slightly overshoots; the second function ends at line 1067 (1068-1072 are blank + the \"# 安装\" section banner). Substance unaffected.\n\nCommands and decisive output:\n1. `grep -n 'get_server_ip\\|get_current_ssh_ip' b-ui-client.sh` -> only lines 1053 and 1064 (the two definitions). Zero call sites.\n2. `grep -rn 'get_server_ip\\|get_current_ssh_ip' --include='*.sh' --include='*.js' --include='*.json' --include='*.html' . | grep -v docs/` -> other hits are separate, self-contained definitions+callers in other files: server/core.sh:52 (def) / :61 (call), server/residential-helper.sh:332 (def) / :360, :463 (calls), b-ui-server.sh:165 (def) / :285 (call, legacy v3.1 installer). None of these files source b-ui-client.sh; each defines its own copy. get_current_ssh_ip has no definition or caller anywhere else in the repo.\n3. `grep -rn 'SSH_CONNECTION' --include='*.sh' --include='*.js' .` -> only b-ui-client.sh:1066 (inside the orphan). So the \"SSH 保护\" feature this helper was meant for never consumes it.\n4. Indirect invocation ruled out: `grep -nE 'declare -f|type -t|compgen -A function' b-ui-client.sh` -> no hits; `source`/`.` in the client only touches /etc/os-release (:539), /etc/profile.d/proxy.sh (:1669), .ufw_state (:2200). CLI dispatch at b-ui-client.sh:5219-5253 is a fixed `case \"$cmd\"` (switch/tun/reload-tun/import/start/stop/restart/status/list/help) with `*)` -> \"未知命令\" error, so `bui-c get_server_ip` cannot reach the function. web/server.js only rewrites SCRIPT_VERSION when serving the script (server.js:34); server/core.sh:1630-1642 and server/b-ui-cli.sh:559-561 merely download/copy the file. Nothing outside the file sources it.\n5. History: `git log --oneline --follow -S'get_current_ssh_ip' -- b-ui-client.sh` -> 1be5ed8 (rename to B-UI) and 6ff280d (2025-12-08 \"feat: 增强客户端 - 路由规则、TUN模式、SSH保护\", introduced as hysteria2-client.sh:66/77). In 6ff280d the file already contained only the two definitions (`git show 6ff280d:hysteria2-client.sh | grep -n` -> lines 66, 77 only). `git log -G'^\\s+[^#]*\\b(get_server_ip|get_current_ssh_ip)\\b' --all -- '*client*.sh'` -> empty: no commit in the entire history ever contained an indented (call-site) reference in any client script. The functions were dead on arrival.\n6. `bash -n b-ui-client.sh` -> OK (baseline; removing two unreferenced functions cannot affect parsing).\n\nSSH to live hosts not used: the claim concerns static code, and the deployed client is a byte copy of this repo file (core.sh:1635 curl from GitHub raw; server.js only substitutes SCRIPT_VERSION).",
  "deletable_under_v4": true,
  "correction": "Minor line-range nit only: get_current_ssh_ip() ends at b-ui-client.sh:1067, not 1072 (lines 1068-1072 are blank lines and the \"# 安装\" section header). Dead-code finding stands regardless of the v4 decision — these were never called in any commit, so they are not migration code either.",
  "high_impact": false
 },
 {
  "id": "client-C5",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "File: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (5810 lines, 120 function defs).\n\nENTRY POINTS (all enumerated): BASH_SOURCE guard 5798-5809 -> `auto` => auto_update_all (5632-5724); otherwise first_run_setup (5752-5794) then main \"$@\" (5259-5285). main -> dispatch_subcommand (5217-5257; case: switch/tun/reload-tun/import/start/stop/restart/status/list/help -> cmd_switch,cmd_tun,cmd_reload_tun,cmd_import,cmd_start,cmd_stop,cmd_restart,cmd_status,cmd_list) or tui_main_loop (5287-5313; case 1-8 -> tui_switch_node, tui_toggle_tun, tui_import_node, tui_service_control, test_proxy, advanced_settings_menu, update_all, uninstall). advanced_settings_menu (5138-5187) only dispatches to `1)` inline block, `2) edit_rules`, `0) return`. show_menu (5193-5215) prints only.\n\nCALL-SITE ENUMERATION for the 13 names (cmd: `for f in <13 names>; do grep -nE \"(^|[^A-Za-z0-9_])$f($|[^A-Za-z0-9_])\" b-ui-client.sh; done`):\n- list_configs: def 2456; calls 2970 (in switch_config), 3001 (delete_config), 3135 (activate_imported_config), 3271 (config_management); 2369 is a comment only.\n- switch_config: def 2964; call 3272 (config_management).\n- delete_config: def 2995; call 3273 (config_management).\n- import_batch: def 3040; 4745 is a comment only -> NO callers.\n- activate_imported_config: def 3133; calls 3128 (import_batch), 4792 (import_node).\n- _configure_and_save: def 3168; call 3165 (activate_imported_config).\n- config_management: def 3256; NO callers.\n- import_from_subscription: def 3286; call 4718 (import_node).\n- import_from_uri: def 3424; NO callers.\n- configure_client: def 3555; call 4508 (quick_install).\n- quick_install: def 4503; call 4711 (import_node).\n- import_node: def 4680; NO callers.\n- service_control_menu: def 4801; NO callers.\n=> every call site lies inside the 13; the 5 roots (import_batch, config_management, import_from_uri, import_node, service_control_menu) have zero callers anywhere in the file. Closed, unreachable cluster.\n\nINDIRECT-INVOCATION CHECK: `grep -nE 'eval |declare -f|type -t|trap |complete -|\\$\\{?[A-Za-z_]+\\}?_(menu|config|node)\\b|\"\\$\\{?(fn|func|cmd|handler|action)\\}?\"'` -> no dynamic function-name dispatch anywhere (only gum/tui_menu string choices at 251, 5529, 5554 which return labels handled by literal case arms). Heredoc-generated scripts (1238,1309,1499,1626,1656,2432,3618,3699,3748,3924,3963 HEALTHEOF,4016,4025,5230 HELP) contain none of the 13 names (the per-name grep covers the whole file including heredoc bodies). first_run_setup and auto_update_all bodies contain none of the 13 names (sed range + grep, empty). Repo-wide `grep -rnE 'config_management|import_from_uri|service_control_menu|import_node\\b|quick_install|configure_client' --include=*.sh --include=*.js --include=*.md .` -> only b-ui-client.sh hits; no external sourcing caller in server/ or web/ (the BASH_SOURCE guard permits `. bui-c` from outside, but nothing in the repo does it).\n\n\"INDEPENDENT REIMPLEMENTATION\" PART — verified via per-function callee extraction (awk fn table + grep -oE of all 120 names):\n- switch_config (2964-2993) calls list_configs + _switch_to_profile; tui_switch_node (5315-5389) calls _get_node_display_name, get_active_config, _switch_to_profile — does NOT call switch_config/list_configs. cmd_switch (2751-2770) calls _switch_to_profile directly.\n- import_batch (3040-3131) calls parse_*_uri, safe_import_parsed, save_config_meta, activate_imported_config; cmd_import (2839-2906) and tui_import_node (5410-5496) call the same low-level parse/save helpers + _switch_to_profile / cmd_import — never import_batch/activate_imported_config/_configure_and_save.\n- service_control_menu (4801-4926) uses setup_system_proxy/remove_system_proxy; tui_service_control (5498-5565) uses tui_menu + inline systemctl, no shared call.\n- list_configs (2456-2515) and cmd_list (2707-2749) both only call get_active_config; separate bodies.\nSo the live paths share only the leaf helpers (_switch_to_profile, parse_hysteria_uri, parse_vless_uri, safe_import_parsed, save_config_meta, get_active_config); the orchestration/menu layer is duplicated, as claimed.\n\nGIT: `git log -1 -L` on roots: config_management 0474614 (2025-12-10), import_from_uri f76beda (2025-12-08), import_node/service_control_menu 523b18c (2026-03-20 v3.0.0). `git log -S` shows the TUI replacements landed in a2e1fe3 / 596335b (TUI menus), i.e. the old menu cluster was orphaned when the numeric TUI + subcommand dispatcher was introduced.\n\nINACCURACIES IN THE CLAIM (do not change the verdict): (1) the quoted line ranges overlap and overcount — \"2456-3285\" encloses LIVE functions _switch_to_profile 2517-2659, cmd_list 2707-2749, cmd_switch 2751-2770, cmd_import 2839-2906; exact cluster bounds are list_configs 2456-2515, switch_config 2964-2993, delete_config 2995-3038, import_batch 3040-3131, activate_imported_config 3133-3166, _configure_and_save 3168-3254, config_management 3256-3280, import_from_subscription 3286-3422, import_from_uri 3424-3549, configure_client 3555-3614, quick_install 4503-4523, import_node 4680-4795, service_control_menu 4801-4926. (2) Combined size is 958 lines (awk sum), not \"~1190\". Counter-claim: none recorded.",
    "deletable_under_v4": true,
    "correction": "Claim substance is correct; only bookkeeping is off: the ranges \"2456-3285 and 3040-3423\" overlap and swallow live functions (_switch_to_profile 2517-2659, cmd_list 2707-2749, cmd_switch 2751-2770, cmd_import 2839-2906 must NOT be deleted), and the cluster totals 958 lines, not ~1190. Deleting the 13 functions changes nothing on a fresh install (no entry-point path reaches them; no eval/indirect dispatch; no external sourcing caller in the repo). After deletion, re-check second-order helpers whose remaining callers may vanish (e.g. check_and_suggest_ports callers at 1289/3189/3495/3572 -> 3189/3495/3572 are inside the cluster; create_default_rules 3210/3525/3608 inside cluster, 3763/3847 outside; install_xray_client 3229/3535 inside, 755/1102 outside) — those stay alive via other callers but are worth a second pass."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "Repo: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (5810 lines, HEAD 2929947, version.json 3.6.3).\n\n1) Definitions (grep -nE '^(name)\\(\\)'): list_configs:2456-2515, switch_config:2964-2993, delete_config:2995-3038, import_batch:3040-3131, activate_imported_config:3133-3166, _configure_and_save:3168-3254, config_management:3256-3280, import_from_subscription:3286-3422, import_from_uri:3424-3549, configure_client:3555-3614, quick_install:4503-4523, import_node:4680-4795, service_control_menu:4801-4926. Entry points: dispatch_subcommand:5217, main:5259, tui_main_loop:5287; top-level guard at 5793-5809 calls only auto_update_all / first_run_setup / main.\n\n2) Callers (grep -nE \"\\b<name>\\b\" minus definition line): config_management, import_from_uri, import_node, service_control_menu have ZERO callers anywhere. The rest are called only from inside the cluster: list_configs <- switch_config:2970, delete_config:3001, activate_imported_config:3135, config_management:3271; switch_config/delete_config <- config_management:3272/3273; activate_imported_config <- import_batch:3128, import_node:4792; _configure_and_save <- activate_imported_config:3165; import_from_subscription <- import_node:4718; quick_install <- import_node:4711; configure_client <- quick_install:4508; import_batch <- nothing (only a comment at 4745).\n\n3) Full reachability BFS (python script over all 120 function defs, comments stripped, seeded from top-level code {auto_update_all, first_run_setup, main, get_active_config, log, print_*, set_server_address}): 87 reachable, 33 unreachable; ALL 13 named functions are in the unreachable set. dispatch_subcommand:5217-5257 routes only to cmd_switch/cmd_tun/cmd_reload_tun/cmd_import/cmd_start/cmd_stop/cmd_restart/cmd_status/cmd_list; tui_main_loop:5287-5313 routes only to tui_switch_node/tui_toggle_tun/tui_import_node/tui_service_control/test_proxy/advanced_settings_menu/update_all/uninstall. None of tui_switch_node:5315, tui_import_node:5410, tui_service_control:5498, cmd_switch:2751, cmd_list:2707, cmd_import:2839, _switch_to_profile:2517 reference any cluster function (confirmed by the caller grep above). No eval/indirect dispatch or trap referencing them (grep -nE 'eval |trap ' returns only comments). No other repo file sources bui-c or names these functions (grep -rnE over server/ web/ install.sh: only b-ui-server.sh has its own unrelated quick_install()).\n\n4) HISTORY: `git log -S'import_node()' -- b-ui-client.sh` -> introduced 523b18c 2026-03-20 (v3.0.0 refactor; config_management 0474614 2025-12-10, import_from_uri 1be5ed8 2025-12-08). tui_main_loop/dispatch_subcommand introduced 4ce3795 2026-05-07; tui_switch_node/tui_import_node/tui_service_control introduced 596335b 2026-05-07 \"feat(bui-c): TUI status bar and gum choose main menu\". `git show 596335b -- b-ui-client.sh | grep -E '^[-+]\\s+[0-9]\\) '` shows the decisive removal: \"-            1) import_node ;;  -            2) config_management ;;  -            3) service_control_menu ;;  -            4) toggle_tun ;;\" replaced by the new tui_* menu. That commit orphaned the cluster and nothing since re-wired it.\n\n5) LIVE: `ssh bwg-rick 'md5sum /opt/b-ui/b-ui-client.sh; grep -nE \"^\\s+[0-9]\\) (import_node|config_management|service_control_menu|tui_...) \"'` -> served client md5 602c5388e5b43715f2934ec0b95cb386 == repo b-ui-client.sh md5 (identical file); menu wiring on the served copy is 5301: tui_switch_node, 5303: tui_import_node, 5304: tui_service_control — the dead cluster ships to clients but is never entered. (bwg-tizi not queried: the served client file is byte-identical to the repo and the claim is about code reachability, not host config.)",
    "correction": "Core claim holds; only the numbers in it are sloppy: the 13 functions total 958 lines, not ~1190, and the cited ranges \"2456-3285\" and \"3040-3423\" overlap LIVE code (_switch_to_profile:2517, cmd_list:2707, cmd_switch:2751, cmd_import:2839 sit inside 2456-3285) — the correct extents are the per-function ranges in the evidence. Also \"closed call graph\" is only true inbound: the cluster calls out to shared live helpers (generate_config, create_service, save_config_meta, parse_*_uri, safe_import_parsed, install_hysteria, setup_system_proxy, ...), which must NOT be deleted. Conversely, deleting the cluster additionally orphans 8 more helpers that are reachable only through it and are already dead today: check_and_suggest_ports:1007, find_available_port:988, get_occupied_ports:979, is_port_in_use:964, generate_xray_config:1287, toggle_tun:3873, start_client:4043, remove_system_proxy:1674 (plus unrelated dead code: check_dependencies:839, show_status:4054, print_banner:44, tui_filter:273, tui_write:300, get_server_ip, get_current_ssh_ip, spinner trio).",
    "deletable_under_v4": true
   },
   {
    "lens": "runtime",
    "verdict": "PARTIALLY",
    "evidence": "Repo: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (5810 lines; md5 602c5388… identical to the copy bwg-rick serves at /opt/b-ui/b-ui-client.sh and /opt/b-ui/packages/b-ui-client.sh, v3.6.3, so runtime delivery == repo file).\n\n1) Closed call graph — CONFIRMED (static + every runtime entry).\nCall-site grep: `grep -nE '\\b(list_configs|switch_config|delete_config|import_batch|activate_imported_config|_configure_and_save|config_management|import_from_subscription|import_from_uri|configure_client|quick_install|import_node|service_control_menu)\\b' b-ui-client.sh` minus definitions yields ONLY: 2970/3001/3135 (list_configs ← switch_config/delete_config/activate_imported_config), 3128 (activate_imported_config ← import_batch), 3165 (_configure_and_save ← activate_imported_config), 3271-3273 (← config_management), 4508 (configure_client ← quick_install), 4711/4718 (quick_install, import_from_subscription ← import_node), 4792 (activate_imported_config ← import_node), plus comments 2369/4745. import_batch, import_from_uri, config_management, import_node, service_control_menu have zero callers anywhere. Nothing outside the script references these names (`grep -rnw … install.sh server web docs` → only historical plans).\nRuntime entry points all checked:\n- Script tail 5796-5810: BASH_SOURCE guard → `auto` → auto_update_all; else first_run_setup; main \"$@\". awk-extracted bodies of first_run_setup and auto_update_all contain no cluster names.\n- main() 5259-5285 → parse_global_flags/check_os → dispatch_subcommand 5217 (case: switch/tun/reload-tun/import/start/stop/restart/status/list → cmd_*) or tui_main_loop 5287 (1-8 → tui_switch_node/tui_toggle_tun/tui_import_node/tui_service_control/test_proxy/advanced_settings_menu/update_all/uninstall). Same menu confirmed on bwg-rick's served copy.\n- advanced_settings_menu, show_menu, uninstall, update_all, tui_toggle_tun, test_proxy, check_client_update, do_client_update bodies: no cluster names.\n- No dynamic dispatch: only `case \"$cmd\"` at 5219 and 2774; no eval/\"$fn\"/declare -f/compgen.\n- cron: 5745 `0 */6 * * * /usr/local/bin/bui-c auto` → auto_update_all only.\n- `. /usr/local/bin/bui-c` source path (5797 comment) — no script in repo (server/, web/, install.sh) sources bui-c or calls cluster functions.\n- Server side: web/server.js:1514-1560 /install-client only reads BASE_DIR/b-ui-client.sh (or packages copy / GitHub) through injectClientVersion (version string substitution) — no code injection; server.js never spawns the client. core.sh:1630-1642, update.sh:264/290/2147/2193, install.sh:246 only copy the file.\n- Shared helpers used by the live paths are outside the cluster: safe_import_parsed 2218, parse_hysteria_uri 2240, parse_vless_uri 2302, _find_config_by_uri 2398, save_config_meta 2412, _switch_to_profile 2517.\n\n2) Line ranges / size — WRONG. Exact extents (awk): list_configs 2456-2515(60), switch_config 2964-2993(30), delete_config 2995-3038(44), import_batch 3040-3131(92), activate_imported_config 3133-3166(34), _configure_and_save 3168-3254(87), config_management 3256-3280(25), import_from_subscription 3286-3422(137), import_from_uri 3424-3549(126), configure_client 3555-3614(60), quick_install 4503-4523(21), import_node 4680-4795(116), service_control_menu 4801-4926(126) = 958 lines, not ~1190. The claimed span \"2456-3285\" wrongly swallows the LIVE functions _switch_to_profile 2517, cmd_status 2665, cmd_list 2707, cmd_switch 2751, cmd_tun 2772, cmd_reload_tun 2814, cmd_import 2839, cmd_start 2908, cmd_stop 2938, cmd_restart 2949 (2517-2963 must NOT be deleted).\n\n3) \"live paths reimplement equivalent functionality\" — only partly true. Subscription-URL import has NO live equivalent: tui_import_node 5449-5450 on `^https?://` prints \"订阅地址请通过导入节点菜单的订阅功能导入\" and counts it as fail; cmd_import 2839-2907 has no https/订阅 handling at all; the referenced \"订阅功能\" is the unreachable import_from_subscription 3286. Node switching/deletion/service control are indeed reimplemented (tui_switch_node, cmd_switch, _switch_to_profile, tui_service_control).",
    "correction": "The cluster is dead, but (a) it is 958 lines in 13 disjoint blocks, not ~1190; the claim's \"2456-3285\" range includes the live cmd_status/cmd_list/cmd_switch/cmd_tun/cmd_reload_tun/cmd_import/cmd_start/cmd_stop/cmd_restart and _switch_to_profile (b-ui-client.sh:2517-2963) which must be kept; (b) \"equivalent functionality\" is not fully reimplemented: subscription-URL import exists only in the dead import_from_subscription (3286-3422); the live tui_import_node (5449-5450) explicitly rejects https:// URIs and points users to that dead menu, and cmd_import (2839-2907) never handles them. Deleting the cluster is behaviour-neutral, but if Linux-client subscription import is a kept product capability it needs a fresh live implementation, not this code.",
    "deletable_under_v4": true
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "client-C6",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Claim text retrieved with: jq -r '.[] | select(.id==\"client-C6\") ...' docs/superpowers/audits/2026-09-11-claims.json (no counter_claim, no also_raised_as; the same point is embedded in claim at claims.json:183 and readers-raw-UNVERIFIED.json:242/249).\n\nFACTS CONFIRMED (reproduced myself):\n- sed -n '1070,1085p' docs/superpowers/plans/2026-05-07-cli-redesign.md -> line 1078 `tui_import_node()    { import_node; }`, line 1079 `tui_service_control() { service_control_menu; }`.\n- grep -n 'tui_import_node\\|tui_service_control\\|^import_node()\\|^service_control_menu()' b-ui-client.sh -> tui_import_node() defined at b-ui-client.sh:5410, tui_service_control() at :5498 (next section header at 5577), import_node() at :4680 (closes at 4795), service_control_menu() at :4801.\n- sed -n '5410,5574p' b-ui-client.sh: neither body contains the token import_node or service_control_menu; tui_import_node parses URIs itself via parse_hysteria_uri/parse_vless_uri/_find_config_by_uri/safe_import_parsed/save_config_meta; tui_service_control drives systemctl/journalctl directly.\n- grep -rn '\\bimport_node\\b\\|\\bservice_control_menu\\b' --include='*.sh' --include='*.js' . (excluding docs/) -> only the two definition lines (4680, 4801). Zero callers repo-wide.\n\nINTERPRETATION REFUTED (\"original design intent = thin delegating wrappers\"):\n- The plan itself labels the one-liners as stubs to be replaced: plan line 1073 \"**Step 3: 添加三个 TUI 函数存根（后续 Task 9-10 实现）**\"; plan line 1187-1190 \"## Task 10 ... Modify: b-ui-client.sh（替换三个 TUI 存根）\"; line 1220 \"**Step 2: 替换 `tui_import_node`**\"; line 1299 \"**Step 3: 替换 `tui_service_control`**\". The plan's Task 10 bodies (lines 1223-1298, 1302-1372) are already full inline implementations (its tui_import_node carries the comment \"委托给现有 import_node 的核心逻辑（通过临时文件传递）\" but actually inlines parse_*_uri/safe_import_parsed/save_config_meta, never calling import_node).\n- diff of plan Task-10 tui_import_node vs shipped (awk-extracted, diff'd in scratchpad): only cosmetic/hardening deltas (lines_input rename, v3.4.33 _find_config_by_uri dedup, sanitized config_name, error check on save_config_meta). Shipped == plan Task 10, not a deviation.\n- git log -S'{ import_node; }' -- b-ui-client.sh -> 596335b \"feat(bui-c): TUI status bar and gum choose main menu\" (added stubs, Task 8) then a2e1fe3 2026-05-07 \"feat(bui-c): TUI toggle TUN, import node, and service control menus\" whose message says \"Replace three one-liner stubs with full TUI implementations\" and whose diff removes `-tui_import_node()     { import_node; }` / `-tui_service_control() { service_control_menu; }`. The stubs lived for one commit, by design.\n- git log -S'import_node ;;' -- b-ui-client.sh -> last direct menu call to import_node removed in 596335b (the old numeric menu case) — import_node/service_control_menu have been unreachable since Task 8 (2026-05-07), i.e. throughout v3.5.x/v3.6.x; no cron/systemd/panel/sourcing path invokes them (b-ui-client.sh is executed only as `bui-c`, main()->tui_main_loop/dispatch_subcommand; tui_main_loop:5303-5304 calls tui_import_node/tui_service_control).\n\nSo: the line references and the \"never calls\" fact are correct; the \"design intent was thin delegation\" framing is wrong — the plan explicitly scheduled and the commit explicitly executed the replacement. The real, defensible finding is simply that import_node (4680-4795) and service_control_menu (4801-~4931) are dead code (same as the duplicate claim at claims.json:183).",
  "deletable_under_v4": true,
  "correction": "The plan did not specify thin delegation as the design; plan:1073 calls those one-liners \"存根（后续 Task 9-10 实现）\" and plan:1187-1190/1220/1299 order them replaced with full implementations, which commit a2e1fe3 (2026-05-07) did. The shipped tui_import_node (b-ui-client.sh:5410-5497) and tui_service_control (5498-5574) match the plan's Task 10 bodies plus v3.4.33 dedup hardening. What is true and actionable: import_node (b-ui-client.sh:4680-4795) and service_control_menu (4801-4931) have had zero callers since 596335b and can be deleted with no behaviour change; the tui_* functions are live (called from tui_main_loop at 5303-5304) and must stay. This is a duplicate of claims.json:183 (dead old-menu cluster), not an independent finding.",
  "high_impact": false
 },
 {
  "id": "client-C7",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "File: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (5810 lines). No counter-claim exists in the audit JSON (COUNTER: none, ALSO: empty).\n\nEntry points enumerated (static call graph):\n1. Script entry guard, lines 5798-5809: `if [[ \"${BASH_SOURCE[0]}\" == \"${0}\" ]]` -> `auto` => auto_update_all; otherwise first_run_setup; main \"$@\".\n2. main() 5259-5283 -> dispatch_subcommand (subcommands) or tui_main_loop (no args).\n3. dispatch_subcommand() 5216-5257: fixed `case \"$cmd\"` with switch/tun/reload-tun/import/start/stop/restart/status/list/help — line 5223 `import) cmd_import \"$@\"`. No indirect `\"$fn\"`/eval dispatch anywhere (grep -nE 'eval |declare -f|\\$\\{cmd\\}|\"\\$cmd\"' hits only line 5219 `case \"$cmd\" in` and line 817/824 `command -v \"$cmd\"` inside a package-install helper).\n4. tui_main_loop() 5286-5310: `3) tui_import_node ;;` (line 5303). Other branches: tui_switch_node, tui_toggle_tun, tui_service_control, test_proxy, advanced_settings_menu (5138: only autostart toggle + edit_rules), update_all, uninstall.\n\nReference counts (grep -n over the whole file):\n- `grep -n 'import_from_subscription'` -> 3286 (definition) and 4718 (called ONLY inside import_node).\n- `grep -n 'import_node'` -> 4680 (definition of import_node), 5303 (tui_import_node call), 5410 (tui_import_node def). import_node itself has ZERO callers.\n- `grep -n 'config_management'` -> 3256 (definition only). ZERO callers.\n- Repo-wide `grep -rnE 'import_from_subscription|config_management|import_node\\b'` outside b-ui-client.sh hits only docs (docs/superpowers/plans/2026-05-07-cli-redesign.md, plans/specs 2026-09-10-ipv6-takeover) — no shell/JS caller sources bui-c and invokes them. `grep -rn 'bui-c import'` finds only the usage string (2845) and docs.\n\nRejection of https:// in both live paths:\n- tui_import_node, lines 5443-5451: `elif [[ \"$uri\" =~ ^https?:// ]]; then tui_info \"订阅地址请通过导入节点菜单的订阅功能导入\"; ((fail++)); continue` — the \"导入节点菜单的订阅功能\" it points to is import_node (4680)/import_from_subscription (3286), which nothing calls.\n- cmd_import, lines 2849-2858: only `^(hysteria2|hy2)://` and `^vless://` accepted; else `tui_error \"不支持的 URI 格式（仅支持 hysteria2:// 和 vless://）\"; exit 2`.\n- The only code that would call import_from_subscription is import_node line 4715-4719 (`if [[ ${#lines[@]} -eq 1 && \"$first\" =~ ^https?:// ]] ... import_from_subscription \"$first\"`), and import_node is unreachable.\n\nVerified line numbers claimed by the audit: tui_import_node is at 5410 (claim says ~5443 = the https branch, matches); cmd_import 2839-2907 matches. Live hosts not consulted: claim is purely about static code in the file, not host state.",
    "deletable_under_v4": true,
    "correction": "Nothing to correct on the claim itself. Two side notes: (a) config_management (3256-3280), import_node (4680-~4760), import_from_subscription (3286-~3480) and their exclusive helpers (list_configs/switch_config/delete_config, if unreferenced elsewhere — verify before deleting) are dead on a fresh install, so deletion is zero-behaviour-change; but the client-side sing-box/Clash subscription pull (`https://${SERVER_ADDRESS}/api/subscription/<user>`, lines 3329-3334) is the ONLY code in the client that consumes those server-side subscription formats, so deleting it means bui-c has no subscription-URL import at all — confirm that fits the \"8 capabilities stay\" decision (the capability may be considered v2rayN-only). (b) docs/superpowers/specs/2026-09-10-ipv6-takeover-design.md:125 and plans/2026-09-10-ipv6-takeover.md:238,346 prescribe modifying import_from_subscription's sing-box branch — that spec item targets dead code and should be dropped or re-targeted."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "Repo (worktree, HEAD = v3.6.3, b-ui-client.sh 5810 lines):\n\n1. tui_import_node (b-ui-client.sh:5410-5454) is the only interactive import entry: tui_main_loop dispatches `3) tui_import_node ;;` at :5303 (menu \"导入节点\"). Its loop at :5449-5451 does `elif [[ \"$uri\" =~ ^https?:// ]]; then tui_info \"订阅地址请通过导入节点菜单的订阅功能导入\"; ((fail++)); continue` — https input is rejected, never fetched.\n\n2. The \"订阅功能\" it points to is import_from_subscription (:3286). Its only call site is inside import_node (:4718 `import_from_subscription \"$first\"`). import_node (:4680) has no callers: `grep -nw 'import_node\\|config_management' b-ui-client.sh | grep -v '() {'` returns nothing; `grep -nE '(^|[^_a-z])import_node($|[^_a-z(])'` on the deployed copy exits 1. config_management (:3256) likewise has zero callers. advanced_settings_menu (:5138-5200) offers only 自启动/路由规则; main() (:5259) goes to dispatch_subcommand or tui_main_loop only.\n\n3. CLI path cmd_import (:2839-2906): `if hysteria2|hy2:// … elif vless:// … else tui_error \"不支持的 URI 格式（仅支持 hysteria2:// 和 vless://）\"; exit 2` (:2850-2859). dispatch_subcommand `import) cmd_import \"$@\"` (:5223). No `sub`/`subscribe` subcommand exists.\n\nHistory (git log -S): import_from_subscription introduced in 01135c5 (客户端订阅支持 v2.3.0). 523b18c (v3.0.0 重构) and 596335b (2026-05-07, \"TUI status bar and gum choose main menu\") removed the old menu entries `1) import_node ;;` / `2) config_management ;;` and left a wrapper `tui_import_node() { import_node; }` (596335b diff lines 77-78,148). One minute later a2e1fe3 (2026-05-07 19:32) replaced that wrapper with the full tui_import_node body and added the \"订阅地址请通过导入节点菜单的订阅功能导入\" message (a2e1fe3 diff lines 27,53,93) — from that commit on import_node/import_from_subscription/config_management have been unreachable. v3.6.0 still edited the dead import_from_subscription body (IPv6 TUN address strip at :3361-3370), i.e. maintenance effort spent on dead code.\n\nLive state: `ssh bwg-rick 'md5sum /opt/b-ui/packages/b-ui-client.sh; grep -n … ; jq -r .version /opt/b-ui/version.json'` → md5 602c5388e5b43715f2934ec0b95cb386 identical to repo HEAD, version 3.6.3, same line numbers (3286/4680/4718/5303/5410/5450), no import_node caller. The file served to clients is exactly the one analysed. No counter-claim exists; the \"ALSO\" list is empty.",
    "correction": "",
    "deletable_under_v4": true
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): no COUNTER, no ALSO ids.\n\nStatic facts in /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (5810 lines):\n- `grep -nE 'import_node|import_from_subscription|config_management|quick_install' b-ui-client.sh` -> only: 3256 `config_management() {` (definition, zero call sites), 3286 `import_from_subscription() {`, 4680 `import_node() {` (definition, zero call sites), 4718 `import_from_subscription \"$first\"` (inside the uncalled import_node), 5303 `3) tui_import_node ;;`, 5410 `tui_import_node() {`. So import_from_subscription's ONLY caller is import_node, and import_node has no caller.\n- tui_import_node 5445-5453: `^(hysteria2|hy2)://` -> parse_hysteria_uri; `^vless://` -> parse_vless_uri; `^https?://` -> line 5450 `tui_info \"订阅地址请通过导入节点菜单的订阅功能导入\"; ((fail++)); continue`. The referenced \"订阅功能\" does not exist in any reachable menu (main menu 5300-5311 is 1 tui_switch_node / 2 tui_toggle_tun / 3 tui_import_node / 4 tui_service_control / 5 test_proxy / 6 advanced_settings_menu / 7 update_all / 8 uninstall; advanced_settings_menu cases are 1 / 2 edit_rules / 0).\n- cmd_import 2850-2859: same two regexes, else `tui_error \"不支持的 URI 格式（仅支持 hysteria2:// 和 vless://）\"; exit 2`. dispatch_subcommand 5219-5228 has no subscription subcommand; help text 5236-5243 lists none.\n\nRUNTIME-INVOCATION lens (all checked, none reaches the subscription code):\n- Script entry tail (lines 5786-5810): `case \"${1:-}\" in auto) auto_update_all ;; *) first_run_setup; main \"$@\" ;;` -> main -> dispatch_subcommand or tui_main_loop only.\n- No indirect dispatch: `grep -nE '\\beval\\b|declare -f|type -t|compgen -A function|\"\\$\\{?[a-z_]+\\}?\" +\"\\$@\"'` -> only line 251 (gum choose args) and comment 2217 \"替代 eval\"; no \"$fn\" style invocation.\n- Client cron: ensure_client_cron writes `0 */6 * * * /usr/local/bin/bui-c auto` -> auto_update_all (5632), no subscription call. No systemd unit in the script references import/subscription (`grep -nE 'hysteria-health|ExecStart' | grep -iE 'subscription|import'` -> empty).\n- Server side: web/server.js only SERVES the script verbatim (/install-client 1530-1611, /packages 1641-1670, SCRIPT_VERSION substitution at 29-30) and the generated install-client.sh (lines ~150-190) ends with `echo \"运行 sudo bui-c 进行配置管理\"` - it never invokes bui-c with an argument. server/core.sh:1633-1642, server/update.sh:264/290/2147/2193, install.sh:246, server/b-ui-cli.sh:559-561 only download/copy b-ui-client.sh into /opt/b-ui and packages/. version.json files[] line 18 delivers the same file. No spawnSync/execSync/execFile in server.js targets bui-c/b-ui-client.sh (grep at 80,101,495,561,1084,1093,1724-1732,2134-2257 are ip/getent/helper/curl/hysteria/xray/sing-box).\n- The only way to reach it is manual `source /usr/local/bin/bui-c; import_from_subscription` via the BASH_SOURCE guard (5798) - not a code path in the file.\n\nLive host (READ-ONLY): `ssh bwg-rick 'grep -nE \"\\bimport_node\\b|config_management|import_from_subscription\" /opt/b-ui/b-ui-client.sh /opt/b-ui/packages/b-ui-client.sh'` -> identical 4 lines (3256, 3286, 4680, 4718) in both served copies (SCRIPT_VERSION=\"3.4.15\" in file, substituted at serve time); crontab and /etc/systemd/system contain no bui-c/b-ui-client invocation.\n\nHistory proving how it died: `git show 596335b -- b-ui-client.sh` (2026-05-07) removed `1) import_node ;;` and `2) config_management ;;` from the menu and added `tui_import_node() { import_node; }`; the next commit a2e1fe3 (2026-05-07, \"TUI toggle TUN, import node, and service control menus\") replaced that wrapper with the current standalone tui_import_node that no longer calls import_node. Since then import_node, import_from_subscription, config_management (and its sub-flow) have had no caller; the \"订阅功能\" hint at 5450 has pointed at nothing for 4 months. Note: quick_install (4503) is still live via other callers? Not checked - only referenced at 4711 inside dead import_node in the grep above, so it is likely dead too but outside this claim's scope.",
    "deletable_under_v4": true,
    "correction": ""
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "client-C8",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Repo: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (HEAD 2929947). No counter-claim, no other ids.\n\n1) `grep -n 'create_health_check\\|generate_xray_config\\|create_service\\b\\|hysteria-health' b-ui-client.sh` → create_health_check is defined at 3960 and invoked exactly once, at 3953 inside create_service() (3912-3959). generate_xray_config() (1287-1333) contains no call to it: it does check_and_suggest_ports, _write_xray_json, writes /etc/systemd/system/xray-client.service (1309-1324, only `Restart=always`/`RestartSec=5`, no timer), daemon-reload, enable, start.\n\n2) create_service() callers are all Hysteria2-only: 2598 (switch_config, inside `if [[ \"$protocol\" == \"hysteria2\" ]]` at 2595), 3216 (add-node flow, HY2 branch; VLESS `else` at 3227-3230 calls install_xray_client + generate_xray_config instead), 3531 (HY2 branch; VLESS else at 3533-3536 calls generate_xray_config), 4509 (quick_install → configure_client at 3555, which is a hysteria-only prompt flow: install_hysteria at 4507, asks 认证密码/带宽, no VLESS path).\n\n3) The second VLESS unit writer in switch_config (2607-2630) also writes xray-client.service inline via printf and never touches the health timer — so neither VLESS path ever creates hysteria-health.timer.\n\n4) The watchdog itself is HY2-specific by construction: health-check.sh (3965-4012) hardcodes `SERVICE=\"hysteria-client.service\"` (3968), greps SOCKS port from /opt/hysteria-client/config.yaml (3993), and restarts only that unit. It never references xray-client. Consequence beyond the claim: on a box that once had an HY2 node (timer exists, is only stopped during TUN at 2052 and re-started at 2195 when leaving TUN), a VLESS SOCKS-mode user gets a timer that every minute finds hysteria-client inactive (switch_config stopped it at 2566) and runs `systemctl restart hysteria-client` (3979-3981) — hysteria-client.service only has `Conflicts=bui-tun.service` (3928), not xray-client, so it will try to bind the same 1080/8080 ports as xray-client (StartLimitBurst=10/300s, 3930-3931). Static reading only; not reproduced on a live client host (baiyi is not in the allowed host list).\n\n5) History: `git log --oneline -S'create_health_check' -- b-ui-client.sh` → single commit 5396e18 \"feat(client): 增强服务自动重启和健康检查功能\" (introduced with HY2 only; never extended). Later TUN-related commits (fbfd9aa, 14768ee) only added the bui-tun early-exit and timer stop/start, not xray awareness. `grep -rn 'create_health_check\\|hysteria-health' --include='*.sh' --include='*.js' .` shows no server-side or panel involvement; the client script is self-contained (downloaded from the panel, not sourced elsewhere).\n\nNet: VLESS-node users get only systemd `Restart=always` crash-restart on xray-client, no hysteria-health.timer connectivity watchdog; HY2-node users get both. Claim is accurate as stated, including line ranges.",
  "deletable_under_v4": false,
  "correction": "Not a correction, an addition: the asymmetry is not just \"missing for VLESS\" — the existing watchdog is hardcoded to hysteria-client.service (b-ui-client.sh:3968), so when it is present on a mixed HY2/VLESS box it actively restarts hysteria-client every minute while a VLESS node is active (3979-3981), competing for the SOCKS/HTTP ports since hysteria-client.service lacks Conflicts=xray-client.service (3928). The code named (generate_xray_config, create_service, create_health_check) is live client functionality, not migration-only code, so v4 clean-reinstall does not make it deletable; the fix is to make the health check protocol-aware and call it from both SOCKS-mode paths.",
  "high_impact": false
 },
 {
  "id": "client-C9",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "File: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/b-ui-client.sh (5810 lines).\n\nCommands run:\n1) grep -n 'systemctl stop\\|stop_all\\|stop_services\\|stop_client\\|_stop_' b-ui-client.sh\n2) grep -nE '^[a-zA-Z_]*stop[a-zA-Z_]*\\(\\)' b-ui-client.sh  -> only stop_spinner (147), stop_tun_mode (2123), cmd_stop (2938). No stop_all_client_services / equivalent helper anywhere.\n3) grep -rn 'stop_all\\|stop_client_services\\|stop_services()' --include='*.sh' . -> no hits (whole repo).\n4) grep -c 'systemctl stop bui-tun' b-ui-client.sh -> 6 (but see per-site analysis: two of those are inside stop_tun_mode itself / its failure path, not \"three-service\" sequences).\n5) git log -S per site: cmd_stop/cmd_restart block last touched bb74c82 \"fix(client): bui-c 全功能 smoke test 后修 4 bug\" (v3.4.32 reset-failed line); uninstall block from 136f226/f1ae3e3 (v3.2.0 TUN rework); _switch_to_profile stop block from 8eaedc5 \"refactor(bui-c): extract _switch_to_profile core function\".\n\nPer-site verification of the claim's six locations (sed -n on each range):\n- start_tun_mode 2007-2035: lines 2018-2029 stop ONLY hysteria-client and xray-client, each conditionally (`if systemctl is-active --quiet hysteria-client ... systemctl stop hysteria-client`; same for xray-client). bui-tun is NOT stopped here (it is about to be started). NOT a three-service sequence.\n- _switch_to_profile 2517+: line 2560 `stop_tun_mode --no-restore` (conditional on tun_was_active), then lines 2564-2566 inside `tui_spin ... bash -c \"systemctl stop '$CLIENT_SERVICE'; systemctl stop xray-client; ...\"` plus a 5s port-release wait and pkill fallback. Two-service stop + separate helper call for TUN. NOT a literal three-service sequence (and it has extra wait/pkill semantics that a naive helper would not replicate).\n- cmd_stop 2938-2947: lines 2940-2942 stop $CLIENT_SERVICE, xray-client, bui-tun + reset-failed. CONFIRMED three-service sequence.\n- cmd_restart 2949-2962: lines 2957-2959 stop $CLIENT_SERVICE, xray-client, bui-tun. CONFIRMED three-service sequence (the claim's \"switch_config-equivalent\" is actually cmd_restart; switch_config at 2964 contains no systemctl stop at all).\n- uninstall 4529+: lines 4555-4557 stop bui-tun, $CLIENT_SERVICE, xray-client (plus pkill -9 sing-box/hysteria/xray, ip link delete), THEN again individually at 4569, 4578, 4587 each guarded by is-active. CONFIRMED (and internally duplicated twice within the same function).\n- tui_service_control 5498+: lines 5537 and 5549 are separate menu cases \"停止 Hysteria2\" and \"停止 Xray\", each stopping ONE service on user choice. NOT a sequence. REFUTED as an instance.\n- auto_update_all 5632-5720: contains NO `systemctl stop` at all — only conditional `systemctl restart` of whichever unit is active (lines ~5684, 5695, 5711). REFUTED as an instance.\n- Additional site the claim missed: legacy numeric menu at 4863 / 4895 — again single-service per menu case, not a sequence.\n\nNet: the structural point (no shared stop helper; the hysteria+xray(+bui-tun) stop logic is hand-written in several places) is CONFIRMED, but the enumeration is wrong: strict three-service stop sequences exist in exactly 3 functions (cmd_stop 2940-2942, cmd_restart 2957-2959, uninstall 4555-4557 + 4569/4578/4587), two-service (hy2+xray) variants in 2 more (start_tun_mode 2021/2028, _switch_to_profile 2565-2566), and two of the six named locations (tui_service_control, auto_update_all) do not contain the pattern. \"At least 6 locations\" of the three-service sequence is overstated; 4-5 sites total with differing semantics (conditional guards, port-wait/pkill fallback, reset-failed).",
  "deletable_under_v4": false,
  "correction": "Actual state (b-ui-client.sh): three-service stop (hysteria-client + xray-client + bui-tun) is hand-written in cmd_stop (2940-2942), cmd_restart (2957-2959) and uninstall (4555-4557, and again guarded at 4569/4578/4587). start_tun_mode (2021, 2028) and _switch_to_profile (2565-2566, inside tui_spin bash -c, followed by port-release wait + pkill -9 fallback; TUN handled via stop_tun_mode --no-restore at 2560) stop only the two SOCKS clients. tui_service_control (5537, 5549) and the legacy menu (4863, 4895) stop single services per menu item, and auto_update_all (5632-5720) issues no systemctl stop at all. No helper named stop_all_client_services or similar exists (only stop_spinner, stop_tun_mode, cmd_stop). This is live client runtime code, not migration code — under v4 it would be consolidated into a helper, not deleted; deletion would change behaviour.",
  "high_impact": false
 },
 {
  "id": "ctrl-C1",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"server/update.sh:1093-1121 (D6 hy2 http→userpass migration) duplicates the exact awk rewrite script already present at server/core.sh:669-692 (apply_hy2_userpass_auth), which core.sh now calls at install time per its own comment at core.sh:660-661.\" No counter-claim, no also_raised_as.\n\n1) Duplication is real, byte-level modulo variable names. Command: `diff <(sed -n '683,687p' server/core.sh | sed 's/^ *//') <(sed -n '1108,1112p' server/update.sh | sed 's/^ *//')` -> only differences are `$tmp_up` vs `$_upf` and `$cfg` vs `$_cfg`; the awk program text `/^auth:/ {print \"auth:\"; print \"  type: userpass\"; print \"  userpass:\"; while ((getline line < upfile) > 0) print line; close(upfile); skip=1; next} skip && /^[a-zA-Z]/ {skip=0} !skip {print}` is identical in server/core.sh:684-686 and server/update.sh:1109-1111. The whole surrounding block (jq -r '.[] | \"    \\(.username): \\(.password)\"' users.json -> mktemp; loop over \"config.yaml:hysteria-server\" \"config-residential.yaml:hysteria-residential\"; guard `grep -qE '^  type: http$'`; chmod 644; `systemctl is-active --quiet svc && systemctl restart svc`) is likewise the same: core.sh:669-692 vs update.sh:1089-1120 (claim's line numbers are off by ~4 lines; the D6 block starts at the `# v3.5.15 D6` comment at update.sh:1089 and ends at 1120; the core.sh comment is at 658-661 with the call at 662). Only deltas: update.sh prints a print_success per service and sets `updated=1`; core.sh wraps it as a function.\n\n2) core.sh really calls it at install time: server/core.sh:662 `apply_hy2_userpass_auth` inside the hysteria config generator, right after users.json is written with the first user (core.sh:522-523, non-empty, so the `[[ -s \"$tmp_up\" ]]` early-return does not trigger) and after config.yaml/config-residential.yaml are written with `type: http` (core.sh:561, 619). `grep -rn apply_hy2_userpass_auth` -> only core.sh:662 (call) and core.sh:669 (def); update.sh never sources core.sh (`grep -n 'source\\|core\\.sh' server/update.sh` shows only download/file-map references at 259, 294, 2135), which is the structural reason the code was copied rather than shared.\n\n3) History: `git log -S'print \"  type: userpass\"' -- server/core.sh server/update.sh` -> both introduced in the same commit 5b6cde3 (2026-06-03, \"fix: v3.5.15 高并发硬化 — hy2 auth http→userpass\"); later only the guard was touched (v3.6.0 `$` anchor, per docs/superpowers/plans/2026-09-10-obfs-kernel-version.md:272,329 which explicitly says \"core.sh apply_hy2_userpass_auth() 有同型守卫则同改\" — i.e. the maintainers already had to patch both copies in lockstep).\n\n4) Runtime invocation of D6: apply_systemd_configs() (update.sh:756) is called from do_update (313), check_and_update (2099, also on \"already latest\"), auto_update (2198, 2226); cron `0 */6 * * * /opt/b-ui/update.sh auto` (core.sh:1223, update.sh:2064); b-ui-cli.sh sources update.sh at 511/520. So D6 runs every 6h on every install.\n\n5) Live hosts (read-only): `ssh bwg-rick` and `ssh -J bwg-rick bwg-tizi` -> both v3.6.3; config.yaml and config-residential.yaml both `type: userpass` (rick lines 24/24, tizi 32/17); installed /opt/b-ui/core.sh contains apply_hy2_userpass_auth (2 hits) and /opt/b-ui/update.sh contains the D6 marker (1 hit); /var/log/b-ui-update.log on both shows historical \"✓ hysteria-server/hysteria-residential auth http→userpass\" lines (D6 did fire in the past — consistent with the pre-v3.6.0 guard bug rewriting on every run). With the anchored guard, D6 is now a no-op on both hosts.\n\n6) Remaining non-migration writers of `type: http` that D6 backstops: update.sh:434-460 repair_hysteria_config() regenerates config.yaml with http auth when auth/trafficStats/masquerade/sniff sections are missing (guard at update.sh:850-863, a corruption self-heal for \"旧版 update.sh 的 sed 范围删除 bug\"), and update.sh:1527-1600 block A regenerates config-residential.yaml with http auth but only when hysteria-residential.service is absent (v3.4→v3.5 path, never on a fresh v4 install). Also web/server.js:302-329 updateHysteriaConfig/updateHysteriaResidentialConfig rewrite the auth block to userpass on every user save, and /auth/hysteria still exists at server.js:2691, so an http-auth config remains functional until then.",
  "correction": "Minor: line ranges in the claim are slightly off — the D6 block is update.sh:1089-1120 (awk at 1108-1112), the core.sh comment is at 658-661 and the call at 662; substance is correct.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "ctrl-C10",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Commands run (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1) `sed -n '1072,1087p' server/update.sh` — D5 block: guard `[[ -f /etc/systemd/system/b-ui-admin.service ]] && [[ ! -f .../b-ui-admin.service.d/10-memory.conf ]] && ! grep -q 'MemoryMax' /etc/systemd/system/b-ui-admin.service` (update.sh:1074-1076), then writes drop-in `/etc/systemd/system/b-ui-admin.service.d/10-memory.conf` with `MemoryHigh=150M / MemoryMax=200M` (update.sh:1078-1082), daemon-reload + restart b-ui-admin (1083-1084).\n2) `grep -n 'MemoryMax\\|b-ui-admin.service.d' server/core.sh server/update.sh server/b-ui-cli.sh install.sh` — core.sh has NO drop-in for b-ui-admin; core.sh:1739-1757 `cat > /etc/systemd/system/b-ui-admin.service` writes the MAIN unit with `MemoryHigh=150M` (1754) and `MemoryMax=200M` (1755) inline. Only b-ui-cli.sh:694 (uninstall) references b-ui-admin.service.d.\n3) `awk` enclosing function: core.sh:1722 `create_services()`; called from install.sh:577 inside `run_core_install()` (install.sh:549), which is the fresh-install/reinstall path (install.sh:557 sources core.sh). Upgrade path (install_type==1, install.sh:839) does not call create_services.\n4) `git log --oneline -S'10-memory.conf'` and `-S'MemoryMax=200M'` on server/core.sh server/update.sh → single commit `2a7af40 2026-06-03 bump: v3.5.14 安装健壮性 + 多用户稳定性根因加固` introduced both the core.sh inline lines (+MemoryHigh=150M/+MemoryMax=200M in the unit heredoc) and the update.sh D5 drop-in in the same release.\n5) Live hosts (read-only): `ssh bwg-rick` and `ssh -J bwg-rick bwg-tizi` with grep/ls/stat/systemctl show:\n   - bwg-rick: main unit mtime 2026-06-03 06:46:35 (pre-v3.5.14 template, no Memory* lines); drop-in 10-memory.conf mtime 2026-06-03 08:13:31 written by D5; `systemctl show -p DropInPaths` = .../10-memory.conf; effective MemoryHigh=157286400 MemoryMax=209715200; /opt/b-ui/core.sh:1755 has MemoryMax=200M; version 3.6.3.\n   - bwg-tizi: main unit mtime 2026-05-09 (no Memory* lines); drop-in 2026-06-03 09:02:23 by D5; same effective values; version 3.6.3.\n   So on both production hosts the limit currently comes ONLY from the D5 drop-in (main unit was never regenerated by v3.5.14+ core.sh), which is exactly the migration case D5 exists for.\n6) `grep -n 'b-ui-admin.service' install.sh` — install.sh:518 `rm -f /etc/systemd/system/b-ui-admin.service` on reinstall prep, but no removal of the .service.d dir (only uninstall via b-ui-cli.sh:694 removes it). A leftover drop-in after a v4 reinstall would carry identical values (150M/200M) → no behaviour difference.\n\nAssessment: the substance is right — D5 and core.sh set identical MemoryHigh/MemoryMax and D5 is a guarded no-op on a fresh install (the `! grep -q MemoryMax` guard at update.sh:1076 skips it when core.sh's unit is present). The wording is wrong in one detail: core.sh does not \"produce a drop-in\"; it writes the limits inline in the main unit file (core.sh:1739-1757), while D5 writes a separate drop-in file. Also, D5 is not dead today: both live hosts are v3.6.3 with pre-v3.5.14 main units and rely on the D5 drop-in for the limit.",
  "correction": "core.sh does not write a drop-in; it embeds MemoryHigh=150M/MemoryMax=200M directly in /etc/systemd/system/b-ui-admin.service (server/core.sh:1739-1757, via create_services() at core.sh:1722, called only on the fresh/reinstall path install.sh:577). update.sh D5 (server/update.sh:1072-1087) writes a separate drop-in /etc/systemd/system/b-ui-admin.service.d/10-memory.conf with the same values and is guarded by `! grep -q MemoryMax` on the main unit (update.sh:1076), so it is a no-op on a fresh install but is the sole source of the limit on both current production hosts (bwg-rick, bwg-tizi: main units predate v3.5.14, limit comes from the D5 drop-in per `systemctl show -p DropInPaths`). Both pieces were introduced together in commit 2a7af40 (v3.5.14).",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "ctrl-C11",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Claim lines verified: server/update.sh:575-612 is migrate_admin_env; server/core.sh:1727-1736 writes ${BASE_DIR}/admin.env (chmod 600) and core.sh:1746 writes `EnvironmentFile=-${BASE_DIR}/admin.env` into b-ui-admin.service. Both were introduced in the same commit (git log -S migrate_admin_env -- server/update.sh -> f403df0 only; git log -S admin.env -- server/core.sh -> f403df0 only; `git show f403df0:version.json | jq .version` = 3.4.18, dated 2026-05-09), never touched since.\n\nCall graph (grep -n 'migrate_admin_env' server/*.sh install.sh web/server.js): only definition at update.sh:575 and ONE caller at update.sh:761 inside apply_systemd_configs() (update.sh:756). apply_systemd_configs is invoked on every update.sh run: :313 (version-changed path), :2099 (v3.5.10 \"already latest\" path), :2198, :2226 (auto/cron path). Cron installs `update.sh auto` every 6h and `update.sh kernel` every 12h (install.sh:850-851). So the function IS reached/invoked every 6h -- the claim's word \"unreachable\" is wrong -- but its body is a no-op: update.sh:577 `[[ -f /opt/b-ui/admin.env ]] && return 0`. The body past :577 only executes when admin.env is absent AND the unit exists, i.e. a pre-3.4.18 install (unit with `Environment=ADMIN_PASSWORD=` lines) being upgraded.\n\nEvery install path that runs core.sh creates admin.env: install.sh:764-800 main() -> install_type 0 (upgrade from legacy /opt/hysteria) and 2 (fresh) both call run_core_install (install.sh:550) -> create_services (install.sh:577 -> core.sh:1722). install_type 1 (already modular) sources update.sh and calls check_and_update, which hits apply_systemd_configs -> migrate_admin_env -> returns at :577. Nothing in the repo ever removes admin.env (grep -rn 'rm .*admin\\.env' server/ web/ install.sh -> none; uninstall at b-ui-cli.sh:692-699 removes unit files but the cli uninstall removes the whole dir separately). Residual edge: if admin.env were deleted by hand on a core.sh-installed host, the body would run but find no Environment=ADMIN_* lines (core.sh-written units have none) and write defaults ADMIN_PASSWORD=admin123 / PORT 8080 (update.sh:585-586) -- a degenerate fallback, not the intended migration. b-ui-cli.sh:415-440 contains a duplicated copy of this same missing-file fallback logic; web/server.js:2646-2674 writes admin.env directly.\n\nLive hosts (read-only): `ssh bwg-rick 'ls -l /opt/b-ui/admin.env; grep -n \"EnvironmentFile\\|^Environment=\" /etc/systemd/system/b-ui-admin.service; jq -r .version /opt/b-ui/version.json'` -> `-rw------- root 212 Jun 3 06:46 /opt/b-ui/admin.env`, unit line 7 `EnvironmentFile=-/opt/b-ui/admin.env`, NO `Environment=` lines, version 3.6.3. `ssh -J bwg-rick bwg-tizi '<same>'` -> `-rw------- root 212 May 9 11:21 /opt/b-ui/admin.env`, unit line 7 EnvironmentFile only, version 3.6.3. bwg-tizi's admin.env mtime (May 9 11:21) coincides with the v3.4.18 release date, meaning migrate_admin_env DID execute its body once there historically (it was a pre-3.4.18 install upgraded via update.sh); bwg-rick's (Jun 3) matches its clean reinstall via core.sh. On both hosts today the guard at update.sh:577 returns immediately on every cron run.\n\nConclusion: the claim's substance is right (body is dead on any host installed by current core.sh, and on both production hosts), but \"unreachable\" is inaccurate -- the function is called on every update.sh/cron run and short-circuits at :577; the body is reachable only on the pre-3.4.18 upgrade path, which is exactly the migration-only code v4 abandons.",
  "correction": "Not \"unreachable\": migrate_admin_env is invoked every update.sh run via apply_systemd_configs (server/update.sh:761; apply_systemd_configs called at update.sh:313, :2099, :2198, :2226, cron every 6h/12h per install.sh:850-851). It returns immediately at update.sh:577 whenever /opt/b-ui/admin.env exists, which core.sh:1727-1736 guarantees on every core.sh install; only pre-3.4.18 installs (unit with Environment=ADMIN_PASSWORD=) ever execute the body (bwg-tizi's admin.env dated May 9 shows it ran once there during the v3.4.18 upgrade). So: reached but effectively a no-op on current installs.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "ctrl-C12",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Repo: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5\n\n1) Script heredoc identity. Extracted core.sh setup_cert_sync heredoc body (server/core.sh:931-999, between `cat > \"$sync_script\" << 'SYNCEOF'` at :930 and SYNCEOF at :1000) and the update.sh block body (server/update.sh:1437-1504, heredoc at :1436, SYNCEOF at :1505):\n  sed -n '931,999p' server/core.sh > core-sync.sh; sed -n '1437,1504p' server/update.sh > upd-sync.sh; diff core-sync.sh upd-sync.sh\n  -> only hunk: core.sh:989-991 (3 comment lines \"# 证书变了 → 重启所有在跑的 hysteria 实例 / # hysteria 仅在启动时读证书... / # 两个实例...\") vs update.sh:1495-1496 (same idea in 2 comment lines). So it is a 3-line-vs-2-line comment block, not literally \"one comment line\", but zero code differs.\n  diff <(grep -v '^\\s*#' core-sync.sh | grep -v '^\\s*$') <(grep -v '^\\s*#' upd-sync.sh | grep -v '^\\s*$') -> rc=0; comment-stripped md5 of both = 9a7d56d7b3e6b4f59ef4fe32df48380e.\n\n2) Service unit identity. core.sh:1008-1017 writes b-ui-cert-sync.service with ExecStart=${sync_script} where sync_script=\"${BASE_DIR}/cert-sync.sh\" and BASE_DIR=\"/opt/b-ui\" (core.sh:18). update.sh:1508-1517 writes the same unit with literal ExecStart=/opt/b-ui/cert-sync.sh. diff after substitution -> rc=0 (identical: Description, After=caddy.service, Type=oneshot, same ExecStartPre until-caddy loop). update.sh does not touch the timer (core.sh:1020 writes it); nothing else in the block.\n\n3) Guard / trigger. update.sh:1435 `if [[ -f /opt/b-ui/cert-sync.sh ]] || [[ -f /etc/systemd/system/b-ui-cert-sync.service ]]` — only overwrites an existing deployment, exactly as claimed; no content guard, so it fires on EVERY apply_systemd_configs run (called at update.sh:313, :2099 \"already latest\" path, :2198, :2226 cron auto path) and sets updated=1 (:1519) -> only consequence is `systemctl daemon-reload` (:1775-1777) and the print_success at :1786. Not a behaviour change.\n\n4) History: git log -S'仅当老服务器已有 cert-sync 部署时才覆盖' -- server/update.sh and git log -S'找不到/不完整时 exit 0' -- server/core.sh both -> single commit 92e5bc4 2026-05-09 (version.json at that commit = 3.4.17). Both heredocs were introduced together in one commit and never diverged; the block exists solely to migrate pre-3.4.17 installs whose cert-sync.sh still had `exit 1` on missing cert.\n\n5) Live hosts (read-only):\n  ssh bwg-rick 'md5sum /opt/b-ui/cert-sync.sh; md5sum /etc/systemd/system/b-ui-cert-sync.service; stat ...; grep -c \"cert-sync 修复 race condition\" /var/log/b-ui-update.log'\n  ssh -J bwg-rick bwg-tizi '<same>'\n  Both hosts: cert-sync.sh md5 890306ea91e780a5b0f126ce3ac802ca == md5sum of the update.sh heredoc (upd-sync.sh incl. shebang); service md5 a1c369b080659bbddbd908a4afa9fb0e == local upd-svc; both version 3.6.3; mtimes 2026-09-11 06:03:55 (rick) / 06:11:55 (tizi) = today's cron run; the block's success line appears 362 (rick) / 469 (tizi) times in b-ui-update.log — it rewrites the same bytes every 6h. Deployed content on both hosts is exactly what core.sh would produce (modulo the comment wording, which the block overwrites).\n\nRefutation attempts that failed: no code-level difference found; no consumer reads the comment text; the timer is untouched by the block; the only side effects (file mtime, forced daemon-reload, log line) do not change behaviour.",
  "deletable_under_v4": true,
  "correction": "Minor precision: the difference is a 3-line comment block (core.sh:989-991) vs a 2-line comment block (update.sh:1495-1496), not \"one comment line\"; and \"pure no-op\" holds for content/behaviour but the block does have trivial side effects on every run (rewrites both files, forces updated=1 -> systemctl daemon-reload at update.sh:1775-1777, and logs \"✓ cert-sync 修复 race condition\" — 362/469 times on bwg-rick/bwg-tizi). Under a v4 clean reinstall core.sh:923-1037 setup_cert_sync produces functionally identical files, so update.sh:1433-1521 can be deleted outright.",
  "high_impact": false
 },
 {
  "id": "ctrl-C13",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json, id ctrl-C13, no counter-claim, no also_raised_as): two independently maintained heredoc copies of hy2-portjump-cleanup.sh in server/update.sh:932-978 and server/core.sh:139-186 that \"have already drifted in their comments\".\n\n1) Two copies exist — CONFIRMED. `grep -n hy2-portjump-cleanup -r server install.sh web b-ui-client.sh version.json`: core.sh:139 `cat > /opt/b-ui/hy2-portjump-cleanup.sh <<'CLEANUP_EOF'` … core.sh:190 chmod; update.sh:932 same `cat >` … update.sh:979 chmod, update.sh:980 `rm -f /opt/b-ui/hy2-nft-cleanup.sh`. The script is not in version.json's file list, so it is never downloaded as a file — only written by these two heredocs. Consumers: core.sh:230 and :267 (ExecStartPre in both hy2 units), update.sh:918, :1561, :1636 (same ExecStartPre lines in migration blocks).\n\n2) Comments differ, code identical — CONFIRMED for content, but \"drifted\" is wrong wording. Extracted both heredocs with awk to scratchpad and diffed: core copy 49 lines (md5 3739e802…), update copy 45 lines (md5 c103fd61…). `diff` shows only 3 hunks, all comment/blank lines (header comment 5 lines vs 3 lines; \"# 后端 A\" / \"# 后端 B\" comments shortened). `diff <(grep -v '^\\s*#' core | grep -v '^\\s*$') <(same for update)` → CODE-IDENTICAL.\n\n3) It is NOT drift that accrued — REFUTES the \"drifted\" framing. `git log -S'hy2-portjump-cleanup.sh' -- server/core.sh server/update.sh` → only 2a7af40 (2026-06-03, v3.5.14) introduced it; `git log -L` on both regions shows exactly one later touch, 0ce9df2 (v3.5.16). Re-extracting both heredocs at 2a7af40: already DIFFER (full) in the same three comment hunks, code identical (40 vs 37 lines). At 0ce9df2: DIFFER (full) only in comments, code identical (49 vs 45 lines). I.e. the update.sh copy was deliberately written with abbreviated comments from birth, and the only subsequent change (v3.5.16 dual-locator by --dport range) was applied to BOTH copies in the same commit in lockstep. There is zero evidence of divergence over time; the comment difference is intentional terseness, not drift.\n\n4) Maintenance cost is real but the core.sh copy is effectively dead on every live host: update.sh:868 `if [[ -d /etc/systemd/system/hysteria-server.service.d ]]; then` (no version gate) rewrites the file on every `update.sh auto` run inside apply_systemd_configs. Live check: `ssh bwg-rick 'md5sum /opt/b-ui/hy2-portjump-cleanup.sh; wc -l …'` → c103fd6136d261bcda89b451cd8b603c, 45 lines (= update.sh copy); `ssh -J bwg-rick bwg-tizi …` → same md5 c103fd61…, 45 lines; both at version 3.6.3, both units carry `ExecStartPre=-/opt/b-ui/hy2-portjump-cleanup.sh /opt/b-ui/config.yaml` and `… /opt/b-ui/config-residential.yaml`. So on any installed host the core.sh version of the file survives at most until the first 6-hourly cron update, after which the update.sh copy wins (behaviourally identical). The commit 0ce9df2 having to touch both heredocs is the concrete maintenance cost the claim points to; the \"already drifted\" wording overstates it.",
  "correction": "The two copies have never diverged in code and the comment difference did not \"drift\" — it existed from the very first commit that introduced the script (2a7af40, v3.5.14) and both copies were edited together in the only later change (0ce9df2, v3.5.16). The comment differences are: core.sh:141-145 five-line header vs update.sh:934-936 three-line header; core.sh:160 vs update.sh:955 (\"后端 A\" comment); core.sh:181 vs update.sh:977 (\"后端 B\" comment). Code lines are byte-identical. The true observation is: duplication forces every fix to be applied twice (proven by 0ce9df2), and the update.sh copy (update.sh:868 gate, runs every update) is what actually lives on both production hosts.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "ctrl-C14",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): three near-identical SSH-hardening implementations at core.sh:1867-1935, update.sh:614-659, b-ui-cli.sh:1157-1208. No counter-claim, no also_raised_as.\n\nLine ranges verified exactly (awk 'NR>=START && /^}/' on each file): core.sh harden_ssh() opens 1867 closes 1935; update.sh migrate_ssh_hardening() opens 614 closes 659; b-ui-cli.sh cmd_harden_ssh() opens 1157 closes 1208.\n\nBodies are near-identical (read all three in full):\n- Same pubkey detection: `grep -cE '^[^#]*\\s*(ssh-(rsa|ed25519|dss)|ecdsa-sha2-)' /root/.ssh/authorized_keys` + `${pubkey_count//[^0-9]/}` sanitizer — core.sh:1872-1877, update.sh:627-632, b-ui-cli.sh:1163-1168.\n- Same 5-line drop-in heredoc (`# B-UI SSH 加固（00- 前缀...）/ PasswordAuthentication no / PermitRootLogin prohibit-password / KbdInteractiveAuthentication no / ChallengeResponseAuthentication no`) — core.sh:1901-1907, update.sh:642-648, b-ui-cli.sh:1184-1190.\n- Same `rm -f 99-b-ui-hardening.conf` + write 00-b-ui-hardening.conf + chmod 644 + `sshd -t` then reload sshd||ssh||restart, else rm conf and report rollback — core.sh:1895-1931, update.sh:641-658, b-ui-cli.sh:1178-1206.\n- b-ui-cli.sh:1156 comment literally admits it: \"与 core.sh 的 harden_ssh 同款逻辑（避免 source core.sh 引入意外副作用）\". version.json changelog 3.4.37 confirms the divergence bit them once (\"cmd_harden_ssh 在 b-ui-cli.sh 是独立实现没走 core.sh 的 harden_ssh ... agent 修 core.sh 的幂等逻辑对 b-ui harden-ssh 命令无效\"), and 3.5.20 says \"core.sh/update.sh/b-ui-cli.sh 三处同步\" for the 99→00 rename.\nResidual differences: core+cli have the content-compare idempotency guard (core.sh:1910, cli:1192); update.sh instead early-returns on file existence (614:623) and on the `.ssh-not-hardened` marker (614:625); update.sh additionally does the 99→00 mv (617-621); cli exits instead of returning.\n\nAll three are live (not dead):\n- harden_ssh: install.sh:557 `source \"${BASE_DIR}/core.sh\"` then install.sh:589 `harden_ssh` (fresh-install path).\n- migrate_ssh_hardening: update.sh:762 inside apply_systemd_configs(), which runs from do_update (update.sh:313), from check_and_update even when version unchanged (2099), and from the auto path (2198, 2226); `update.sh auto` is cron'd every 6h (core.sh:1223, install.sh:850).\n- cmd_harden_ssh: dispatcher b-ui-cli.sh:1248 `harden-ssh) cmd_harden_ssh ;;`, documented in help at 1271; core.sh:1881 tells users to run `b-ui harden-ssh`. web/server.js and app.js have no shell-out to it (grep 'harden' web/ → no hits).\n\nGit: `git log -S'harden_ssh'` → d6f0312 (v3.2.4 introduced), f403df0 (stability round 2 — same commit introduced migrate_ssh_hardening and cmd_harden_ssh). Last touched together in the v3.5.20 99→00 rename.\n\nCommands run: jq claim extraction; grep -n -i 'harden_ssh|harden-ssh|migrate_ssh|b-ui-hardening' over install.sh server/*.sh web/*.js b-ui-client.sh; sed -n of the three ranges; awk closing-brace check; grep apply_systemd_configs call sites; grep cron lines; git log -S for the three identifiers. No SSH to live hosts (claim is purely about repo code).",
  "deletable_under_v4": false,
  "correction": "Not a refutation, but a precision note on deletability: of the three copies, only update.sh migrate_ssh_hardening (614-659) is migration-flavoured. On a fresh v4 install harden_ssh always runs first (install.sh:589) and leaves either 00-b-ui-hardening.conf or /opt/b-ui/.ssh-not-hardened (core.sh:1883), so migrate_ssh_hardening returns at 623/625 on every cron cycle; the 99→00 mv (617-621) is pure legacy. The one edge where deleting it changes behaviour: if `sshd -t` fails at install time, core.sh:1929-1931 removes the conf without creating the marker, and migrate_ssh_hardening would then retry hardening every 6h. harden_ssh (install) and cmd_harden_ssh (manual `b-ui harden-ssh`, the documented remedy when no pubkey exists at install) are the capability itself and cannot be deleted — they should be collapsed into one shared function (e.g. keep core.sh harden_ssh and have the CLI source/call it, or move it to a small shared lib), not removed.",
  "high_impact": false
 },
 {
  "id": "ctrl-C15",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Line ranges and existence CONFIRMED: `grep -n update_service_paths install.sh server/update.sh` -> install.sh:374 (call), install.sh:378 (def, ends 422 via awk); server/update.sh:310 (call inside do_update(), def at 368, ends 406). Only two callers in the whole repo (core.sh, b-ui-cli.sh, client, web/ have zero hits).\n\n\"Verbatim copies\" REFUTED: extracted both bodies with awk and ran `diff` -> exit 1, 45 vs 39 lines. install.sh wraps the hysteria-server / b-ui-admin / xray blocks in an extra `if [[ -f \"<unit>\" ]]; then ... fi` (install.sh:382,401,410) while update.sh relies on `grep -q ... 2>/dev/null` alone (update.sh:372,388,395). The override.conf block and the daemon-reload tail are textually identical. Functionally the two are equivalent (grep -q on a missing file with 2>/dev/null is false either way), so it is a functional duplicate, not a verbatim one.\n\nPurpose CONFIRMED: both only `sed -i 's|/opt/hysteria|/opt/b-ui|g'` on 4 systemd files + daemon-reload. Git: `git log -S update_service_paths` -> introduced 2025-12-11 in d66f767 (install.sh, path move /opt/hysteria->/opt/b-ui) and copied into update.sh in 34c6a8c same day (\"fix: 更新流程中自动修正服务配置路径\"); never touched since. install.sh copy is only reached from the OLD_BASE_DIR migration branch (install.sh:341,345 `[[ -d /opt/hysteria ]] && [[ ! -d /opt/b-ui ]]`); update.sh copy runs unconditionally at update.sh:310 on every do_update but is a no-op unless a unit still contains /opt/hysteria.\n\nRuntime relevance: core.sh writes every unit with /opt/b-ui paths (core.sh:206,253,1747-1748,1774), so on a fresh install neither function ever matches. Live check (read-only, one ssh each; bwg-tizi via -J bwg-rick): `test -d /opt/hysteria` -> no on both; `grep -l /opt/hysteria` over hysteria-server.service, override.conf, b-ui-admin.service, xray.service -> empty on both; both on v3.6.3. /opt/hysteria is the legacy b-ui-server.sh v3.1 layout (not in version.json files list).",
  "deletable_under_v4": true,
  "correction": "The two functions are not verbatim copies: install.sh:378-422 adds an outer `[[ -f unit ]]` guard around three of the four blocks (install.sh:382,401,410) that server/update.sh:368-406 lacks (update.sh:372,388,395 use bare `grep -q ... 2>/dev/null`). They are behaviourally equivalent duplicates of a /opt/hysteria->/opt/b-ui (pre-v3.2, Dec 2025) unit-path migration. Under the v4 clean-reinstall decision both copies (plus install.sh's OLD_BASE_DIR data-migration block at 341-376 that is the install.sh caller) can be deleted: core.sh writes units with /opt/b-ui, and both production hosts have no /opt/hysteria dir and no unit file referencing it.",
  "high_impact": false
 },
 {
  "id": "ctrl-C16",
  "lenses": [
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): install_type==0 branch unreachable on any host that has completed a modular install. No counter-claim, no also_raised_as.\n\nStatic gate (install.sh, worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- install.sh:429-432: `if [[ -f \"${BASE_DIR}/version.json\" ]]; then ... return 1` — checked FIRST, before the legacy test.\n- install.sh:436: `if [[ -f \"/usr/local/bin/b-ui\" ]] && [[ ! -f \"${BASE_DIR}/version.json\" ]]; then ... return 0` — the only path to install_type 0 (install.sh:784-790).\n- BASE_DIR is hard-coded `/opt/b-ui` in install.sh:22 (no env override), so the two tests always look at the same dir.\n\nRuntime-invocation lens — every way version.json can be absent while /usr/local/bin/b-ui exists, on a modular host:\n1. Fresh install ordering: download_all_files() downloads `version.json:${BASE_DIR}/version.json` as the FIRST entry (install.sh:240); any failed file → `return 1` (install.sh:268-271) and `set -e` (install.sh:9) aborts before /usr/local/bin/b-ui is ever created (create_global_command at install.sh:583 runs after run_core_install; the `ln -sf` at install.sh:806 runs after the case). So a partial fresh install can never leave b-ui present + version.json absent.\n2. Update path: update.sh:258 downloads version.json into the file list, update.sh:2134/2192 `cp -f` it; nothing in install.sh/core.sh/update.sh/b-ui-cli.sh ever rm/mv's version.json (grep -nE 'version\\.json' over all four scripts: only reads/cp). update.sh:324, 2017-2020 (ensure_cli_exists) only create the symlink, never remove version.json.\n3. Uninstall: b-ui-cli.sh:703-704 removes `/usr/local/bin/b-ui` AND `rm -rf /opt/b-ui` in the same block → both gone → next run returns 2 (fresh), not 0.\n4. Cron/systemd/panel: no invocation of install.sh anywhere at runtime — `grep -rn 'install\\.sh' server/*.sh web/server.js b-ui-client.sh` only hits the unrelated Content-Disposition filename in web/server.js:1611; live crontabs on both hosts only run /opt/b-ui/update.sh auto|kernel and cert-check.sh; `grep -rl install.sh /etc/systemd/system/` empty on both hosts. install.sh is only ever run by hand via `bash <(curl …)`, and its type detection is the code above.\n5. The only real producer of the 0-state is the legacy single-file product: b-ui-server.sh:10 `SCRIPT_VERSION=\"3.1.2\"` and b-ui-server.sh:972-993 writes a heredoc /usr/local/bin/b-ui, with data in /opt/hysteria; migrate_old_path (install.sh:343-376) copies /opt/hysteria → /opt/b-ui WITHOUT a version.json, so install.sh:436 fires. b-ui-server.sh is not in version.json files[] (jq '.files' → 14 entries, none is b-ui-server.sh), so no modular host ever receives it. This matches the claim's scoping (\"unreachable on any host that has ever completed a modular install\"), while the branch is genuinely reachable for a v3.1 host — the claim does not deny that.\n\nLive hosts (read-only):\n`ssh bwg-rick 'ls -ld /opt/hysteria; ls -l /usr/local/bin/b-ui; jq -r .version /opt/b-ui/version.json'` → \"No such file or directory\"; `/usr/local/bin/b-ui -> /opt/b-ui/b-ui-cli.sh`; 3.6.3.\n`ssh -J bwg-rick bwg-tizi '<same>'` → same: no /opt/hysteria, symlink to b-ui-cli.sh, version.json 3.6.3.\nBoth hosts are firmly in the return-1 state; neither has legacy /opt/hysteria data that could ever trigger migrate_old_path. (Note: on a modular host `grep -oP 'SCRIPT_VERSION=\"\\K…'` against the symlinked b-ui-cli.sh would also yield \"未知\" since b-ui-cli.sh:35 uses `SCRIPT_VERSION=$(get_version)` — further evidence the branch was designed only for the heredoc legacy launcher.)\n\nCould not find any refutation: no runtime path (cron, systemd, panel spawn, update.sh, uninstall, partial-download failure) produces \"/usr/local/bin/b-ui present AND /opt/b-ui/version.json absent\" on a host that once completed a modular install; only manual `rm /opt/b-ui/version.json` would, which is not a code path.",
    "deletable_under_v4": true,
    "correction": "Minor precision only: the branch is not dead code in the absolute sense — it is the live upgrade path for legacy b-ui-server.sh v3.1.x hosts (/opt/hysteria + heredoc /usr/local/bin/b-ui). It is dead for every modular host and for both production servers (neither has /opt/hysteria). Under v4 clean re-install, migrate_old_path + update_service_paths (install.sh:343-422), the install.sh:436-458 legacy branch, restore_backup (install.sh:671-682), case 0 (install.sh:784-790), and the duplicated /tmp-backup restore + update_service_paths in server/update.sh:300-310 and :368 can all be deleted: on a fresh host /opt/hysteria is absent (migrate_old_path no-op) and /usr/local/bin/b-ui is absent (return 2), so behaviour is identical."
   },
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): install_type==0 branch unreachable on any host that ever completed a modular install; no COUNTER, no ALSO ids.\n\nCall graph (install.sh, single entry: `main \"$@\"` at install.sh:869, `set -e` at :9):\n- main :764 -> select_download_source -> `set +e; check_old_version; install_type=$?; set -e` (:779-782) -> `case $install_type` :784. Case `0)` is :785-790 (download_all_files, restore_backup, run_core_install). After the case, :805-808 unconditionally `ln -sf ${BASE_DIR}/b-ui-cli.sh /usr/local/bin/b-ui` when b-ui-cli.sh exists.\n- check_old_version :424-463: calls migrate_old_path :343-376 (only acts if `-d /opt/hysteria && ! -d /opt/b-ui`, copies users/config/xray/keys, then update_service_paths :378-422). Then :429 `[[ -f ${BASE_DIR}/version.json ]] -> return 1`; :436 `[[ -f /usr/local/bin/b-ui ]] && [[ ! -f version.json ]] -> return 0`; else return 2. check_old_version/migrate_old_path/restore_backup have no other callers: `grep -n 'check_old_version\\|migrate_old_path\\|restore_backup' install.sh server/*.sh` -> only install.sh:343,374,378,424,426,671,780,788 (update.sh has its own separate update_service_paths :368, not install.sh's).\n- Producers of /usr/local/bin/b-ui as a REGULAR file (the only thing that satisfies `-f` when version.json is gone): install.sh create_global_command :621-631 (heredoc wrapper, called from run_core_install :583, but always overwritten by the symlink at :806 unless install aborts between :583 and :806) and the legacy b-ui-server.sh :972-993 (BASE_DIR=/opt/hysteria wrapper). Modular paths always end with a symlink: install.sh:806, update.sh:324, update.sh ensure_cli_exists :2017-2020. A symlink whose target /opt/b-ui/b-ui-cli.sh is gone fails `-f` (follows links), so `rm -rf /opt/b-ui` on a modular host yields install_type=2, not 0.\n- Producers/consumers of version.json: install.sh download_all_files :240 writes it in every fresh/legacy-upgrade run; update.sh :2134 replaces it via download_and_validate (:50-90: tmp + validate + atomic `mv -f`, never deletes on failure). Nothing in core.sh/update.sh/b-ui-cli.sh removes version.json except the full uninstall b-ui-cli.sh:703-704, which also `rm -f /usr/local/bin/b-ui` -> next run is install_type=2. `grep -n 'rm -rf' server/core.sh install.sh` shows no removal of $BASE_DIR.\n- Therefore the state (regular-file /usr/local/bin/b-ui AND no /opt/b-ui/version.json) can arise only from (a) a legacy b-ui-server.sh v3.1 host (/opt/hysteria), which is exactly what the branch targets, or (b) manual tampering (install aborted between :583 and :806 AND later manual `rm -rf /opt/b-ui`). No install.sh/update.sh-driven path produces it after a completed modular install.\n\nLive hosts (read-only):\n`ssh bwg-rick 'ls -la /usr/local/bin/b-ui; readlink -f ...; ls -la /opt/b-ui/version.json; jq -r .version ...; test -d /opt/hysteria && echo OLD_DIR_EXISTS || echo NO_OLD_DIR; grep -rl /opt/hysteria /etc/systemd/system/...'` -> `/usr/local/bin/b-ui -> /opt/b-ui/b-ui-cli.sh`, version.json present (3.6.3), NO_OLD_DIR, no systemd unit references /opt/hysteria.\n`ssh -J bwg-rick bwg-tizi '<same>'` -> identical: symlink to /opt/b-ui/b-ui-cli.sh, version.json 3.6.3, NO_OLD_DIR, no /opt/hysteria in units.\nSo on both production hosts check_old_version returns 1 at :429 before the :436 test is ever evaluated; migrate_old_path is a no-op (:345 false).\n\nNuance not affecting verdict: the claim's line refs are accurate (migrate_old_path 343-376, check_old_version 424-463, case 0 at 784-790). The wording \"cannot arise from any install.sh-driven upgrade path\" is correct for modular hosts; the branch IS reachable (by design) on a legacy /opt/hysteria + b-ui-server.sh host, which neither live host is.",
    "deletable_under_v4": true,
    "correction": "Minor: install.sh also contains a second, still-called legacy remnant create_global_command (:621-631, called at :583) that writes /usr/local/bin/b-ui as a regular-file wrapper which :806 immediately replaces with a symlink; and update.sh carries its own copy of update_service_paths (:368-400, called at :310) doing the same /opt/hysteria -> /opt/b-ui sed. Under a v4 clean reinstall all of these (install.sh migrate_old_path/update_service_paths/check_old_version's :436-461 block, restore_backup :671-682, case 0 :785-790, create_global_command, update.sh:310/368-400) are dead together; check_old_version can collapse to `[[ -f version.json ]] -> update : fresh`. The /tmp/b-ui-*-backup.* files consumed by restore_backup are also produced by update.sh:240-241 for its own path, so deleting install.sh's restore_backup does not affect update.sh."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM text (jq on docs/superpowers/audits/2026-09-11-claims.json): install_type==0 branch unreachable on any host that has completed a modular install. COUNTER: none. ALSO: none.\n\nCODE (worktree HEAD, install.sh):\n- install.sh:424-433 check_old_version(): calls migrate_old_path, then `if [[ -f \"${BASE_DIR}/version.json\" ]]; then ... return 1` — any host with /opt/b-ui/version.json is routed to the update path before the legacy test is ever evaluated.\n- install.sh:435-436: legacy branch guard `[[ -f \"/usr/local/bin/b-ui\" ]] && [[ ! -f \"${BASE_DIR}/version.json\" ]]`, then greps `SCRIPT_VERSION=\"...\"` out of /usr/local/bin/b-ui.\n- install.sh:240 download_all_files lists `version.json:${BASE_DIR}/version.json` FIRST; install.sh:267-270 returns 1 (under set -e => abort) if any file failed, so a modular install can never finish with version.json missing.\n- server/update.sh:324-325 and :2017-2020 create /usr/local/bin/b-ui as a SYMLINK to ${BASE_DIR}/b-ui-cli.sh; server/b-ui-cli.sh:703-704 uninstall_all removes /usr/local/bin/b-ui AND /opt/b-ui together, so post-uninstall the host is type 2 (fresh), never type 0.\n- The ONLY producer of the type-0 state is the legacy installer b-ui-server.sh (not in version.json, not called by install.sh — `grep -rn b-ui-server version.json install.sh server/*.sh` -> no hits): b-ui-server.sh:23 BASE_DIR=/opt/hysteria; :972-993 writes /usr/local/bin/b-ui as a regular heredoc file (which contains NO `SCRIPT_VERSION=` line, so install.sh:436 would print \"未知\"); :916 downloads version.json into /opt/hysteria, but migrate_old_path (install.sh:343-376) copies only users.json/config.yaml/xray-config.json/reality-keys.json — not version.json — so after migration /opt/b-ui/version.json is absent and type 0 fires. I.e. the branch is reachable only from a v3.1.x /opt/hysteria host, never from a modular one.\n- Note: b-ui-cli.sh:35 contains `SCRIPT_VERSION=$(get_version)`; on both live hosts `grep -c SCRIPT_VERSION= /usr/local/bin/b-ui` = 3 via the symlink. So the only way a modular host could enter the branch is manual deletion of /opt/b-ui/version.json while keeping b-ui-cli.sh — not an install.sh/update.sh/uninstall path.\n\nHISTORY:\n- `git log --oneline -S'check_old_version' -- install.sh` -> 97aa45d, 808f081 (2025-12-11 \"feat: 模块化重构\"). At 808f081 install.sh:23 BASE_DIR=\"/opt/hysteria\" and check_old_version had no version.json test (only `-f /usr/local/bin/b-ui`).\n- `git log --oneline -S'migrate_old_path' -- install.sh` / `-S'OLD_BASE_DIR'` -> d66f767 (2025-12-11 \"refactor: 将安装路径从 /opt/hysteria 改为 /opt/b-ui\"); c2c3187 \"fix: 修复版本检测逻辑 - 优先检查 version.json\" added the version.json-first guard. All targeting the Dec-2025 v3.1 -> modular hop; no changelog entry after that touches it.\n\nLIVE (read-only):\n`ssh bwg-rick '...'` and `ssh -J bwg-rick bwg-tizi '...'`: both hosts: `/usr/local/bin/b-ui -> /opt/b-ui/b-ui-cli.sh` (symlink, Sep 11), `/opt/hysteria dir: NO`, `/opt/b-ui/version.json` = 3.6.3, `units referencing /opt/hysteria: 0`, no /tmp/b-ui-*backup* files. Neither production host is or ever will be in the type-0 state; only the reality-keys.json backups (/root/.b-ui-backup, /var/backups/b-ui) from the separate Cluster-I logic exist.",
    "correction": "Claim conclusion holds; its stated reasoning is slightly off: the type-0 state is not \"impossible\" in general — it is exactly what a legacy b-ui-server.sh (v3.1.x, /opt/hysteria, regular-file /usr/local/bin/b-ui) host produces after migrate_old_path (install.sh:343-376, which deliberately omits version.json). It is unreachable from modular hosts because install.sh:429-433 returns 1 whenever version.json exists, download_all_files (install.sh:240,267-270) cannot complete without version.json, and uninstall_all (b-ui-cli.sh:703-704) removes the symlink and /opt/b-ui together. Also install.sh:436's SCRIPT_VERSION grep never matches the legacy wrapper (b-ui-server.sh:972-993 has no such line) so the branch would always report \"未知\".",
    "deletable_under_v4": true
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "ctrl-C17",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"install_tui_tools() is defined identically in both install.sh:688-758 and server/update.sh:2237-2297, but update.sh's copy is only reachable via update.sh's own direct-execution dispatch block ... which no sourcing caller (b-ui-cli.sh:511/520, install.sh:793) or cron invocation ever exercises.\" No counter-claim, no also_raised_as.\n\nPART 1 — \"defined identically\": REFUTED.\nCommand: `diff <(awk '/^install_tui_tools\\(\\) \\{/{s=1} s{print} s && /^}/{exit}' install.sh) <(same on server/update.sh)` → non-empty diff. install.sh:711-719 and :740-748 use `local gum_bin=\"\"; ... gum_bin=$(find \"$tmp\" -name \"gum\" -type f | head -1); if [[ -n \"$gum_bin\" ]]; then install -m 755 \"$gum_bin\" /usr/local/bin/gum` (same for fzf), whereas server/update.sh:2260-2261 / :2284-2285 still do `if curl -fsSL \"$gum_url\" -o \"$tarball\" && tar -xz ...; then install -m 755 \"$tmp/gum\" /usr/local/bin/gum` (hard-coded extraction path, no `2>/dev/null` on curl). Git: `git log -S install_tui_tools -- install.sh server/update.sh` → only 4391cc2 2026-05-07 \"feat(install): auto-install gum and fzf TUI tools\"; `git log -S gum_bin -- install.sh server/update.sh` → 37016f1 2026-05-07 \"fix(install): use find to locate gum/fzf binary after tar extraction\" touched install.sh ONLY. So the two copies diverged the same day they were introduced; update.sh holds the older, un-fixed variant. Line ranges in the claim are otherwise correct (awk on `^install_tui_tools() {`…`^}`: install.sh 688-758, server/update.sh 2237-2297; dispatch block server/update.sh:2300-2326 with `[[ \"${BASH_SOURCE[0]}\" == \"${0}\" ]]` at :2300).\n\nPART 2 — reachability: CONFIRMED.\n`grep -rn install_tui_tools --include='*.sh' --include='*.js' .` (excluding docs) → exactly 4 hits: install.sh:586 (call, inside main install path after create_global_command :583), install.sh:688 (def), server/update.sh:2237 (def), server/update.sh:2322 (call). The :2322 call sits in the `*)` (no-arg/unknown-arg interactive) branch of the direct-exec case at :2302-2325, guarded by `if ! command -v gum || ! command -v fzf` (:2321). All in-repo invokers of update.sh (`grep -rn \"update\\.sh\" --include='*.sh' --include='*.js' . | grep -v docs/`): install.sh:793 `source \"${BASE_DIR}/update.sh\"` then `check_and_update` (:794); server/b-ui-cli.sh:511 and :520 `source \"${BASE_DIR}/update.sh\"` (check_bui_update / update_kernel); cron lines install.sh:850-851, server/core.sh:1223-1224, server/update.sh:2064-2065 all pass `auto` or `kernel`. web/server.js has no update.sh shell-out. No caller passes no args or `-y`. Nothing else in the sourced context (b-ui-cli.sh, install.sh, core.sh) calls install_tui_tools after sourcing. Live check `ssh bwg-rick`: crontab shows only `/opt/b-ui/update.sh auto` and `/opt/b-ui/update.sh kernel`; deployed /opt/b-ui/update.sh (v3.6.3) has the same def :2237 / guard :2300 / call :2322; /usr/local/bin/b-ui is b-ui-cli.sh (sources, never execs update.sh); `command -v gum fzf` → neither installed on bwg-rick, so the CLI runs on its numeric fallback regardless (b-ui-cli.sh:116-158 gum wrappers are all `command -v gum &&`-guarded). The only way to hit update.sh's copy is a human manually running `/opt/b-ui/update.sh` with no arguments — an undocumented path (README/docs only mention `auto`/`kernel`).\n\nNet: the reachability/redundancy point stands; the word \"identically\" is wrong — update.sh's copy is a stale pre-37016f1 duplicate.",
  "deletable_under_v4": true,
  "correction": "The two copies are NOT identical: install.sh received fix 37016f1 (find-based binary location, install.sh:711-719 / :740-748) while server/update.sh:2260-2261 / :2284-2285 still install from the hard-coded `$tmp/gum` / `$tmp/fzf` path. Reachability part is correct. Under v4 the update.sh copy (server/update.sh:2233-2297) plus its only call site (:2320-2323) can be removed with no change to any product path (fresh install installs the tools via install.sh:586; cron/CLI/install.sh never reach the update.sh copy); the sole observable difference would be a manual no-arg `/opt/b-ui/update.sh` run no longer re-installing missing gum/fzf, which is not a documented or product path and gum/fzf are not even present on the production host bwg-rick.",
  "high_impact": false
 },
 {
  "id": "ctrl-C18",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Static: server/update.sh:2110 `auto_update()`; :2124-2126 `version_compare \"$remote_ver\" \"$local_ver\"; local result=$?; if [[ $result -eq 1 ]]; then` opens the version-update branch; the `npm install` at :2200-2203 (`if [[ -f \"${ADMIN_DIR}/package.json\" ]]; then cd \"${ADMIN_DIR}\" && npm install 2>&1 && cd - > /dev/null || true`) is INSIDE that branch. The else branch (:2222-2226, \"已是最新版本\") runs only `apply_systemd_configs` — no npm install. So the claim's framing \"on every 6-hour auto_update() cron cycle\" is wrong: npm runs only when a new version.json is detected. The other half is correct: `web_changed` is computed at :2173-2186 (set to 1 for `web/*` or `version.json` changes) and gates `systemctl restart b-ui-admin` at :2207, but the npm install at :2202 has no such gate — it runs on every version bump even when `web/package.json` (which is in file_map at :2141 and therefore appears in `changed_files` when changed) is byte-identical. Same unconditional pattern exists in do_update() at :318 (CLI-triggered manual update). Cron: ensure_cron_jobs :2064 `0 */6 * * * ${BASE_DIR}/update.sh auto`. Git: `git log -S'web_changed' -- server/update.sh` -> fef6097 (2026-09-10, v3.6.0 P0-3 \"自更新按变更文件门控重启\"); that commit added the gate for the restart but left npm install ungated (git show fef6097 line 79 of hunk). npm install in auto_update originally from d6f0312 (v3.2.4). Live (bwg-rick, read-only): `grep -c \"已是最新版本\" /var/log/b-ui-update.log` = 362, `grep -c \"变更文件\"` = 2 — i.e. 362 cron cycles did NOT reach the npm install, only the 2 version-update runs did; /opt/b-ui/admin/node_modules mtime 2026-06-03 (install day), package-lock.json mtime 2026-09-11 05:07 (touched by a version-update npm run). Deployed /opt/b-ui/update.sh has identical structure (`result -eq 1` at fn-line 17, npm install at fn-line 93, web_changed gate at fn-line 98). Dependency need: web/server.js:8 `import { convertOutboundToLink } from \"singbox-converter\"` — the import is live (ESM resolves at load), so node_modules must exist and npm install cannot simply be removed. Minor: the gate is 4 lines after, not \"three lines later\".",
  "correction": "npm install in auto_update() runs only inside the `if [[ $result -eq 1 ]]` new-version branch (server/update.sh:2126-2221), never on the \"已是最新版本\" path (server/update.sh:2222-2226) that the 6-hourly cron hits ~99% of the time (bwg-rick log: 362 no-op cycles vs 2 update runs). What IS true: within a version update it is not gated on `web/package.json` appearing in `changed_files`/`web_changed` (server/update.sh:2173-2186 vs :2200-2203), so every release bump re-runs npm even when package.json is unchanged; do_update() at :318 has the same ungated pattern.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "ctrl-C19",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): the inline kernel-update fallback in server/b-ui-cli.sh update_kernel() runs only when ${BASE_DIR}/update.sh is absent; no counter-claim, no also_raised_as.\n\nStatic: server/b-ui-cli.sh:20 `BASE_DIR=\"/opt/b-ui\"`; :518-541 `update_kernel() { if [[ -f \"${BASE_DIR}/update.sh\" ]]; then source ...; update_kernel; else # 内联更新 ... (curl get.hy2.sh, Xray-install, systemctl restart) fi`; the else-branch is lines 523-541 (the block through :556 also includes the always-run client-script download, which is NOT part of the fallback and is used on every path). Called only from the menu at :1370. After `source update.sh`, update.sh:1859 `update_kernel()` overwrites the CLI's own definition, so the inline branch is unreachable whenever the file exists.\n\nFile sets (all unconditional, no gating): install.sh:243 `\"server/update.sh:${BASE_DIR}/update.sh\"` and install.sh:267-269 `if [[ $failed -gt 0 ]]; then print_error ...; return 1` (install aborts if any file missing); install.sh:276 chmod +x update.sh; server/update.sh:261 do_update files list; server/update.sh:2137 auto_update file_map. download_and_validate (update.sh:51-77) writes to `.dl.$$` tmp and only mv's on success, so a failed download never removes an existing update.sh (update.sh:2170 \"未通过校验，未覆盖现有版本\"). update.sh is also the cron target (core.sh:1223-1224, install.sh:850-851, update.sh:2064-2065) and is sourced by install.sh:793 and b-ui-cli.sh:511 (check_bui_update, same guard). version.json `.files` includes server/update.sh.\n\nOnly theoretical reach: legacy v3.1 b-ui-server.sh (BASE_DIR=/opt/hysteria, b-ui-server.sh:23/976, not in version.json, not used by install.sh) — a new b-ui-cli.sh hardcodes /opt/b-ui, so on a pure /opt/hysteria host /opt/b-ui/update.sh would be missing; but b-ui-server.sh:921-922 downloads update.sh alongside b-ui-cli.sh, and install.sh:436 migrates such hosts. Neither production host has /opt/hysteria.\n\nLive (read-only): `ssh bwg-rick 'ls -la /opt/b-ui/update.sh /opt/b-ui/b-ui-cli.sh; readlink -f /usr/local/bin/b-ui; grep -n \"^update_kernel()\" ...; jq -r .version /opt/b-ui/version.json'` → update.sh 105353 bytes present, /usr/local/bin/b-ui → /opt/b-ui/b-ui-cli.sh, update_kernel defined at update.sh:1859 and b-ui-cli.sh:518, version 3.6.3. Same via `-J bwg-rick bwg-tizi`: update.sh present, v3.6.3, `/opt/hysteria` does not exist.\n\nGit: `git log -S'内联更新' -- server/b-ui-cli.sh` → only 808f081 2025-12-11 \"feat: 模块化重构，支持自动更新\" (block written once, never touched since; f8a3d7b/5a49864 only touched the client-download part below it).",
  "deletable_under_v4": true,
  "correction": "Minor scoping: the fallback proper is server/b-ui-cli.sh:523-541 (else-branch); lines 543-573 (client-script download using select_download_source from the sourced update.sh) run on every path and must stay. On deletion, keep the `[[ -f update.sh ]]` guard with the existing print_error (as check_bui_update at :510-515 does) or drop the guard entirely — both give zero behaviour change on a fresh v4 install.",
  "high_impact": false
 },
 {
  "id": "ctrl-C2",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"server/update.sh:672-745 (migrate_ipv4_only_egress, D9) is a no-op on any host installed by the current core.sh, since core.sh:588, core.sh:649 and core.sh:829 already emit IPv4-only egress (direct mode:4, ForceIPv4) at install time.\" No counter-claim, no also_raised_as.\n\nStatic: the D9 block is exactly server/update.sh:677-745 (function `migrate_ipv4_only_egress`, comment header at 672), called once at server/update.sh:1196 inside apply_systemd_configs() (runs on every `update.sh auto` cron and on \"already latest\" per update.sh:2092). It has three guarded branches, each fires ONLY when a marker is absent:\n - update.sh:681 `[[ -f \"$cfg\" ]] && ! grep -qE '^outbounds:' \"$cfg\"` (config.yaml)\n - update.sh:692 `grep -qE '^[[:space:]]+- name: direct$' \"$rcfg\" && ! grep -qE '^[[:space:]]+mode: 4$' \"$rcfg\"` (config-residential.yaml)\n - update.sh:726-727 `jq -e '.outbounds[]?|select(.tag==\"direct\" and .protocol==\"freedom\" and (.settings.domainStrategy // \"\") != \"ForceIPv4\")'` (xray-config.json)\ncore.sh writes all three markers at install time, verified by `grep -n 'ForceIPv4\\|mode: 4\\|outbounds:' server/core.sh`: core.sh:584-588 (`outbounds:` / `- name: direct` / `direct:` / `      mode: 4` in the config.yaml heredoc), core.sh:641-649 (config-residential.yaml heredoc: relay + `- name: direct` with `      mode: 4`, 6-space indent matches the `^[[:space:]]+mode: 4$` regex), core.sh:829 (`{\"tag\": \"direct\", \"protocol\": \"freedom\", \"settings\": {\"domainStrategy\": \"ForceIPv4\"}}`). So on a core.sh-generated host all three conditions are false and the function does nothing.\n\nRefutation attempts that failed:\n 1. Post-install rewriters could strip the markers? Checked every writer: web/server.js:302-323 (updateHysteriaResidentialConfig/updateHysteriaConfig) use regex `auth:[\\s\\S]*?(?=\\n[a-zA-Z]|$)` — stops at the next column-0 key, cannot reach `outbounds:`; server.js:2584 (masquerade) and 2632 (bandwidth) likewise replace only their own section; server.js:387 updateXrayConfig mutates only inbounds; b-ui-cli.sh:924-954 cmd_obfs only prepends an obfs block; core.sh apply_hy2_userpass_auth (awk, skips only the auth: block until next `^[a-zA-Z]`). None touch outbounds/domainStrategy.\n 2. install.sh restore_backup (install.sh:671-684) copies an OLD config.yaml back — but it is only invoked on install_type 0 (legacy /opt/hysteria upgrade, install.sh:788) and runs BEFORE run_core_install → configure_hysteria (install.sh:789, 567), so core.sh's fresh heredoc overwrites it. On install_type 2 (fresh) restore_backup is never called; on install_type 1 (version.json present) only update.sh runs and core.sh does not regenerate configs — so the only way to have a config lacking the markers is a pre-v3.6.0 install that was never freshly reinstalled.\n 3. git history: `git log --oneline -S'migrate_ipv4_only_egress' -- server/update.sh` and `-S'ForceIPv4' -- server/core.sh` and `-S'mode: 4' -- server/core.sh` all return the single commit 4c18228 \"feat(ipv6): 服务端出站 IPv4-only ... update.sh D9 幂等迁移\" — core.sh emitters and the D9 migration were introduced together; D9 has never been the sole source of these settings.\n\nLive hosts (read-only, secrets filtered): `ssh bwg-rick` / `ssh -J bwg-rick bwg-tizi` running grep/jq on /opt/b-ui: both ver=3.6.3, cfg_outbounds=1, cfg_mode4=1, resi_mode4=1, resi_direct_name=1, xray_direct=[{\"protocol\":\"freedom\",\"ds\":\"ForceIPv4\"}], D9_needed_xray=no. Both hosts hold *.bak.v360.<ts> backups (rick 2026-09-11 02:22, tizi 02:26) and /var/log/b-ui-update.log shows the three \"✓ D9 ...\" lines — i.e. D9 fired exactly once because these hosts were UPGRADED from pre-3.6.0, and is now a no-op on every subsequent 6-hourly run. This is consistent with the claim: D9 is upgrade-only glue, not something a current-core.sh install ever needs.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "ctrl-C3",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM (no counter-claim, no also_raised_as): block A in server/update.sh cannot fire on the current fleet.\n\nStatic call graph of block A (server/update.sh:1527-1623):\n- Block A lives inside apply_systemd_configs() (defined update.sh:756). Guard at update.sh:1527-1528: `[[ -f \"${BASE_DIR}/config.yaml\" ]] && [[ ! -f /etc/systemd/system/hysteria-residential.service ]]`. It only fires when config.yaml exists AND the residential unit file is absent.\n- Callers of apply_systemd_configs (grep -n apply_systemd_configs server/update.sh): do_update():313, check_and_update():2099 (no-update path), auto_update():2198 and :2226. Entry points: `[[ \"${BASH_SOURCE[0]}\" == \"${0}\" ]]` dispatch at the end of update.sh (case auto→auto_update, kernel→auto_update_kernel, -y→check_and_update, *→check_and_update); sourced by install.sh:793 (install_type==1 → check_and_update) and by server/b-ui-cli.sh:511/520. Every path reaches the same guard; there is no eval/indirect invocation that bypasses it.\n\nWho creates the unit unconditionally (core.sh:244):\n- server/core.sh:107 install_hysteria() writes `/etc/systemd/system/hysteria-residential.service` at core.sh:244 with `cat > ... <<'UNIT_EOF'` — outside any conditional; the \"do not reinstall binary\" branch at core.sh:113-115 is a no-op `:` with the comment \"但仍要生成 systemd unit\", so the unit is written regardless of the prompt answer.\n- install_hysteria is called from install.sh run_core_install (install.sh:560, after `source core.sh` at :557), which is invoked for install_type 0 (upgrade from legacy) and 2 (fresh install) at install.sh:786-802. Hence every fresh/re-install yields the unit file and the guard is false.\n\nWho deletes the unit (grep -rn 'hysteria-residential.service' across .sh/.js):\n- server/b-ui-cli.sh:692 (uninstall) removes it, but b-ui-cli.sh:704 in the same uninstall also `rm -rf /opt/b-ui`, deleting config.yaml → the first half of block A's guard is false too, so A still cannot fire after an uninstall until a fresh core.sh install recreates the unit anyway.\n- install.sh:517 (reinstall cleanup) removes only hysteria-server.service, NOT hysteria-residential.service. No other deleter exists (update.sh:884-885 only removes a drop-in .d file).\n\nLive fleet (read-only SSH, commands: `ls -l /etc/systemd/system/hysteria-residential.service /opt/b-ui/config-residential.yaml; systemctl is-active/is-enabled hysteria-residential; head -3 config-residential.yaml`):\n- bwg-rick (ver 3.6.3): unit exists (940 B, Jun 3 09:25), config-residential.yaml header \"Residential 实例 (v3.5)\" i.e. generated by core.sh at the 2026-06-03 reinstall; hysteria-residential active+enabled.\n- bwg-tizi (ver 3.6.3, via -J bwg-rick): unit exists (662 B, Jun 3 09:27), config-residential.yaml header \"Residential 实例 (v3.5 迁移)\" generated May 14 2026 — proof block A DID fire once historically on tizi, but the unit now exists, so the guard is permanently false; hysteria-residential active+enabled.\n- Neither host has a chance of re-triggering the guard: only manual deletion of the unit file while keeping config.yaml would do it, and no code path does that.\n\nConclusion: the claim's evidence reproduces exactly. Block A is a one-shot v3.4→v3.5 migration whose precondition is unreachable on both production hosts and on any v4 fresh install. Note the separate A.fix block (update.sh:1625+) is guarded on the unit EXISTING and is independent of A.",
    "deletable_under_v4": true,
    "correction": ""
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "Claim text (docs/superpowers/audits/2026-09-11-claims.json, id ctrl-C3, no counter-claim, no also_raised_as).\n\nSTATIC: server/update.sh:1527-1528 guard is `[[ -f \"${BASE_DIR}/config.yaml\" ]] && [[ ! -f /etc/systemd/system/hysteria-residential.service ]]` — block A only fires when the residential unit file is ABSENT. server/core.sh:244 `cat > /etc/systemd/system/hysteria-residential.service <<'UNIT_EOF'` is inside install_hysteria() (core.sh:107), which has NO early return (awk for `return` in lines 107-300 -> none; the \"already installed, don't reinstall binary\" branch at core.sh:114-115 is a `:` no-op and falls through to the unit write). install_hysteria is called unconditionally by install.sh:560 run_core_install(), which is the path for install_type 0 (legacy upgrade) and 2 (fresh) at install.sh:784-800. Uninstall (server/b-ui-cli.sh:692) removes the unit, but the next install re-runs core.sh before any update.sh path.\n\nRUNTIME INVOCATION paths of apply_systemd_configs (update.sh:313, 2099, 2198, 2226): cron `update.sh auto` every 6h / `kernel` every 12h (core.sh:1223-1224, install.sh:850-851, update.sh:2064-2065), `source update.sh` from b-ui-cli.sh:511/520 and install.sh:793 (install_type==1, existing install only). None of these run on a fresh install before core.sh:244 has written the unit; web/server.js does not invoke update.sh (grep 'update\\.sh' web/server.js -> no hits). version.json files[] delivers server/update.sh and server/core.sh together, so any host that received update.sh also has a core.sh that writes the unit.\n\nLIVE (read-only, `ssh -o BatchMode=yes -o ConnectTimeout=25 bwg-rick` and `-J bwg-rick bwg-tizi`): both hosts ver=3.6.3; /etc/systemd/system/hysteria-residential.service present on both (bwg-rick 940B Jun 3 09:25; bwg-tizi 662B Jun 3 09:27); hysteria-residential and hysteria-server both `active`; cron has `update.sh auto`/`kernel` lines; `grep -c \"v3.5 迁移: 安装 hysteria-residential\" /var/log/b-ui-update.log` = 0 on both. Nuance: bwg-tizi's /opt/b-ui/config-residential.yaml header reads \"Residential 实例 (v3.5 迁移) 生成时间: Thu May 14 06:18:36 AM UTC 2026\" — i.e. block A DID fire once historically on bwg-tizi (May 2026, when it was upgraded from v3.4.x); bwg-rick's header reads \"(v3.5)\" from core.sh (Jun 3 2026 reinstall). Today, with the unit present on both, the guard is false and the block cannot fire again; it is dead on the current fleet and on any v4 fresh install.",
    "deletable_under_v4": true,
    "correction": "Minor precision only: block A is not \"never fired\" historically — it ran on bwg-tizi on 2026-05-14 (config-residential.yaml header \"v3.5 迁移\"). It is dead going forward: both servers now have the unit file, and core.sh:244 writes it unconditionally on every install, so under a v4 clean reinstall update.sh:1527-1623 is pure v3.4->v3.5 migration code with zero behaviour on a fresh install."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM (ctrl-C3, no counter-claim, no also_raised_as): update.sh block A (hysteria-residential unit + config-residential.yaml) can never fire on the current fleet.\n\nCODE:\n- server/update.sh:1527-1528 guard: `if [[ -f \"${BASE_DIR}/config.yaml\" ]] && [[ ! -f /etc/systemd/system/hysteria-residential.service ]]; then` — fires only when the unit file is ABSENT. Body 1529-1622 writes unit (RESI_UNIT_EOF heredoc), config-residential.yaml with header \"# Hysteria2 服务器配置 — Residential 实例 (v3.5 迁移)\", enable+start, updated=1.\n- server/core.sh:244 `cat > /etc/systemd/system/hysteria-residential.service <<'UNIT_EOF'` inside install_hysteria() (core.sh:107); core.sh:592-596 writes config-residential.yaml with header \"(v3.5)\" (no 迁移 suffix).\n- install.sh:557-560 run_core_install(): `source \"${BASE_DIR}/core.sh\"; install_hysteria` — called for fresh install (case 2) and for legacy-upgrade (case 0, install.sh:788). update.sh is only sourced in case 1 (install.sh:793, already-installed re-run) i.e. AFTER core.sh has already created the unit. So on any fresh/clean install the unit exists before block A can ever be evaluated.\n- b-ui-cli.sh:692 removes the unit only during full uninstall (with everything else), so no path leaves config.yaml present with the unit deleted.\n\nHISTORY:\n- `git log -S'RESI_UNIT_EOF' --oneline -- server/update.sh` → f751b0f \"feat(v3.5): update.sh migration + Web UI 多 URL + global toggle\" (only commit; block never touched since).\n- `git log -S'config-residential.yaml' --oneline -- server/core.sh` → introduced 73b20f2 \"feat(v3.5): core.sh 双 hy2 + 双 xray 入站 ...\". version.json changelog 3.5.0 explicitly lists \"老服务器自动迁移 ... A. 安装 hysteria-residential systemd unit + config-residential.yaml\" as a v3.4.x→v3.5 migration.\n\nLIVE (read-only):\n- bwg-rick (`ssh bwg-rick 'ls -l --time-style=long-iso /etc/systemd/system/hysteria-residential.service; head -3 /opt/b-ui/config-residential.yaml; systemctl is-enabled/is-active hysteria-residential; jq -r .version /opt/b-ui/version.json'`): unit exists (940 B, 2026-06-03 09:25), config header \"(v3.5)\" generated \"Wed Jun 3 06:46:32 AM UTC 2026\" → written by core.sh at the 2026-06-03 reinstall, NOT by block A; enabled/active; version 3.6.3; grep for \"v3.5 迁移: 安装 hysteria-residential\" in /var/log/b-ui-update.log → no hits.\n- bwg-tizi (`ssh -J bwg-rick bwg-tizi ...`): unit exists (662 B, 2026-06-03 09:27 mtime, patched by A.fix), config-residential.yaml header \"(v3.5 迁移) 生成时间: Thu May 14 06:18:36 AM UTC 2026\" → block A DID fire once historically on 2026-05-14 (the v3.4→v3.5 upgrade); enabled/active; version 3.6.3. Since the unit now exists the guard at update.sh:1528 is false, so A cannot fire again (the 6434-line update log shows only A.fix/userpass messages for hysteria-residential, no further \"v3.5 迁移: 安装\" lines).\n\nAttempted refutation: the only way A fires is unit file missing while config.yaml exists; no code path in install.sh/core.sh/update.sh/b-ui-cli.sh produces that state on a v3.5+ install, and both fleet hosts already have the unit. Nuance: the claim's \"never\" is true going forward; historically it did fire on bwg-tizi (2026-05-14), which does not contradict \"current fleet\".",
    "deletable_under_v4": true,
    "correction": "Minor precision only: block A is not dead code historically — it ran on bwg-tizi on 2026-05-14 (config-residential.yaml header \"(v3.5 迁移)\"), but it is a pure v3.4.x→v3.5 migration whose guard (update.sh:1528) is permanently false on both hosts and on every fresh install (install.sh:557-560 → core.sh:244). Under a v4 clean reinstall it is deletable with zero behaviour change; note the neighbouring A.fix block (update.sh:1625+) is a separate item, not covered by this claim."
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "ctrl-C4",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "CLAIM (jq on docs/superpowers/audits/2026-09-11-claims.json): \"server/update.sh:1670-1699 (block B, xray dual-inbound migration) is unreachable on any host installed by current core.sh, since core.sh:811 already emits the vless-residential inbound tag that this block's guard checks for.\" No counter-claim, no also_raised_as.\n\nSTATIC:\n- server/update.sh:1671-1672 guard: `[[ -f xray-config.json ]] && ! jq -e '.inbounds[] | select(.tag == \"vless-residential\")' ...` — block only fires when the file exists AND lacks the vless-residential inbound.\n- server/core.sh:755 `configure_xray()` writes `$BASE_DIR/xray-config.json` via an unconditional heredoc (core.sh:775 `cat > \"$BASE_DIR/xray-config.json\" << EOF`), containing `\"tag\": \"vless-direct\"` (line 794) and `\"tag\": \"vless-residential\"` (line 811, port 10002), plus the relay outbound and both routing rules (lines 835-836). Only early exit is core.sh:761-764 (reality-keys.json missing → return 1 before writing), in which case xray-config.json does not exist and block B's `-f` test is also false.\n- Call sites (`grep -rn configure_xray --include='*.sh' .`): install.sh:570 inside run_core_install(); install.sh:784-799 shows run_core_install runs for install_type 0 (legacy upgrade) and 2 (fresh). The legacy /opt/hysteria copy at install.sh:362-363 is overwritten afterwards by configure_xray in the same run. install_type 1 sources update.sh only (never touches core.sh's generator) — this is the sole path where block B's guard matters, and only if the pre-existing file lacks the tag (i.e. a v3.4.x install).\n- No other writer removes the tag: web/server.js:328-389 updateXrayConfig() only mutates clients/serverNames/ws inbound in place (lines 339-349, 353-356) and server.js:2585-2595 masquerade handler edits realitySettings only; update.sh:242/303 backup-restore copies the same file; D9 (update.sh:679-742) and B.fix (update.sh:1705-1715) are jq in-place edits that keep inbounds. grep of b-ui-cli.sh / residential-helper.sh / resi-health.sh: only read/path refs (b-ui-cli.sh:430).\n- History: `git blame -L1670,1673 server/update.sh` → f751b0f 2026-05-14 \"feat(v3.5): update.sh migration\"; `git log -L1670,1699` → touched only by f751b0f, aae6394, 1ac41f4 (all 2026-05-14, v3.5.x). core.sh tag added in 73b20f2 2026-05-14 (git log -S '\"tag\": \"vless-residential\"' -- server/core.sh). Nothing since v3.5.9.\n\nLIVE (read-only):\n- bwg-rick (`ssh bwg-rick 'jq ...'`): version 3.6.3; inbounds [api:10085, vless-direct:10001, vless-residential:10002]; routing direct/relay rules present; only backup is xray-config.json.bak.v360.* — no `.bak.<epoch>` from block B (host was clean-reinstalled 2026-06-03), so block B has never fired there.\n- bwg-tizi (`ssh -J bwg-rick bwg-tizi 'jq ...'`): version 3.6.3; same three inbounds and rules; has xray-config.json.bak.1778739517 (May 14 06:18, 2603 B) — that filename format matches block B's `_xray_bak` (update.sh:1674 `.bak.$(date +%s)`), i.e. block B ran exactly once on tizi during the v3.4→v3.5 upgrade on 2026-05-14 and is a no-op on every update.sh run since (guard false).\n\nConclusion: the claim's evidence reproduces exactly. Block B is only reachable via the update.sh in-place upgrade path (install_type 1 / cron `update.sh auto`) on a host whose xray-config.json predates v3.5.0 — no host produced by current core.sh can ever satisfy its guard.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "ctrl-C5",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json, id ctrl-C5): update.sh block C is unreachable on current-core.sh-installed hosts because core.sh:554 / core.sh:612 already write `resolver:`. No counter-claim, no also_raised_as.\n\nStatic:\n- server/update.sh:1721-1736 block C guard: `[[ -f config.yaml ]] && ! grep -q '^resolver:' config.yaml` → awk-inserts a DoH resolver after `ignoreClientBandwidth:`. It only touches config.yaml (never config-residential.yaml).\n- server/core.sh:553-558 (config.yaml heredoc) and core.sh:612-616 (config-residential.yaml heredoc) both emit `resolver:\\n  type: https\\n  https:\\n    addr: \"1.1.1.1:443\"\\n    sni: cloudflare-dns.com`. So a config.yaml generated by current core.sh always matches `^resolver:` and block C's guard is false.\n- Every other writer of config.yaml preserves or re-adds resolver: update.sh repair_hysteria_config heredoc (update.sh:484 emits `resolver:`); update.sh:1581 residential heredoc emits `resolver:`; A.fix2 (update.sh:1657-1666) only strips the `# B-UI:RESIDENTIAL-START/END` marker block; b-ui-cli.sh cmd_obfs (b-ui-cli.sh:922-955) only prepends an `obfs:` section / removes it; core.sh:677-692 userpass awk rewrites only the `auth:` section. `grep -nE \"resolver.*d'|/resolver/|delete.*resolver\" server/*.sh web/server.js` → no hits: nothing ever deletes `resolver:`.\n- Only path that can put a resolver-less config.yaml on a host is the backup/restore carry-over of a pre-v3.5 file: install.sh:454-456/487-489 back up the OLD config.yaml to /tmp/b-ui-config-backup.yaml and install.sh:677-679 (restore_backup, called at install.sh:788) copies it back OVER what core.sh just generated; update.sh:241/302 does the same. That is an upgrade-from-pre-v3.5 path, not a \"current-core.sh-installed\" host.\n- git: `git log -S'config.yaml 加 DoH resolver' -- server/update.sh` → f751b0f 2026-05-14 feat(v3.5) (never touched since); `git log -S'DoH 防 DNS 投毒（hy2 v2.x 原生支持）' -- server/core.sh` → 73b20f2 2026-05-14 (same release). Block C and core.sh resolver were born together in v3.5; repo version is now 3.6.3.\n\nRuntime (live, read-only): ssh bwg-rick / ssh -J bwg-rick bwg-tizi, `grep -c '^resolver:'` on both yaml files: bwg-rick config.yaml=1 (header \"Direct 实例 (v3.5)\"), config-residential.yaml=1; bwg-tizi config.yaml=1 (header \"Hysteria2 服务器配置 (v2.9.0)\" — a carried-over pre-v3.5 file that got its resolver from block C once, proving the block did have a real job on the legacy upgrade path), config-residential.yaml=1. Both hosts at version 3.6.3. So block C is an idempotent no-op on both production hosts today; it is reached only by apply_systemd_configs (update.sh:313, :2099, :2198, :2226) and its guard is always false there.\n\nMinor precision: the claim's mention of config-residential.yaml is irrelevant to block C (it only guards config.yaml), but that does not weaken the conclusion.",
  "deletable_under_v4": true,
  "correction": "Scope nuance only: block C is reachable on hosts whose config.yaml predates v3.5 and was carried over by install.sh restore_backup (install.sh:677-679) or update.sh:302 — bwg-tizi's config.yaml still carries a \"(v2.9.0)\" header and got its resolver this way. Under the v4 clean-reinstall decision both hosts get a fresh core.sh-generated config.yaml (which contains resolver, core.sh:554), so block C can be deleted with zero behaviour change; v4's clean reinstall should also not restore the old /tmp/b-ui-config-backup.yaml, otherwise the legacy path re-appears.",
  "high_impact": false
 },
 {
  "id": "ctrl-C6",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "REFUTED",
    "evidence": "CLAIM (ctrl-C6): update.sh block D (server/update.sh:1738-1752) is unreachable because core.sh:1286-1317 already writes+locks static resolv.conf at install time. COUNTER (data-C2): setup_static_dns() is never called on the install path, so block D IS the live path.\n\nStatic call graph — data-C2 is right, ctrl-C6 is wrong:\n1. `grep -rn 'setup_static_dns\\|static_dns' --exclude-dir=docs --exclude-dir=.git .` → exactly ONE hit: `server/core.sh:1289:setup_static_dns() {` (the definition). No call site in any .sh/.js/.json.\n2. core.sh has no main/dispatch/`BASH_SOURCE` block (grep for `BASH_SOURCE|^main|main \"$@\"|eval |\"$fn\"|$func` → only a heredoc-embedded /bin/sh helper at core.sh:140-160, unrelated). core.sh is a pure function library; its only entry is `source \"${BASE_DIR}/core.sh\"` at install.sh:557 (inside run_core_install) and install.sh:841 (cron-setup re-source).\n3. install.sh:550-600 run_core_install() call list: prepare_install_env → install_hysteria → install_nodejs → install_nginx → install_xray → collect_user_input → configure_hysteria → configure_xray → configure_nginx_proxy → deploy_admin_panel → create_services → start_all_services → create_global_command → install_tui_tools → harden_ssh → configure_performance (declare -F guarded) → configure_system_hygiene (declare -F guarded). setup_static_dns is absent. No indirect invocation (`grep -n 'setup_\\$\\|\\$func\\|\\$fn\\b\\|eval \\|declare -f' install.sh server/core.sh server/update.sh` → only install.sh:812/857 setup_auto_update).\n4. Git history: `git log --all -G'[^(]setup_static_dns[^(]'` → empty; `git show 73b20f2 | grep setup_static_dns` → only the commit message and the `+setup_static_dns() {` definition line. The function has been dead since it was introduced in v3.5 (commit 73b20f2).\n5. Distinguishing fingerprint: core.sh:1306-1313 writes a resolv.conf that STARTS with two comment lines (`# B-UI v3.5: 静态 DNS…` / `# hy2/xray/sing-box 自己用 DoH…`); update.sh:1744-1749 block D writes the same 3 nameservers + options line WITHOUT any header.\n\nLive host evidence (read-only):\n- `ssh bwg-tizi 'cat /etc/resolv.conf; lsattr /etc/resolv.conf; systemctl is-enabled systemd-resolved'` → file is exactly `nameserver 1.1.1.1 / 8.8.8.8 / 9.9.9.9 / options edns0 timeout:2 attempts:2 single-request` with NO \"# B-UI v3.5\" header, `----i---------e-------` (immutable), systemd-resolved `disabled`/`inactive`, Ubuntu 26.04, v3.6.3. That content is byte-for-byte update.sh block D's heredoc, not core.sh's — proving block D is what actually ran on a current host, i.e. it is reachable and was the live path.\n- `ssh bwg-rick 'cat /etc/resolv.conf; lsattr …'` → `nameserver 203.0.113.53 / 203.0.113.54` (provider DNS), not immutable, systemd-resolved `not-found` (reinstalled 2026-06-03 on v3.6.x). Neither core.sh's function (never called) nor block D (guard `-L /etc/resolv.conf || grep 127.0.0.53` does not match a plain provider file) ran here — further confirming core.sh does NOT write static DNS at install time; on a non-systemd-resolved distro a fresh install gets no static DNS at all.\n- Both hosts: `grep -n setup_static_dns /opt/b-ui/core.sh` → only line 1289 (definition), same as repo.\n\nConclusion: ctrl-C6's premise (\"core.sh already writes and chattr +i locks a static resolv.conf at install time\") is false; the function exists but is unreachable dead code. update.sh block D is the only executed static-DNS path (on systemd-resolved hosts such as bwg-tizi) and is therefore NOT unreachable. data-C2 wins.",
    "deletable_under_v4": false,
    "correction": "The reverse is true. core.sh:1289 setup_static_dns() is the dead code — defined once, called nowhere (install.sh:550-600 run_core_install never invokes it; core.sh has no self-dispatch). update.sh:1738-1752 block D is the only static-DNS code that ever executes: on a fresh v4 clean reinstall of an Ubuntu host (systemd-resolved stub symlink), core.sh leaves /etc/resolv.conf untouched and block D fires on the first `update.sh auto` cron run (up to 6h later), which is exactly how bwg-tizi's current header-less, chattr +i resolv.conf was produced. Deleting block D as \"migration-only\" would silently remove static DNS from fresh installs (behaviour change), unless run_core_install is first made to call setup_static_dns() (or the two duplicates are merged into one function called from both install and update paths). Separately, setup_static_dns() in core.sh can be deleted with zero behaviour change today, but that is the opposite of what ctrl-C6 asserts."
   },
   {
    "lens": "runtime",
    "verdict": "REFUTED",
    "evidence": "The claim's premise (\"core.sh already writes and chattr +i locks a static resolv.conf at install time\") is false: core.sh only DEFINES setup_static_dns(), nothing ever calls it. The counter-claim (data-C2) is correct.\n\nSTATIC / GIT (run in /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -rn 'setup_static_dns' . --exclude-dir=.git` -> only server/core.sh:1289 `setup_static_dns() {` (definition) plus the audit JSON files. No call site in install.sh, core.sh, update.sh, b-ui-cli.sh, web/server.js.\n- `git log --oneline -S'setup_static_dns' --all` -> single commit 73b20f2 (feat v3.5, 2026-05-14). `git show 73b20f2 -- server/core.sh install.sh | grep -n setup_static_dns` -> only the `+setup_static_dns() {` definition line; the commit never added a caller. So the function has been dead since birth, not removed later.\n\nRUNTIME-INVOCATION LENS (every way core.sh code can execute):\n- install.sh:557 `source \"${BASE_DIR}/core.sh\"` then run_core_install() calls, verbatim (install.sh:560-600): install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance (guarded by declare -F), configure_system_hygiene. No setup_static_dns.\n- install.sh:841 sources core.sh again but only calls configure_cron_tasks / ensure_cron_jobs (install.sh:842-845).\n- server/b-ui-cli.sh:628 sources core.sh and calls only configure_port_hopping (b-ui-cli.sh:629).\n- core.sh has no self-dispatch: `grep -n 'eval\\|\"\\$1\"\\|case \"\\$' server/core.sh` -> only arg parsing at :147-149 and local var assignments; no name-based dispatch that could reach setup_static_dns.\n- update.sh never sources core.sh (only downloads it, update.sh:259/294/2135). web/server.js shell-outs (server.js:80-2634) call residential-helper.sh, systemctl, hysteria/xray/sing-box version, tail — never core.sh.\n- Cron (core.sh:1223-1224, install.sh:850-851, update.sh:2064-2065): only `update.sh auto` / `update.sh kernel` / cert-check.sh. `update.sh auto` runs apply_systemd_configs (update.sh:313, 2099, 2198, 2226) which contains block D at update.sh:1738-1752 — that IS the only live static-DNS writer, and it only fires when `[[ -L /etc/resolv.conf ]] || grep -q 127.0.0.53 /etc/resolv.conf` (update.sh:1739).\n\nLIVE HOSTS (`ssh -o BatchMode=yes -o ConnectTimeout=25 bwg-rick '<cmd>'` and `... -J bwg-rick bwg-tizi '<cmd>'`, cmd = cat /etc/resolv.conf; lsattr; ls -l; systemctl is-active/is-enabled systemd-resolved; grep of /var/log/b-ui-update.log; grep setup_static_dns /opt/b-ui/*.sh; jq .version version.json; crontab -l):\n- bwg-rick (fresh core.sh install 2026-06-03, now v3.6.3): /etc/resolv.conf = `nameserver 203.0.113.53` / `nameserver 203.0.113.54` (provider DNS), lsattr `--------------e-------` (NOT immutable), plain file dated Sep 2, systemd-resolved `inactive`/`not-found`. A fresh core.sh install therefore did NOT produce a static, locked resolv.conf — directly falsifying the claim's premise. Block D also did not fire there (guard false: no symlink, no 127.0.0.53), which is why the box still has provider DNS.\n- bwg-tizi (v3.5 migrated, now v3.6.3): /etc/resolv.conf is exactly the update.sh block-D heredoc (4 lines, 107 bytes, no `# B-UI v3.5:` comment header that core.sh:1307-1308 would emit), `----i---------e-------` (immutable), dated May 14 06:18 = the day of commit 73b20f2's rollout, systemd-resolved inactive/disabled. Provenance = update.sh block D, not core.sh.\n- On both hosts `grep -n setup_static_dns /opt/b-ui/core.sh /opt/b-ui/update.sh /opt/b-ui/b-ui-cli.sh` -> only the definition at /opt/b-ui/core.sh:1289.\n\nDecisive lines: install.sh:560-600 (call list without setup_static_dns) vs update.sh:1739 (block D guard) — block D is the sole reachable implementation; core.sh:1289-1319 is the unreachable one. The claim inverted which copy is dead.",
    "deletable_under_v4": false,
    "correction": "Reality is the inverse of the claim: server/core.sh:1289-1319 setup_static_dns() is the dead code (defined, never called by install.sh:557-600 run_core_install, install.sh:841, b-ui-cli.sh:628, or any cron/systemd/panel path), while server/update.sh:1738-1752 block D is the ONLY code that ever makes /etc/resolv.conf static — it runs from cron `update.sh auto` (core.sh:1223) via apply_systemd_configs (update.sh:313/2099/2226), gated by `-L /etc/resolv.conf || 127.0.0.53` at update.sh:1739. Live proof: bwg-tizi's immutable resolv.conf is byte-for-byte the block-D heredoc (no core.sh header), and the freshly core.sh-installed bwg-rick has non-static, non-immutable provider DNS (203.0.113.53/54), i.e. core.sh wrote nothing. Under v4 the correct cut is to delete setup_static_dns() from core.sh (or wire it into run_core_install and then drop block D); deleting block D alone would remove the only static-DNS writer and change behaviour on fresh systemd-resolved (Ubuntu) installs, so deletable_under_v4 for the code the claim names is false."
   },
   {
    "lens": "history-live",
    "verdict": "REFUTED",
    "evidence": "The COUNTER-claim (data-C2) is right; ctrl-C6's premise is false. Code: `grep -rn setup_static_dns` over the repo hits ONLY its definition at server/core.sh:1289 (plus audit JSON). install.sh:551-617 run_core_install() sources core.sh (install.sh:557) and calls install_hysteria…configure_system_hygiene…residential-helper setup — no setup_static_dns anywhere; core.sh has no main/top-level call either; update.sh never sources core.sh. `git log -G'setup_static_dns'` shows a single commit 73b20f2 (2026-05-14, v3.5) that added the definition without a call site; block D was added the same day in f751b0f (2026-05-14). So on a fresh install nothing writes a static resolv.conf; only update.sh:1738-1752 block D (via apply_systemd_configs, called from update.sh:313/2099/2198/2226 on every cron run) can, and only when its guard fires (`[[ -L /etc/resolv.conf ]] || grep -q 127.0.0.53`). Live state confirms: bwg-rick (reinstalled 2026-06-03, v3.6.3): `ls -la /etc/resolv.conf` → regular file 46 bytes, Sep 2 06:39 (= boot time); `lsattr` → `--------------e-------` (NOT immutable); content = provider DNS `nameserver 203.0.113.53 / 203.0.113.54` — no B-UI header, not 1.1.1.1; systemd-resolved not-found; `grep -c \"resolv.conf 静态化\" /var/log/b-ui-update.log` → 0. I.e. a host installed by current core.sh has NO static/locked resolv.conf at all, directly contradicting \"core.sh already writes and chattr +i locks a static resolv.conf at install time\"; block D simply never triggered because the guard conditions were absent. bwg-tizi (running since May): `lsattr` → `----i---------e-------`, content is exactly the 4 lines 1.1.1.1/8.8.8.8/9.9.9.9/options…, size 107 bytes which equals the byte count of update.sh block D's heredoc (printf … | wc -c → 107) and lacks core.sh's `# B-UI v3.5: 静态 DNS` comment header — so it was written by update.sh block D (mtime 2026-05-14 06:18 = the v3.5 release day), not by core.sh's setup_static_dns. Both hosts' /opt/b-ui/core.sh:1289 still only defines the function. Commands: ssh bwg-rick / ssh -J bwg-rick bwg-tizi with `ls -la /etc/resolv.conf; lsattr /etc/resolv.conf; cat /etc/resolv.conf; systemctl is-enabled/is-active systemd-resolved; grep -c \"resolv.conf 静态化\" /var/log/b-ui-update.log; grep -n setup_static_dns /opt/b-ui/core.sh /opt/b-ui/update.sh; stat -c %y /etc/resolv.conf`.",
    "correction": "Reality is the inverse of the claim: server/core.sh:1289-1319 setup_static_dns() is dead code (defined, never called — install.sh:551-617 run_core_install omits it, and no other caller exists), so a fresh core.sh install does NOT write or lock /etc/resolv.conf (bwg-rick, reinstalled 2026-06-03, has provider DNS 203.0.113.53/54, no immutable bit). server/update.sh:1738-1752 block D is the only live path and is reachable; it ran on bwg-tizi on 2026-05-14 (107-byte file matching block D's heredoc, chattr +i set) and never on bwg-rick (guard needs a symlink or 127.0.0.53 stub, neither present). Whether the static-DNS capability should be kept at all is a product decision; if kept for v4, the correct fix is the opposite of the claim: call one shared function from the install path and drop the duplicate in core.sh.",
    "deletable_under_v4": false
   }
  ],
  "verdict": "REFUTED",
  "deletable_under_v4": false,
  "high_impact": true
 },
 {
  "id": "ctrl-C7",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "REFUTED",
    "evidence": "CLAIM (ctrl-C7): update.sh block E (1754-1767) unreachable because core.sh:1252-1270 opens the same v3.5 ports at install time. COUNTER (data-C1): configure_firewall() has zero callers.\n\nStatic call graph — the counter-claim is right:\n1. core.sh:1237 `configure_firewall() {` is the ONLY occurrence of that name in the deployed file set. Command: `grep -rn 'configure_firewall' --include='*.sh' .` → server/core.sh:1237 (definition), b-ui-server.sh:611 (definition) and b-ui-server.sh:1256 `configure_firewall \"$PORT\" \"$ADMIN_PORT\"` (caller) — but b-ui-server.sh is the legacy v3.1 monolith, not in version.json, not installed by install.sh.\n2. core.sh has no main/dispatch/BASH_SOURCE block (`grep -n -E 'BASH_SOURCE|^main\\b|main \"\\$@\"' server/core.sh` → empty); it is only ever sourced. Sourcers: install.sh:557 (run_core_install), install.sh:841 (setup_auto_update, only to call configure_cron_tasks), b-ui-cli.sh:628 (only calls configure_port_hopping). No eval / \"$fn\" / indirect invocation: `grep -n -E '\\beval\\b|\\$\\{?[A-Za-z_]+\\}?\\(\\)' server/core.sh` → empty; `declare -F` uses in install.sh:594/599/842 name only configure_performance / configure_system_hygiene / configure_cron_tasks / ensure_cron_jobs.\n3. Fresh-install chain install.sh:550-618 run_core_install calls: install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, residential-helper setup, download_client_packages. start_all_services (core.sh:1794) ends with configure_port_hopping (core.sh:1856) + setup_hy2_watchdog. configure_firewall never appears.\n4. The only firewall opens reachable at install time are core.sh:1407-1416 inside configure_port_hopping — they open ONLY ${start_port}:${end_port}/udp (direct hop range 20000-30000); nothing opens 10002/tcp, 40000/udp, 41000-50000/udp. Command: `grep -n -E 'ufw allow|firewall-cmd --permanent' server/core.sh ... | grep -v core.sh:12[5-8]x` → only core.sh:1408 and :1414.\n5. Block E IS reachable: apply_systemd_configs() (update.sh:756) is called from update.sh:313 (perform_update), :2099 (check_and_update, \"already latest\" path), :2198 and :2226 (auto path); dispatch at update.sh:2302 `auto)`, :2308 `kernel)`, :2316/:2324 check_and_update. Block E has no guard other than ufw/firewalld being active, so it runs on every 6h cron tick. It is therefore the ONLY code in the deployed set that opens 10002/tcp, 40000/udp and 41000-50000/udp.\n6. git history: `git log -S'configure_firewall' -- server/core.sh` → only 808f081 (initial modularisation), i.e. it was defined dead and never wired; block E added in f751b0f (v3.5 migration).\n\nLive hosts (read-only): bwg-rick and bwg-tizi both report `ufw: none`, `firewalld: inactive`, iptables INPUT has 1 line (default policy only), `/var/log/b-ui-update.log` has 0 hits for \"v3.5 新端口\", and on-host /opt/b-ui/*.sh has no caller of configure_firewall (the core.sh:1 hit is the definition line). So on the two production servers block E is a runtime no-op purely because no host firewall is active — not because core.sh already opened the ports.",
    "correction": "core.sh:1237-1283 configure_firewall() is dead code: defined, never called from install.sh (run_core_install install.sh:550-618), core.sh's own functions, update.sh, or b-ui-cli.sh; the only caller is legacy b-ui-server.sh:1256 which is not deployed. At install time only configure_port_hopping (core.sh:1407-1416, called from start_all_services core.sh:1856) touches the firewall and it opens only the direct hop range. update.sh:1754-1767 block E is reachable via apply_systemd_configs (update.sh:313/2099/2198/2226) and is the sole code that opens 10002/tcp, 40000/udp, 41000-50000/udp on any host with ufw/firewalld active. data-C1 is correct; ctrl-C7's premise is false.",
    "deletable_under_v4": false
   },
   {
    "lens": "history-live",
    "verdict": "REFUTED",
    "evidence": "COUNTER-CLAIM (data-C1) IS RIGHT; ctrl-C7's premise is false.\n\n1. configure_firewall() in server/core.sh:1237-1283 is defined but never invoked.\n   grep -rn 'configure_firewall' --include='*.sh' . (excluding docs) →\n     server/core.sh:1237:configure_firewall() {          (definition only)\n     b-ui-server.sh:611 / b-ui-server.sh:1256            (legacy v3.1 all-in-one installer, not in version.json, not used by install.sh)\n   No call site in install.sh, server/core.sh or server/update.sh.\n\n2. It was never called in any commit in history:\n   git log --all -p -S'configure_firewall' -- install.sh server/core.sh | grep -E '^[+-].*configure_firewall' → only \"+configure_firewall() {\" (commit 808f081 \"feat: 模块化重构，支持自动更新\", 2025-12-11). git blame -L1237 confirms the definition dates from 808f081; the v3.5 port additions (10002/40000/41000-50000, commit 73b20f2 \"feat(v3.5): core.sh 双 hy2 + 双 xray 入站…\") were added into a function that already had no caller.\n\n3. install.sh run_core_install() call chain (awk '/^run_core_install\\(\\) \\{/,/^\\}/' install.sh): prepare_install_env → install_hysteria → install_nodejs → install_nginx → install_xray → collect_user_input → configure_hysteria → configure_xray → configure_nginx_proxy → deploy_admin_panel → create_services → start_all_services → create_global_command → install_tui_tools → harden_ssh → configure_performance → configure_system_hygiene. No firewall step. The only ufw call in the live install path is configure_port_hopping (core.sh:1407-1408), which opens only the direct-instance hop range ${start_port}:${end_port}/udp — not 10002/tcp, 40000/udp or 41000:50000/udp.\n\n4. Therefore update.sh:1754-1767 (block E, \"# E. 防火墙放行 v3.5 新端口\", commit f751b0f) is NOT made unreachable by core.sh — the opposite: on a host with ufw or firewalld active, block E is the ONLY code that ever opens 10002/tcp, 40000/udp and 41000:50000/udp. Block E is also self-guarded (`ufw status | grep -q \"10002/tcp\" || ufw allow …`), so it is idempotent rather than \"unreachable\".\n\n5. LIVE STATE (read-only, both hosts running 3.6.3, deployed /opt/b-ui/update.sh still has block E at line 1754, deployed core.sh has the def and no caller):\n   ssh bwg-rick / ssh -J bwg-rick bwg-tizi: `command -v ufw` → not found on both; `systemctl is-active firewalld` → inactive on both; `iptables -S INPUT` → `-P INPUT ACCEPT` on both; `nft list tables` → empty on bwg-rick, only hysteria_* tables on bwg-tizi (Hysteria2's own port-hopping redirect, not a firewall). No install/update log line \"配置防火墙\"/\"ufw 规则已添加\"/\"v3.5 新端口\" on either host. So on the two production machines BOTH configure_firewall (dead) and block E (no ufw/firewalld → skipped) are no-ops; the claimed \"core.sh already opened the ports\" never happened on either host — the ports are reachable simply because there is no host firewall at all.",
    "correction": "Actually true: server/core.sh:1237-1283 configure_firewall() has zero callers (install.sh run_core_install never references it; git history shows it was never called in any commit since its introduction in 808f081), so a fresh core.sh install opens NO ufw/firewalld rules for 10002/tcp, 40000/udp, 41000:50000/udp (nor 22/PORT/80/443/10001). server/update.sh:1754-1767 block E is therefore the sole code path that opens the v3.5 residential ports on ufw/firewalld hosts and is reachable on every `update.sh` run (it is idempotent via the `grep -q || ufw allow` guards, not unreachable). On the live hosts bwg-rick and bwg-tizi neither ufw nor firewalld is installed (iptables INPUT policy ACCEPT), so both code blocks are no-ops there for an unrelated reason.",
    "deletable_under_v4": false
   },
   {
    "lens": "runtime",
    "verdict": "REFUTED",
    "evidence": "COUNTER-CLAIM data-C1 IS RIGHT; ctrl-C7 is wrong. The claim's premise (\"core.sh:1252-1270 already opens the same v3.5 ports at install time\") is false because configure_firewall() is never invoked on any runtime path.\n\n1) Static + runtime call search (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n   `grep -rn 'configure_firewall' server/ install.sh web/server.js b-ui-client.sh` -> only `server/core.sh:1237:configure_firewall() {` (the definition). No caller.\n   Indirect/dynamic invocation check: `grep -nE 'eval |declare -F|compgen -A function' server/core.sh install.sh server/b-ui-cli.sh server/update.sh` -> the only `declare -F` guarded calls are configure_performance/configure_system_hygiene (install.sh:594-600), configure_cron_tasks/ensure_cron_jobs (install.sh:842-844), enable_bbr (core.sh:1524). None reach configure_firewall.\n   core.sh has no `main`; it is only `source`d by install.sh:557 inside run_core_install() (install.sh:550-620), whose full sequence is: prepare_install_env, install_*, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, residential-helper.sh setup, download_client_packages. configure_firewall absent. install.sh:838 also sources core.sh but only to call configure_cron_tasks.\n   web/server.js shell-outs (lines 80,101,294,495,561,1084,1093,1724-1732,2134,2143,2208-2395,2525-2677) invoke ip/getent/systemctl/curl/hysteria/xray/sing-box/residential-helper.sh — never core.sh.\n   `git log --oneline -G'^\\s*configure_firewall\\s*$' --all` -> 808f081 and 1be5ed8, and `git grep` at those commits shows the only-ever caller was legacy `b-ui-server.sh` (`configure_firewall \"$PORT\" \"$ADMIN_PORT\"`, now b-ui-server.sh:1256), which is not in version.json files[] and not used by install.sh. So configure_firewall in core.sh has been dead since the modular refactor.\n\n2) What DOES open firewall ports at install time: only configure_port_hopping() (core.sh:1374-1416), called from start_all_services at core.sh:1856. Its ufw/firewalld section (core.sh:1407-1416) opens ONLY `${start_port}:${end_port}/udp` (the 20000-30000 direct hop range, with comment \"Hysteria2 端口跳跃\"). It never opens 10002/tcp, 40000/udp or 41000-50000/udp.\n\n3) Runtime reachability of update.sh block E (server/update.sh:1754-1767, inside apply_systemd_configs defined at update.sh:756): apply_systemd_configs is invoked at update.sh:313 (check_and_update), :2099 (no-update path), :2198 and :2226 (auto path). Entry points: cron `0 */6 * * * /opt/b-ui/update.sh auto` (core.sh:1223, install.sh:850; confirmed live on both hosts via `crontab -l`), `sudo b-ui` menu -> b-ui-cli.sh:511 `source update.sh; check_and_update`, and install.sh:793 (install_type 1 re-run). On a fresh install with ufw/firewalld active, block E — reached at the first cron tick or first `b-ui update` — is therefore the ONLY code that ever opens 10002/tcp, 40000/udp, 41000-50000/udp. It is not unreachable; it is load-bearing.\n\n4) Live hosts (read-only): `ssh bwg-rick '...'` and `ssh -J bwg-rick bwg-tizi '...'`: both report `command -v ufw` -> \"no ufw\", `systemctl is-active firewalld` -> inactive, version 3.6.3, `grep -c \"放行 v3.5 新端口\" /var/log/b-ui-update.log` -> 0, and `grep configure_firewall /opt/b-ui/*.sh | grep -v '() {'` -> empty. I.e. on these two production hosts neither block E nor configure_firewall has ever fired (no host firewall), which is consistent with the dead-function finding and does not rescue the claim.",
    "correction": "Actually true: configure_firewall() (server/core.sh:1237-1283) is dead code with zero callers (install.sh:550-620 run_core_install never calls it; the sole historical caller is legacy b-ui-server.sh:1256). Install-time firewall handling is limited to configure_port_hopping() (core.sh:1374-1416, called at core.sh:1856), which opens only the 20000-30000/udp direct hop range. Consequently update.sh block E (server/update.sh:1754-1767) is the only path that opens 10002/tcp, 40000/udp and 41000-50000/udp, reached via cron `update.sh auto` (core.sh:1223) -> apply_systemd_configs (update.sh:2198/2226), `b-ui` CLI (b-ui-cli.sh:511 -> update.sh:313), or install.sh:793. The real redundancy is the opposite of the claim: core.sh carries an unused function while update.sh does the work.",
    "deletable_under_v4": false
   }
  ],
  "verdict": "REFUTED",
  "deletable_under_v4": false,
  "high_impact": true
 },
 {
  "id": "ctrl-C8",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "1) Key/value content identical, comments differ. `diff <(sed -n '1017,1034p' server/update.sh | grep -v '^#' | grep -v SYSCTL_EOF) <(sed -n '1471,1497p' server/core.sh | grep -v '^#' | grep -v \"cat >\" | grep -v '^EOF')` -> only difference is core.sh's trailing `sysctl --system` line outside the heredoc; all 15 sysctl keys identical (same md5 2ce36dcb… for key/value lines of both templates). core.sh template (server/core.sh:1471-1497) has 9 comment lines; D1 (server/update.sh:1017-1034) has 1. So \"verbatim\" is true for sysctl semantics, not byte-for-byte.\n2) Guard: server/update.sh:1016 `if [[ ! -f /etc/sysctl.d/99-b-ui-network.conf ]]` — D1 only fires when the file is absent.\n3) Who writes the file at fresh install: core.sh:1443 `configure_performance()` contains the block, but core.sh never calls it itself (`grep -n configure_performance server/core.sh` -> only the definition at 1443). It is called from install.sh:594-596 inside `run_core_install()` (install.sh:550), which is the path for install_type 2 (fresh, install.sh:796-798) and install_type 0 (legacy upgrade, install.sh:785-789). `git blame -L 590,597 install.sh` -> 8955e555 2026-05-09 \"v3.4.20 hotfix\"; the comment at install.sh:592-593 says that before v3.4.20 install.sh did NOT call configure_performance, so the sysctl file only appeared 6h later via cron update.sh D1. `git show 9e9a459:install.sh | grep -c configure_performance` -> 0 (v3.4.19 core.sh had the block but install.sh didn't invoke it). So the exact window where D1 was the real writer on a fresh install is v3.4.19 only (same-day hotfix v3.4.20 closed it).\n4) History: `git log -S'99-b-ui-network.conf' -- server/core.sh` -> 9e9a459 (2026-05-09 v3.4.19); `-- server/update.sh` -> 9e9a459, 2a7af40 (2026-06-03 v3.5.14, which added ip_local_reserved_ports to both templates and the D3 append-block at update.sh:1039-1048); `-- server/b-ui-cli.sh` -> 55d3c72 (uninstall rm at b-ui-cli.sh:707).\n5) Live hosts (read-only): bwg-rick (v3.6.3): file dated Jun 3 08:13, 9 comment lines (= current core.sh template), key/value md5 2ce36dcb… == repo template; `grep -c \"应用网络栈调优\" /var/log/b-ui-update.log` -> 0 (D1 never fired). bwg-tizi (v3.6.3, via -J bwg-rick): file dated Jun 3 09:02, 6 comment lines (= v3.4.19-era core.sh template), D1 log count 0, D3 \"补充端口保留\" count 1 (reserved_ports was appended by D3, not D1). On neither production host did D1 ever execute its body.\nCommands: ssh -T -n -o BatchMode=yes bwg-rick '<jq version; ls; grep -c \"^#\"; grep -v \"^#\" | md5sum; grep -c in log; grep -c in /opt/b-ui/core.sh,update.sh>'; same via -J bwg-rick bwg-tizi.",
  "correction": "The substantive conclusion holds (D1 is a guard-skipped no-op on any fresh install, and never fired on either prod host), but two details are imprecise: (a) \"verbatim\" — the 15 sysctl key/value lines are identical, but the file text is not: core.sh:1472-1496 writes 9 comment lines, update.sh:1018 writes 1 (bwg-rick's file has 9 comments, bwg-tizi's 6, proving core.sh, not D1, wrote both). (b) \"any host installed since core.sh gained this block\" is too broad by one release: at v3.4.19 (9e9a459) core.sh had the block but install.sh did not call configure_performance (git show 9e9a459:install.sh -> 0 hits); only from v3.4.20 (8955e55, install.sh:594-596, same day) does a fresh install run it. Hosts installed at exactly v3.4.19 relied on D1 via the 6h cron. Also note D3 (update.sh:1039-1048) is a separate block that did fire on bwg-tizi; it is likewise dead on a fresh install since core.sh:1493 already includes ip_local_reserved_ports.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "ctrl-C9",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"server/update.sh:1050-1070 (D4, conntrack sysctl) writes a file already produced by server/core.sh:1500-1521 (nf_conntrack_max tiering logic) at fresh install.\" No counter-claim, no also_raised_as.\n\nSTATIC:\n- server/core.sh:1443 `configure_performance() {` contains, at 1500-1521, the conntrack block: awk MemTotal → ct_max tier (131072 / >2048MB→262144 / >4096MB→524288), `cat > /etc/sysctl.d/99-b-ui-conntrack.conf` (nf_conntrack_max, udp_timeout=20, udp_timeout_stream=60), writes /etc/modprobe.d/b-ui-nf_conntrack.conf (hashsize=ct_max/4) and /etc/modules-load.d/b-ui-conntrack.conf, modprobe, `sysctl --system`. Unconditional (no existence guard).\n- server/update.sh:756 `apply_systemd_configs() {` contains at 1050-1070 block \"v3.5.14 D4\": identical tiering, identical three files, identical modprobe/sysctl — but guarded by `if [[ ! -f /etc/sysctl.d/99-b-ui-conntrack.conf ]]` (line 1051). Only textual difference: core.sh heredoc has an extra comment line \"# UDP 流…\" (core.sh:1512). `diff` of non-comment heredoc lines: identical.\n- Fresh-install path: install.sh:797-799 case 2 (全新安装) → `run_core_install` (install.sh:550) → install.sh:594-596 `if declare -F configure_performance; then configure_performance; fi` (comment at 592-593 says this call was added precisely so fresh installs no longer wait 6h for update.sh). So on a fresh install core.sh writes the file first; D4's guard then makes it a no-op forever.\n- apply_systemd_configs is invoked only from update.sh (lines 313, 2099, 2198, 2226) — i.e. cron `update.sh auto` (core.sh:1223) / install_type==1 re-run (install.sh:793-794). core.sh never calls update.sh functions.\n- Removal: server/b-ui-cli.sh:708-710 uninstall rm -f's all three files; reinstall recreates via core.sh.\n- git: `git log -S'99-b-ui-conntrack.conf' -- server/core.sh server/update.sh` → single commit 2a7af40 2026-06-03 \"bump: v3.5.14 安装健壮性 + 多用户稳定性根因加固\"; `git blame` update.sh:1050-1051 and core.sh:1500 → 2a7af40. Both blocks born together in v3.5.14, never touched since (current version.json 3.6.3).\n\nLIVE (read-only):\n- `ssh bwg-rick 'ls -l --time-style=long-iso /etc/sysctl.d/99-b-ui-conntrack.conf …; cat …; sysctl -n net.netfilter.nf_conntrack_max; grep -c \"上调 conntrack\" /var/log/b-ui-update.log; jq -r .version /opt/b-ui/version.json'` → file 313 bytes dated 2026-06-03 08:13, contains the extra \"# UDP 流…\" comment (= core.sh variant, matches the 2026-06-03 clean reinstall), nf_conntrack_max=131072, 0 D4 log hits, v3.6.3.\n- `ssh -J bwg-rick bwg-tizi '<same>'` → file 223 bytes dated 2026-06-03 09:02, NO \"# UDP 流\" comment (= update.sh D4 variant, in-place upgrade), nf_conntrack_max=131072, 1 D4 log hit (\"上调 conntrack\"), v3.6.3.\nBoth hosts: same effective keys/values. D4 was the migration shim for pre-v3.5.14 installs; on a fresh v4 install core.sh's configure_performance produces the file before update.sh ever runs and D4's guard at update.sh:1051 short-circuits.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "data-C1",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM data-C1 is correct; COUNTER-claim ctrl-C7 is wrong on its premise.\n\nStatic call graph (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1) `grep -rn 'configure_firewall' --include='*.sh' --include='*.js' --include='*.json' --include='*.md' .` -> only hits: server/core.sh:1237 (definition), b-ui-server.sh:611 (legacy def) + b-ui-server.sh:1256 (legacy self-call, file not in version.json), and the two audit JSON files. Zero call sites in install.sh / server/core.sh / server/update.sh / server/b-ui-cli.sh / web/server.js.\n2) core.sh has no main/dispatch (`grep -nE '^main|BASH_SOURCE' server/core.sh` -> empty; tail is the end of configure_system_hygiene). It is a pure function library. Its only sourcers: install.sh:557 (run_core_install), install.sh:841 (setup_auto_update), server/b-ui-cli.sh:628 (port-hopping menu). `grep -rn 'core\\.sh' install.sh server/*.sh` shows no other loader.\n3) Walked every sourcer: install.sh:550-607 run_core_install calls install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, residential-helper.sh setup, download_client_packages — no configure_firewall. install.sh:843 calls only configure_cron_tasks. b-ui-cli.sh:629 calls only configure_port_hopping. main() at install.sh:764-869 dispatches to run_core_install / check_and_update / configure_residential_interactive only.\n4) Indirect invocation ruled out: `grep -rnE 'configure_\\$|\"configure_|_firewall' install.sh server/*.sh web/server.js` -> only core.sh:1237. `grep -nE 'eval |declare -f|for (fn|f|step) in' ...` -> no dynamic function dispatch in install.sh/core.sh/update.sh (only `declare -F` guards at install.sh:594/599/842 for other functions). `grep -nE \"^\\s*configure_[a-z_]+\" server/core.sh server/update.sh install.sh server/b-ui-cli.sh` lists every direct call: configure_caddy_proxy(core:915), configure_port_hopping(core:1856, cli:629), configure_hysteria/xray/nginx_proxy/performance/system_hygiene/residential_interactive/cron_tasks (install.sh) — never configure_firewall. Heredoc-generated scripts in core.sh (hy2-portjump-cleanup.sh 132-190, cert-sync 940-1040, watchdog 1047-1127, cert-check 1175-1230) contain no firewall calls.\n5) The only ufw/firewalld code that actually runs: core.sh:1407-1416 inside configure_port_hopping (called at core.sh:1856 from start_all_services) — opens only the direct hop range; update.sh:1338-1348 (port-hopping.json migration) and update.sh:1754-1767 (block E: 10002/tcp, 40000/udp, 41000:50000/udp).\n6) git: `git log --oneline -S'configure_firewall' -- server/core.sh install.sh` -> single commit 808f081 (模块化重构) introduced the definition; no commit ever added a caller.\n\nCounter-claim ctrl-C7 (\"core.sh:1252-1270 already opens the v3.5 residential ports at install time\"): lines 1252-1270 are the ufw/firewalld body of configure_firewall() (core.sh:1237-1283) and are therefore never executed on any install path. Decisive lines: core.sh:1237 `configure_firewall() {` … 1258 `ufw allow ${xray_resi}/tcp # v3.5 vless-residential` … 1283 `}` with no caller; install.sh:557-607 (the fresh-install sequence) never names it. So data-C1 is right and ctrl-C7's premise is false — on a fresh core.sh install, 10002/tcp, 40000/udp, 41000-50000/udp (and 22/80/443/PORT/10001) are NOT opened by core.sh at all; update.sh block E (1754-1767) is the only code that opens the v3.5 residential ports on a ufw/firewalld host, i.e. it is reachable and load-bearing, not redundant.\n\nLive hosts (read-only): `ssh bwg-rick 'grep -rn configure_firewall /opt/b-ui/*.sh; ufw status; systemctl is-active firewalld'` and same via `-J bwg-rick bwg-tizi` -> both: only /opt/b-ui/core.sh:1237 definition, \"no ufw\", firewalld inactive, no install log line \"配置防火墙\" (the print_info at core.sh:1249 would have logged if the function had ever run).",
    "deletable_under_v4": true,
    "correction": "Correction applies to the COUNTER-claim ctrl-C7, not to data-C1: core.sh:1252-1270 is dead code inside uncalled configure_firewall() (core.sh:1237-1283), so update.sh:1754-1767 block E is the sole executed opener of the v3.5 residential ports on ufw/firewalld hosts. Deleting configure_firewall() is zero-behaviour-change on a fresh install (never invoked; both production hosts have no ufw and firewalld inactive). Note for v4 design (separate from this claim): if v4 should open base ports on ufw/firewalld hosts, a real call must be added — today only configure_port_hopping (core.sh:1407-1416) opens anything at install time."
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM (data-C1): configure_firewall() at server/core.sh:1237-1283 has zero callers. COUNTER (ctrl-C7): core.sh:1252-1270 \"opens the v3.5 residential ports at install time\".\n\nVERDICT: data-C1 is right; ctrl-C7 is wrong. Lines 1252-1270 are the body of configure_firewall() (def at core.sh:1237, `ufw allow ...` at 1252-1265, `firewall-cmd` at 1267-1280) and nothing ever invokes that function, so those lines never execute at install time or any other time.\n\nStatic grep (repo, worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n  `grep -rn 'configure_firewall' --exclude-dir=.git .` -> only server/core.sh:1237 (definition), b-ui-server.sh:611 (legacy def) + b-ui-server.sh:1256 (legacy self-call; b-ui-server.sh is not in version.json files[] and not used by install.sh), and the two audit JSONs. No call in install.sh, core.sh, update.sh, b-ui-cli.sh, residential-helper.sh, resi-health.sh, web/server.js.\n  `git log --oneline -S'configure_firewall' -- server/core.sh install.sh server/update.sh` -> single commit 808f081 (the modular refactor that introduced it); it was never wired up and never removed.\n\nRUNTIME-INVOCATION lens (every non-grep-able path checked):\n  1. install.sh run_core_install() (install.sh:550-614): explicit sequential call list install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance (guarded by declare -F), configure_system_hygiene (declare -F), then `bash residential-helper.sh setup` and download_client_packages. No configure_firewall. The upgrade path (install.sh:785-789, case 0) reuses run_core_install; case 1 sources update.sh -> check_and_update; case 2 = run_core_install + configure_residential_interactive. setup_auto_update (install.sh:838-855) sources core.sh but only calls configure_cron_tasks / ensure_cron_jobs via declare -F.\n  2. No dynamic dispatch: `grep -n -E '\"\\$@\"|eval |declare -f|\\$\\{?fn\\}?...'` over all shell files shows the only `\"$@\"` dispatchers are b-ui-cli.sh:1240-1246 (status/restart/logs/residential/obfs/export-keys/import-keys) and main \"$@\" in install.sh:869 / b-ui-cli.sh:1381; no eval, no `${func}` indirection, no `for step in ...` loop.\n  3. core.sh has no entrypoint of its own (tail sed -n 1983,1995p ends inside configure_system_hygiene's heredoc/closing brace; no main, no case \"$1\"). It only runs when sourced: install.sh:557, install.sh:840, b-ui-cli.sh:628 — the CLI source at 628 calls only configure_port_hopping (b-ui-cli.sh:629).\n  4. Cron: core.sh:1219-1226 and install.sh:848-853 write exactly three lines (update.sh auto, update.sh kernel, cert-check.sh). update.sh never sources core.sh (update.sh:259/261 only copy the file). \n  5. systemd: all ExecStart/ExecStartPre in core.sh/update.sh/residential-helper.sh point at hysteria/xray/node/sing-box/cert-sync.sh/hy2-watchdog.sh/hy2-portjump-cleanup.sh/resi-health.sh — none run core.sh.\n  6. web/server.js shell-outs (lines 80,101,495,561,1084,1093,1724-1732,2134,2143,2208-2395,2525-2634): ip/getent/curl/residential-helper.sh/systemctl/inline iptables — none source core.sh.\n  7. version.json files[] delivers server/core.sh but delivery is not invocation; b-ui-server.sh (the only file that calls a configure_firewall) is not in the list.\n\nWhat actually opens firewall ports today (the real, running code): core.sh configure_port_hopping():1406-1417 (called at core.sh:1856 from start_all_services and from b-ui-cli.sh:629) opens only the direct hop range; update.sh:1337-1349 (hop range) and update.sh:1754-1767 block \"E. 防火墙放行 v3.5 新端口\" (10002/tcp, 40000/udp, 41000-50000/udp) — these are ufw/firewall-cmd calls written inline, not calls to configure_firewall. So ctrl-C7's substantive point (\"residential ports get opened\") is true only via update.sh's migration block E on the next cron run, not at install time via core.sh:1252-1270.\n\nLive hosts (read-only):\n  `ssh bwg-rick 'grep -rn configure_firewall /opt/b-ui/*.sh /usr/local/bin/b-ui; crontab -l; grep -h ^Exec /etc/systemd/system/*.service /etc/systemd/system/*.timer; command -v ufw; command -v firewall-cmd; systemctl is-active firewalld'` -> only /opt/b-ui/core.sh:1237 (definition); cron = the 3 b-ui lines (+ unrelated other-site-keepalive); no ExecStart references core.sh; ufw: command not found; firewalld inactive; version 3.6.3.\n  `ssh -J bwg-rick bwg-tizi '<same>'` -> only /opt/b-ui/core.sh:1237; same 3 cron lines; \"no ufw\", \"no firewall-cmd\", firewalld inactive; version 3.6.3.\n  Both production hosts have neither ufw nor firewalld, so even if configure_firewall were called it would be a no-op (both branches at core.sh:1251/1266 are `command -v` gated).",
    "deletable_under_v4": true,
    "correction": "ctrl-C7 is incorrect: core.sh:1252-1270 lies inside the never-called configure_firewall() (server/core.sh:1237-1283), so it does not open anything at install time. The v3.5 residential ports are opened only by server/update.sh:1754-1767 (block E, ufw/firewall-cmd inline, runs on cron `update.sh auto`), and the direct hop range by core.sh configure_port_hopping():1406-1417 (called from core.sh:1856 and b-ui-cli.sh:629). Deleting configure_firewall() changes nothing on a fresh install; if v4 wants base-port firewall rules at install time on ufw/firewalld hosts, that is a new call to add, not existing behaviour to preserve."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM (data-C1): configure_firewall() at server/core.sh:1237-1283 has zero callers in install.sh / core.sh / update.sh. COUNTER (ctrl-C7): core.sh:1252-1270 \"opens the v3.5 residential ports at install time\".\n\nVerdict: data-C1 is right; ctrl-C7 is wrong. Lines 1252-1270 are the body of configure_firewall() (definition opens at core.sh:1237 `configure_firewall() {`, closes at 1283) and that function is never invoked, so nothing at those lines runs at install time.\n\n1. Static grep, repo-wide:\n   `grep -rn 'configure_firewall' --include='*.sh' --include='*.js' --include='*.json' .` -> only b-ui-server.sh:611 (legacy def), b-ui-server.sh:1256 (legacy self-call `configure_firewall \"$PORT\" \"$ADMIN_PORT\"`), server/core.sh:1237 (def), and audit JSON files. No call in install.sh, server/core.sh, server/update.sh.\n   No dynamic dispatch either: `grep -n 'configure_\\${\\|\"configure_\\|eval \\|\\$fn\\b' server/core.sh` -> empty.\n   install.sh run_core_install() (install.sh:550-600) calls configure_hysteria (569), configure_xray (570), configure_nginx_proxy (571), and guards only configure_performance (594) / configure_system_hygiene (599) with declare -F; configure_firewall is absent. core.sh start_all_services (core.sh:1855-1859) calls only configure_port_hopping and setup_hy2_watchdog.\n\n2. History: `git log --all -S'configure_firewall' --oneline -- server/ install.sh` -> single commit 808f081 (2025-12-11, \"feat: 模块化重构，支持自动更新\"), which added only the definition (`+configure_firewall() {` at core.sh:890 in that commit); `git show 808f081:install.sh | grep configure_firewall` -> exit 1. Walking every commit that touched install.sh/core.sh/update.sh and grepping each historical blob for a non-definition `configure_firewall` reference -> zero hits. The function has never been called since it was ported from the legacy all-in-one script. The \"v3.5\" residential-port lines inside it (1241-1247, 1259-1260, 1266) were added to a function that was already dead.\n\n3. What actually opens firewall ports: core.sh configure_port_hopping (1406-1416) opens only the direct hop range ${start_port}:${end_port}/udp; update.sh block \"E. 防火墙放行 v3.5 新端口\" (update.sh:1754-1767) opens 10002/tcp, 40000/udp, 41000:50000/udp on every update run. Neither opens 22, PORT, 80, 443, or 10001 - those exist only in the dead function.\n\n4. Live hosts (read-only), commands run via `ssh -o BatchMode=yes bwg-rick` and `ssh -J bwg-rick bwg-tizi`:\n   - both v3.6.3; `grep -rn configure_firewall /opt/b-ui/*.sh` minus the definition -> exit 1 (no caller deployed either).\n   - `command -v ufw` -> not installed on either host; `systemctl is-active firewalld` -> inactive on both; `iptables -S INPUT` -> `-P INPUT ACCEPT` on both. bwg-rick nft ruleset: 0 lines; bwg-tizi nft ruleset: only hysteria_* nat tables (port-hopping DNAT from the hysteria binary), no filter/INPUT hooks.\n   - All service ports listen and are reachable without any firewall rule: udp 10000/40000 hysteria, tcp 10001/10002 xray, tcp/udp 80/443 caddy on both hosts.\n   So on both production machines the function's absence has had no effect (no host firewall exists), which is consistent with nobody noticing it was never wired up.",
    "deletable_under_v4": true,
    "correction": "ctrl-C7 is incorrect: core.sh:1252-1270 lie inside configure_firewall() (def core.sh:1237, close 1283), which has no call site in install.sh, core.sh or update.sh in the current tree, in any historical commit (808f081 onward), or in the deployed /opt/b-ui on bwg-rick/bwg-tizi. The residential ports (10002/tcp, 40000/udp, 41000-50000/udp) are opened only by server/update.sh:1754-1767 (block E, migration path, runs on cron updates) and the direct hop range by core.sh configure_port_hopping:1406-1416; base ports 22/PORT/80/443/10001 are opened by nothing. On both live hosts ufw is not installed, firewalld inactive, INPUT policy ACCEPT, so the gap is currently harmless. Deleting configure_firewall() changes nothing on a fresh install; if v4 wants a host-firewall step it must be newly wired into install.sh's run_core_install, not merely retained."
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "data-C2",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "STATIC CALL GRAPH (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1) `grep -rn \"setup_static_dns\" --include='*.sh' --include='*.js' --include='*.json' .` -> only server/core.sh:1289 (definition) plus audit JSON files. No caller.\n2) Indirect-call sweep: `grep -nE 'eval|\\$\\{?fn\\}?|\\$\\{?func\\}?|static_dns|_dns\\b|declare -f|type -t' install.sh server/core.sh server/update.sh server/b-ui-cli.sh` -> no eval/indirect invocation of any *_dns function; the only `declare -F` guards in install.sh are for configure_performance / configure_system_hygiene.\n3) Entry points of core.sh (it has no `BASH_SOURCE==$0` block, is only sourced): install.sh:557 (`source \"${BASE_DIR}/core.sh\"` inside run_core_install, which then calls install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, residential-helper setup, download_client_packages — no setup_static_dns); install.sh:841 (re-source for configure_cron_tasks only); server/b-ui-cli.sh:628 (source then calls configure_port_hopping only). No heredoc-generated script references it.\n4) History: `git log --all -p -S'setup_static_dns' --oneline | grep -E '^[+-].*setup_static_dns'` -> single hit `+setup_static_dns() {` from 73b20f2 (feat v3.5). It has never had a caller in any commit.\n5) Block D location: `awk 'NR>=756 && NR<=1800 && /^[a-zA-Z_][a-zA-Z0-9_]*\\(\\)/' server/update.sh` -> apply_systemd_configs() opens at 756 and the next real col-0 function is _is_newer() at 1796 (the log()/check_instance() at 1364/1366/1446 are inside heredocs that close before 1508/1539). Heredoc openers 1400-1740 all close before 1738, so update.sh:1738-1751 is live code inside apply_systemd_configs(), which is called at update.sh:313, 2099, 2198, 2226 (cron `update.sh auto`, CLI, and the \"already latest\" path).\n6) Content diff decisive for live attribution: core.sh:1306-1313 heredoc writes a 2-line `# B-UI v3.5: ...` comment header before the nameservers; update.sh:1744-1749 heredoc writes ONLY 4 lines (3x nameserver + options) = exactly 107 bytes.\n\nLIVE HOSTS (read-only):\n- `ssh bwg-tizi` (via -J bwg-rick): `ls -l /etc/resolv.conf; lsattr; cat` -> `-rw-r--r-- 1 root root 107 May 14 06:18`, attrs `----i---------e-------`, content is exactly the 4 lines `nameserver 1.1.1.1 / 8.8.8.8 / 9.9.9.9 / options edns0 timeout:2 attempts:2 single-request` with NO `# B-UI v3.5` comment; systemd-resolved `disabled`/`inactive`; Ubuntu 26.04. 107 bytes with no comment header == update.sh block D output, not core.sh's setup_static_dns output. So block D is the path that actually executed on the production host.\n- `ssh bwg-rick`: /etc/resolv.conf is `nameserver 203.0.113.53 / 203.0.113.54`, not immutable, systemd-resolved `not-found` -> block D's guard (`-L /etc/resolv.conf || grep 127.0.0.53`) never matched, so neither path wrote it; static DNS is simply absent there (consistent with setup_static_dns never running at install).\n- Both hosts: `grep -n setup_static_dns /opt/b-ui/*.sh` -> only /opt/b-ui/core.sh:1289 definition.\n\nCOUNTER-CLAIM ctrl-C6 (\"core.sh:1286-1317 already writes static resolv.conf at install time, making block D unreachable\") is WRONG: the lines it cites are the body of setup_static_dns(), which has zero call sites (evidence 1-4), so nothing runs at install time; and bwg-tizi's resolv.conf bytes match update.sh:1744-1749, proving block D is what reached the host. data-C2 is right on both points (dead function + block D is the live path).",
    "deletable_under_v4": true,
    "correction": "Minor nuance only: on bwg-rick (no systemd-resolved, plain /etc/resolv.conf) block D's guard never fires, so today that host has no static DNS from either path — the claim's \"block D actually executes\" holds for the systemd-resolved (Ubuntu) case, verified on bwg-tizi. For v4: setup_static_dns() in server/core.sh:1289-1319 can be deleted with zero behaviour change (never called). Note however that update.sh block D (1738-1751) is currently the ONLY thing that gives a fresh Ubuntu install static DNS (up to 6h later via cron); if v4 deletes migration blocks wholesale, the static-DNS behaviour must be re-homed in core.sh's install path (e.g. actually call a single function from run_core_install) or it will be lost on fresh installs."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "CODE (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -rn setup_static_dns --include='*.sh' --include='*.js' --include='*.json' .` (excluding docs/superpowers/audits) -> exactly one hit: server/core.sh:1289 `setup_static_dns() {` (the definition). No call site in install.sh, core.sh, update.sh, b-ui-cli.sh.\n- install.sh run_core_install() sequence (awk '/^run_core_install\\(\\)/,/^}/' install.sh): prepare_install_env, install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, download_client_packages. setup_static_dns is absent; core.sh's own internal calls (grep '^    setup_\\|^    configure_' server/core.sh) only invoke setup_cert_sync:910, configure_caddy_proxy:915, configure_port_hopping:1856, setup_hy2_watchdog:1859.\n- `git log -S'setup_static_dns' --oneline --all` -> single commit 73b20f2 \"feat(v3.5): core.sh 双 hy2 + 双 xray 入站 + DoH + setup_static_dns\"; `git show 73b20f2 -- server/core.sh install.sh | grep setup_static_dns` shows only the `+setup_static_dns() {` definition at diff line 441 — it was born without a caller and never gained one.\n- update.sh block D: server/update.sh:1738-1751 inside apply_systemd_configs() (defined :756; called :313, :2099 \"版本相同也跑一次\", :2198, :2226). Introduced by f751b0f \"feat(v3.5): update.sh migration + Web UI 多 URL + global toggle\". Guard :1739 `if [[ -L /etc/resolv.conf ]] || grep -q '127.0.0.53' /etc/resolv.conf`. Its heredoc :1744-1749 is 4 lines with NO comment header (107 bytes); core.sh's heredoc :1306-1313 has two `# B-UI v3.5...` comment lines on top.\n\nLIVE STATE (read-only):\n`ssh bwg-rick '...'` and `ssh -J bwg-rick bwg-tizi '...'` running: ls -l/lsattr/cat /etc/resolv.conf; systemctl is-enabled/is-active systemd-resolved; grep -c setup_static_dns /opt/b-ui/core.sh /opt/b-ui/update.sh; grep for 静态化/静态 DNS in /var/log/b-ui-*.log; jq .version /opt/b-ui/version.json.\n- bwg-tizi (running since 2026-05-09, v3.6.3): /etc/resolv.conf = 107 bytes, attrs `----i---------e-------` (immutable), content exactly `nameserver 1.1.1.1 / 8.8.8.8 / 9.9.9.9 / options edns0 timeout:2 attempts:2 single-request` with no comment header; mtime 2026-05-14 06:18:38 (v3.5 rollout via update). 107 bytes == update.sh block D template (verified with printf | wc -c = 107); core.sh's template would contain the `# B-UI v3.5` header lines. systemd-resolved: disabled/inactive. => static DNS on tizi was written by update.sh block D, not by core.sh.\n- bwg-rick (clean reinstall 2026-06-03, v3.6.3): /etc/resolv.conf = provider DNS `nameserver 203.0.113.53 / 203.0.113.54`, NOT immutable (`--------------e-------`), mtime Sep 2, systemd-resolved `not-found`. /var/log/b-ui-install.log (498 lines) contains no \"静态 DNS\"/\"chattr +i 锁定\" line. => the reinstall never ran setup_static_dns; block D's guard (symlink or 127.0.0.53) is false on rick because systemd-resolved is not installed, so rick has no static DNS at all. This is direct proof that the install path does not write static resolv.conf.\n- On both hosts `grep -c setup_static_dns /opt/b-ui/core.sh`=1 (definition only), `/opt/b-ui/update.sh`=0.\n\nCOUNTER-CLAIM ctrl-C6 (\"core.sh:1286-1317 already writes static resolv.conf at install time, making update.sh block D unreachable\") is WRONG: those lines are the body of a function that nothing invokes (only hit for the identifier is its own definition), and the freshly reinstalled bwg-rick has provider DNS with no chattr lock and no install-log trace of the function. Block D is not only reachable, it is the sole path that ever produced static DNS (bwg-tizi's file matches block D's byte-for-byte template). data-C2 is right.",
    "deletable_under_v4": true,
    "correction": "Minor precision on the claim's last clause: block D \"actually executes\" only when its guard fires (resolv.conf is a symlink or contains 127.0.0.53, i.e. systemd-resolved present). On bwg-rick (no systemd-resolved) neither path ran, so the host currently has unlocked provider DNS — a fresh v4 install on such a host gets no static DNS unless v4 deliberately calls one shared implementation from core.sh. setup_static_dns() itself is dead and deletable with zero behaviour change; block D is not pure migration code (it is the only live implementation) and should be moved/merged into the install path rather than deleted outright."
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM (data-C2): setup_static_dns() core.sh:1289-1319 never called; block D update.sh:1738-1751 is the executing path. COUNTER (ctrl-C6): core.sh already writes static resolv.conf at install time so block D is unreachable.\n\nSTATIC (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1. `grep -rn setup_static_dns --include='*.sh' --include='*.js' --include='*.json' . | grep -v docs/` -> exactly one hit: server/core.sh:1289 (the definition). No caller anywhere.\n2. `grep -rn -E \"resolv\\.conf|systemd-resolved|chattr|static_dns\" install.sh server/*.sh web/server.js b-ui-server.sh` -> only core.sh:1286-1318 (definition body) and update.sh:1738-1751 (block D). No other code touches resolv.conf.\n3. `git log --all -S'setup_static_dns'` -> single commit 73b20f2 (feat v3.5) which added the definition; no commit ever added a call site in install.sh/update.sh/b-ui-cli.sh.\n4. RUNTIME INVOCATION paths checked:\n   - Fresh install (install.sh:797-800 case 2 -> download_all_files; run_core_install). run_core_install (install.sh:550-607) sources core.sh and calls install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, residential-helper.sh setup, download_client_packages. setup_static_dns is absent. core.sh has no self-executing entrypoint (tail of file ends in configure_system_hygiene's `}`), so sourcing never runs it.\n   - install.sh case 1 (install.sh:793-794): `source update.sh; check_and_update` -> apply_systemd_configs (update.sh:313, 2099, 2198).\n   - b-ui-cli.sh:510-512 / 519-521 source update.sh and call check_and_update / update_kernel; b-ui-cli.sh:627-629 sources core.sh but calls only configure_port_hopping.\n   - cron (install.sh:850-851, update.sh:2064-2065; live crontab on both hosts): `update.sh auto` every 6h -> do_update -> apply_systemd_configs (update.sh:2092-2099 runs it even on \"already latest\").\n   - systemd ExecStart/ExecStartPre lines in core.sh/update.sh reference only hysteria/xray/node/sing-box/caddy/hy2-portjump-cleanup.sh/resi-health.sh/cert-sync.sh/watchdog; none run core.sh functions.\n   - web/server.js shell-outs (spawnSync/execSync/execFile lines 80,101,495,561,1084,1093,1724-1732,2134,2143,2208-2355) call only ip/getent/curl/hysteria/xray/sing-box version and residential-helper.sh subcommands; never core.sh.\n   - version.json files[] delivers core.sh/update.sh but that is delivery, not invocation.\n   - Block D (update.sh:1738-1751) lies inside apply_systemd_configs() (opens 756, closes at 1789; the intermediate `log()`/`check_instance()` at 1364-1448 are heredoc-embedded script text), so it executes on every cron/CLI update run, guarded by `[[ -L /etc/resolv.conf ]] || grep -q 127.0.0.53 /etc/resolv.conf`.\n\nLIVE HOSTS (read-only; ssh -o BatchMode=yes -o ConnectTimeout=25 bwg-rick / -J bwg-rick bwg-tizi):\n- bwg-rick (clean reinstall 2026-06-03 per memory): /etc/resolv.conf is a regular file, 46 bytes, `nameserver 203.0.113.53 / 203.0.113.54` (provider DNS), lsattr shows NO immutable flag, systemd-resolved \"inactive / not-found\". `grep -rn setup_static_dns /opt/b-ui/*.sh` minus the definition -> empty. This is a host installed by current core.sh: static DNS was NOT written at install time. This directly falsifies ctrl-C6's premise. Block D also never fired here because the guard (symlink or 127.0.0.53) is false - the host simply has no B-UI static DNS at all.\n- bwg-tizi (older, upgraded host): /etc/resolv.conf regular file, 107 bytes, immutable (`----i---------e-------`), content `nameserver 1.1.1.1 / 8.8.8.8 / 9.9.9.9`, systemd-resolved \"inactive / disabled\". 107 bytes = exactly the 4 lines block D writes (19+19+19+50); core.sh's setup_static_dns would have prepended two `# B-UI v3.5:` comment lines (larger file). So the file on bwg-tizi was produced by update.sh block D, not by core.sh. Both hosts: installed /opt/b-ui/update.sh contains block D (`grep -c \"resolv.conf 静态化\"` = 1). /var/log/b-ui-update.log on both has been rotated (no historical hits), so the file-content fingerprint is the decisive evidence.\n\nCOUNTER-CLAIM RESOLUTION: ctrl-C6 is WRONG. core.sh:1286-1317 only DEFINES setup_static_dns; nothing calls it (grep + install.sh:550-607 call list + git history). A current-core.sh-installed host (bwg-rick) has provider DNS, no immutable flag - proof that install does not write static resolv.conf. Block D is therefore reachable and is the only implementation that ever runs (bwg-tizi's resolv.conf bears block D's exact fingerprint). data-C2 is right on every point.\n\nNOTE ON DELETABILITY: setup_static_dns() is dead and deletable with zero behaviour change. Block D is NOT redundant migration-only code: on a fresh Ubuntu install (systemd-resolved stub 127.0.0.53 present by default) it is what fires on the first `update.sh auto` cron run and provides the static-DNS capability; deleting it without wiring setup_static_dns into run_core_install would lose the behaviour on v4 fresh installs. Also observed: on a fresh install the static DNS only lands up to 6h after install (cron), never during install - a latent gap the v4 rewrite should close by calling one implementation from the install path.",
    "deletable_under_v4": true,
    "correction": "Claim holds as written. Refinement only: the counter-claim ctrl-C6 is refuted (core.sh never invokes setup_static_dns - install.sh:550-607 call list, single grep hit at core.sh:1289, bwg-rick live state shows provider DNS 203.0.113.53 unlocked after a clean core.sh install). deletable_under_v4=true applies to setup_static_dns() (core.sh:1289-1319) only; update.sh:1738-1751 block D is the sole live implementation and fires on fresh Ubuntu installs via cron, so it must be kept or replaced by wiring a single implementation into the install path."
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "data-C3",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "PARTIALLY",
    "evidence": "Substantive claim (core.sh's install_chinese_fonts has zero callers) is CONFIRMED; the literal evidence statement (\"repo-wide grep returns only its own definition line\") is wrong.\n\n1) Repo-wide grep: `grep -rn 'install_chinese_fonts' . --exclude-dir=.git` -> \n   b-ui-server.sh:849 `install_chinese_fonts() {`  (separate legacy copy)\n   b-ui-server.sh:1252 `    install_chinese_fonts`  (call to the LEGACY copy)\n   server/core.sh:394 `install_chinese_fonts() {`\n   + docs/superpowers/audits/*.json (the audit itself). So grep returns 3 code hits, not 1 — but the two extra hits are in b-ui-server.sh, the legacy v3.1 all-in-one installer that is not in version.json's file list (`jq -r '.files[]' version.json` lists install.sh, server/core.sh, server/b-ui-cli.sh, server/update.sh, server/residential-helper.sh, server/resi-health.sh, web/*, b-ui-client.sh — no b-ui-server.sh) and is never referenced by install.sh (`grep -n 'b-ui-server' version.json install.sh` -> empty). It defines its own copy and never sources core.sh.\n\n2) Entry points of server/core.sh (static call graph): `tail -40 server/core.sh` shows no main block / no `[[ \"${BASH_SOURCE[0]}\" == \"$0\" ]]` dispatch; the file is a pure function library. `grep -rn 'core\\.sh' --exclude-dir=.git --exclude-dir=docs .` -> sourced only at install.sh:557 (`source \"${BASE_DIR}/core.sh\"` inside run_core_install), install.sh:841 (re-source for configure_cron_tasks), server/b-ui-cli.sh:628 (source then `configure_port_hopping` only, line 629). update.sh never sources core.sh (`grep -n 'source' server/update.sh` -> none of core.sh). web/server.js only spawns systemctl / residential-helper.sh / curl / getent (grep lines 80,101,294,495,561,2208-2395,2677) — never core.sh.\n\n3) Walk from run_core_install (install.sh:550-615): calls install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, residential-helper.sh setup, download_client_packages. No install_chinese_fonts.\n\n4) Indirect invocation check: `grep -n 'eval\\|\"\\$fn\"\\|\\${fn}\\|\\$func\\|\\$step\\|\"\\$cmd\"' server/core.sh` -> empty (no eval / variable-dispatched function calls in core.sh). Per-function external-ref loop over install.sh, update.sh, b-ui-cli.sh, residential-helper.sh, resi-health.sh: `install_chinese_fonts: 0 external refs` (also 0 for configure_firewall, setup_static_dns, install_caddy, configure_caddy_proxy, setup_cert_sync, wait_and_sync_certs, verify_domain_dns, generate_reality_keys — the latter two/three are called internally within core.sh).\n\n5) Fonts elsewhere: `grep -rn -i 'font' install.sh server/update.sh server/b-ui-cli.sh server/residential-helper.sh server/resi-health.sh web/server.js` -> only install.sh:177-295 web-font (Fontsource CDN woff) download into ${ADMIN_DIR}/fonts — unrelated to system CJK fonts / fc-cache.\n\n6) Git history: `git log --all --oneline -S'install_chinese_fonts' -- install.sh server/` -> only 808f081 (feat: 模块化重构) which introduced the definition; `git show 808f081:install.sh | grep install_chinese_fonts` -> empty. The function has never had a caller in the modular (v3.2+) code path.\n\nNo SSH needed: the claim is about static repo code, not live host state.",
    "correction": "The parenthetical is inaccurate: a repo-wide grep also hits b-ui-server.sh:849 (a separate definition) and b-ui-server.sh:1252 (a call to that legacy copy). Those belong to the legacy v3.1 monolithic installer, which is not in version.json, is not downloaded/used by install.sh, and never sources server/core.sh — so they are not callers of core.sh:394. Correct statement: server/core.sh:394-410 install_chinese_fonts() is dead in every live entry path (install.sh:557 run_core_install, install.sh:841, b-ui-cli.sh:628), with no eval/indirect dispatch; the only invocation anywhere is of the independent duplicate inside the unused legacy b-ui-server.sh.",
    "deletable_under_v4": true
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "Substance of the claim holds; only the parenthetical evidence sentence is imprecise.\n\nREPO (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5, HEAD v3.6.3):\n- `git grep -n install_chinese_fonts` -> server/core.sh:394 (definition only), b-ui-server.sh:849 (a separate copy of the definition) and b-ui-server.sh:1252 (call to that legacy copy). So \"repo-wide grep returns only its own definition line\" is literally wrong, but b-ui-server.sh is the legacy v3.1 all-in-one installer: `jq -r '.files[]' version.json` lists install.sh, server/core.sh, server/b-ui-cli.sh, server/update.sh, server/residential-helper.sh, server/resi-health.sh, web/*, b-ui-client.sh — b-ui-server.sh is not deployed and not referenced from install.sh (`grep -n b-ui-server version.json install.sh` -> empty).\n- The only way core.sh functions run is install.sh sourcing it: install.sh:557 `source \"${BASE_DIR}/core.sh\"` (and install.sh:841 re-source for configure_cron_tasks). `grep -rn install_chinese_fonts install.sh server/update.sh server/b-ui-cli.sh` -> no hits. core.sh has no main/entry body calling it either (`grep -n install_chinese core.sh` -> only :394).\n- Definition body server/core.sh:394-410: apt-get/yum/dnf install fonts-noto-cjk / google-noto-sans-cjk-sc-fonts + fc-cache -fv.\n\nHISTORY:\n- `git log -S'install_chinese_fonts' --oneline --all` -> 808f081 \"feat: 模块化重构，支持自动更新\" and 72eb7ee \"feat: Hysteria2 一键安装脚本 v1.0\".\n- 72eb7ee (hysteria2-install.sh v1.0) had both the definition (+549) and a call (+914). In the modular refactor 808f081 the function was copied into server/core.sh:200 with no caller (`git show 808f081:server/core.sh | grep -n install_chinese_fonts` -> only the definition). `git log -p -- server/core.sh install.sh | grep -E '^[-+].*install_chinese_fonts'` -> a single `+install_chinese_fonts() {` line: no commit in the modular tree ever added a call. Dead since the v2-era refactor.\n\nLIVE HOSTS (read-only):\n- `ssh bwg-rick 'grep -rn install_chinese_fonts /opt/b-ui/; dpkg -l | grep -iE \"fonts-noto|noto-cjk|fontconfig\"; command -v fc-cache'` -> only /opt/b-ui/core.sh:394 (definition); dpkg output empty (no fonts-noto-cjk, no fontconfig); no fc-cache binary; /opt/b-ui/version.json 3.6.3. Host reinstalled 2026-06-03 and the function's side effects are absent -> it never ran.\n- `ssh -J bwg-rick bwg-tizi '<same>'` -> only /opt/b-ui/core.sh:394; dpkg shows fontconfig/fontconfig-config/libfontconfig1 only (base-system deps), NOT fonts-noto-cjk; version 3.6.3. Running since 2026-05-09, fonts-noto-cjk never installed -> function never ran there either.\n- No log on either host mentions \"中文字体\" (grep over /opt/b-ui/*.log, /var/log/b-ui*).\n\nNet: in the deployed file set (version.json) install_chinese_fonts() is defined once in core.sh and has zero callers; the only caller in the repo is b-ui-server.sh:1252 calling its own legacy copy, which is not shipped. The claim's grep wording overstates (\"only its own definition line\") but the conclusion is correct.",
    "deletable_under_v4": true,
    "correction": "Minor: `grep -rn install_chinese_fonts` returns 3 hits, not 1 — b-ui-server.sh:849 (legacy copy of the definition) and b-ui-server.sh:1252 (call to that legacy copy) in addition to server/core.sh:394. This does not rescue the core.sh function: b-ui-server.sh is not in version.json's file list and is not invoked by install.sh, and b-ui-server.sh:1252 calls its own in-file copy, not core.sh's. Both production hosts lack fonts-noto-cjk, proving the core.sh copy has never executed."
   },
   {
    "lens": "runtime",
    "verdict": "PARTIALLY",
    "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"install_chinese_fonts() at server/core.sh:394-410 is defined but has no callers anywhere in the repo (repo-wide grep for 'install_chinese_fonts' returns only its own definition line).\" No counter-claim, no also_raised_as.\n\n1) Repo grep — the literal evidence statement is FALSE:\n   `grep -rn 'install_chinese_fonts' --exclude-dir=.git --exclude-dir=node_modules .` returns FOUR hits, not one:\n   - server/core.sh:394 `install_chinese_fonts() {` (definition)\n   - b-ui-server.sh:849 `install_chinese_fonts() {` (a second, separate definition in the legacy v3.1 all-in-one installer)\n   - b-ui-server.sh:1252 `    install_chinese_fonts` (a CALL — inside the legacy installer's own install flow)\n   - the audit JSON files themselves.\n   So \"repo-wide grep returns only its own definition line\" is not reproducible. However the b-ui-server.sh call targets b-ui-server.sh's own copy, not core.sh's; b-ui-server.sh is not in version.json `files[]` (jq '.files[]' lists install.sh, server/core.sh, server/b-ui-cli.sh, server/update.sh, server/residential-helper.sh, server/resi-health.sh, web/*, b-ui-client.sh — no b-ui-server.sh), and nothing except its own self-help text (b-ui-server.sh:64-65) references it.\n\n2) Runtime-invocation lens for the core.sh copy — no path executes it:\n   - core.sh is a pure function library, only ever `source`d: install.sh:557 `source \"${BASE_DIR}/core.sh\"`, install.sh:841 (same, guarded), server/b-ui-cli.sh:628 `source \"${BASE_DIR}/core.sh\"` followed only by `configure_port_hopping` (b-ui-cli.sh:629). update.sh only downloads/chmods core.sh (update.sh:259, 294, 2135) and never sources it. core.sh has no `$1`-driven dispatcher: the only `case \"$arg\"` at core.sh:147-156 is inside the heredoc that writes /opt/b-ui/hy2-portjump-cleanup.sh (CLEANUP_EOF), not core.sh's own control flow.\n   - install.sh's explicit call list after sourcing (install.sh:560-600): install_hysteria, install_nodejs, install_nginx, install_xray, collect_user_input, configure_hysteria, configure_xray, configure_nginx_proxy, deploy_admin_panel, create_services, start_all_services, create_global_command, install_tui_tools, harden_ssh, configure_performance, configure_system_hygiene, configure_cron_tasks — install_chinese_fonts absent. `grep -n '^\\s*install_[a-z_]*\\s*$' server/core.sh install.sh server/update.sh server/b-ui-cli.sh` → only install_hysteria/nodejs/nginx/xray/tui_tools (install.sh), install_caddy (core.sh:365), install_tui_tools (update.sh:2322).\n   - No dynamic name invocation: `grep -rn 'eval\\|\"\\$fn\"\\|declare -f\\|compgen -A function' server/*.sh install.sh` → only a comment at b-ui-client.sh:2217 (\"替代 eval\"). `declare -F` in install.sh:842-845 only tests configure_cron_tasks/ensure_cron_jobs.\n   - web/server.js shell-outs (lines 80,101,294,495,561,1084,1093,1724-1732,2134,2143,2208-2395,2525-2677) invoke ip/getent/systemctl/curl/residential-helper.sh/hysteria/xray/sing-box/tail — never core.sh or bash -c with a function name.\n   - Cron lines written by the installer (install.sh:848-853, core.sh configure_cron_tasks) and systemd ExecStart/ExecStartPre (core.sh:205-267, 1016-1017, 1102) run update.sh / cert-check.sh / hysteria / cert-sync / watchdog scripts — none source core.sh.\n   - fc-cache/fonts-noto-cjk appear nowhere else in delivered code; install.sh:279-296 downloads woff2 web fonts (Fontsource CDN) into ADMIN_DIR/fonts, which is unrelated to system CJK font packages.\n\n3) Live hosts (read-only) confirm it never ran:\n   bwg-rick (`ssh bwg-rick 'grep -rn install_chinese_fonts /opt/b-ui /opt/hysteria /usr/local/bin; ls -d /opt/hysteria; crontab -l; grep -rln \"core.sh\\|b-ui-server\" /etc/systemd/system; dpkg -l | grep -i \"noto-cjk\\|fonts-\"'`): only /opt/b-ui/core.sh:394 definition; /opt/hysteria absent (legacy installer never used); crontab = update.sh auto / update.sh kernel / cert-check.sh (+ unrelated other-site-keepalive); no systemd unit references core.sh; NO fonts-* packages at all; version 3.6.3.\n   bwg-tizi (via -J bwg-rick, same command): only /opt/b-ui/core.sh:394; /opt/hysteria absent; same three cron lines; no systemd refs; installed fonts = fonts-dejavu-core, fonts-dejavu-mono only (no fonts-noto-cjk); version 3.6.3.\n   Since the function does `apt-get install -y fonts-noto-cjk` and neither production host has that package, it has never executed there.\n\n4) git: `git log --oneline -S'install_chinese_fonts' -- server/core.sh` → single commit 808f081 \"feat: 模块化重构，支持自动更新\" — it was copied in from the legacy monolith at the modularization and never wired up.\n\nConclusion: the substantive point (core.sh's install_chinese_fonts is dead code with no runtime path) is CONFIRMED; the stated evidence (\"grep returns only its own definition line\") is wrong because the legacy b-ui-server.sh defines and calls a same-named function at :849/:1252.",
    "deletable_under_v4": true,
    "correction": "The function IS dead in the delivered code path, but the grep evidence as stated is false: b-ui-server.sh:849 defines its own install_chinese_fonts and b-ui-server.sh:1252 calls it. That file is the legacy v3.1 monolith, not in version.json files[] and not downloaded/sourced by install.sh, update.sh, b-ui-cli.sh or web/server.js, so it does not make core.sh:394-410 reachable. Correct statement: \"server/core.sh:394-410 install_chinese_fonts() has no callers in any delivered file (install.sh, server/*.sh, web/server.js); the only call to a function of that name is the legacy root-level b-ui-server.sh:1252 invoking its own copy at b-ui-server.sh:849.\" Deleting core.sh:390-410 (comment block + function) changes nothing on a fresh v4 install; neither bwg-rick nor bwg-tizi has fonts-noto-cjk installed, proving it never ran."
   }
  ],
  "verdict": "PARTIALLY",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "data-C4",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static (repo worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -n -E 'install_nginx|configure_nginx_proxy|install_caddy|configure_caddy_proxy' install.sh` -> only `562:    install_nginx` and `571:    configure_nginx_proxy` (inside the fresh-install branch right after `557: source \"${BASE_DIR}/core.sh\"`).\n- Same grep on server/core.sh -> `328:install_caddy() {`, `364:install_nginx() {` / `365:    install_caddy`, `849:configure_caddy_proxy() {`, `914:configure_nginx_proxy() {` / `915:    configure_caddy_proxy`. Lines 363 and 913 are the comment `# 兼容旧调用`. The wrappers are exactly one-line delegates; the caddy-named functions have zero call sites by their real name in install.sh or core.sh.\n- Repo-wide grep (excluding .git and audits): the only other hits are b-ui-server.sh:676/997/1251/1259 — the legacy v3.1 all-in-one installer, which defines its OWN install_nginx/configure_nginx_proxy and never sources core.sh (b-ui-server.sh:920 only downloads core.sh). Not in version.json's file list, so irrelevant to the delegate chain.\n- Other sourcers of core.sh: server/b-ui-cli.sh:628 sources it but only calls `configure_port_hopping` (b-ui-cli.sh:629); install.sh:841 re-sources it on re-run but only for configure_cron_tasks; server/update.sh never sources core.sh (only downloads it, update.sh:259/294). web/server.js has no shell-out to these functions (only reads /etc/caddy/Caddyfile at server.js:405). No cron/systemd ExecStart references (`grep` on bwg-rick /etc/cron.d, crontab -> 0 hits).\n- Git: `git log -S'install_nginx' -- server/core.sh install.sh` -> 808f081 (模块化重构, original nginx implementation). `git log -S'install_caddy'` -> 314c99b \"feat(caddy): 替换 nginx+certbot 为 Caddy 自动 HTTPS\" whose message says \"保留 install_nginx/configure_nginx_proxy 兼容别名\"; that commit touched only server/core.sh (1 file), and `git show 314c99b:install.sh` still called install_nginx (447) / configure_nginx_proxy (456). `git log -S'install_caddy' -- install.sh` and `-S'configure_caddy_proxy' -- install.sh server/update.sh server/b-ui-cli.sh` -> empty: install.sh has never called the caddy names.\nLive (read-only): `ssh bwg-rick 'grep -n ... /opt/b-ui/core.sh'` -> identical lines 328/364-365/849/914-915; /opt/b-ui/version.json = 3.6.3; /opt/b-ui/install.sh does not exist on the host (install.sh is always fetched fresh from GitHub per README:138, so it is never version-skewed from core.sh). bwg-tizi not queried: claim is purely static and bwg-rick already confirms the deployed copy.\nCounter-claim: none recorded.",
  "deletable_under_v4": true,
  "correction": "Minor precision only: the \"compat aliases\" were kept in 314c99b because that commit did not touch install.sh, not because any external caller needed them. Deleting install_nginx()/configure_nginx_proxy() (core.sh:363-366, 913-916) is zero-behaviour-change on a fresh install provided install.sh:562 and :571 are renamed to install_caddy / configure_caddy_proxy in the same release (install.sh and core.sh always ship together via version.json). Nothing in update.sh, b-ui-cli.sh, cron, systemd or the panel calls the nginx-named wrappers, so no upgrade-path dependency exists even for v3.5.x/v3.6.x installs.",
  "high_impact": false
 },
 {
  "id": "data-C5",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): no counter-claim, no also_raised_as.\n\nStatic (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- web/server.js:336 builds ONE array `realityClients = realityUsers.map(u => ({ id: u.uuid, flow: \"xtls-rprx-vision\", email: u.username }))`; web/server.js:339-342 loops `for (const inboundTag of [\"vless-direct\", \"vless-residential\", \"vless-reality\"])` and assigns `inbound.settings.clients = realityClients` to every matching inbound — identical uuid/email set on both :10001 and :10002 (comment at :334 says \"共用 clients/SNI\"). Caller: web/server.js:286 `updateXrayConfig(vlessUsers, ...)` from saveUsers(), where vlessUsers = every user with a uuid (:282), no direct/residential distinction per user.\n- server/core.sh:794-828 fresh-install template: both inbounds get the same `{\"id\": \"${FIRST_USER_UUID}\", ..., \"email\": \"${FIRST_USER}\"}` clients; :835-836 routing is keyed solely on inboundTag: `{\"inboundTag\": [\"vless-direct\"], \"outboundTag\": \"direct\"}` / `{\"inboundTag\": [\"vless-residential\"], \"outboundTag\": \"relay\"}`. server/update.sh:1678 v3.5 migration clones vless-direct into vless-residential (same clients) and :1689-1690 installs the same two inboundTag rules.\n- `grep -n '\"user\"' server/core.sh server/update.sh web/server.js` -> no Xray routing rule uses the `user`/email selector; the only per-connection discriminator is the inbound (i.e. the port dialled). Subscription/URI generators encode the choice as port only: web/server.js:709-723, :915-919, :1854-1866 (`includeResi ? 10002 : 10001`), web/app.js:314, :563 (\"勾选 → 节点指 :10002 vless-residential 经住宅 IP；不勾 → :10001 直连\").\n- git log -S: loop introduced in 4328926 (feat v3.5), core.sh rules in 73b20f2 (feat v3.5); unchanged since.\n\nLive hosts (read-only, secrets dropped):\n`ssh bwg-rick` and `ssh -J bwg-rick bwg-tizi` running `jq '[.inbounds[]|{tag,port,nclients}]'` + sha256 diff of sorted {id,email,flow} for the two inbounds + `.routing.rules`:\n- bwg-rick: vless-direct:10001 nclients=5, vless-residential:10002 nclients=5, client-set identical: YES, routing = [api->api, vless-direct->direct, vless-residential->relay].\n- bwg-tizi: same shape, 5/5, identical: YES, same three rules (users.json has 6 entries; the 6th has no uuid, so correctly absent from Xray per server.js:282).\n\nNuance (does not refute): there IS a server-side routing rule, but it is per-inbound (inboundTag), not per-user/per-connection — so the account's direct-vs-residential choice is indeed determined purely by which port the client dials, exactly as claimed.",
  "deletable_under_v4": false,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "data-C6",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- server/core.sh:641-653 (heredoc for config-residential.yaml): `outbounds: - name: relay type: socks5 socks5: addr: \"127.0.0.1:2080\"` + `- name: direct ... mode: 4` then `acl: inline: - relay(all)`. Unconditional; no variable/branch around it. The `direct` outbound in that file is never referenced by any ACL rule.\n- server/core.sh:829-836: outbounds `{\"tag\":\"relay\",\"protocol\":\"socks\",\"servers\":[{\"address\":\"127.0.0.1\",\"port\":2080}]}` and routing rule `{\"type\":\"field\",\"inboundTag\":[\"vless-residential\"],\"outboundTag\":\"relay\"}` — unconditional.\n- server/residential-helper.sh:462-504 `write_singbox_config_direct()`: single outbound `[{\"type\":\"direct\",\"tag\":\"direct\"}]`, `route.final: \"direct\"`; called from :612/:616 (write_singbox_config_from_state, empty pool / enabled=false), :707 (`enable --remove` last URL) and :743 (`disable`). Comment at :741: \"v3.5.0: 不停 b-ui-relay 服务，写空池直连配置 → hy2-resi/vless-resi fallback 直连\"; header :5 \"b-ui-relay (sing-box) 永远运行\".\n- `grep -n \"config.yaml\\|xray-config\\|hysteria-residential\" server/residential-helper.sh` → zero hits: the helper never rewrites config-residential.yaml or xray-config.json, so the hy2/xray side cannot be flipped to bypass the relay; the only place the relay is bypassed is hy2-direct/vless-direct (update.sh:437 comment \"hy2-direct 内置直连，绕开 b-ui-relay\").\n- git: `git log --oneline -S'relay(all)' -- server/core.sh` → 73b20f2 (feat v3.5 双 hy2) and 9e9a459; `git log -S'write_singbox_config_direct'` → 71067ec, 2d37f6e \"feat(residential): sing-box as permanent relay, always-on regardless of mode\" — i.e. the always-transit design is deliberate and unchanged since v3.5.\n- update.sh:1527-1621 (block A) and :1654-1711 duplicate the same relay(all)/outboundTag relay content for upgrades — same unconditional shape.\n\nLive (read-only):\n- `ssh bwg-rick 'grep -nA3 \"^outbounds:\\|^acl:\" /opt/b-ui/config-residential.yaml; jq -c .routing.rules /opt/b-ui/xray-config.json; systemctl is-active b-ui-relay hysteria-residential xray; ss -ltnp | grep 2080'` → acl:58-60 `relay(all)`; xray rules `vless-residential → relay`; all three active; sing-box (pid 641496) listening 127.0.0.1:2080; state {enabled:true,global:true,urls:1}, singbox-relay.json final=resi-pool.\n- `ssh -J bwg-rick bwg-tizi '...'` → identical: acl:48-50 `relay(all)`, :43 addr 127.0.0.1:2080, same xray rules, all active, sing-box on :2080, {enabled:true,global:true,urls:1}.\nBoth hosts currently run with a non-empty pool, so the \"direct-mode still hops through sing-box\" scenario is not live now, but the code paths (disable / --remove) reproduce it exactly as claimed. No counter-claim exists.",
  "deletable_under_v4": false,
  "correction": "Nuance only: the extra hop in empty-pool mode is the intended fail-open design (commit 2d37f6e, helper header line 5, spec docs/superpowers/specs/2026-09-10-residential-hardening-design.md:85-86), not an accident. The code the claim names (core.sh:641-653, :830-836, residential-helper.sh:462-504) is the live residential capability path plus its fail-open fallback and is exercised by `disable`/`enable --remove` on a fresh install, so it is not deletable under v4. Only the duplicated migration copies in server/update.sh:1527-1621 and :1654-1711 (not named by the claim) are v4-deletable.",
  "high_impact": false
 },
 {
  "id": "eff-C1",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static: web/server.js:4 imports execSync; :1071 `function fetchXrayUserStats()`; :1076 `loadUsers().filter(u => u.protocol === \"vless-reality\" || u.protocol === \"vless-ws-tls\")`; :1077 `for (const u of users)` sequential; :1083-1084 `execSync(upCmd, { encoding: \"utf8\", timeout: 3000 })` for uplink; :1092-1093 `execSync(downCmd, ...)` for downlink — exactly two synchronous child processes per matched user, in series, no async/exec/execFile variant. Note :1085-1092 does not use the cheaper single `xray api statsquery` call that returns every counter at once. Callers: :1118 fetchXrayOnline(), :1136 getMergedStats(), :1142 getMergedOnline(); runtime invocation: :1423-1465 traffic-sync `setInterval(...,10000)` calls getMergedStats(); :2068-2069 GET /api/stats and /api/online call getMergedStats()/getMergedOnline(); web/app.js:109 `setInterval(load, 5000)` so a logged-in panel adds 2 more fetchXrayUserStats() runs per 5 s. Git: `git log -S'fetchXrayUserStats' -- web/server.js` -> cdb5605 \"fix: 修复 VLESS/WS 流量统计和在线设备显示\" (introduced there, never restructured). Live cost per spawn: bwg-rick `time xray api stats ...` real 0.035s; bwg-tizi real 0.024s (so ~50-70 ms of event-loop block per matched user per call; 3 calls/10 s with panel open). Practical-impact caveat (does not refute the code claim): on both production hosts zero users match the :1076 filter — bwg-rick users.json protocols: fusion 3, hysteria2 1, unset 1; bwg-tizi: fusion 4, hysteria2 1, unset 1 (jq group_by, credentials dropped) — so today the loop body runs 0 times and spawns nothing; it only bites for users created with the index.html:198/199 \"vless-reality\"/\"vless-ws-tls\" options. Side finding: `xray api statsquery` on bwg-rick shows `user>>>…>>>traffic>>>uplink/downlink` counters exist for the fusion users (2 each, names redacted), yet the :1076 filter excludes protocol \"fusion\", so VLESS traffic of fusion users is never accounted by the panel (correctness gap, same function). journalctl b-ui-admin last 24h: 0 \"Xray stats query failed\" lines. No counter-claim recorded.",
  "deletable_under_v4": false,
  "correction": "Claim is accurate as written. Nuance: the blocking loop is currently a no-op on both production servers because no user has protocol vless-reality/vless-ws-tls (fusion users are excluded by web/server.js:1076), so the real defect is twofold — sync execSync-per-user design AND fusion users' VLESS traffic being silently uncounted. Fix is a rewrite (one async `xray api statsquery`, include fusion users), not deletion: fetchXrayUserStats is live traffic-accounting code, not migration code, so v4 clean-reinstall does not make it deletable.",
  "high_impact": false
 },
 {
  "id": "eff-C2",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static: web/server.js:1071 `function fetchXrayUserStats()` loops loadUsers() filtered to vless users and per user runs two blocking `execSync(\"xray api stats …uplink\")` / `…downlink` (lines 1083, 1092) => 2N subprocess spawns, no memo/cache variable anywhere (grep -n \"fetchXrayUserStats\\|fetchXrayOnline\\|getMergedStats\\|getMergedOnline\\|Cache\" web/server.js shows only lines 1071,1113,1118,1134,1136,1140,1142,1426,2068,2069 — no cache). web/server.js:1113-1131 `fetchXrayOnline()` calls `fetchXrayUserStats()` at 1118 (only extra work: compares with module-level `lastXrayTraffic`). web/server.js:1134-1138 `getMergedStats()` calls `fetchXrayUserStats()` directly at 1136; web/server.js:1140-1144 `getMergedOnline()` calls `fetchXrayOnline()` at 1142. Routes: web/server.js:2068 `if (r === \"stats\") return sendJSON(res, await getMergedStats());` and :2069 `if (r === \"online\") return sendJSON(res, await getMergedOnline());`. \"Invoked together\" is real, not hypothetical: web/app.js:114 `Promise.all([api(\"/users\"), api(\"/online\"), api(\"/stats\")])` inside `load()`, driven by web/app.js:109 `setInterval(load, 5000)` — every open panel tab fires both routes every 5 s. Additionally web/server.js:1423-1460 traffic-sync `setInterval(async () => { const stats = await getMergedStats(); … }, 10000)` runs a third fetchXrayUserStats every 10 s. Git: `git log -S'function fetchXrayOnline' -- web/server.js` => cdb5605 (Thu Dec 11 2025, \"fix: 修复 VLESS/WS 流量统计和在线设备显示\") introduced both fetchXrayOnline and getMergedOnline; untouched since.\nRuntime reproduction (scratch dir, stub `xray` on PATH logging every invocation, 3 vless users in users.json, `BASE_DIR=$S/bui-dev ADMIN_DIR=$PWD ADMIN_PORT=18099 ADMIN_PASSWORD=test123 node server.js`, login via POST /api/login, then `curl /api/stats` followed by `curl /api/online`): after /api/stats: 6 xray spawns; after /api/online too: 12 xray spawns; `sort | uniq -c` shows every `-name user>>>uN>>>traffic>>>uplink|downlink` query executed exactly 2 times; after waiting 11 s the tracker tick brought the count to 18. So for N users, one panel refresh cycle costs 4N serial blocking execSync spawns (each with a 3 s timeout) plus 2N more per tracker tick — exactly the double work the claim describes. Minor precision note (does not refute): the two calls are not byte-identical in side effects — fetchXrayOnline() also updates `lastXrayTraffic` (server.js:1125) — but the subprocess work (the O(N) cost) is identical. No counter-claim exists. Test artefacts were confined to the scratchpad; the panel's kernel-sync wrote a gitignored packages/ dir into the worktree which I removed; `git status` is back to the pre-run state.",
  "deletable_under_v4": false,
  "correction": "Not a migration/upgrade-path artefact: fetchXrayUserStats/fetchXrayOnline/getMergedStats/getMergedOnline are the live traffic-accounting and online-count capability on a fresh install, so nothing here is deletable under the v4 clean-reinstall decision. The redundancy is fixable by a short-TTL memo of fetchXrayUserStats() (or a single merged /api endpoint), which is a refactor, not a deletion.",
  "high_impact": false
 },
 {
  "id": "eff-C3",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static call graph (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- web/app.js:100-110 `init()` calls `load()` then `setInterval(load, 5000);` (line 109). No clearInterval / visibilitychange guard anywhere (`grep -n \"setInterval\\|clearInterval\\|visibilitychange\\|document.hidden\" web/app.js` -> only line 109).\n- web/app.js:113-114 `function load() { Promise.all([api(\"/users\"), api(\"/online\"), api(\"/stats\")]) ...`; web/app.js:37-43 `api(ep)` does `fetch(\"/api\" + ep, …)` -> GET /api/online and GET /api/stats every 5 s.\n- web/server.js:1688-1689 `if (p.startsWith(\"/api/\")) { const r = p.slice(5);` then line 2068 `if (r === \"stats\") return sendJSON(res, await getMergedStats());` and line 2069 `if (r === \"online\") return sendJSON(res, await getMergedOnline());`.\n- web/server.js:1134-1138 `getMergedStats()` -> `fetchXrayUserStats()`; web/server.js:1140-1144 `getMergedOnline()` -> `fetchXrayOnline()`; web/server.js:1113-1118 `fetchXrayOnline()` itself calls `const currentStats = fetchXrayUserStats();`.\n- web/server.js:1071-1111 `fetchXrayUserStats()` loops over every vless user and runs two blocking `execSync(\"xray api stats … uplink\")` / `(\"… downlink\")` (lines 1083, 1092, timeout 3000 each) with no cache/memo (`grep -n cache web/server.js` shows only _hostV4Cache and kernel-version cache, nothing for stats). So each of the two requests independently performs a full 2×N-user sweep of synchronous xray CLI spawns.\n- Extra, beyond the claim: web/server.js:1424-1460 a server-side `setInterval(async () => { const stats = await getMergedStats(); …}, 10000)` traffic-sync loop runs a third full sweep every 10 s regardless of whether the panel is open. With a dashboard open that is 2.5 full sweeps / 5 s (each sweep = 2 execSync per vless user, event-loop blocking).\n- History: `git log -S'setInterval(load, 5000)' -- web/app.js` -> introduced in 808f081 (模块化重构); `git log -S'fetchXrayUserStats' -- web/server.js` -> cdb5605 (fix: 修复 VLESS/WS 流量统计和在线设备显示). Unchanged since.\nLive check: `ssh bwg-rick 'grep -n \"setInterval(load\" /opt/b-ui/admin/app.js; grep -n \"if (r === \\\"stats\\\")\\|if (r === \\\"online\\\")\" /opt/b-ui/admin/server.js; jq -r .version /opt/b-ui/version.json'` -> `109:    setInterval(load, 5000);`, lines 2068/2069 identical, deployed version 3.6.3. Deployed code matches the worktree. No counter-claim exists for eff-C3.",
  "deletable_under_v4": false,
  "correction": "Claim is accurate as stated. Only refinement: fetchXrayUserStats/fetchXrayOnline are synchronous (execSync), so the two \"independent\" sweeps are serialized in the single Node event loop rather than concurrent, and the server's own 10 s traffic-sync loop (server.js:1424-1460) adds a third sweep. The code is not migration-only and cannot be deleted without behaviour change (dashboard would stop refreshing); it is a redundancy candidate for a shared cached sweep (one fetchXrayUserStats result reused by /stats, /online and the sync loop), not a deletion candidate.",
  "high_impact": false
 },
 {
  "id": "eff-C4",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Static (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -n 'setInterval\\|getMergedStats\\|execSync' web/server.js` -> web/server.js:1423 `setInterval(async () => {` ... web/server.js:1426 `const stats = await getMergedStats();` ... web/server.js:1461 `}, 10000);` — top-level, module scope, no gating on any dashboard/session state; it is the traffic-accounting writer (updates u.usage.total / u.usage.monthly and writeFileSync users.json at 1454).\n- web/server.js:1134-1138 `getMergedStats()` = `await fetchStats(\"/traffic\")` (one HTTP GET to Hysteria traffic API) + `fetchXrayUserStats()`.\n- web/server.js:1071-1111 `fetchXrayUserStats()` runs 2 x `execSync(xray api stats ...)` per user in a sequential loop — BUT only over `loadUsers().filter(u => u.protocol === \"vless-reality\" || u.protocol === \"vless-ws-tls\")` (line 1076).\n- Other triggers: web/app.js:109 `setInterval(load, 5000)` -> app.js:114 `api(\"/online\"), api(\"/stats\")` -> server.js:2068 `getMergedStats()` and 2069 `getMergedOnline()` (-> fetchXrayOnline -> fetchXrayUserStats, line 1119). So structurally there are indeed three independent callers of fetchXrayUserStats with no shared cache: /api/stats, /api/online, and the 10s loop. That part of the claim is CONFIRMED; line number 1423 is exact.\n- History: `git log -S'fetchXrayUserStats' -- web/server.js` -> cdb5605 2025-12-11 \"fix: 修复 VLESS/WS 流量统计和在线设备显示\" (introduced), 808f081 (modularisation). The vless-reality/vless-ws-tls filter dates from before the \"fusion\" protocol was introduced and was never widened (lines 707/910/1851 treat fusion as having VLESS creds, but 1076 does not).\n\nLive (read-only):\n- `ssh bwg-rick 'jq \"[.[]|.protocol]|group_by(.)|map({p:.[0],n:length})\" /opt/b-ui/users.json; grep -c \"setInterval(async\" /opt/b-ui/admin/server.js; jq -r .version /opt/b-ui/version.json; systemctl is-active b-ui-admin'` -> protocols: null x1, fusion x3, hysteria2 x1; setInterval present (1); v3.6.3; active.\n- `ssh -J bwg-rick bwg-tizi '<same>'` -> null x1, fusion x4, hysteria2 x1; setInterval present; v3.6.3; active.\n- Consequence: on BOTH production hosts the filter at line 1076 matches ZERO users, so fetchXrayUserStats spawns zero execSync children; the 10s loop's real cost is one localhost HTTP request to Hysteria + one users.json read/parse. The \"same O(N) execSync work\" is O(0) in production today — the execSync cost the claim describes only materialises for legacy `vless-reality`/`vless-ws-tls` users, which neither server has.\n\nWhy PARTIALLY rather than CONFIRMED: the structural fact (third uncached caller, runs regardless of dashboard) is correct; the quantitative framing (O(N) execSync per 10s) is not reproducible on either live host because N is 0. Side finding surfaced while refuting: because of that same filter, Xray/VLESS traffic of `fusion` users is never accounted at all (the loop only ever sees Hysteria2 /traffic counters), which is a correctness gap independent of this efficiency claim.",
  "deletable_under_v4": false,
  "correction": "The loop at web/server.js:1423 is not migration code and not redundant as a whole: it is the only place that persists traffic usage (usage.total / usage.monthly, lines 1442-1455) and therefore the basis of traffic-limit enforcement (checkUserLimits, line 1149) — a retained product capability. What is actually true about the execSync cost: fetchXrayUserStats (web/server.js:1076) only queries users whose protocol is literally \"vless-reality\" or \"vless-ws-tls\"; on bwg-rick and bwg-tizi (v3.6.3) all users are fusion/hysteria2/null, so the loop currently spawns no xray subprocesses at all. The cuttable redundancy is only the lack of a shared short-TTL cache between getMergedStats (1134), getMergedOnline (1140) and the loop — and if v4 fixes the filter to include fusion users, that cache becomes necessary, since then /stats + /online every 5 s plus the loop every 10 s would each spawn 2N xray CLI processes.",
  "high_impact": false
 },
 {
  "id": "eff-C5",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "PARTIALLY",
    "evidence": "STATIC (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- server/residential-helper.sh:506-522 `start_relay_service()` heredoc writes /etc/systemd/system/b-ui-relay.service with only Type/ExecStart/Restart/RestartSec/LogRateLimit* — no LimitNOFILE, MemoryHigh, MemoryMax, Nice. CONFIRMED.\n- `grep -rn 'b-ui-relay.service.d\\|RELAY_UNIT\\|RELAY_SERVICE=' server web install.sh` → only residential-helper.sh:39,40,507,538,783,788. No drop-in or other writer of the relay unit anywhere (update.sh:661-670 migrate_relay_log only re-invokes `reapply`, which calls the same start_relay_service).\n- Call graph of the unit writer: start_relay_service ← reload_relay_service (527-533) and direct calls at 671 (setup), 785 (reapply); reload_relay_service ← 690,713,728,744 (enable variants), 823 (set-domains), 850 (global). External callers: install.sh:606 (`setup`), install.sh:652 (`enable`), update.sh:347/545/666/2216 (`reapply`), b-ui-cli.sh:1005-1025, web/server.js:46 (residentialHelper path). Every path ends in the same heredoc, so the omission is universal.\n- server/core.sh:197-238 hysteria-server override: Nice=-5 (210), LimitNOFILE=1048576 (211), MemoryHigh=500M/MemoryMax=700M (223-224). core.sh:244-274 hysteria-residential: LimitNOFILE=1048576 (258), Nice=-5 (260), MemoryHigh=300M/MemoryMax=500M (263-264). CONFIRMED.\n- server/core.sh:1766-1784 xray drop-in: CPUSchedulingPolicy/Nice=-5 (1778)/LimitNOFILE=1048576 (1779) — NO MemoryHigh/MemoryMax. The claim's wording \"which all explicitly set LimitNOFILE=1048576 plus memory limits\" is WRONG for xray. (b-ui-admin gets MemoryHigh=150M/MemoryMax=200M at core.sh:1754-1755, not xray.)\n\nLIVE (read-only; `ssh -o BatchMode=yes -o ConnectTimeout=25 bwg-rick '…'` and `… -J bwg-rick bwg-tizi '…'`), identical on both hosts:\n- `cat /etc/systemd/system/b-ui-relay.service` == the heredoc above; `ls /etc/systemd/system/b-ui-relay.service.d` → No such file or directory.\n- `systemctl show b-ui-relay -p LimitNOFILE -p LimitNOFILESoft -p MemoryHigh -p MemoryMax -p Nice` → LimitNOFILE=524288 LimitNOFILESoft=1024 MemoryHigh=infinity MemoryMax=infinity Nice=0 (i.e. inherited from `systemctl show -p DefaultLimitNOFILE -p DefaultLimitNOFILESoft` → 524288 / 1024).\n- hysteria-server, hysteria-residential: LimitNOFILE=1048576/Soft=1048576, Nice=-5, MemoryHigh/Max set. xray: LimitNOFILE=1048576/Soft=1048576, Nice=-5, MemoryHigh=infinity MemoryMax=infinity.\n- `grep \"open files\" /proc/<relay MainPID>/limits` → \"Max open files 524287 524288\" (bwg-rick pid 647972, bwg-tizi pid 1414183); hysteria pid → \"1048576 1048576\". Relay `ps -o ni` → 0; fd count 61 (rick) / 35 (tizi).\n\nCOUNTER-CLAIM resolution: the prod-state reader's \"ulimits (1048576 open files) match\" is correct ONLY for hysteria-server/hysteria-residential/xray; it did NOT cover the sing-box relay PID. The relay's /proc limits are 524287/524288, not 1048576, on both hosts. So the counter-claim does not refute eff-C5; the original claim is right about the relay unit. Mitigating fact the claim omits: the Go runtime raises the soft limit to the hard limit at start, so the effective relay fd ceiling is ~524k (not the systemd soft 1024), and the relay currently uses <100 fds — so this is a consistency gap, not an observed fault.",
    "correction": "Core of the claim holds (relay unit lacks LimitNOFILE/MemoryHigh/MemoryMax/Nice; confirmed in code at server/residential-helper.sh:507-522 and live on both hosts). Incorrect detail: xray does NOT set memory limits — server/core.sh:1766-1784 contains only CPUSchedulingPolicy/Nice=-5/LimitNOFILE=1048576, and live `systemctl show xray` reports MemoryHigh=infinity MemoryMax=infinity. Only the two hysteria units (core.sh:223-224, 263-264) and b-ui-admin (core.sh:1754-1755) set memory limits. Also, the practical fd gap is 524288 vs 1048576 (Go auto-raises soft→hard), not 1024 vs 1048576.",
    "deletable_under_v4": false
   },
   {
    "lens": "history-live",
    "verdict": "PARTIALLY",
    "evidence": "REPO (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5, v3.6.3):\n- server/residential-helper.sh:506-524 start_relay_service() writes b-ui-relay.service with only Type/ExecStart/Restart=always/RestartSec=3/LogRateLimitIntervalSec/LogRateLimitBurst — no LimitNOFILE, MemoryHigh, MemoryMax, Nice. (Claim's \"507-522\" is off by one; body of the heredoc is 507-522, function 506-524.)\n- `grep -n -E 'LimitNOFILE|MemoryHigh|MemoryMax|Nice=' server/*.sh` → hits only in core.sh (210,211,223,224 hysteria-server; 258,260,263,264 hysteria-residential; 1754,1755 b-ui-admin; 1778,1779 xray) and update.sh (898-912, 1076-1081, 1301-1302, 1553-1559). Zero hits in residential-helper.sh. `grep -n b-ui-relay server/update.sh` → only C3 block (update.sh:662-667) which strips StandardOutput=null and lets reapply rewrite the unit; no migration adds limits to the relay unit.\n- DETAIL WRONG IN CLAIM: xray override (core.sh:1766-1784) sets CPUSchedulingPolicy/Nice=-5/LimitNOFILE=1048576 only — NO MemoryHigh/MemoryMax. So \"all ... plus memory limits\" is false for xray; only the two hysteria units have memory limits (core.sh:223-224, 263-264).\n- HISTORY: `git log --oneline -S'B-UI Outbound Relay' -- server/residential-helper.sh` → 2d37f6e \"feat(residential): sing-box as permanent relay, always-on regardless of mode\" (v3.5 relay introduction). `git log -S'LogRateLimitBurst=200'` → f403df0 (稳定性专项二期). Unit never gained limits since introduction.\n\nLIVE (read-only; ssh bwg-rick / ssh -J bwg-rick bwg-tizi; `systemctl show <svc> -p MainPID -p LimitNOFILE -p MemoryHigh -p MemoryMax -p Nice -p DropInPaths` + `grep 'open files' /proc/$pid/limits` + `cat /etc/systemd/system/b-ui-relay.service`; both hosts on v3.6.3):\n- bwg-rick b-ui-relay (PID 647972): LimitNOFILE=524288, MemoryHigh=infinity, MemoryMax=infinity, Nice=0, DropInPaths= (empty; /etc/systemd/system/b-ui-relay.service.d does not exist). /proc/647972/limits: \"Max open files 524287 524288\". Unit file on disk is byte-identical to the heredoc in residential-helper.sh:507-522.\n- bwg-tizi b-ui-relay (PID 1414183): identical — LimitNOFILE=524288, Memory*=infinity, Nice=0, no drop-in, /proc limits \"Max open files 524287 524288\".\n- Both hosts: `systemctl show -p DefaultLimitNOFILE` → 524288, i.e. the relay just inherits the systemd default.\n- For contrast on both hosts: hysteria-server LimitNOFILE=1048576 Nice=-5 MemoryHigh=524288000 MemoryMax=734003200; hysteria-residential LimitNOFILE=1048576 Nice=-5 MemoryHigh=314572800 MemoryMax=524288000; xray LimitNOFILE=1048576 Nice=-5 MemoryHigh=infinity MemoryMax=infinity (confirms xray has no memory limit live either).\n\nCOUNTER-CLAIM RESOLUTION: the prod-state reader's \"ulimits (1048576 open files) match\" is correct ONLY for hysteria-server, hysteria-residential and xray. It did not cover the sing-box relay PID: on both hosts /proc/<relay pid>/limits shows 524288, not 1048576, and MemoryMax=infinity/Nice=0. So the original claim's core is right and the counter-observation does not contradict it — it simply never looked at b-ui-relay.\n\nNet: CONFIRMED that b-ui-relay.service lacks all four settings in code and on both live hosts (relay runs at systemd default 524288 fds, no cgroup memory cap, nice 0). REFUTED on the sub-assertion that xray sets memory limits (it sets none, in code and live).",
    "deletable_under_v4": false,
    "correction": "Claim's comparison set is overstated: xray's override (server/core.sh:1766-1784) sets only CPUSchedulingPolicy=other, Nice=-5, LimitNOFILE=1048576 — no MemoryHigh/MemoryMax (live: MemoryHigh=infinity, MemoryMax=infinity on both hosts). Only hysteria-server (core.sh:223-224) and hysteria-residential (core.sh:263-264) carry memory limits. The b-ui-relay part of the claim is fully accurate: server/residential-helper.sh:506-524 writes no limits and no migration/drop-in adds them; live relay PID limits are 524288 fds (systemd DefaultLimitNOFILE), Nice=0, no memory cap on both bwg-rick and bwg-tizi. Nothing here is migration-only code to delete; the finding is a missing setting, not redundancy."
   },
   {
    "lens": "runtime",
    "verdict": "PARTIALLY",
    "evidence": "CORE CLAIM CONFIRMED. Repo: server/residential-helper.sh:506-527 start_relay_service() writes the unit heredoc (lines 507-522) containing only Type/ExecStart/Restart/RestartSec/LogRateLimit* — no LimitNOFILE, MemoryHigh, MemoryMax or Nice. `grep -rn 'b-ui-relay.service.d\\|DefaultLimitNOFILE\\|limits\\.d\\|limits\\.conf\\|/etc/systemd/system\\.conf' server/*.sh install.sh web/server.js` -> no hits: no drop-in or global default is ever written for the relay. Runtime-invocation lens: every path that (re)creates the unit ends in the same template — residential-helper.sh:531 (reload_relay_service), :671 (enable), :785 (reapply, comment at :776 says the unit is rewritten on migration); server/update.sh:661-671 migrate_relay_log() shells `residential-helper.sh reapply`; web/server.js:2257 spawnSync(helperPath, [\"enable\",\"-\"]). None of these add limits. Peers in repo: core.sh:210-211,223-224 (hysteria-server Nice=-5, LimitNOFILE=1048576, MemoryHigh=500M, MemoryMax=700M); core.sh:258-264 (hysteria-residential LimitNOFILE=1048576, Nice=-5, MemoryHigh=300M, MemoryMax=500M); core.sh:1777-1779 (xray Nice=-5, LimitNOFILE=1048576).\n\nLIVE (both hosts, read-only): `systemctl show b-ui-relay -p MainPID,LimitNOFILE,LimitNOFILESoft,MemoryHigh,MemoryMax,Nice,DropInPaths` -> bwg-rick: LimitNOFILE=524288 LimitNOFILESoft=1024 MemoryHigh=infinity MemoryMax=infinity Nice=0 DropInPaths=(empty); bwg-tizi: identical. `ls /etc/systemd/system/b-ui-relay.service.d` -> No such file or directory on both. Deployed unit file content matches the repo template byte-for-byte (no limits). By contrast hysteria-server/hysteria-residential/xray on both hosts show LimitNOFILE=1048576 LimitNOFILESoft=1048576 Nice=-5. systemd 259; DefaultLimitNOFILE=524288 / DefaultLimitNOFILESoft=1024 (system.conf line is still commented `#DefaultLimitNOFILE=1024:524288`).\n\nCOUNTER-CLAIM (\"ulimits 1048576 match on both hosts\") is WRONG as applied to the relay: `grep 'open files' /proc/<relay MainPID>/limits` -> \"Max open files 524287 524288\" on bwg-rick (pid 647972) and bwg-tizi (pid 1414183). The 1048576 observation only covers hysteria/xray. The relay's soft limit is 524287 (not 1024) only because the Go runtime auto-raises soft to hard at startup — it inherits systemd's 524288 default, half of the peers' 1048576. Practical impact today is small (relay holds 63 fds on rick, 35 on tizi) but the unit itself sets nothing.\n\nWHY ONLY PARTIALLY: the claim's clause \"xray ... which all explicitly set LimitNOFILE=1048576 plus memory limits\" is inaccurate for xray — core.sh:1766-1784 sets Nice/LimitNOFILE only, no MemoryHigh/MemoryMax; live confirms xray MemoryHigh=infinity MemoryMax=infinity on both hosts. Only the two hysteria units carry memory limits.",
    "correction": "Correct statement: b-ui-relay.service (residential-helper.sh:507-522) sets no LimitNOFILE/MemoryHigh/MemoryMax/Nice; hysteria-server (core.sh:210-224) and hysteria-residential (core.sh:258-264) set LimitNOFILE=1048576 + Nice=-5 + MemoryHigh/MemoryMax; xray (core.sh:1777-1779) sets LimitNOFILE=1048576 + Nice=-5 but NO memory limits. Live relay PID limits are 524287/524288 (systemd default, Go-raised), not 1048576 — the counter-claim's \"match\" did not include the relay.",
    "deletable_under_v4": false
   }
  ],
  "verdict": "PARTIALLY",
  "deletable_under_v4": false,
  "high_impact": true
 },
 {
  "id": "eff-C7",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- web/server.js:303-314 updateHysteriaResidentialConfig(): builds the whole `auth:\\n  type: userpass\\n  userpass:` block from ALL users (line 307-308), regex-replaces the entire `auth:` section (309), `if (next === c) return;` (310), else writeFileSync + `restartServiceAsync(\"hysteria-residential\")` (312).\n- web/server.js:316-326 updateHysteriaConfig(): identical pattern, `if (next === c) return;` (322), `restartServiceAsync(\"hysteria-server\")` (324).\n- web/server.js:292-300 restartServiceAsync() = detached `systemctl restart <unit>` — a full process restart, not a reload.\n- web/server.js:276-289 saveUsers() calls both on every user save (283, 285), so any username/password add/remove/change changes the serialized block -> whole instance restarts. Only limit/expiry-only edits are skipped by the `next === c` guard (comment on 310: \"改限额/到期日不再踢掉所有在线用户\").\n- No hot path exists: `grep -n \"ExecReload|SIGHUP|kill -HUP\" server/core.sh server/update.sh web/server.js` -> no hits; live `systemctl cat hysteria-server` on both hosts shows no ExecReload. Hysteria2 userpass has no in-place reload; the only hot-updatable auth mode is `type: http`, which the project deliberately abandoned: core.sh:560-564/618-622 first writes `type: http` (url http://127.0.0.1:8080/auth/hysteria) and then core.sh:662 calls apply_hy2_userpass_auth() (core.sh:669-691) which rewrites both configs to userpass on fresh install; update.sh:1090-1121 does the same migration for existing installs (rationale in comment: http auth makes the single-threaded panel a SPOF under reconnect storms). The `/auth/hysteria` endpoint (server.js:2691-2716) still exists but is unreachable on a fresh install.\n- git: `git log -S'if (next === c) return' -- web/server.js` -> 2840628 2026-09-10 \"fix(panel): 用户变更只在配置真变化时非阻塞重启对应服务\"; `git log -S'type: userpass' -- server/core.sh web/server.js` -> 5b6cde3 2026-06-03 \"v3.5.15 高并发硬化 — hy2 auth http→userpass\". So restart-on-any-credential-change has been the design since v3.5.15; v3.6.0 only narrowed it to \"block actually changed\".\nLive (read-only, no credentials printed):\n- `ssh bwg-rick 'awk ... /opt/b-ui/config.yaml /opt/b-ui/config-residential.yaml'` -> both `auth: type: userpass`; deployed /opt/b-ui/admin/server.js lines 310/312/322/324 identical to worktree; version.json 3.6.3; no ExecReload in unit.\n- `ssh -J bwg-rick bwg-tizi ...` -> same: both configs `type: userpass`, same server.js lines, version 3.6.3, no ExecReload.\nAttempted refutation: looked for a per-user hot-update mechanism (HTTP auth in use, SIGHUP/ExecReload, trafficStats API used for auth) — none active; the trafficStats API is only used for stats/kick (server.js:1052, 2070), not auth. The claim's factual description is exact; the only nuance is that the restart is the intentional cost of choosing local userpass over the http-callback SPOF, and that limit/expiry edits already avoid it.",
  "deletable_under_v4": false,
  "correction": "Not a refutation, only context: the restart-on-change behaviour is not migration residue — it is the live user-sync path on a fresh install (saveUsers -> updateHysteria*Config -> systemctl restart) and must stay unless v4 re-adopts Hysteria2 `type: http` auth (which would revive the currently dead server.js:2691 /auth/hysteria endpoint and re-introduce the panel-as-SPOF that v3.5.15 removed). What IS dead on a fresh v4 install is the `/auth/hysteria` endpoint itself and the `type: http` intermediate write in core.sh:560-564/618-622 (immediately overwritten by apply_hy2_userpass_auth at core.sh:662), plus the update.sh:1090-1121 http->userpass migration block.",
  "high_impact": false
 },
 {
  "id": "hist-C1",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Literal claim is false, substantive point (file frozen since 2026-03-20) is true.\n\n1) \"zero commits since 523b18c\" — REFUTED. `git log --oneline 523b18c..HEAD -- b-ui-server.sh | wc -l` = 6. The six commits, all dated 2026-03-20 (same day as 523b18c, 13:47 +0800; last one ea0e64b at 15:21:16 +0800), each change exactly one line (`git log --stat 523b18c..HEAD -- b-ui-server.sh` shows \"1 insertion(+), 1 deletion(-)\" for every one): 29de054 SCRIPT_VERSION 3.0.0→3.0.1, 1f0b363 →3.0.2, 1ac94a9 →3.0.3, 54430af →3.1.0, e2897a2 →3.1.1, ea0e64b →3.1.2. Full `git log --oneline -- b-ui-server.sh` lists these 6 above 523b18c, so the auditor's own cited command contradicts the claim. Blob SHAs differ: `git rev-parse 523b18c:b-ui-server.sh` = 8233718…, `HEAD:b-ui-server.sh` = 25cab04…; `git diff 523b18c HEAD -- b-ui-server.sh` = single hunk at line 10 `-SCRIPT_VERSION=\"3.0.0\"` / `+SCRIPT_VERSION=\"3.1.2\"`. So the file is NOT byte-identical to 523b18c; the \"SCRIPT_VERSION never bumped past pre-rewrite version\" line in the raw reader evidence is also wrong — it was bumped 6 times, after the rewrite.\n\n2) \"1332 lines both\" — CONFIRMED: `wc -l b-ui-server.sh` = 1332, `git show 523b18c:b-ui-server.sh | wc -l` = 1332 (but equal line count ≠ unchanged content, see above).\n\n3) Substantive point (frozen for the second half of project history) — CONFIRMED: last touching commit is ea0e64b 2026-03-20 15:21:16 +0800 (`git log -1 --format='%h %ad' --date=iso -- b-ui-server.sh`); nothing since, across all commits to HEAD 2929947 (v3.6.2, 2026-09-11). Correct statement would be \"zero commits since ea0e64b (v3.1.2 bump, 2026-03-20), i.e. only same-day SCRIPT_VERSION bumps after 523b18c.\"\n\n4) Reachability (supports deletability): repo-wide `grep -rn 'b-ui-server' --include='*.sh,*.js,*.json,*.md,*.html'` hits only its own self-referential curl lines (b-ui-server.sh:64-65), CLAUDE.md:20 (documenting it as legacy), and two pre-rewrite docs (docs/cert-health-check-requirement.md, docs/hy2-tls-debug-report.md) with stale line refs (L2094, L2253 — file is only 1332 lines). `jq -r '.files[]' version.json` lists install.sh, server/*.sh, web/*, b-ui-client.sh — b-ui-server.sh absent, so install.sh never downloads it. Live hosts (read-only): bwg-rick and bwg-tizi both `ls /opt/b-ui/b-ui-server.sh /opt/hysteria` → \"No such file or directory\"; `grep -rl b-ui-server /opt/b-ui /etc/systemd/system /etc/cron* /var/spool/cron` → no output; no systemd unit references legacy `/opt/hysteria`; both at version.json 3.6.3. No counter-claim exists for this id.",
  "deletable_under_v4": true,
  "correction": "The file did receive commits after 523b18c: six same-day (2026-03-20) one-line SCRIPT_VERSION bumps, the last being ea0e64b (3.1.1→3.1.2) at 15:21 +0800; b-ui-server.sh:10 now reads `SCRIPT_VERSION=\"3.1.2\"` whereas 523b18c:b-ui-server.sh:10 reads `SCRIPT_VERSION=\"3.0.0\"` (blob 8233718 vs 25cab04). The line count (1332) matches but the content does not. The accurate claim is \"no commits since ea0e64b on 2026-03-20; only trivial version bumps after the v3.0.0 rewrite commit 523b18c\". The file is unreferenced by install.sh/version.json/panel/cron/systemd on both production hosts and can be deleted in v4 with zero behaviour change.",
  "high_impact": false
 },
 {
  "id": "hist-C2",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"b-ui-client.sh:183-184 and server/b-ui-cli.sh:96-97 hardcode TUI_AVAILABLE=false ... making every gum/fzf branch ... permanently unreachable dead code.\" COUNTER: none. ALSO: none.\n\nStatic call graph (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1. Only assignment sites in the whole repo: `grep -rn TUI_AVAILABLE .` (excl .git/docs) -> b-ui-client.sh:184 `TUI_AVAILABLE=false` (comment :183 \"永远不会变 true\") and server/b-ui-cli.sh:97 `TUI_AVAILABLE=false` (comment :96 \"永远为 false\"). Every other hit is a read: client :204,:211,:217,:225,:237,:250,:275,:302 (tui_info/success/error/spin/confirm/menu/filter/write) plus inline gates :5346 (tui_switch_node fzf) and :5413 (tui_import_node gum write); cli :115,:122,:128,:135,:146,:157 (tui_info/success/error/spin/confirm/menu) plus :307 (status box gum style). No `command -v gum ... && TUI_AVAILABLE=true` line exists anymore (only in docs/superpowers/plans/2026-05-07-cli-redesign.md:138 and specs, which are not executed).\n2. Entry points: b-ui-client.sh:5798 `if [[ \"${BASH_SOURCE[0]}\" == \"${0}\" ]]` -> `auto` => auto_update_all, else first_run_setup; main \"$@\". server/b-ui-cli.sh:1381 `main \"$@\"` (no guard). cli sources ${BASE_DIR}/update.sh (:511,:520) and core.sh (:628): `grep -n TUI_AVAILABLE server/update.sh server/core.sh install.sh` -> no matches, so sourced files never flip it. No eval/indirect \"$fn\" calls reference the flag (grep -E 'eval |BASH_SOURCE|^\\s*(source|\\.) ' shows only /etc/os-release, proxy.sh, .ufw_state, update.sh, core.sh). web/server.js serves the client script with only a SCRIPT_VERSION substitution (web/server.js:34), no TUI patching. No script in the repo sources bui-c and sets the flag (grep for b-ui-client.sh/bui-c outside changelogs: only install.sh:246 file list and HANDOVER.md prose).\n3. Dynamic reproduction: `TUI_AVAILABLE=true PATH=<stubs with gum/fzf that print \"GUM INVOKED\">:$PATH bash -c 'source ./b-ui-client.sh; echo $TUI_AVAILABLE; tui_info hello; tui_success ok; tui_error bad; echo x | tui_write'` -> prints `client TUI_AVAILABLE=false` and the plain ANSI echo fallbacks; the gum stub was never invoked. The environment override is clobbered by line 184.\n4. Call-site count correction: the claim's \"~20+8\" undercounts. Code-line call sites of tui_* (excluding definitions/comments): b-ui-client.sh 81 (tui_info 36, tui_success 14, tui_error 23, tui_spin 4, tui_confirm 3, tui_menu 2, tui_filter 1, tui_write 0), server/b-ui-cli.sh 7 (tui_info 3, tui_success 2, tui_error 1, tui_menu 1; tui_spin/tui_confirm 0). Additionally tui_write (client) and tui_spin/tui_confirm (cli) have zero callers at all, so those functions are dead in their entirety, not just their gum branch.\n5. Related-but-not-refuting facts: install.sh:586 and server/update.sh:2322 still call install_tui_tools() (install.sh:688, update.sh:2237) which downloads gum/fzf to /usr/local/bin — binaries nothing consumes since the flag is constant. b-ui-client.sh:4640-4649 (uninstall cleanup of /usr/local/bin/{gum,fzf}) is a reachable gum/fzf-related branch but is not gated by TUI_AVAILABLE and is not one of the helpers the claim names. Lock-in introduced by commits ee03b99 / 72558e3 lineage and version.json 3.4.10 changelog (\"TUI_AVAILABLE 永久锁为 false\").",
    "deletable_under_v4": true,
    "correction": "Only the count is off: the gum/fzf-gated branches sit behind 8 client helpers + 2 inline gates (client :5346,:5413) and 6 cli helpers + 1 inline gate (cli :307), with 81 + 7 code call sites (not ~20+8). Substantive claim stands: with TUI_AVAILABLE a plain constant `false` (b-ui-client.sh:184, server/b-ui-cli.sh:97) and no other assignment anywhere in the executed code, every `[[ \"$TUI_AVAILABLE\" == \"true\" ]]` branch is unreachable; deleting the gum/fzf branches (keeping the else bodies), the uncalled tui_write/tui_spin/tui_confirm helpers, and install_tui_tools (install.sh:688, update.sh:2237 and their call sites :586/:2322) changes no behaviour on a fresh v4 install."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "Source (worktree HEAD, v3.6.3):\n- b-ui-client.sh:183-184: `# TUI_AVAILABLE 保留为常量 false 兼容旧代码里的 if 分支，但永远不会变 true` / `TUI_AVAILABLE=false` (unconditional assignment, so an env var cannot override it).\n- server/b-ui-cli.sh:96-97: `# 旧分支保留但 TUI_AVAILABLE 永远为 false` / `TUI_AVAILABLE=false`.\n- `grep -n 'TUI_AVAILABLE=' b-ui-client.sh server/b-ui-cli.sh server/core.sh server/update.sh install.sh` -> only those two lines assign it; nothing else (no sed -i in update.sh, no `command -v gum && TUI_AVAILABLE=true`) ever sets it true.\n- Gated gum/fzf arms (all guarded by `[[ \"$TUI_AVAILABLE\" == \"true\" ]]`): b-ui-client.sh:204,211,217 (gum style), 225 (gum spin), 237 (gum confirm), 250 (gum choose), 275 (fzf), 302 (gum write), 5346 (fzf node switch), 5413 (gum write import) = 10 arms; server/b-ui-cli.sh:115,122,128 (gum style), 135 (gum spin), 146 (gum confirm), 157 (gum choose), 307 (gum style box) = 7 arms. Every `gum`/`fzf` invocation in both files sits inside one of these arms (grep -nE '\\bgum\\b|\\bfzf\\b'), except b-ui-client.sh:4640-4649 which only offers to `rm -f /usr/local/bin/{gum,fzf}` at uninstall (live, not gated).\n- Nuance on the claim's wording: the tui_* wrapper functions themselves are NOT dead — their else/fallback arms are the live path and are called ~80 times in b-ui-client.sh (tui_info/success/error/spin/confirm/menu at 2524..5554) and 5 times in b-ui-cli.sh (895,900,1348,1353,1357). Only the gum/fzf arms (10+7, not \"~20+8 call sites\") are unreachable.\n\nHistory:\n- `git log -S'TUI_AVAILABLE=false' --oneline` -> ee03b99 (2026-05-07 feat(bui-c): add TUI helpers…) and 72558e3 (2026-05-07 feat(b-ui): … gum TUI main menu) introduced TUI_AVAILABLE with runtime detection `command -v gum && command -v fzf && TUI_AVAILABLE=true`.\n- `git log -S'TUI 路径已废弃' --oneline` -> c76ba3a 2026-05-08 \"refactor(ui): replace gum arrow menus with numeric two-column layout\" (v3.4.10). `git show c76ba3a` diff removes `-command -v gum &>/dev/null && command -v fzf &>/dev/null && TUI_AVAILABLE=true` in both files and removes ensure_tui_tools() from the client; version.json changelog 3.4.10 item (4): \"TUI_AVAILABLE 永久锁为 false\". Untouched since (v3.4.10 -> v3.6.3).\n- Related leftover not in the claim: install.sh:688 install_tui_tools() (called at install.sh:586 on fresh install) and server/update.sh:2237 (called at update.sh:2322 in interactive mode) still download gum/fzf from github.com/charmbracelet — binaries that no shipped code path consumes.\n\nLive hosts (read-only):\n- `ssh bwg-rick 'grep -n \"TUI_AVAILABLE=\" /opt/b-ui/b-ui-cli.sh; ls -la /usr/local/bin/gum /usr/local/bin/fzf'` -> `97:TUI_AVAILABLE=false`; gum and fzf absent (reinstalled 2026-06-03, v3.6.3).\n- `ssh -J bwg-rick bwg-tizi '<same>'` -> `97:TUI_AVAILABLE=false`; `/usr/local/bin/gum` (v0.17.0) and `/usr/local/bin/fzf` present, dated May 9 11:21 (installed by the v3.4.x installer), yet unused because the constant is false. Neither host has bui-c installed.",
    "deletable_under_v4": true,
    "correction": "Minor: the tui_* wrapper functions are not dead code — their fallback arms are the only reachable path and are heavily used (~80 calls in b-ui-client.sh, 5 in server/b-ui-cli.sh). What is dead is the `TUI_AVAILABLE` constant plus the 10 (client) + 7 (cli) gum/fzf arms; these can be collapsed to the fallback bodies with zero behaviour change. Additionally install_tui_tools() in install.sh:688-750 and server/update.sh:2237-2299 (+ callers at install.sh:586 and update.sh:2321-2323) install gum/fzf that nothing uses and are likewise deletable."
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "Static facts (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- b-ui-client.sh:183-184: \"# TUI_AVAILABLE 保留为常量 false 兼容旧代码里的 if 分支，但永远不会变 true\" / `TUI_AVAILABLE=false`. server/b-ui-cli.sh:96-97: \"# 旧分支保留但 TUI_AVAILABLE 永远为 false\" / `TUI_AVAILABLE=false`.\n- `grep -rn 'TUI_AVAILABLE=' . --include='*.sh'` -> exactly two assignments (the two above), both plain `=false` (not `${TUI_AVAILABLE:-...}`), so an environment variable cannot pre-set it. Reproduced: `PATH=<stub gum>:$PATH TUI_AVAILABLE=true bash -c 'source ./b-ui-client.sh; echo $TUI_AVAILABLE; tui_info hello'` prints `TUI_AVAILABLE after source=false` and the plain ANSI echo path; the stub gum was never invoked.\n- Every gum/fzf invocation in the tui_* helpers is inside `[[ \"$TUI_AVAILABLE\" == \"true\" ]]`: b-ui-client.sh:204-218 (tui_info/success/error), 225-226 (tui_spin), 237-238 (tui_confirm), 250-251 (tui_menu), 275-276 (tui_filter), 302-303 (tui_write), 5346-5348 (fzf node switch), 5413-5416 (gum write import); server/b-ui-cli.sh:115-129, 135-136, 146-147, 157-158, 307-311 (gum style status box). `grep -c '\"$TUI_AVAILABLE\" == \"true\"'` = 10 (client) / 7 (cli); gum/fzf invocation lines = 15 (client, all gated) / 8 (cli, all gated).\nRuntime-invocation lens (why nothing else can flip it):\n- Nothing sources either file: `grep -rn 'source.*b-ui-client\\|source.*b-ui-cli' --include='*.sh' --include='*.js'` -> only b-ui-client.sh:453 (a download URL string). b-ui-cli.sh sources update.sh/core.sh (b-ui-cli.sh:511,520,628), never the reverse. install.sh sources core.sh/update.sh (557,793,841) only.\n- /usr/local/bin/b-ui is a symlink/exec of /opt/b-ui/b-ui-cli.sh (install.sh:627,806) with no env injection. Cron on both hosts: only `update.sh auto`, `update.sh kernel`, `cert-check.sh` — none touch b-ui-cli.sh/b-ui-client.sh. No systemd unit/drop-in mentions TUI_AVAILABLE/gum/fzf (`grep -rl` over /etc/systemd/system on both hosts -> none).\n- web/server.js serves b-ui-client.sh (lines 1530-1670) and only rewrites `SCRIPT_VERSION` via injectClientVersion (server.js:31-35, regex `^SCRIPT_VERSION=\".*\"`); no TUI rewrite. server.js shell-outs (spawnSync/execSync at 80,101,294,495,561,1084,1724-1732,2134-2330) target ip/getent/systemctl/curl/residential-helper.sh — never b-ui-cli.sh.\n- Live hosts (read-only): bwg-rick `/opt/b-ui/b-ui-cli.sh:97:TUI_AVAILABLE=false`, `/opt/b-ui/b-ui-client.sh:184:TUI_AVAILABLE=false`, no gum/fzf binaries. bwg-tizi: same two lines, BUT `/usr/local/bin/gum` and `/usr/local/bin/fzf` exist (dated May 9, v3.4.5-era) — decisive: even with the binaries present, the flag stays false, i.e. it is a constant, not a detection.\nGit: c76ba3a \"refactor(ui): replace gum arrow menus with numeric two-column layout\" is the only commit adding `TUI_AVAILABLE=false`; earlier `TUI_AVAILABLE=true` detection came from ee03b99/72558e3/5972df9 and is gone.\nCaveats (not refuting, but adjacent live code the claim does not name): (1) install.sh:586 still calls install_tui_tools() (install.sh:688-…) on every FRESH install, and server/update.sh:2318-2322 re-installs gum/fzf whenever `update.sh` is run interactively without them — so the server still downloads gum+fzf that nothing uses (bwg-rick apparently lacks them because that download failed/was skipped). (2) b-ui-client.sh:4640-4649 uninstall-cleanup of /usr/local/bin/{gum,fzf} is NOT gated and IS reachable (and would trigger on bwg-tizi-style machines). These are separate leftovers, not the tui_* branches the claim describes.",
    "deletable_under_v4": true,
    "correction": "Claim stands as written. Precision notes: the gated gum/fzf invocation count is 15 (client) + 8 (cli), of which the client's 15 includes two in-menu sites (5348 fzf, 5416 gum write) beyond the eight tui_* helpers; the client's other 4 gum/fzf mentions (4640-4649) are the reachable uninstall cleanup, not a TUI branch. Under v4 clean reinstall: delete TUI_AVAILABLE + all `== \"true\"` branches in both files (zero behaviour change, proven by the stub test), and also delete install_tui_tools() in install.sh:688 and server/update.sh:2237 plus their call sites (install.sh:586, update.sh:2318-2322) and the client uninstall block 4640-4649 — those change behaviour only by no longer downloading/removing binaries no code path uses."
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "hist-C3",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Commands run in /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5:\n\n1) `for c in 4391cc2 596335b 72558e3 fcdf1c7 37016f1 5972df9 c76ba3a; do git log -1 --format='%h %ad %s' --date=iso $c; done` ->\n4391cc2 2026-05-07 18:47 feat(install): auto-install gum and fzf TUI tools\n596335b 2026-05-07 19:24 feat(bui-c): TUI status bar and gum choose main menu\n72558e3 2026-05-07 19:31 feat(b-ui): add non-interactive subcommands and gum TUI main menu\n37016f1 2026-05-07 20:04 fix(install): use find to locate gum/fzf binary after tar extraction\nfcdf1c7 2026-05-07 20:27 fix(tui): remove 2>/dev/null from gum choose to fix immediate exit bug\n5972df9 2026-05-07 21:40 feat(bui-c): auto-install gum and fzf on first interactive launch\nc76ba3a 2026-05-08 14:25 refactor(ui): replace gum arrow menus with numeric two-column layout\n=> all six ids, both dates and the c76ba3a subject line are exactly as claimed. CONFIRMED part.\n\n2) `git log --date=short -S'gum' --all` / `-S'fzf'` show the same-day feature set is larger than the claim lists: d569270 (spec), 1ef0114 (plan), ee03b99 (TUI helpers), 757975c (fzf node switch), a2e1fe3 (TUI toggle TUN/import/service menus), d67de37 (fix install_tui_tools interactive path) are all 2026-05-07 and omitted. Minor incompleteness.\n\n3) \"reverted\" is overstated. `git show --stat c76ba3a` touches only b-ui-client.sh, server/b-ui-cli.sh, version.json (221+/217-). It removed the client's ensure_tui_tools auto-installer and the gum choose menus, and hard-set the flag (server/b-ui-cli.sh:96-97 \"旧分支保留但 TUI_AVAILABLE 永远为 false / TUI_AVAILABLE=false\"; b-ui-client.sh:183-184 same). version.json 3.4.10 changelog (added in c76ba3a) says explicitly \"TUI_AVAILABLE 永久锁为 false\" and \"客户端…删除 ensure_tui_tools\" — i.e. menus reverted, but not the feature's code. Still present today:\n - install.sh:688 `install_tui_tools()` (downloads gum+fzf from GitHub to /usr/local/bin), called unconditionally on fresh install at install.sh:586; `git log -S install_tui_tools -- install.sh server/update.sh` shows it untouched since 4391cc2.\n - server/update.sh:2237 second copy of `install_tui_tools()`, invoked at update.sh:2320-2323 in the interactive `sudo b-ui`->update path (\"补装缺失的 TUI 工具 ... if ! command -v gum || ! command -v fzf; then install_tui_tools\").\n - Dead tui_* wrappers gated on the constant-false flag: server/b-ui-cli.sh:115-158,307 (gum style/spin/confirm/choose); b-ui-client.sh:204-303, 5346 (fzf node picker), 5413 (gum write). grep counts: install.sh gum=12 fzf=12, update.sh gum=12 fzf=12, b-ui-cli.sh gum=8, b-ui-client.sh gum=16 fzf=7.\n - b-ui-client.sh:4640-4649 uninstall prompt offering to delete /usr/local/bin/{gum,fzf}.\nPost-c76ba3a commits still touching fzf strings (dedef30, 46d7d93 on 2026-05-09) only edit the dead tui_switch_node/status-bar branches.",
  "correction": "The chronology (2026-05-07 introduction, 2026-05-08 c76ba3a) is right, but c76ba3a did not revert the gum/fzf feature; it disabled the gum/fzf menus by setting TUI_AVAILABLE=false as a constant (server/b-ui-cli.sh:97, b-ui-client.sh:184) and deleted only the client-side auto-installer. The server-side installer still downloads gum and fzf on every fresh install (install.sh:586 -> install.sh:688 install_tui_tools) and on interactive updates (server/update.sh:2237, called at 2322), and both CLI scripts still carry the unreachable tui_* gum/fzf wrappers (b-ui-cli.sh:115-158,307; b-ui-client.sh:204-303,5346,5413). Also the introduction spans ~10 commits that day (adds ee03b99, 757975c, a2e1fe3, d67de37, plus spec d569270/plan 1ef0114), not the 6 listed.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "hist-C4",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "All three assertions in the claim reproduce; only the version attribution for the multi-URL pool is slightly off (it first shipped in v3.4.19, before v3.5.0).\n\n(1) Spec rules the features out. docs/superpowers/specs/2026-05-07-residential-ip-design.md:15-19 (claim says 16-21; the block is actually 15-19, lines 20-21 are blank/`---`):\n  15 **非目标**:\n  16 - 不做多 SOCKS5 池负载均衡或自动切换。一次只用一个上游。\n  17 - 不做 HTTP/HTTPS 上游代理。仅 SOCKS5。\nand repeated at :368 \"本期不做：…多 SOCKS5 上游池、Per-user 路由、HTTP/HTTPS 上游代理、自动测速选最佳 POP\". Also :27 decision table \"上游代理协议 | SOCKS5 (账密模式)\". Cmd: `sed -n '1,40p' …residential-ip-design.md | cat -n; sed -n '360,372p' …`.\n\n(2) Both shipped later.\n  Multi-upstream pool/auto-switch: `git log -S'urltest' -- server/residential-helper.sh` → 9e9a459 2026-05-09 (v3.4.19 \"多住宅 IP CLI Cluster F … urls[] 数组 … 包进 urltest … 自动选最快\"; diff adds write_singbox_config_residential_multi), then 4328926 2026-05-14 (v3.5.0 \"urltest 多 URL + global toggle\", tag resi-pool), a6db9aa 2026-06-04 v3.5.22 reliability-aware failover, 8a38f24 2026-09-10 selector + Clash API hot switch. `jq -r '.changelog[\"3.5.0\"]' version.json` → \"多住宅 URL urltest 池\"; `.changelog[\"3.4.19\"]` → Cluster F multi-residential urltest. Current code: server/residential-helper.sh:356 \"# v3.5.0: 多 URL urltest 池 + global toggle 支持\", :429 `\"tag\": \"resi-pool\"`, :6 selector + resi-health hot switch.\n  HTTP upstream: `git log -S'\"type\":\"http\"'` → 155b2d8 2026-09-10 \"上游类型自动探测(SOCKS5/HTTP)\", d3f387f, 9b0fa89; spec docs/superpowers/specs/2026-09-10-residential-upstream-type-ui-design.md (R10, v3.6.0); version.json changelog 3.6.0 \"住宅上游类型与填写交互（R10…）… 自动探测上游是 SOCKS5 还是 HTTP\"; current server/residential-helper.sh:148 `http://*) RESI_TYPE=\"http\"`, :152 \"只支持 socks5:// 与 http://\".\n\n(3) Spec never updated. `git log --follow -- docs/superpowers/specs/2026-05-07-residential-ip-design.md` → only 890a227, f609d74, 090c602, all dated 2026-05-07 (last touch 2 days BEFORE 9e9a459 introduced the pool). `grep -rn '2026-05-07\\|supersede\\|取代\\|废止\\|作废' docs/superpowers/specs/2026-09-1* docs/superpowers/plans/2026-09-1*` → no hits; no later doc marks it superseded. Corroborating: docs/superpowers/specs/2026-09-10-residential-hardening-design.md §1 \"本次不做（记录待办）：… 旧 spec 更新\" — the team itself recorded the old spec as un-updated on 2026-09-10.\n\nNo counter-claim and no also_raised_as ids exist for hist-C4. No SSH needed: claim concerns repo docs/history only.",
  "correction": "Minor precision only: the multi-URL urltest pool first shipped in v3.4.19 (commit 9e9a459, 2026-05-09, changelog 3.4.19 \"Cluster F\"), not v3.5.0; v3.5.0 (4328926) reworked it into the `resi-pool` tag + global toggle and v3.5.22 (a6db9aa) added reliability-aware auto-switch. The non-goals block is at spec lines 15-19, not 16-21. Neither detail changes the conclusion.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "hist-C7",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "All file:line facts reproduced in worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5 except the version attribution \"dated v3.5.17\".\n\n1. README.md:9 = `**当前版本**: v3.5.18` (sed -n 1,15p README.md). version.json:2 = `\"version\": \"3.6.3\"` (sed -n 1,4p version.json). CONFIRMED. `git log -S'当前版本**: v3.5.18' -- README.md` → last set by 7857be5 2026-06-03 \"docs(README): 版本号 v3.5.13→v3.5.18\"; README was touched later (e4c7500, 7bc9d59 on 2026-09-10) without bumping the line, and the current bump commit a27d5b0 (v3.6.3) did not touch it.\n\n2. README.md:219 = `| 10002 | TCP | VLESS-WS-TLS |` (git blame: 46356558 2025-12-12, untouched since). server/core.sh:811-812 = `\"tag\": \"vless-residential\", \"port\": 10002` (sed -n 800,830p). CONFIRMED. Extra: README contradicts itself — README.md:43 (v3.5.0 changelog section) already says \"vless-residential :10002\". core.sh creates no ws-tls inbound at all (`grep -n 'ws-tls\\|10003' server/core.sh` → no hits); ws-tls only exists when web/server.js creates it at port 10003 (web/server.js:364 `port: 10003`; web/app.js:320 `cfg.wsPort || 10003`).\n\n3. \"ws-tls moved to :10003 per web/server.js:353's comment, dated v3.5.17\" — WRONG date. The comment that dates the move is web/server.js:350: `// v3.5.0: 端口从 10002 改到 10003（10002 被 vless-residential 占用）`; line 353 is the un-dated migration comment `// 老配置若有 vless-ws-tls 在 :10002 且 vless-residential 也在 :10002，强制把 ws-tls 改到 :10003`. `git blame -L 349,356 web/server.js` and `git log -S'v3.5.0: 端口从 10002 改到 10003' -- web/server.js` → both introduced by aae6394 2026-05-14 \"fix(v3.5): code review 后修 3 Critical + 2 Important\". `grep -n 'v3.5.17' web/server.js` → no hits. v3.5.17 is the QR-code change (README.md:21), unrelated.\n\nRuntime relevance: `jq -r '.files[]' version.json | grep -i readme` → README not in the deployed file list; `grep -rn README install.sh server/*.sh web/server.js web/app.js b-ui-client.sh` → no hits. README is documentation only; the discrepancy has no runtime effect. No live-host check needed (claim is purely about repo text).",
  "correction": "The port move is dated v3.5.0 (web/server.js:350, commit aae6394 2026-05-14), not v3.5.17; server.js:353 is the undated \"old config\" fixup comment. Everything else in the claim is accurate. Under v4: README.md:9 and README.md:219 need correcting (v3.6.x / \"10002 TCP VLESS-Reality(住宅)\", plus a 10003 VLESS-WS-TLS row and 40000/41000-50000 UDP rows), and the migration block web/server.js:353-356 (force-move ws-tls from 10002→10003 when both inbounds sit on 10002) becomes dead on a clean install since core.sh never writes a ws-tls inbound and server.js:364 creates new ones at 10003.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "prod-C1",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "All four sub-claims reproduce.\n\n1) Repo never references the template units. Ran in the worktree: `grep -rn 'hysteria-server@\\|xray@\\|@\\.service' --include='*.sh' --include='*.js' --include='*.json' --include='*.md' .` -> the only hits are the two audit JSON files themselves (docs/superpowers/audits/2026-09-11-claims.json:328, ...-readers-raw-UNVERIFIED.json:671-756). `git log --oneline -S'hysteria-server@' --all` and `git log -S'xray@' --all` -> empty (never present in any commit). b-ui manages only the singular units and drop-ins: server/core.sh:23 `HYSTERIA_SERVICE=\"hysteria-server.service\"`, core.sh:196-197 writes `/etc/systemd/system/hysteria-server.service.d/override.conf`, core.sh:1459-1460 `priority.conf`, core.sh:1763-1766 `xray.service.d/99-b-ui-override.conf`, core.sh:244 writes `hysteria-residential.service` as a plain (non-template) unit; update.sh:372-397 only sed-patches `hysteria-server.service` / `xray.service`.\n\n2) Provenance = upstream installers, which b-ui invokes: server/core.sh:116,119 and update.sh:1876,1958 run `bash <(curl -fsSL https://get.hy2.sh/)`; core.sh:381 and update.sh:1897,1977 run `XTLS/Xray-install/raw/main/install-release.sh @ install`. Live unit contents are the stock upstream templates: xray@.service `ExecStart=/usr/local/bin/xray run -config /usr/local/etc/xray/%i.json`, `RuntimeDirectory=xray-%i`; hysteria-server@.service `ExecStart=/usr/local/bin/hysteria server --config /etc/hysteria/%i.yaml`, `Description=Hysteria Server Service (%i.yaml)`. Their mtimes equal the sibling singular units written by the same installer runs (bwg-rick: hysteria-server@ and hysteria-server.service both `Sep 2 12:00`; xray@ and xray.service both `Jun 3 06:46`. bwg-tizi: `Aug 23 06:00` and `May 9 11:21` respectively).\n\n3) Identical on both hosts. `md5sum` bwg-rick vs bwg-tizi: hysteria-server@.service `1343887a83b58106478d7b8cf9f76ce9` on both; xray@.service `0ac5703b4c0072fb0d60a2ba92999b0b` on both (sizes 440 / 508 bytes on both).\n\n4) Never active, never instantiated, never referenced on the hosts. On both hosts: `systemctl list-unit-files \"hysteria-server@*\" \"xray@*\"` -> `disabled enabled` (unit file present, not enabled); `systemctl list-units --all \"hysteria-server@*\" \"xray@*\" --no-legend` -> empty (no instance ever loaded); `journalctl -u \"hysteria-server@*\" -u \"xray@*\"` -> \"Failed to add filter for units: No data available\" (no journal entries ever); `grep -rl \"hysteria-server@\\|xray@\" /etc/systemd /etc/cron* /var/spool/cron /opt/b-ui` -> no files.\n\nCommands: `ssh -o BatchMode=yes -o ConnectTimeout=25 bwg-rick '<batch>'` and `ssh ... -J bwg-rick bwg-tizi '<batch>'` where batch = ls -l/md5sum/cat of the two unit files, systemctl list-unit-files / list-units --all, journalctl -u, grep -rl over /etc/systemd,/etc/cron*,/var/spool/cron,/opt/b-ui. No counter-claim exists.\n\nSide observation (outside the claim): the live singular `/etc/systemd/system/hysteria-server.service` is also the upstream one (`After=network.target`, `ExecStart=... /etc/hysteria/config.yaml`, `HYSTERIA_LOG_LEVEL=info`), with b-ui's behaviour layered via drop-ins override.conf/priority.conf — consistent with the claim that b-ui relies on the upstream installer's units rather than owning them.",
  "deletable_under_v4": false,
  "correction": "Not a correction, a scoping note: the claim names no b-ui code, so there is nothing in the repo to delete. The two template unit files on the hosts could be removed with zero behaviour change (never active, no references), but a fresh v4 install that still calls get.hy2.sh / Xray-install install-release.sh (core.sh:116,119,381) will recreate them; they only go away if v4 stops using the upstream installers or explicitly `rm`s them after install.",
  "high_impact": false
 },
 {
  "id": "prod-C2",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "LIVE STATE (bwg-tizi via `ssh -J bwg-rick bwg-tizi`): `crontab -l` shows `0 */12 * * * /opt/b-ui/cert-check.sh >> /var/log/b-ui-cert-check.log 2>&1`; `ls -la /opt/b-ui/cert-check.sh` -> \"No such file or directory\" (cert-sync.sh exists, cert-check.sh does not); `tail /var/log/b-ui-cert-check.log` -> `/bin/sh: 1: /opt/b-ui/cert-check.sh: not found`; journalctl -u cron shows CRON firing it Sep 09 12:00, Sep 10 00:00/12:00, Sep 11 00:00; `/opt/b-ui/version.json` = 3.6.3 with key `files` (not `file_map`) = [install.sh, server/core.sh, server/b-ui-cli.sh, server/update.sh, server/residential-helper.sh, server/resi-health.sh, web/*, b-ui-client.sh] — no cert-check.sh. `sort log | uniq -c` -> 241/241 lines are \"not found\"; `stat -c %w` log birth = 2026-05-09 12:00:01 UTC (install-key.txt 2026-05-09 11:23:44) i.e. the very first cron run after install already failed. CONTRAST bwg-rick (`ssh bwg-rick`): /opt/b-ui/cert-check.sh exists (Jun 3 08:13, 1335 B), log tail \"[2026-09-11 00:00:01] OK: Caddy 运行正常\" — file created by the v3.5.14 reinstall on 2026-06-03.\nSTATIC: `grep -rn cert-check server install.sh web b-ui-client.sh version.json`: only writer of file content is server/core.sh:1170-1225 (configure_cron_tasks heredoc `cat > \"${BASE_DIR}/cert-check.sh\"`); server/update.sh:2030-2069 ensure_cron_jobs only (re)writes the crontab line — and only when `needs_fix=1` (lines 2043-2055), not \"unconditionally\" — never the file; install.sh:841-843 sources /opt/b-ui/core.sh and calls configure_cron_tasks on every install.sh run (incl. re-run), so re-running install.sh would create it, but update.sh (`ensure_cron_jobs` called at update.sh:351 and :2222) never does; b-ui-cli.sh:745 only removes it on uninstall. `git log -S'cert-check.sh'`: b92bf15 (2026-03-04, v2.16.3) added configure_cron_tasks to core.sh; 8837037 (2026-03-04, v2.16.4) added the cron line to update.sh ensure_cron_jobs; 2a7af40 (2026-06-03, v3.5.14) wired configure_cron_tasks into install.sh — its changelog explicitly says configure_cron_tasks was \"原定义了从不调用\" (dead code). `git rev-list -1 --before=2026-05-09 main` = 95323e5 (v3.4.35): `git show 95323e5:server/core.sh | grep -n configure_cron_tasks` -> only the definition (line 906), no caller; `git show 95323e5:install.sh` lines 825-841 write the cert-check.sh cron line inline without creating the script. `git log -S cert-check -- version.json` hits only 2a7af40's changelog text; cert-check.sh was never in the files list.",
  "correction": "Core facts confirmed (cron line present, file absent, \"not found\" every 12h, only core.sh writes the file, not in version.json `files`). Two inaccuracies: (1) it has NOT been failing \"since some point after the 2026-05-09 install\" — cert-check.sh never existed on bwg-tizi. The 2026-05-09 install ran v3.4.35 (95323e5) whose install.sh:825-841 wrote the cron line inline while core.sh's configure_cron_tasks (the only creator of the file) was dead code with no caller until v3.5.14 (2a7af40, 2026-06-03). Log birth 2026-05-09 12:00:01 and 241/241 \"not found\" lines prove failure from the first run. (2) update.sh:2044-2066 does not write the line \"unconditionally\": ensure_cron_jobs only rewrites crontab when needs_fix=1 (update.sh:2043-2055). Minor: the version.json key is `files`, not `file_map`; and install.sh:841-843 (not only core.sh at install) calls configure_cron_tasks, so a re-run of install.sh would repair bwg-tizi — only update.sh can't. Not strictly \"silent\": the failure is logged 241 times in /var/log/b-ui-cert-check.log, just never surfaced.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "prod-C3",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "STATIC: server/update.sh:1353-1354 `# Hysteria2 watchdog 迁移：老服务器没有 hy2-watchdog.timer 则部署` / `if [[ ! -f /etc/systemd/system/hy2-watchdog.timer ]]; then` gates the whole block (heredoc WDOGEOF at 1360-1391, chmod 1392, service/timer units 1394-1418, enable/start 1419-1420). The only condition is timer-file absence; no version stamp, no checksum, no diff of the heredoc body. `grep -n hy2-watchdog server/*.sh install.sh web/server.js` shows the only other writer is server/core.sh:1047-1122 setup_hy2_watchdog() (fresh install path via install.sh:557 `source core.sh`); b-ui-cli.sh:683/697 only stops/removes on uninstall; web/server.js:2525-2552 only reads status. No other code path rewrites /opt/b-ui/hy2-watchdog.sh on an existing install.\nGIT: `git log -S'check_instance hysteria-residential 40000' -- server/update.sh server/core.sh` -> 2a7af40 2026-06-03 bump v3.5.14 (heredoc body changed from single-instance FAIL_FILE=/tmp/hy2-watchdog-fail-count to dual-instance check_instance). `git log -S'hy2-watchdog.timer ]]' -- server/update.sh` -> f403df0 2026-05-09 (block introduced, v3.4.18; old body at f403df0:server/update.sh:858-868 uses FAIL_FILE=/tmp/hy2-watchdog-fail-count). So the body was in fact revised once after introduction, and that revision cannot reach hosts whose timer already existed.\nLIVE (read-only, `ssh -o BatchMode=yes bwg-rick` and `ssh -J bwg-rick bwg-tizi`, same batched cmd):\nbwg-rick: version 3.6.3; /opt/b-ui/hy2-watchdog.sh 1484 bytes mtime 2026-06-03 08:13:07, sha256 f202c0d8…, residential_line_count=1 (new dual-instance body), old_failfile_count=0; timer 155 bytes mtime 2026-06-03; update.sh 105353 bytes mtime 2026-09-11 with line 1354 gate present and update_sh_has_new_body=1; timer active; no .bak.\nbwg-tizi: version 3.6.3; /opt/b-ui/hy2-watchdog.sh 1280 bytes mtime 2026-05-09 11:21:39, sha256 ca027bec…, residential_line_count=0, old_failfile_count=1 (still the v3.4.18 single-instance body — hysteria-residential is NOT watched there); timer 155 bytes mtime 2026-05-09; update.sh/core.sh identical size (105353/78094) and updated 2026-09-11 04:34 with the same line-1354 gate and new body; timer active; no .bak.\nBoth hosts run byte-identical current update.sh (line 1354 gate, new heredoc), yet bwg-tizi's watchdog is the old 2026-05-09 body and bwg-rick's is the 2026-06-03 body (rick was reinstalled that day, so core.sh wrote it). This reproduces the claim's sizes/mtimes exactly and shows the mechanism: the timer file pre-exists on both, so update.sh never rewrites the script. Side note (out of scope): web/server.js:2546 still reads /tmp/hy2-watchdog-fail-count, the OLD body's file name — the panel's fail-count readout is stale on hosts with the new body.",
  "correction": "",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "prod-C4",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Commands run (all from /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1) `grep -n '\\.bak' server/update.sh` -> .bak write sites (cp to a backup): 682 (config.yaml .bak.v360.$ts), 703 (config-residential.yaml .bak.v360.$ts), 734 (xray-config.json .bak.v360.$ts), 774 (config.yaml .bak.$(date +%Y%m%d-%H%M%S), QUIC-window migration), 858 (config.yaml .bak.broken-<ts>, repair path), 1660 (config.yaml.bak.v357.$(date +%s)), 1674-1675 (_xray_bak=xray-config.json.bak.$(date +%s); cp ... \"$_xray_bak\"), 1709 (xray-config.json.bak.v359.$(date +%s)). Line 719 is a RESTORE (cp FROM the .bak), not a write. Total = 8 write sites, not 11. `grep -n 'cp .*\\.bak\\.' server/update.sh | grep -v '^719:' | wc -l` = 7, plus line 1675 via the $_xray_bak variable = 8.\n2) `grep -n 'rm ' server/update.sh | grep -i bak` -> exactly one hit, 721: `rm -f \"${rcfg}.bak.v360.${ts}\"`, inside the D9 \"形态不认识 -> 还原\" branch (lines 717-723). Comment on 720 confirms it is rollback-only (\"还原后备份和原文件一模一样，留着只会每个自愈周期攒一个\"). `grep -rn -E 'find .*-delete|find .*-mtime|logrotate|tmpwatch' server/ install.sh web/server.js` -> no output: no retention policy anywhere. CONFIRMED for this part.\n3) git: `git log -S'.bak.v360' -- server/update.sh` -> 4c18228 2026-09-10 (D9 added), 6556900 2026-09-10 (rm at 721 added: \"恢复路径不累积备份\"). `git log -S'.bak.v357'` -> 7e00b1c 2026-05-14. `git log --all -S'.bak.$(date +%Y%m%d%H%M%S)'` -> that no-dash timestamp format existed only in the pre-v3.5 residential-helper.sh (007077a/2d37f6e 2026-05-07, removed in 4328926 2026-05-14); update.sh never used it.\n4) Live: `ssh -o BatchMode=yes -o ConnectTimeout=25 -J bwg-rick bwg-tizi 'ls -la --time-style=long-iso /opt/b-ui/*.bak*; ls /opt/b-ui/config.yaml.bak* /opt/b-ui/xray-config.json.bak* | wc -l; grep -n \"rm -f.*bak\" /opt/b-ui/update.sh'` -> 29 .bak files total in /opt/b-ui; 21 (not 26) for config.yaml + xray-config.json; oldest 2026-05-09 11:21, newest 2026-09-11 02:26 (date span CONFIRMED); installed update.sh (v3.6.3) has the single rm at line 721 only. Nothing pruned CONFIRMED. Attribution however: of those 21, 14 are the paired `config.yaml.bak.2026MMDDhhmmss` / `xray-config.json.bak.2026MMDDhhmmss` (7 timestamps, 05-09..05-13) written by the OLD residential-helper.sh, 2 are `config.yaml.bak.obfs.*` written by server/b-ui-cli.sh:939/963 (obfs toggle), and only 5 come from update.sh (config.yaml.bak.v357 [older no-ts format], config.yaml.bak.v360.*, xray-config.json.bak.1778739517 [line 1675], .bak.v359.*, .bak.v360.*). The remaining 8 of the 29 are manual/ad-hoc (.bak.qa, .bak.ippure, .bak.rmdc, .bak.null, .bak.pre-null, residential-proxy.json.bak.*, b-ui-cli.sh.bak.*) or config-residential.yaml.bak.v360.",
  "correction": "Two numbers are wrong and the live evidence is misattributed. (a) server/update.sh has 8 .bak write call sites (lines 682, 703, 734, 774, 858, 1660, 1675, 1709), not 11; line 719 is a restore, not a write. (b) bwg-tizi holds 21 config.yaml/xray-config.json .bak files (29 .bak files of all kinds), not 26; and 16 of those 21 were NOT written by update.sh migration blocks — 14 by the pre-v3.5 residential-helper.sh (deleted in commit 4328926, 2026-05-14) and 2 by server/b-ui-cli.sh:939/963 obfs toggle. The substantive point stands: the only deletion is update.sh:721 (rollback-only, D9 branch), there is no retention/prune anywhere in the repo, and the live files span 2026-05-09 to 2026-09-11 unpruned.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "prod-C5",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "STATIC (repo HEAD 5b0da6c, worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1. `grep -n -iE 'bak|backup' server/residential-helper.sh` -> no output, exit=1 (reproduces the claim's grep exactly).\n2. `grep -rn -E 'bak\\.null|bak\\.rmdc|bak\\.pre-null|namebak|bak\\.ippure' --include='*.sh' --include='*.js' --include='*.json' --include='*.md' .` (audits/ excluded) -> zero hits.\n3. Only shipped backup writers in the repo, none of which touch residential-proxy.json or the helper: server/b-ui-cli.sh:939,963 (`config.yaml.bak.obfs.<ts>`), server/update.sh:682/703/734 (`*.bak.v360.<ts>` for config.yaml / config-residential.yaml / xray-config.json), update.sh:774 (`config.yaml.bak.<ts>`), 858 (`.bak.broken-`), 1660 (`config.yaml.bak.v357`), 1674/1709 (`xray-config.json.bak.*`), b-ui-client.sh:3359 (`singbox-tun.json.bak`). The residential-proxy.json migration block `migrate_residential_keywords()` at server/update.sh:510-560 writes via `${cfg}.tmp.$$` + mv, creates no backup. web/server.js: `grep -n -iE '\\.bak|backup'` -> zero.\n4. Whole-history check: `git log --all --oneline -S'namebak'`, `-S'.bak.null'`, `-S'rmdc'`, `-S'pre-null'`, `-S'bak.ippure'`, `-S'residential-proxy.json.bak'`, `-S'RESIDENTIAL_CONFIG}.bak'` -> all empty. `git log -S'.bak' -- server/residential-helper.sh` hits 007077e/2d37f6e/4328926 but those are the REMOVED v3.4-era lines `local backup=\"${XRAY_CONFIG}.bak.$(date +%Y%m%d%H%M%S)\"` / `\"${config}.bak.…\"` (xray-config/config.yaml, numeric-timestamp suffix only, deleted in 4328926) — never a `.null/.rmdc/.pre-null/.namebak/.ippure` tag.\n\nLIVE bwg-tizi (`ssh -o BatchMode=yes -o ConnectTimeout=25 -J bwg-rick bwg-tizi`, read-only):\n5. `ls -la --time-style=full-iso /opt/b-ui/ | grep -E 'bak|namebak'` shows exactly the five claimed files: residential-helper.sh.bak.ippure.20260911-032327 (mtime 03:05:05), residential-proxy.json.bak.pre-null.20260911-030943 (mtime 02:59:39), .bak.rmdc.032630 (mtime 03:25:57), .bak.null.032740 (mtime 03:26:30), .namebak.1778841019 (2026-05-15), plus .bak.1778838522 (2026-05-14). Suffix formats are mutually inconsistent (YYYYMMDD-HHMMSS vs bare HHMMSS vs epoch) and for ippure/pre-null the mtime is OLDER than the suffix time — the signature of a hand-run `cp -p`, not any code path.\n6. `md5sum /opt/b-ui/residential-helper.sh` = 0050b4e85fd129a011c4fae272060c85 == `md5 -q server/residential-helper.sh` at repo HEAD (identical script). `grep -c -E 'namebak|bak\\.null|bak\\.rmdc|pre-null|bak\\.ippure' /opt/b-ui/update.sh /opt/b-ui/b-ui-cli.sh /opt/b-ui/admin/server.js /opt/b-ui/residential-helper.sh` -> 0,0,0,0. `grep -n -iE 'bak|backup' /opt/b-ui/residential-helper.sh` -> empty.\n7. `diff residential-helper.sh.bak.ippure.* residential-helper.sh`: the .bak.ippure copy lacks the `set-domains null` doc line, the `\"api.123169\" \"ipip.net\" \"cf.999831\"` DEFAULT_DOMAINS entries and the R12 `rounds=2` retry — i.e. it is the pre-v3.6.1 helper, snapshotted by hand after update.sh had already replaced it.\n8. /var/log/b-ui-update.log: update.sh ran only at 03:05:04 (v3.6.0->v3.6.1, \"变更文件: server/residential-helper.sh version.json\") and 03:46:40 (->v3.6.2); neither writes a residential-proxy.json or helper backup (see item 3). `journalctl -u b-ui-admin 02:50-03:35 | grep -iE 'helper|domains|住宅'` -> empty (panel made no helper calls). `crontab -l` only has update.sh auto/kernel + cert-check.sh.\n9. `journalctl -u ssh` 02:50-03:35: root publickey logins at 03:05:04, 03:09:43, 03:13:30, 03:15:04, 03:23:27 — the 03:09:43 login matches the `.bak.pre-null.20260911-030943` suffix and the 03:23:27 login matches `.bak.ippure.20260911-032327` to the second; rmdc (03:26:30) / null (03:27:40) fall inside the same interactive session. Bash history on the host has no matching lines (`grep -hnE … /root/.bash_history` empty), consistent with a non-saved/`ssh host '<cmd>'` style session (HANDOVER.md:80 documents exactly that invocation pattern).\n\nConclusion: the grep result is reproduced, no current or historical shipped script produces those suffixes, the host helper is byte-identical to the repo, and the file timestamps coincide with interactive root SSH logins rather than cron/update/panel activity. The claim is correct on both halves (not produced by shipped code; residue of ad-hoc edits on the production host).",
  "correction": "Minor precision only: the residue is not just from 2026-09-11 — `residential-proxy.json.bak.1778838522` (2026-05-14) and `.namebak.1778841019` (2026-05-15) on the same host also have no producer anywhere in git history (`git log --all -S'namebak'` / `-S'residential-proxy.json.bak'` empty), so the ad-hoc-edit pattern predates today. The `.bak.ippure` file is a snapshot of the pre-v3.6.1 helper (mtime 03:05:05 = the update.sh pull that replaced it; diff shows it lacks the ippure DEFAULT_DOMAINS and R12 retry), copied by hand at 03:23:27.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "prod-C6",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "LIVE HOST STATE (read-only SSH, 2026-09-11):\n\nbwg-tizi — `ssh -J bwg-rick bwg-tizi 'ls -la /etc/systemd/system/hysteria-residential.service.d/; cat .../99-debug.conf; systemctl show hysteria-residential -p ExecStart -p DropInPaths -p Environment; pgrep -a -f \"hysteria.*residential\"; journalctl -u hysteria-residential --since \"24 hours ago\" | wc -l'`:\n- `-rw-r--r-- 1 root root 136 May 14 09:05 99-debug.conf` (mtime 2026-05-14 09:05:04 UTC, owner root)\n- File content: `[Service]` / `Environment=` / `ExecStart=` / `ExecStart=/usr/local/bin/hysteria server --config /opt/b-ui/config-residential.yaml --log-level debug`\n- `DropInPaths=/etc/systemd/system/hysteria-residential.service.d/99-debug.conf`\n- Effective `ExecStart=... argv[]=/usr/local/bin/hysteria server --config /opt/b-ui/config-residential.yaml --log-level debug`; `Environment=` (empty — the drop-in also wipes GOMEMLIMIT=200MiB and HYSTERIA_LOG_LEVEL=warn from the base unit)\n- Running process 1405093: `/usr/local/bin/hysteria server --config /opt/b-ui/config-residential.yaml --log-level debug`\n- journal lines last 24h: hysteria-residential = 71885, hysteria-server = 1857\n- Level histogram (grep -oE \"(DEBUG|INFO|WARN|ERROR)\" | sort | uniq -c): 66648 DEBUG, 129 INFO, 5074 WARN — i.e. ~93% of the volume is DEBUG-level \"TCP request\"/\"TCP closed\" lines that only exist because of --log-level debug.\n- journalctl --disk-usage: 379.8M; b-ui version 3.6.3.\n\nbwg-rick — `ssh bwg-rick '<same commands>'`:\n- `ls: cannot access '/etc/systemd/system/hysteria-residential.service.d/': No such file or directory`\n- `DropInPaths=` (empty); `Environment=GOMEMLIMIT=200MiB HYSTERIA_LOG_LEVEL=warn`; running cmdline has no --log-level flag\n- journal lines last 24h: hysteria-residential = 3782, hysteria-server = 1283; all residential lines are WARN \"TCP error\" or systemd start/stop messages, zero DEBUG.\n- b-ui version 3.6.3.\n\nRatio 71885/3782 = 19.0x (audit's 72425/3789 = 19.1x; difference is window drift only).\n\nREPO (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -rn \"99-debug\\|log-level\\|service.d\" --include='*.sh' --include='*.js' --include='*.json' --include='*.md' .` → no hit for 99-debug or `--log-level debug` anywhere; only `web/server.js:986 log-level: warning` (mihomo YAML) and drop-in handling for hysteria-server/xray/caddy.\n- `git log --oneline -S\"99-debug\"` and `git log --oneline -S\"log-level debug\"` → empty: the file was never in repo history; it is a manual override.\n- server/core.sh:244-270 writes hysteria-residential.service with `Environment=HYSTERIA_LOG_LEVEL=warn` (core.sh:262) and ExecStart without --log-level; it never creates a service.d dir for the residential unit.\n- server/update.sh:884-885: `rm -f /etc/systemd/system/hysteria-residential.service.d/99-no-cross-cleanup.conf; rmdir /etc/systemd/system/hysteria-residential.service.d 2>/dev/null || true` — the only code touching that directory; rmdir silently fails on the non-empty dir, so every update since May 14 has left 99-debug.conf in place.\n- server/b-ui-cli.sh:693 (`rm -rf ... /etc/systemd/system/hysteria-residential.service.d`) removes it only on full uninstall.\n\nAdditional finding beyond the claim: the drop-in's `Environment=` line clears GOMEMLIMIT=200MiB on bwg-tizi's residential instance as well, so the memory cap intended by core.sh:261 is not in effect there either.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "prod-C7",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Live-host measurements (read-only), both hosts at version 3.6.3 with identical journald config.\n\nbwg-rick (`ssh bwg-rick 'df -h /; sudo du -xsh /var /opt /usr /root ...'`):\n  /dev/sda2 19G 2.1G 16G 13% ; /opt 205M, /var 494M, /usr 885M, /root 900K; /opt/b-ui 205M; `find /opt/b-ui -name \"*.bak*\"` -> 8 bak files, 51,482 bytes; journalctl --disk-usage 143.2M; /opt/b-ui/web does not exist; /root/.vscode-server does not exist; swap: /swap 545M only.\n\nbwg-tizi (`ssh -J bwg-rick bwg-tizi ...`):\n  /dev/sda3 19G 7.5G 11G 43% ; `du -xh --max-depth=2 / | sort -rh | head`: 3.6G /root/.vscode-server (5 Stable-* server builds, each with ~139MB copilot runtime.node + 124MB node), 1.3G /usr, 770M /var (391M /var/log, of which 380M /var/log/journal), 205M /opt/b-ui, plus /swap.img 1.2G AND /swap 546M (both in fstab and active: `swapon --show`). `find /opt/b-ui -name \"*.bak*\"` -> 30 bak files, 234,621 bytes (0.2MB). /opt/b-ui/web: \"No such file or directory\". /opt/b-ui total 205M — byte-identical size to rick.\n\nJournal: /etc/systemd/journald.conf.d/99-b-ui.conf is identical on both (SystemMaxUse=500M, SystemKeepFree=1G, MaxRetentionSec=14day, Compress=yes) — no MaxLevelStore/debug retention setting. `journalctl -p 7..7 --since 24h | wc -l`: rick 0, tizi 54 (52 from an SSH session scope, 2 packagekit) — no debug-level logging from any b-ui service. Log levels: xray {\"loglevel\":\"warning\"}, singbox-relay {\"level\":\"error\"}. The 380M-vs-143M journal gap comes from volume, not level: `journalctl --since 24h -o json | jq -r ._SYSTEMD_UNIT | sort | uniq -c`: tizi hysteria-residential.service 71,845 lines/day vs rick 3,747 (tizi runs global residential mode, so all traffic logs there); xray 19,479 vs 11,496; b-ui-relay ~31.5k on both.\n\nAccounting for the 5.4G delta: vscode-server 3.6G (67%) + extra /swap.img 1.2G (22%) + /usr +0.4G + journal +0.24G ≈ 5.4G. The three causes named in the claim contribute: .bak files 0.18MB extra, stray web/ files 0 (directory does not exist on either host), debug-level retention 0 (no such setting, no debug output).",
  "correction": "The headline numbers are accurate (tizi 7.5G/43% vs rick 2.1G/13%, both on v3.6.3), but the attribution is wrong. The gap is ~3.6G of /root/.vscode-server (five cached VS Code Remote server builds, absent on rick), a second 1.2G /swap.img on tizi (rick has only /swap 545M), ~0.4G more in /usr, and ~0.24G more journal. The journal difference is driven by hysteria-residential log volume under tizi's global residential mode (71.8k lines/day vs 3.7k), not by any debug-level retention — journald.conf.d/99-b-ui.conf is identical on both hosts and neither host stores meaningful debug (priority 7) output. /opt/b-ui is exactly 205M on both hosts; tizi's 30 .bak files total 234,621 bytes (~0.2MB), and no /opt/b-ui/web directory exists on either host. None of the three causes cited in the claim is measurable in the disk delta; they are unrelated to b-ui code.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "resi-C1",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim resi-C1 (no counter-claim, no also_raised_as). Static: `grep -rn 'LEGACY_DEFAULT_DOMAINS' .` (excluding .git) in the worktree hits only server/residential-helper.sh:113 (the definition, lines 113-116: \"openai\" \"chatgpt\" \"google\" \"googleapis\" \"gstatic\" \"anthropic\" \"claude\" \"ping0\" \"grok\" \"tiktok\") plus docs: docs/superpowers/plans/2026-09-10-residential-hardening.md:16 and docs/superpowers/specs/2026-09-10-residential-hardening-design.md:20 (both label it dead code deliberately left), and the two audit JSONs. No dynamic access path exists: `grep -nE 'compgen|declare -p|\\$\\{!|typeset|\\bLEGACY' server/residential-helper.sh` returns only line 113; `grep -rnE '\\$\\{![A-Za-z_]|declare -n|eval ' server/ web/ install.sh b-ui-client.sh` finds only `${!file_map[@]}` in update.sh:2152/2158/2179 (unrelated). Nobody sources the helper (`grep -rnE '(source|^\\s*\\.)\\s+.*residential-helper'` → 0 hits); every caller runs it as a subprocess with a subcommand (install.sh:606,652; update.sh:347,545,666,2216; b-ui-cli.sh:1005-1025; web/server.js:46 residentialHelper path), and the dispatch cases at helper lines 668-774 (setup/enable/disable/status/domains/reapply) never touch the array — `domains)` at :767 calls get_domains() (:118-128) which only reads `.domains` from residential-proxy.json or DEFAULT_DOMAINS. The only place the v3.4.17 legacy list is actually consumed is server/update.sh migrate_residential_keywords() (:511-533), which re-declares its own jq literal `legacy_v3_4_17=$(jq -nc '[\"openai\",\"chatgpt\",\"google\",\"googleapis\",\"gstatic\",\"anthropic\",\"claude\",\"ping0\",\"grok\",\"tiktok\"]')` and never reads the bash array. Git: `git log --oneline -S'LEGACY_DEFAULT_DOMAINS_V3_4_17'` → f403df0 (2026-05-09, introduced) and bff741d (docs only); `git log -p --all -S'LEGACY_DEFAULT_DOMAINS_V3_4_17' -- server web | grep -E '^[-+].*LEGACY_DEFAULT_DOMAINS_V3_4_17' | sort | uniq -c` → exactly 1 line, the `+` definition, i.e. it was never referenced in any commit since birth; `git blame -L 113,116` all f403df0. Live hosts (read-only): bwg-rick and bwg-tizi both run v3.6.3 with /opt/b-ui/residential-helper.sh md5 0050b4e85fd129a011c4fae272060c85 identical to the repo file; `grep -rn LEGACY_DEFAULT_DOMAINS /opt/b-ui/ /etc/cron.d /etc/systemd/system` on each host hits only the definition line 113 and a .bak.ippure.20260911 backup copy — no consumer on either production box.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "resi-C2",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Line numbers in the claim are accurate: server/update.sh:510-568 is migrate_residential_keywords(); the two legacy lists are at 517-527 (10-entry v3.4.17, 25-entry v3.4.18); sole call site is update.sh:1431 inside apply_systemd_configs() (756-1448), which runs on every update path (update.sh:313, 2099 \"already latest\", 2198, 2226 cron/auto -> output to $LOG_FILE). Git: introduced in f403df0 (v3.4.18 stability pass), rewritten in 20316ba 2026-09-10 (\"关键字迁移不再固化过期默认表\") to the current null-follow design; no later touches. Nothing else references the function or the list variables: `grep -n \"migrate_residential_keywords\\|legacy_v3_4_1\" server/*.sh web/*.js install.sh b-ui-client.sh` -> only update.sh:510/516/517/518/535/1431.\n\nExact-match part CONFIRMED: the rewrite path (update.sh:535-550, `.domains = null` + helper reapply) only runs when `.domains` equals one of the two baked lists; nothing on a fresh install writes those lists (core.sh has zero references to residential-proxy.json/.domains; residential-helper.sh set-domains at 797-813 writes whatever the panel sends or null; helper DEFAULT_DOMAINS at residential-helper.sh:89-111 is a ~70-entry list that differs from both legacy lists, so even a panel \"save\" of the shown default cannot equal them).\n\n\"Only fires for exact match\" REFUTED: the function has a third path (update.sh:553-567) that fires on ANY custom list containing \"google\", \"googleapis\" or \"gstatic\" and prints a 4-line print_warning (\"住宅代理含 google/googleapis/gstatic 关键词（v3.4.18 已弃用）\"). Because \"googleapis\" and \"gstatic\" are members of the CURRENT DEFAULT_DOMAINS (residential-helper.sh:98), any v4 fresh-install admin who edits the keyword list in the panel (web/app.js:979-982 POST {domains} -> web/server.js:2227-2231 spawnSync helper set-domains -> .domains becomes a full array) trips this stale warning on every apply_systemd_configs run. Reproduced: extracted the function with awk into scratchpad, wrote residential-proxy.json with domains [\"openai\",\"googleapis\",\"gstatic\",\"claude\"], ran `BASE_DIR=$S bash -c 'source fn.sh; migrate_residential_keywords'` -> printed the 4 WARN lines, file unchanged ([\"openai\",\"googleapis\",\"gstatic\",\"claude\"]). So on a fresh install the function is not inert; it emits a misleading warning (the \"deprecated\" keywords are now in the default table) but never modifies state.\n\nLive hosts (read-only, jq filter dropped urls/credentials): `ssh bwg-rick 'jq -c \"{domains:(.domains|if .==null then \\\"null\\\" else (length|tostring)+\\\" entries\\\" end),global,enabled}\" /opt/b-ui/residential-proxy.json; grep -c migrate_residential_keywords /opt/b-ui/update.sh; jq -r .version /opt/b-ui/version.json'` and the same via `-J bwg-rick bwg-tizi`: both return {\"domains\":\"null\",\"global\":true,\"enabled\":true}, 2, 3.6.3. Both production servers already have domains == null, so the function returns at update.sh:533 as a no-op on them today; the legacy-list rewrite is already spent even before a v4 reinstall.\n\nRelated, not part of this claim: residential-helper.sh:113 defines LEGACY_DEFAULT_DOMAINS_V3_4_17 with no other reference in the repo (grep shows only the definition) — a dead sibling list.",
  "correction": "The function does not fire only on exact legacy-list matches: update.sh:553-567 also fires (warning only, no write) on any custom .domains containing google/googleapis/gstatic, and since googleapis/gstatic are in the current default table (residential-helper.sh:98) this path is reachable on a fresh v4 install whenever an admin customizes the list via the panel. The rewrite path (535-550) and the two lists (517-527) are legacy-only; both production hosts already have domains == null so the function is currently a no-op there.",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "resi-C3",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): A.fix2 in server/update.sh:1653-1668 only strips a '# B-UI:RESIDENTIAL-START..END' block from config.yaml; no COUNTER, no ALSO ids.\n\n1. The block itself — server/update.sh:1653-1668 (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5): comment at 1653-1656 says \"v3.4 时代 residential-helper apply_hysteria 在 config.yaml 插 socks5:2080 + relay(all) acl … v3.5.7: hy2-direct 永远内置直连，主动剥离所有 RESIDENTIAL marker block\"; the guard at 1657-1658 is `[[ -f \"${BASE_DIR}/config.yaml\" ]] && grep -q '# B-UI:RESIDENTIAL-START' \"${BASE_DIR}/config.yaml\"`; 1661 awk removes START..END; 1665 restarts hysteria-server. It is a no-op unless the marker is present.\n\n2. Nothing in the current tree writes the marker. `grep -rln 'RESIDENTIAL-START' server web b-ui-client.sh install.sh b-ui-server.sh` → only server/update.sh (lines 1658, 1661, both the reader). `sed -n '/^repair_hysteria_config()/,/^}/p' server/update.sh | grep RESIDENTIAL` → empty (the v3.5.7 commit also deleted the block-preservation from repair_hysteria_config). core.sh:535 generates config.yaml with \"outbounds: direct mode 4\" and no marker; residential-helper.sh never touches config.yaml (grep config.yaml server/residential-helper.sh → no hits).\n\n3. History: `git log --oneline -S'B-UI:RESIDENTIAL-START' -- server/ b-ui-server.sh` → e31e1c8 (added anchors to core.sh template), 007077e (helper writer), 2d37f6e, 6ac0126, 73b20f2 (v3.5: `git show 73b20f2 -- server/core.sh` removes `# B-UI:RESIDENTIAL-START/END` lines from the template), 4328926 (v3.5: `git show 4328926 -- server/residential-helper.sh` removes the `printf '\\n# B-UI:RESIDENTIAL-START…'` writer and the awk injector). Writer removed at v3.5.0 (5d332d1); A.fix2 introduced in 7e00b1c (v3.5.7) purely as cleanup of that residue, as version.json:41 changelog confirms (\"自动清掉历史残留 … 不再有任何 RESIDENTIAL marker 出现在 config.yaml\").\n\n4. Live state (read-only): `ssh bwg-rick 'grep -c \"B-UI:RESIDENTIAL\" /opt/b-ui/config.yaml /opt/b-ui/config-residential.yaml'` → 0 / 0, no config.yaml.bak.v357.* files, version 3.6.3; same via `-J bwg-rick bwg-tizi` → 0 / 0, no bak.v357 files, version 3.6.3. Both production hosts have the v3.5.8+ `outbounds:` direct block (rick line 49, tizi line 61) but no marker, so A.fix2 is already inert on every live install.\n\nAttempted refutation angles that failed: no other entry point (cron/systemd/panel/CLI/client) references the marker; no v3.5+ code path can regenerate it; the block's only reachable trigger is a config.yaml that upgraded from ≤v3.4 through the v3.5.0–v3.5.6 repair path without ever running v3.5.7+.",
  "correction": "",
  "deletable_under_v4": true,
  "high_impact": false
 },
 {
  "id": "resi-C4",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "STATIC (all paths under /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1) `grep -rn \"resi-health\\|resi_health\" server/ web/ install.sh b-ui-client.sh version.json` — the only writers of /etc/systemd/system/b-ui-resi-health.{service,timer} are server/update.sh:1157-1179 (heredocs inside apply_systemd_configs, D8 block at server/update.sh:1141-1193) and patch_resi_health_timer() at server/update.sh:748. server/core.sh: 0 hits (`grep -n \"resi\\b\\|health\" server/core.sh` returns only port/ufw lines). server/residential-helper.sh: only a comment at line 6; its `setup`/`enable`/`enable --add` branches (lines 669-700) call ensure_singbox / add_url_to_config / write_singbox_config_from_state / reload_relay_service and never touch the timer. server/b-ui-cli.sh:684,698 only DELETE the units (uninstall). web/server.js:2257/2355/2395 spawn the helper (`enable -`, `enable --add -`, `enable --remove`) and never run update.sh.\n2) install.sh fresh-install path: install.sh:796-800 (`case 2` = 全新安装) runs download_all_files → run_core_install → configure_residential_interactive. run_core_install (install.sh:550-609) does `source \"${BASE_DIR}/core.sh\"` (line 559) and only calls core.sh functions plus `residential-helper.sh setup` (line 606); `grep -n \"update.sh\\|apply_systemd_configs\" install.sh server/core.sh` shows update.sh is only sourced on install_type 1 (install.sh:793) and otherwise only appears in the cron lines (install.sh:850-851, server/core.sh:1223-1224: `0 */6 * * * /opt/b-ui/update.sh auto`). So apply_systemd_configs/D8 never executes during a fresh install.\n3) apply_systemd_configs callers: server/update.sh:313, 2099, 2198, 2226 — all inside update.sh's own check_and_update / auto / kernel paths (cron or `sudo b-ui` update menu, b-ui-cli.sh:510-512).\n4) git: `git log --oneline -S\"b-ui-resi-health.timer\" -- server/update.sh server/core.sh server/residential-helper.sh` → a6db9aa (v3.5.22, introduced D8) and 2283da9 (added timer jitter). `git show --stat a6db9aa` touched only install.sh (file list), server/resi-health.sh, server/update.sh. `git log -S\"b-ui-resi-health\" -- server/core.sh install.sh server/residential-helper.sh` → empty: the timer was never in core.sh or the helper in any commit. version.json changelog 3.5.22/3.5.23 explicitly says the timer is delivered by \"update.sh apply_systemd_configs D8 块\".\n5) Premise nuance: install.sh's interactive prompt (install.sh:646-652) accepts ONE url and calls `residential-helper.sh enable \"$resi_url\"`, so \">=2 URLs at setup time\" is not literally reachable through install.sh alone; the 2nd URL comes right after via panel (`enable --add -`, web/server.js:2355) or `sudo b-ui` (b-ui-cli.sh:1011) — neither creates the timer, so the gap described is real and actually wider than the claim (it applies to any pool growth, not just install time).\n\nLIVE (read-only): `ssh bwg-rick '...'` and `ssh -J bwg-rick bwg-tizi '...'` running: `ls /etc/systemd/system/b-ui-resi-health.*; systemctl is-active b-ui-resi-health.timer; jq '[.outbounds[]|select(.type==\"socks\" or .type==\"http\")]|length' /opt/b-ui/singbox-relay.json; jq '(.urls//[])|length' /opt/b-ui/residential-proxy.json; grep -c resi-health /opt/b-ui/core.sh; stat residential-proxy.json singbox-relay.json; journalctl -u b-ui-resi-health.timer -n 5; grep \"开始自动检查更新\" /var/log/b-ui-update.log | tail`.\nBoth hosts (v3.6.3, /opt/b-ui/update.sh contains D8, resi-health.sh present, jq present): relay members = 2, url count = 2, core.sh mentions resi-health = 0, BUT `ls` → \"No such file or directory\", timer = inactive. journalctl: \"Sep 10 18:00:0x systemd[1]: Stopped b-ui-resi-health.timer\" (D8 <2 cleanup at the 18:00 cron run while pool was <2). residential-proxy.json + singbox-relay.json were rewritten at 06:56 today (relay restarted 06:56 per `journalctl -u b-ui-relay`), i.e. AFTER the last cron runs (bwg-rick 06:03:55, bwg-tizi 06:11:54). Result: both production hosts currently have a 2-member pool with no health timer and will not get one until the 12:00 cron — a live reproduction of exactly the ordering gap the claim describes (via the add path rather than a fresh install).",
  "correction": "Claim is correct in substance. Only imprecision: install.sh's fresh-install prompt (install.sh:638-668) can only take a single URL, so a fresh install cannot literally be \"given >=2 URLs at setup time\"; the >=2 state arises seconds later via panel/CLI `enable --add`, which (residential-helper.sh:677-700, web/server.js:2355, b-ui-cli.sh:1011) also never creates the timer. So the bug is broader than stated: timer bootstrap happens only on the next update.sh run (cron ≤6h or manual `sudo b-ui` update), never at install or at pool-change time.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "resi-C5",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): triplication of curl_cfg_escape+curl_proxy_cfg in residential-helper.sh:67-76, resi-health.sh:34-43, and JS curlCfgEscape+cfg building in web/server.js:544-568. No counter-claim, no also_raised_as.\n\nStatic reproduction (grep -n 'curl_cfg_escape\\|curl_proxy_cfg\\|curlCfgEscape\\|curlJsonViaProxy' server/*.sh web/*.js):\n- server/residential-helper.sh:68 `curl_cfg_escape() { printf '%s' \"$1\" | sed -e 's/\\\\/\\\\\\\\/g' -e 's/\"/\\\\\"/g'; }`, :71-76 `curl_proxy_cfg()` (proxy = \"scheme://host:port\" + optional proxy-user line). Used at :240 inside verify() piped into `curl ... -K - https://api.ipify.org`.\n- server/resi-health.sh:35 identical `curl_cfg_escape`, :38-43 identical `curl_proxy_cfg`. Used at :79-80 piped into `curl -s -o /dev/null ... -K - \"$PROBE_URL\"`.\n- `diff <(sed -n '67,76p' server/residential-helper.sh) <(sed -n '34,43p' server/resi-health.sh)` → only line 4 differs (the R10 comment: \"scheme 由调用方给（socks5h / http）\" vs \"scheme 由出站类型决定（socks→socks5h、http→http）\"), rc=1. Exactly \"near-byte-identical, differing only in a comment\" as claimed.\n- web/server.js:546 `const curlCfgEscape = (s) => String(s).replace(/\\\\/g, \"\\\\\\\\\").replace(/\"/g, '\\\\\"');`; :550-568 curlJsonViaProxy builds `proxy = \"${scheme}://...\"` (:559) and `proxy-user = \"...\"` (:560) then `execFile(\"curl\", [\"-K\", \"-\", ...])` and `child.stdin.end(cfg)` (:561-567). Called at :619/:621/:624 (ippure / ipquery / ipinfo egress profiling in the panel add-validation path). Claim's \"544-568\" is off by one line at the start (real block starts at the comment on :545), immaterial.\n- No sourcing between the scripts: grep 'source \\|^\\. ' finds none; resi-health.sh is a standalone systemd timer ExecStart (server/update.sh:1164 `ExecStart=/opt/b-ui/resi-health.sh`), residential-helper.sh is shelled out by the panel (web/server.js:46 `residentialHelper: ${BASE_DIR}/residential-helper.sh`) and by update.sh reapply (:347,:545,:666). So the three copies cannot share code today.\n- The input-validation guard (newline in creds, `\"`/`\\` in host, port regex) is ALSO triplicated: helper :203-211, health :70-72, server.js :554-558 — the redundancy is slightly wider than the claim states.\n\nMaintenance-burden evidence (git log -S'curl_cfg_escape' / -S'curlCfgEscape'): both fixes touched all three files — 62ef12e \"SOCKS5 凭据改经 stdin 传给 curl\" (resi-health.sh +24, residential-helper.sh +20, server.js +19) and c593fbc \"curl 配置行校验 host/port\" (3 files changed). R10 socks5h/http scheme work (155b2d8, 9b0fa89, d3f387f) likewise spanned all three. This is exactly the \"fix must be applied in three places\" cost the claim describes.\n\nMinor divergence found (does not refute, but the copies are not semantically identical): server.js:560 emits proxy-user only `if (user && pass)`, whereas both shell copies emit it when `[ -n \"$user\" ]` even with empty password (helper :74, health :41).\n\nAll three sites are live on a fresh install (version.json files list includes server/residential-helper.sh, server/resi-health.sh, web/server.js; timer created by update.sh:1157-1179 which runs on install too). No SSH needed: claim is about repository code, not live host state.",
  "deletable_under_v4": false,
  "correction": "Claim stands. Only nits: the JS block actually begins at web/server.js:545 (comment) / :546 (curlCfgEscape), and the JS copy differs semantically in one detail (proxy-user line requires both user AND pass, shell copies require only user). None of the three copies is migration-only code — each backs a live v4 capability (panel add-validation, systemd health timer, helper verify), so v4 can consolidate (e.g. helper exposes a `curl-cfg` subcommand or a shared sourced lib) but cannot delete any of them with zero behaviour change.",
  "high_impact": false
 },
 {
  "id": "resi-C6",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): no counter-claim, no also_raised_as. All paths under /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5.\n\nTHREE INDEPENDENT PROBE IMPLEMENTATIONS — verified by reading each:\n1. web/server.js:550-573 `function curlJsonViaProxy(member, url, timeoutSec)` — builds a curl `-K -` cfg (`proxy = \"socks5h://…\"` or `http://…`, `proxy-user = …`) in JS, `execFile(\"curl\", [\"-K\",\"-\",\"-m\",…,\"-sS\",url])`, pipes cfg via child.stdin.end(cfg), JSON.parse(stdout). web/server.js:618-629 `async function probeEgress(member)` chains my.ippure.com/v1/info → api.ipquery.io/?format=json → ipinfo.io/json and classifies (ippureEgress:582-598, ipqueryEgress:600-615). Its own escaper `curlCfgEscape` at server.js:546. Invoked only from the `residential/health` GET handler: server.js:2413 `if (r === \"residential/health\" && req.method === \"GET\")`, server.js:2486 `Promise.all(members.map(m => probeEgress(m)))`. Frontend caller: web/app.js:1067 `api(\"/residential/health\")` inside loadResiHealth(), triggered at app.js:1220 `if (document.getElementById(\"sys-resi-body\")) loadResiHealth();` and by the \"检查\" button.\n2. server/residential-helper.sh:200-277 `verify()` — own `curl_cfg_escape` (:68) + `curl_proxy_cfg` (:71-75), probes https://api.ipify.org via `curl -sS --max-time $probe_timeout -K -` (:240-241), auto-tries socks5h then http with 2 rounds (:219-253), compares to VPS IP (:270-271), then enriches with `https://ipinfo.io/${exit_ip}/json` (:274). Called at helper:685 and :725 (enable / enable --add). The panel add flow does NOT re-implement this: web/server.js ~2355 `spawnSync(CONFIG.residentialHelper, [\"enable\", \"--add\", \"-\"], …)` delegates to the helper (comment at server.js:2346 \"解析与类型探测都交 helper\").\n3. server/resi-health.sh:56-98 — a verbatim copy of `curl_cfg_escape`/`curl_proxy_cfg` (:35-43), loops over relay socks/http outbounds (:98 jq on singbox-relay.json), probes `PROBE_URL=https://www.gstatic.com/generate_204` (:21) with `curl -s -o /dev/null --max-time $TIMEOUT -K -` TRIES times (:78-81), scores with hysteresis and switches via Clash API (:115-139). Runtime: systemd `b-ui-resi-health.service` ExecStart=/opt/b-ui/resi-health.sh + timer written by server/update.sh:1158-1179 (enabled only when relay has ≥2 members, update.sh:1144; removed at :1187-1189 otherwise).\n\nLine-number check: verify() 200-277 exact; resi-health 56-98 exact (while loop 56 → done 98); handler 2413 exact; but `curlJsonViaProxy` lives at server.js:550-573, not inside \"618-629\" (only probeEgress is there) — cosmetic imprecision, substance intact.\n\nThe \"un-shared curl-cfg primitive\" characterisation is exact: the identical 9-line `curl_cfg_escape`+`curl_proxy_cfg` block appears in residential-helper.sh:67-75 and resi-health.sh:34-43 (not sourced from a common file — `grep -n 'source\\|\\. ' server/resi-health.sh` shows no sourcing), and is re-implemented a third time in JS at server.js:546-573. The three differ in target URL (ipify / gstatic 204 / ippure+ipquery+ipinfo), timeout (10 / 6 / 8), retry policy, and output (exit IP + VPS comparison / ok-count → active flag / ISP+residential classification), i.e. three purposes, as the claim says — not one redundant feature coded three times.\n\nGIT: `git log -S'probeEgress' -- web/server.js` → dcc002b 2026-09-10 (体检端点以中继成员为真源…); `git log -S'socks5h' -- web/server.js` → 62ef12e / e14a8f1 / d3f387f all 2026-09-10; `git log -S'curl_proxy_cfg' -- server/residential-helper.sh server/resi-health.sh` → 155b2d8 2026-09-10; verify()'s ipify probe dates to 007077e 2026-05-07; resi-health.sh born a6db9aa 2026-06-04 (v3.5.22). Before dcc002b, server.js already had an inline socks5h curl probe at old line 2293-2297 — so the panel-side probe has existed independently since 62ef12e, it was only refactored into curlJsonViaProxy. All three files are in version.json `files` and are deployed on every install (update.sh:262-263, 2139).\n\nLIVE (bwg-rick, read-only): `grep -c curl_proxy_cfg /opt/b-ui/resi-health.sh /opt/b-ui/residential-helper.sh` → 2 and 2; `grep -c 'curlJsonViaProxy\\|probeEgress' /opt/b-ui/admin/server.js` → 6; /var/log/b-ui-resi-health.log shows the periodic probe ran (last entry 2026-09-10 11:22). Side observation, not part of this claim: `systemctl is-enabled b-ui-resi-health.timer` → not-found / inactive on bwg-rick although singbox-relay.json has 2 socks/http members (update.sh:1144 block should have created it) — the health log stopped at 2026-09-10 11:22:49.",
  "deletable_under_v4": false,
  "correction": "Minor: curlJsonViaProxy is at web/server.js:550-573 (with curlCfgEscape at :546); only probeEgress occupies :618-629. Everything else in the claim reproduces. None of the three is migration-only code — each backs a retained capability (add-time credential/port verification; periodic failover scoring driven by b-ui-resi-health.timer; on-demand ISP/residential classification for the panel's 体检 card) — so the v4 clean-reinstall decision does not make any of it deletable; the only cuttable redundancy is the duplicated 9-line bash curl_cfg_escape/curl_proxy_cfg block (helper:67-75 vs resi-health:34-43), which is a consolidation, not a deletion.",
  "high_impact": false
 },
 {
  "id": "resi-C7",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "No counter-claim exists (jq: COUNTER: none, ALSO: empty). Every factual leg of resi-C7 reproduced.\n\n(1) Both production servers run global=true — verified LIVE, not just from memory notes. Command: ssh bwg-rick / ssh -J bwg-rick bwg-tizi with `jq -c \"{enabled,global,domains_is_null:(.domains==null),...}\" /opt/b-ui/residential-proxy.json` and `jq -c \"{final:.route.final,dns_final:.dns.final,kw_rules:([.route.rules[]|select(.domain_keyword)]|length),dns_kw_rules:...}\" /opt/b-ui/singbox-relay.json`. Output identical on both hosts: `{\"enabled\":true,\"global\":true,\"domains_is_null\":true,\"domains_count\":0,\"urls_count\":2}` and `{\"final\":\"resi-pool\",\"dns_final\":\"dns_resi\",\"kw_rules\":0,\"dns_kw_rules\":0}`, version 3.6.3, b-ui-relay active. So the generated relay config on both hosts contains zero domain_keyword rules — the non-global branch is not exercised anywhere in production. Memory notes bwg-tizi-global-residential.md (\"不要关掉 global 模式\") and bwg-rick-config.md (\"global=true, route.final=resi-pool\") agree.\n\n(2) Owner's v4 capability list names only global: HANDOVER.md:100 \"**8 项能力全部保留**：Hysteria2 节点、VLESS-REALITY 节点、住宅出口（global）、…\". Confirmed verbatim; the spec docs/superpowers/specs/2026-09-11-residential-auto-blacklist-design.md:9 also states \"两台生产服务器都是 global\".\n\n(3) The four cited code sites exist at the cited lines:\n- server/residential-helper.sh:413-416 `\"rules\": (if $is_global then [] else [{\"domain_keyword\": $kw, \"server\": \"dns_resi\"}] end)`, `\"final\": (if $is_global then \"dns_resi\" else \"dns_direct\" end)`; :448-452 `+ (if $is_global then [] else [{\"domain_keyword\": $kw, \"outbound\": \"resi-pool\"}] end)`, `\"final\": (if $is_global then \"resi-pool\" else \"direct\" end)` (function starts ~:356, \"v3.5.0: 多 URL urltest 池 + global toggle 支持\").\n- web/server.js:776-784 `if (resi.global) { routeFinal = \"residential-pool\"; } else { if (resi.domains.length > 0) { routeRules.push({ domain_keyword: resi.domains, outbound: \"residential-pool\" }); } routeFinal = \"direct-pool\"; }`.\n- web/server.js:969-974 `matchTarget = resi.global ? \"住宅自动\" : \"直连自动\"` and `resi.domains.map(kw => \\`  - DOMAIN-KEYWORD,${kw},住宅自动\\`)`.\n- web/app.js:809-834 (toggleResidentialGlobal → POST /residential/global, hint \"全走住宅\"/\"域名分流\"), :858-872 and :944-992 (domains textarea, _resiRenderDomains, resetResidentialDomains, saveDomainsOnly); web/index.html:291-342 is the matching markup.\n\"Share one true keyword source\" confirmed: server/residential-helper.sh:88-111 DEFAULT_DOMAINS is the only table; server.js:492-526 getResidentialConfig spawns `residential-helper.sh domains`; update.sh:504-505 comment says the same.\n\nGit history: `git log --oneline -S'domain_keyword' -- server/residential-helper.sh web/server.js` → 9b897d6 (default table expansion), eb4d607 (v3.5.11 added the two subscription-generator sites), 4328926 (v3.5 added is_global/global toggle), 8955e55, 1377ab7. `git log -S'is_global'` → only 4328926, i.e. the toggle logic itself is untouched since v3.5.\n\nNuance (does not overturn the verdict): \"four places\" UNDERCOUNTS the non-global surface. Additional sites that exist only to serve keyword mode: DEFAULT_DOMAINS table + `domains`/`set-domains`/`global on|off` helper subcommands (residential-helper.sh:88-116, :763, :813, :827-851), server.js REST endpoints /api/residential (domains POST, 2168-2169, 2323-2338 residential/global), update.sh R11/R12 domains-migration blocks (:504-560, :1428-1429), b-ui-cli.sh:1114-1115 status printout of 分流关键词, app.js:1085/1119 \"分流关键词\" system-info rows, README.md:46 \"Global toggle: OFF 域名分流（默认）/ ON 全走住宅\", and docs/superpowers/specs/2026-09-11-residential-default-domains-design.md. So the simplification is larger than the claim states.",
  "deletable_under_v4": false,
  "correction": "The claim is right on facts, but \"deletable with zero behaviour change on a fresh install\" does not hold as-is: the shipped DEFAULT for a fresh install is non-global (residential-helper.sh:365-367 `is_global=$(jq -r '.global // false' ...)`, :624 writes `{\"enabled\":false,\"global\":false,\"urls\":[]}`, :842 same; web/server.js:524 default `{ enabled: false, global: false, domains: [] }`; README.md:46 \"OFF 域名分流（默认）\"). A fresh install that adds a residential URL therefore routes by keyword until the admin flips global on. Removing the keyword branch requires flipping the default to global=true (a behaviour change on fresh install) — exactly the product-scope decision the claim itself flags. Also note the approved-but-unimplemented auto-blacklist spec (specs/2026-09-11-residential-auto-blacklist-design.md:34-35) still models keyword mode in its rule layout, so removal must be coordinated with that plan.",
  "high_impact": false
 },
 {
  "id": "resi-C8",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Files under /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5.\n\nCONFIRMED core: (1) server/b-ui-cli.sh:783 configure_residential_menu offers only items 1 启用/修改凭据, 2 禁用, 3 重新校验, 4 健康检查, 0 (lines 807-811); item 1 runs `\"$helper\" enable \"$resi_url\"` (line 822) and item 3 runs `\"$helper\" enable \"socks5://${user}:${pass}@${host}:${port}\"` (line 855) — both the single-URL overwrite path. No `--add`/`--remove` anywhere in that function (`grep -n -- '--add\\|--remove' server/b-ui-cli.sh` hits only lines 1011 and 1016 inside cmd_server_residential). Reached from main menu item 12 \"住宅 IP 出口 →\" (line 1342 → 1373). (2) server/residential-helper.sh:729-733 in the non-flag `enable <url>` branch: `jq '.urls = []' \"${RESIDENTIAL_CONFIG}\" ...` — wipes the pool, then save_config (line 542) writes single host/port. (3) Multi-URL surface exists only in cmd_server_residential (b-ui-cli.sh:999-1035: add→`enable --add`, remove|rm→`enable --remove`, list|ls→status), dispatched at line 1243, and in the panel (web/app.js:790 POST /residential/urls, :801 DELETE /residential/urls/<hostPort>). (4) No runtime caller of the subcommand path: on both live hosts `grep -rl \"b-ui residential\\|residential-helper\" /etc/cron* /var/spool/cron /etc/systemd/system` returned nothing (helper is invoked by panel via web/server.js:46 residentialHelper, not through b-ui-cli). (5) Live hazard is real, not theoretical: `jq '{enabled,global,urls_count:((.urls//[])|length)}' /opt/b-ui/residential-proxy.json` on bwg-rick (<rick-hostname>) and bwg-tizi (<tizi-hostname>, via -J bwg-rick) both show enabled=true, global=true, urls_count=2 — choosing interactive menu item 1 or 3 on either host would silently clear the 2-entry pool to a single URL.\n\nREFUTED part (\"undocumented / undiscoverable\"): the subcommands ARE documented in the CLI's own help — server/b-ui-cli.sh:1261-1267 (`b-ui --help` lists `residential enable/add/remove/list/disable/status/health`), and on both live hosts `b-ui --help | grep -n \"residential add\"` returns line 11 `residential add <url>      新增住宅 SOCKS5 URL（多住宅）`. The claim's docs grep is also imprecise: docs/superpowers/specs/2026-05-07-cli-redesign.md:194 documents `b-ui residential enable <url>` (predates multi-URL), README.md:205 lists helper verbs `{setup|enable|disable|status|reapply|set-domains}` (stale, no --add). So README does not document add/remove/list, but `b-ui help` does; \"effectively undiscoverable\" is overstated.\n\nHistory: `git log -S'enable --add' -- server/b-ui-cli.sh` → 9e9a459 (v3.4.19 \"多住宅\") added the add/remove subcommands; `git log -S'启用 / 修改凭据'` → da3f045 added the interactive submenu earlier; the submenu was never updated for multi-URL (comment at b-ui-cli.sh:1008 \"v3.4.19 Cluster F: 多 URL 支持\" only touched the dispatcher).",
  "deletable_under_v4": false,
  "correction": "The `b-ui residential add/remove/list` path is not undocumented: server/b-ui-cli.sh:1261-1264 prints it in `b-ui --help` (verified live on both hosts). What is true: README.md:205 and the interactive menu (b-ui-cli.sh:807-811) are stale relative to multi-URL, and the interactive menu's items 1/3 (lines 822, 855) call the overwrite `enable <url>` path that clears urls[] (residential-helper.sh:730), which on both production hosts (urls_count=2) would destroy the pool. It is not migration-only code and no cron/systemd caller exists; deleting the subcommand dispatcher would remove a real CLI capability (scripted add/remove), so not zero-behaviour-change under v4. The actionable defect is the interactive menu, not the subcommand: either route menu item 1 to `enable --add` (and add remove/list items) or drop menu item 1/3's overwrite semantics.",
  "high_impact": false
 },
 {
  "id": "web-C1",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "All three parts of the claim reproduce.\n\n(1) `convertOutboundToLink` referenced only at the import line. `grep -rn --exclude-dir=node_modules --exclude-dir=.git convertOutboundToLink .` -> code hits: web/server.js:8 only (`import { convertOutboundToLink } from \"singbox-converter\";`); every other hit is in docs/superpowers/audits/* (the audit text itself). `grep -nE 'import\\(|require\\(' web/server.js` -> no dynamic imports; the only `require(` hits are comment lines 1546-1547. The full import list of server.js is lines 1-8 (http, fs, crypto, child_process, path, https, url, singbox-converter).\n\n(2) js-yaml never imported by server.js. `grep -nE \"js-yaml|from ['\\\"]yaml|require\\(['\\\"]yaml\" .` (excluding node_modules/.git) -> hits only in web/package.json:12, web/package-lock.json, server/core.sh:1678/1703, version.json:108 changelog. `grep -n yaml web/server.js` -> lines 42,49,302,451,879,936-965,981,1029,1933-1934,2567 are all filename strings, comments, a local variable `const yaml = \\`# B-UI Clash Meta ...\\`` (line 981, a template literal, not the library) and a helper `yamlNames`. js-yaml is only in the tree because singbox-converter depends on it (web/package-lock.json:33-40 `\"node_modules/singbox-converter\" ... \"dependencies\": {\"js-yaml\": \"^4.1.0\"}`) and because v3.2.4 (version.json:108) added it explicitly to work around that transitive dep.\n\n(3) History: `git log --oneline -S convertOutboundToLink -- web/server.js` -> 8a70dd7 (v2.10.0 \"集成 singbox-converter，ESM 模块化重构\", introduced) and a3bcb20 (\"fix: 手动生成 VLESS Reality 链接\" 2025-12-22, which removed the only call `-const vlessLink = convertOutboundToLink(vlessOutbound);` but left the import). Untouched since.\n\nRuntime relevance: the import is not a no-op at load time — Node ESM resolves it at startup, so server.js crashes if node_modules/singbox-converter is absent; that is why server/core.sh:1678 writes package.json with both deps, core.sh:1701-1712 retries `npm install singbox-converter js-yaml` and reports \"依赖安装失败\" if the dir is missing, and server/update.sh:318 and :2202 run `npm install` in ADMIN_DIR. The module itself (web/node_modules/singbox-converter/singbox-converter.js) has no module-scope side effects: top level is only `import fs from 'fs/promises'`, `import {URL,URLSearchParams}`, `import yaml from 'js-yaml'`, function/const declarations and exports (awk over module-scope statements; console/fs calls are all inside function bodies).\n\nRemoval test: `sed '8d' web/server.js > scratch/s.js && node --check scratch/s.js` -> PARSE_OK.\n\nLive (read-only): `ssh bwg-rick 'sed -n 8p /opt/b-ui/admin/server.js; grep -c convertOutboundToLink /opt/b-ui/admin/server.js; ls -d /opt/b-ui/admin/node_modules/*; jq -c .dependencies /opt/b-ui/admin/package.json; jq -r .version /opt/b-ui/version.json'` -> import line present, count=1, node_modules = argparse/js-yaml/singbox-converter, deps {\"singbox-converter\":\"^1.0.8\",\"js-yaml\":\"^4.1.0\"}, version 3.6.3. So the deployed panel matches the repo: the dependency is installed, loaded, and never called.\n\nNo counter-claim exists; hist-C6 is listed under also_raised_as but has no entry of its own in claims.json (jq select id==\"hist-C6\" returned nothing). CLAUDE.md line 22 and docs/superpowers/plans/2026-09-10-ipv6-takeover.md:19,757 already record it as an unused dependency.",
  "deletable_under_v4": true,
  "correction": "None needed. One precision: the import is dead code but not behaviour-free — deleting it must go together with dropping both deps from web/package.json:11-12 and web/package-lock.json, the hard-coded package.json at server/core.sh:1678, the retry/verify block core.sh:1701-1712 (it would otherwise print \"依赖安装失败\" because node_modules/singbox-converter no longer exists), and the `npm install` calls at server/update.sh:318 and :2202. Done together, a fresh v4 install has identical panel behaviour (zero calls into the library) and no npm network egress for the web layer.",
  "high_impact": false
 },
 {
  "id": "web-C10",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"web/server.js:1592 stops/disables systemd units sing-box-tun and sing-box-client which b-ui-client.sh never creates (its units are hysteria-client.service at line 29, xray-client.service at 1309 and bui-tun.service at 1626).\" No counter-claim, no also_raised_as.\n\nStatic evidence (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- web/server.js:1591-1594 (inside sendClientScript(), the injected header of the panel-served client installer):\n  `for svc in hysteria-client xray-client sing-box-tun sing-box-client; do` / `systemctl stop \"$svc\" 2>/dev/null || true` / `systemctl disable \"$svc\" 2>/dev/null || true` / `done`\n- `grep -rn 'sing-box-tun\\|sing-box-client' --include='*.sh' --include='*.js' --include='*.json' --include='*.md' .` (audits dir excluded) -> the ONLY hit is web/server.js:1591. Neither b-ui-client.sh, server/*.sh, core.sh, nor the legacy b-ui-server.sh ever writes a unit with either name (b-ui-server.sh only mentions sing-box as a downloaded binary, lines 344-384).\n- b-ui-client.sh unit definitions confirmed at the cited lines: line 29 `CLIENT_SERVICE=\"hysteria-client.service\"`, line 1309 `cat > /etc/systemd/system/xray-client.service`, line 1626 `cat > /etc/systemd/system/bui-tun.service`; additionally line 4016 writes hysteria-health.service. All bui-tun stop/rm sites in the client use the name bui-tun (lines 2110, 2136, 2942, 2959, 4555, 4587, 4590).\n\nHistory evidence:\n- `git log --all --oneline -S'sing-box-tun'` and `-S'sing-box-client'` and `-G'sing-box-(tun|client)(\\.service)?'` -> only 0bd0faa (2026-03-25, \"fix(web): 客户端安装脚本注入头加固\", which introduced the loop) and 5b0da6c (docs/audit artifacts). No commit in any branch ever created a unit under those names.\n- `git log --all --oneline -S'bui-tun.service' -- b-ui-client.sh | tail` -> f1ae3e3 (2025-12-10, \"feat(client): 重构 TUN 模式使用 sing-box 解决 DNS 污染\") is the first TUN commit and it already writes /etc/systemd/system/bui-tun.service (diff line 222). So the TUN unit was named bui-tun from day one, 3.5 months BEFORE the server.js loop was written; the sing-box-* names were never a real prior naming that an upgrade path would need to clean up.\n\nRuntime angle: the loop runs on the client host at install time (script served by /api/client-install / sendClientScript). `systemctl stop|disable` of a nonexistent unit fails and is swallowed by `2>/dev/null || true`, so the two extra names are pure no-ops on every host. Side observation (not part of the claim): the loop also does NOT stop the real TUN unit bui-tun, so the \"stop old client services\" intent is only partially met; the client script itself stops bui-tun in its own install/switch paths.\n\nNo live-host SSH needed: the claim is about repo code and client-side unit names, not server state (and the client host baiyi is not in the allowed host list).",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "web-C11",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"GET /api/kernel-downloads (web/server.js:1739-1767) has no caller: b-ui-client.sh uses /api/kernel-versions (4958, 5650) and fixed /packages/<file> URLs (5052, 5076, 5100).\" No counter-claim, no also_raised_as.\n\nSTATIC (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -rn \"kernel-downloads\" --exclude-dir=.git --exclude-dir=node_modules .` -> only web/server.js:1739 (route `if (r === \"kernel-downloads\")`), web/server.js:1764 (its error log), README.md:227 (doc table row), and the audit files themselves. Zero hits in b-ui-client.sh, web/app.js, web/index.html, server/*.sh, install.sh, b-ui-server.sh (legacy).\n- Broadened `grep -rnE 'kernel-?downloads|\"downloads\"|kernel_downloads|api/kernel'` -> no split-string/variable construction of the path anywhere; the only `api/kernel*` consumers are b-ui-client.sh:4958 and :5650, both `curl ... \"https://${SERVER_ADDRESS}/api/kernel-versions\"` (verified by sed -n 4950,4965p / 5645,5655p).\n- `grep -noE 'api/[a-z-]+' web/app.js web/index.html | grep -i kernel` -> empty: the panel SPA never calls it either.\n- Client kernel downloads are hard-coded paths, not discovered via the API: b-ui-client.sh:5052 `/packages/hysteria-linux-${ARCH_SUFFIX}`, :5076 `/packages/xray-linux-${ARCH_SUFFIX}.zip`, :5100 `/packages/sing-box-linux-${ARCH_SUFFIX}.tar.gz` (sed -n 5045,5105p). The bootstrap script served by the panel (web/server.js:167,171,178) also uses the same fixed /packages/ paths.\n- Handler body (web/server.js:1739-1767) only reads packages/versions.json via loadCachedVersions() (server.js:1319) and fs.existsSync per arch; it writes nothing and nothing else depends on its output.\n\nGIT HISTORY:\n- `git log --oneline -S'kernel-downloads' -- web/server.js b-ui-client.sh README.md server/ install.sh` -> 54430af feat(kernel-proxy) (introduced), a8c53f8 security/README, a51b26a fix(api) compat. `git log -S'kernel-downloads' -- b-ui-client.sh` -> EMPTY: the client never called it in any commit.\n- `git show 54430af -- b-ui-client.sh | grep -nE '^\\+.*(SERVER_ADDRESS|curl)'` -> even the introducing commit's client side already used the fixed /packages/hysteria-linux-…, /packages/xray-linux-….zip, /packages/sing-box-linux-….tar.gz URLs; the endpoint was dead on arrival.\n\nLIVE HOSTS (read-only, one batched ssh per host; bwg-rick direct, bwg-tizi via -J bwg-rick):\n- `grep -l kernel-downloads /opt/b-ui/*.sh /opt/b-ui/packages/*.sh /opt/b-ui/admin/*.js /usr/local/bin/*` -> only /opt/b-ui/admin/server.js on both hosts (deployed client script /opt/b-ui/packages/b-ui-client.sh does not reference it).\n- Caddy access log /var/log/caddy/b-ui-access.log: bwg-rick kd=0 kv=0; bwg-tizi kd=0 kv=35 — i.e. real clients hit /api/kernel-versions 35 times in the retained log and /api/kernel-downloads 0 times.\n- journalctl -u b-ui-admin -u caddy (retained since Aug 28 on tizi, Sep 4 on rick): 0 hits for kernel-downloads on both.\n- No cron/etc reference (`grep -rl kernel-downloads /etc/cron* /var/spool/cron` empty on both).\n\nAttempted refutation angles (all failed to find a caller): split-string construction, panel SPA, legacy b-ui-server.sh, bootstrap script, deployed scripts on both prod servers, HTTP access logs, journald, cron. The endpoint is unauthenticated and public so an external caller is theoretically possible, but no evidence of one exists in ~2 weeks of logs on either host.",
  "deletable_under_v4": true,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "web-C12",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "Repo (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- `grep -rn \"hy2-watchdog-fail\" .` → only three code hits: web/server.js:2546 `fs.readFileSync(\"/tmp/hy2-watchdog-fail-count\")` (claim says :2544; actual read is :2546, failCount declared :2521, emitted as `fail_count` :2561), server/core.sh:1064 `fail_file=\"/tmp/hy2-watchdog-fail-\\${svc}\"`, server/update.sh:1368 same. No shell code in the repo writes `-fail-count`. Consumer: web/app.js:1184 `r.fail_count || 0` → :1200 renders \"N 次\" / \"0\".\n- `git log -S'hy2-watchdog-fail-count'` → f403df0 (2026-05-09, introduced `FAIL_FILE=/tmp/hy2-watchdog-fail-count` in core.sh+update.sh), 9e9a459 (v3.4.19, added the panel reader in web/server.js), 2a7af40 (v3.5.14, 2026-06-03). `git show 2a7af40 -- server/core.sh server/update.sh` shows `-FAIL_FILE=/tmp/hy2-watchdog-fail-count` / `+fail_file=\"/tmp/hy2-watchdog-fail-\\${svc}\"` in both files, with NO matching change to web/server.js. So the mismatch is a v3.5.14 regression on the shell side.\n- Upgrade path: server/update.sh:1354 only writes hy2-watchdog.sh `if [[ ! -f /etc/systemd/system/hy2-watchdog.timer ]]` — an existing install keeps whatever watchdog script it already has; no block rewrites an existing script.\nLive hosts (read-only):\n- `ssh bwg-rick 'grep -n fail /opt/b-ui/hy2-watchdog.sh; ls /tmp/hy2-watchdog-fail*'` → line 10 `fail_file=\"/tmp/hy2-watchdog-fail-${svc}\"`, no fail files present, version 3.6.3, hy2-watchdog.timer active, b-ui-admin has no User=/PrivateTmp (runs as root, /tmp shared). On rick the panel reads a name that is never written → fail_count is always 0 (claim holds).\n- `ssh -J bwg-rick bwg-tizi 'sed -n 1,8p /opt/b-ui/hy2-watchdog.sh'` → line 5 `FAIL_FILE=/tmp/hy2-watchdog-fail-count` (pre-3.5.14 single-instance watchdog, never migrated), version 3.6.3, timer active, log shows it running through 2026-09-08. On tizi the file name DOES match the panel, so fail_count is not \"always 0\" — it reflects the direct-instance counter (currently 0 because no failure file exists).\nConclusion: the claim is correct for every fresh install since v3.5.14 (core.sh path) and for bwg-rick, but the categorical \"therefore always 0\" is wrong for pre-3.5.14 installs that were never re-installed (bwg-tizi live proves it). Line number off by 2 (2546 not 2544).",
  "deletable_under_v4": true,
  "correction": "web/server.js:2546 (not :2544) reads /tmp/hy2-watchdog-fail-count. On any install whose watchdog was written by core.sh/update.sh >= v3.5.14 (2a7af40) the file never exists and fail_count is always 0 — true for bwg-rick today. But bwg-tizi still runs the pre-3.5.14 watchdog (`/opt/b-ui/hy2-watchdog.sh` line 5: `FAIL_FILE=/tmp/hy2-watchdog-fail-count`, because update.sh:1354 never rewrites an existing watchdog), so there the panel's reader matches the writer and fail_count is live for hysteria-server. Under the v4 clean-reinstall decision the old name disappears everywhere, so the reader at server.js:2546-2548 (and the fail_count field at :2561 plus its app.js:1184/1200 rendering) is dead as written and can be deleted, or replaced by reading /tmp/hy2-watchdog-fail-hysteria-server and -hysteria-residential.",
  "high_impact": false
 },
 {
  "id": "web-C13",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): no counter-claim, no also_raised_as.\n\n1) POST /api/port-hopping never edits `listen:` — web/server.js (POST branch actually starts at :2093; GET at :2077):\n - :2105-2106 only READS config.yaml: `const hyc = fs.readFileSync(CONFIG.hysteriaConfig, \"utf8\"); const pm = hyc.match(/listen:\\s*:(\\d+)/);`\n - :2114 `fs.writeFileSync(phFile, JSON.stringify(phConfig, null, 2));` (port-hopping.json)\n - :2129 `iptables -t nat -A PREROUTING -i \"$iface\" -p udp --dport ${start}:${end} -m comment --comment \"Hysteria2-PortHopping\" -j REDIRECT --to-ports ${listenPort}`\n - :2139-2140 disable path deletes the same rules.\n - `grep -n \"writeFileSync(CONFIG.hysteriaConfig\" web/server.js` → only :323 (masquerade/obfs helper), :2584, :2632 (bandwidth); none inside the port-hopping handler. `grep -n \"listen:\" web/server.js` → :2630 is the only replace() touching a listen line and it inserts a bandwidth section after it; no code rewrites `listen:` with a port range. No hysteria restart in the handler either.\n\n2) getConfig treats listen: as source of truth since v3.6.0 — web/server.js:445-448:\n `// v3.6.0: hysteria 实际监听 (listen: :PORT[,START-END]) 是端口跳跃的真源；有区间即启用且用该区间；无区间沿用 port-hopping.json（老式 iptables 路径兜底）`\n `const lm = hc.match(/^listen:\\s*:?(\\d+)(?:,(\\d+)-(\\d+))?\\s*$/m); if (lm && lm[2]) portHopping = { enabled: true, start:…, end:… };` — overrides whatever :434-441 read from port-hopping.json. Introduced by commit 11febfa (git log -S'端口跳跃的真源').\n\n3) core.sh deletes those rules as obsolete — server/core.sh:1387-1400: comment `hysteria 2.9+ 使用 config.yaml 内置 listen 多端口语法 … 不再需要手动写 REDIRECT`, then :1392 `if … iptables -t nat -L PREROUTING -n | grep -q \"Hysteria2-PortHopping\"; then` :1395 `iptables-save | grep -v \"Hysteria2-PortHopping\" | iptables-restore`. Same cleanup duplicated in server/update.sh:1313-1334 inside apply_systemd_configs() (fn starts :756), which runs on every `update.sh auto` (cron every 6h, core.sh:1223; update.sh:2099/2226 run it even when already latest). core.sh writes the range into listen itself at :528-529/:541 (`direct_listen=\":${PORT},${START}-${END}\"`) and stamps port-hopping.json with `\"implementation\": \"hysteria-builtin-listen\"` (:1422-1430).\n\n4) git history: handler introduced by c17da8c \"feat: 添加网页端口跳跃设置 & UI 优化\" (git log -L2098,2110:web/server.js); iptables text last touched f403df0; the handler was never updated for the v3.6.0 listen-as-truth change. UI still wires it: web/index.html:44 `onclick=\"openPortHopping()\"`, :402 modal; web/app.js:507/:523 GET+POST.\n\n5) Live state (read-only): `ssh bwg-rick` and `ssh -J bwg-rick bwg-tizi` running `grep -n \"^listen:\" /opt/b-ui/config.yaml; jq \"{enabled,startPort,endPort,listenPort,implementation}\" /opt/b-ui/port-hopping.json; sudo iptables -t nat -L PREROUTING -n | grep -c Hysteria2-PortHopping; grep -c Hysteria2-PortHopping /etc/iptables/rules.v4` → both hosts: `listen: :10000,20000-30000`, json enabled=true 20000-30000 implementation=hysteria-builtin-listen, 0 iptables rules, 0 in rules.v4, version 3.6.3. Confirms hysteria built-in hopping is what runs; the panel's iptables path has left no trace.\n\nNuance (does not refute): the handler is not entirely inert. If an operator declined hopping at install (core.sh:528 → `listen: :PORT`, no port-hopping.json), a panel POST enabled=true makes getConfig fall back to port-hopping.json (:446 \"老式 iptables 路径兜底\") and the REDIRECT rule really delivers hopped ports to :PORT — until the next `update.sh auto` (≤6h) migrates listen to multi-port (update.sh:801-815, only when listen is single-port) and strips the rule (:1325-1326). On the default/live path (listen already has a range) POST is a no-op for subscriptions: disable writes enabled:false but :448 keeps reporting enabled; a different range in the POST never reaches listen (update.sh:810 guard `^listen:\\s*:[0-9]+\\s*$` skips it) and the extra REDIRECT rule gets purged by cron. Same iptables-era logic is duplicated in server/b-ui-cli.sh:575-655 (menu \"2\" deletes rules + rm port-hopping.json, never touches listen).",
  "deletable_under_v4": false,
  "correction": "Minor line drift only: the POST branch begins at web/server.js:2093 (GET at :2077), getConfig's listen parse is :445-448, core.sh cleanup is :1387-1400. Not deletable with zero behaviour change: the POST handler is the only panel write path for the (retained) port-hopping capability and is still functional on the hopping-disabled-at-install path; under v4 it must be REPLACED (rewrite `listen:` + restart hysteria-server, drop iptables/port-hopping.json) rather than removed. The core.sh:1392-1400 and update.sh:1313-1334 iptables cleanup blocks and the b-ui-cli.sh:643-650 iptables path become deletable on a fresh v4 install only once the panel stops creating \"Hysteria2-PortHopping\" rules.",
  "high_impact": false
 },
 {
  "id": "web-C14",
  "lenses": [],
  "verdict": "PARTIALLY",
  "evidence": "CLAIM (jq on docs/superpowers/audits/2026-09-11-claims.json, no counter-claim, no also_raised_as): \"syncKernels (web/server.js:1242-1419) duplicates the GitHub release polling done by server/core.sh:1566-1651 at install and by `update.sh kernel` (server/update.sh:1944-2065, cron every 12h), with a different sing-box version cap.\"\n\n1) \"different sing-box version cap\" — REFUTED. All copies carry the identical cap:\n   - web/server.js:1280 `const SINGBOX_MAX_MINOR = \"1.14\";`\n   - server/core.sh:1539 `SINGBOX_MAX_MINOR=\"1.14\"`\n   - server/update.sh:1806 `SINGBOX_MAX_MINOR=\"1.14\"`\n   - b-ui-client.sh:1126 `SINGBOX_MAX_MINOR=\"1.14\"`\n   Cmd: `grep -n \"SINGBOX_MAX_MINOR *=\" web/server.js server/core.sh server/update.sh b-ui-client.sh`. Same on the live host: `ssh bwg-rick grep -n 'SINGBOX_MAX_MINOR *=' /opt/b-ui/update.sh /opt/b-ui/core.sh /opt/b-ui/admin/server.js` → 1806/1539/1280 all \"1.14\". Cap semantics are also equivalent: JS pickLatestTag (server.js:1284-1300) = \"latest stable if minor<=1.14 else first v1.14.x else null\"; bash singbox_latest_version (core.sh:1547-1560 / update.sh:1814-1827) = same (sort -V compare, then capped regex, else empty). Both caps were added in the same commit: `git log -S SINGBOX_MAX_MINOR` → e74fd94 2026-09-10 (+ af726bf 2026-09-10 for the install channel).\n\n2) \"duplicates the GitHub release polling\" — CONFIRMED for the polling/version-pick code, three independent implementations of the same \"releases?per_page=100 → filter tags → cap sing-box\" logic:\n   - web/server.js:1284-1315 pickLatestTag/getGitHubLatest (hysteria still via /releases/latest, 1314).\n   - server/core.sh:1541-1562 gh_latest_tag/singbox_latest_version/xray_latest_version, used by download_client_packages (core.sh:1564-1653; hysteria via /releases/latest at 1580).\n   - server/update.sh:1808-1829 verbatim copy of the same bash helpers (update.sh does not source core.sh: `grep -n \"source\\|^\\. \" server/update.sh` shows none), used by auto_update_kernel (update.sh:1944-2009) and update_kernel (1859).\n   - b-ui-client.sh:1128-1149 a fourth copy.\n   Line numbers in the claim are slightly off: download_client_packages is core.sh:1564-1653; auto_update_kernel is update.sh:1944-2009 (2010-2065 is ensure_cli_exists/ensure_cron_jobs).\n\n3) But the three sites are NOT the same feature, so \"duplicates\" overstates the redundancy:\n   - syncKernels (server.js:1375-1416, scheduled at 1418-1419: 10s after start + every 6h) and core.sh download_client_packages (called once from install.sh:613) both populate /opt/b-ui/packages/{hysteria-linux-*,xray-linux-*.zip,sing-box-linux-*.tar.gz} + versions.json for CLIENT downloads (served by server.js:1633 `/packages/`, described by `/api/kernel-downloads` server.js:1739-1765, consumed by b-ui-client.sh:5052/5076/5100 and the one-click installer at server.js:167-178). core.sh writes a flat versions.json (core.sh:1592-1599, current arch only); server.js writes nested {version,syncedAt} for both arches; server.js:1747-1749 explicitly tolerates both formats. Here the install-time copy IS redundant with syncKernels.\n   - `update.sh kernel` (auto_update_kernel) upgrades the SERVER's own installed binaries (get.hy2.sh at 1953, Xray-install at 1977, install_singbox_version at 1996) and restarts hysteria-server/xray — it never touches packages/. Only the polling helper is shared, not the purpose.\n\nLive verification on bwg-rick (read-only): /opt/b-ui/packages/versions.json is in server.js's nested format (hysteria2 2.12.2 syncedAt 2026-09-02, xray 26.9.9 syncedAt 2026-09-11T02:15, singbox 1.14.0); file mtimes match those syncedAt stamps (xray-linux-*.zip Sep 11 02:15, others Sep 2 06:39) → syncKernels, not core.sh, is what maintains the cache on a running install. `journalctl -u b-ui-admin | grep 内核同步` shows runs at 03:46 and 05:07 on 2026-09-11 (\"已是最新\"). crontab has `30 */12 * * * /opt/b-ui/update.sh kernel`; /var/log/b-ui-kernel-update.log tail shows it running the server-binary path (\"Xray 安装未生效（仍为 v26.3.27）\" — side finding: Xray-install installs /releases/latest so the server-side Xray upgrade is stuck, unrelated to this claim). Installed server binaries: hysteria v2.12.2, Xray 26.3.27 — different from the packages cache (xray 26.9.9), confirming the two paths track different things.\n\ngit: syncKernels introduced 54430af 2026-03-20 feat(kernel-proxy); last touched e74fd94/af726bf 2026-09-10 together with the bash copies.",
  "correction": "The sing-box cap is identical (SINGBOX_MAX_MINOR=\"1.14\") in web/server.js:1280, server/core.sh:1539, server/update.sh:1806 and b-ui-client.sh:1126, both in the repo and as deployed on bwg-rick; the \"different cap\" part is false. What is true: the GitHub releases-list polling + tag-pick + sing-box-cap logic is re-implemented four times (server.js:1284-1315 in JS; identical bash copies at core.sh:1541-1562, update.sh:1808-1829, b-ui-client.sh:1128-1149). Of the three server-side callers only two serve the same purpose — syncKernels (server.js:1375-1419, every 6h) and core.sh download_client_packages (1564-1653, install-time, install.sh:613) both fill /opt/b-ui/packages for client downloads, and on bwg-rick the cache and versions.json are demonstrably maintained by syncKernels. `update.sh kernel` (auto_update_kernel, update.sh:1944-2009, not -2065) upgrades the server's own installed hysteria/xray/sing-box binaries and never writes packages/; it shares only the polling helper.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "web-C15",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"The proto×residential node-selection tree is implemented four times: web/server.js:706-760, 909-934, 1850-1907 and web/app.js:293-350, with ports 10002/40000/41000-50000 hard-coded in each, and /api/sub (1857) hard-codes 10001 while the other three use cfg.xrayPort.\" No counter-claim, no also_raised_as.\n\nCommands run (all in /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n1. `sed -n '700,765p;900,940p;1845,1912p' web/server.js` and `sed -n '285,355p' web/app.js` — read all four ranges.\n2. `grep -nE '10001|10002|40000|41000|50000|xrayPort' web/server.js web/app.js`\n3. `git log --oneline -S'mkHy2(\"hy2-residential\", 40000' -- web/server.js` → eb4d607 (v3.5.11); `-S'buildVlessUrl(10001'` → 1196885 (v3.5); `-S'includeResi ? 10002 : (cfg.xrayPort'` -- web/app.js → 44d0460; `git blame -L1854,1859 web/server.js` → 0b3d9da0 2026-05-14.\n4. `grep -nE '\\$\\{?xray_port\\}?' server/core.sh`; `sed -n '790,798p;1378,1390p;1424,1430p' server/core.sh`.\n5. `grep -nE 'nodeSet|buildNodes|selectNodes|resolveNodes' web/server.js web/app.js` → no shared helper exists.\n\nDecisive facts:\n- Copy 1, generateSingboxConfig (web/server.js:650): tree at 706-744. `vlessDirectPort = cfg.xrayPort || 10001` (700); `mkVless(\"vless-residential\", 10002)` at 714 and 722; `mkHy2(\"hy2-residential\", 40000, 41000, 50000, false)` at 733 and 741. Comment at 706: \"节点集合，严格对齐 web/server.js /api/sub/ 的 proto/residential 逻辑\" — the code itself declares it is a hand-kept mirror of /api/sub.\n- Copy 2, generateClashConfig (web/server.js:850): tree at 909-933. `vlessDirectPort = cfg.xrayPort || 10001` (906); 10002 at 915/919; `40000, 41000, 50000` at 927/931. Comment at 909: \"节点集合，严格对齐 /api/sub/ 的 proto/residential 逻辑\".\n- Copy 3, /api/sub handler (route `if (r.startsWith(\"sub/\"))` at web/server.js:1807): tree at 1850-1906. `buildVlessUrl(10001, \"Reality直连\")` at 1854, `includeResi ? 10002 : 10001` at 1859, `buildVlessUrl(10002, ...)` at 1866, `buildHy2Url(40000, \"41000-50000\", ...)` at 1896 and 1904. This copy never reads cfg.xrayPort — the only one of the four that does not. (The 10001 literal is at 1854 and 1859; line 1857 is a comment — claim's line number is off by 2-5.)\n- Copy 4, app.js genUri (web/app.js:292-351): `includeResi ? 10002 : (cfg.xrayPort || 10001)` at 314; `includeResi ? 40000 : (cfg.port || 10000)` at 330; `\"41000-50000\"` at 332. Caveat: genUri implements only the single-protocol half of the tree — for `x.protocol === \"fusion\"` it returns the `/api/sub/<user>` URL (app.js:295-299) and delegates to copy 3, so app.js is a partial (2-leaf) copy, not a full 2x2 tree. It also carries vless-ws-tls (app.js:317-324) which the other three do not.\n- Practical weight of the 10001 divergence: core.sh writes the vless-direct inbound port as the literal 10001 (server/core.sh:795); `XRAY_PORT` (core.sh:1383) is only echoed into the port-hopping state JSON (core.sh:1428) and never used to write xray-config.json, and getConfig (web/server.js:414-419) reads xrayPort back from that inbound. So on every real install cfg.xrayPort == 10001 and the four copies produce identical ports today; the divergence is a latent inconsistency (would bite only if someone changes the xray-direct port by hand), not a live bug.\n- History: copies 1 and 2 were added in eb4d607 (v3.5.11) explicitly \"对齐 /api/sub\"; copy 4 was re-aligned in 44d0460 \"面板复制链接与订阅生成器对齐\" — i.e. the project has already paid the cost of drift twice, which corroborates the redundancy finding.\n\nNet: the structural claim (four hand-synchronised copies, hard-coded 10002/40000/41000-50000 in each, /api/sub alone hard-coding 10001) is accurate; only the minor details differ (app.js copy is partial; 10001 literal at 1854/1859 not 1857; the 10001 divergence has no observable effect on current installs).",
  "correction": "Minor precision only: (1) web/app.js genUri (292-351) implements only the single-protocol branches; fusion users get the /api/sub URL (app.js:295-299), so it is a partial copy rather than a full fourth tree. (2) The 10001 literals in /api/sub are at web/server.js:1854 and :1859, not 1857. (3) cfg.xrayPort is always 10001 in practice because server/core.sh:795 hard-writes the inbound port and XRAY_PORT (core.sh:1383) only reaches the state JSON (core.sh:1428), so the /api/sub divergence is latent, not currently behaviour-affecting.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "web-C16",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "All three parts of the claim reproduce; no counter-claim exists (jq: COUNTER: none, ALSO: empty).\n\n1) Cert-path mismatch. web/server.js:358-380 (`updateXrayConfig`) builds the `vless-ws-tls` inbound only when `wsUsers.length > 0 && !wsInbound`; it derives `domain` with `hc.match(/\\/live\\/([^\\/]+)\\/fullchain/)` against config.yaml (server.js:360-361) and writes `certificateFile: \"/etc/letsencrypt/live/\" + domain + \"/fullchain.pem\"` / `keyFile: \".../privkey.pem\"` (server.js:373-374). core.sh writes config.yaml with `cert: ${CERTS_DIR}/fullchain.pem` where `CERTS_DIR=\"${BASE_DIR}/certs\"` (server/core.sh:22, :541-547; also core.sh:935 `CERTS_DIR=\"/opt/b-ui/certs\"`, :937 `.domain`). The regex therefore never matches on any v3.5+/v4 install -> domain falls back to \"localhost\" and the inbound points at non-existent /etc/letsencrypt paths (worse than the claim: serverName would also be \"localhost\"). `grep -rn letsencrypt server/ install.sh b-ui-client.sh web/app.js` hits only Caddy's internal storage dir name (core.sh:955, update.sh:1461), never a /etc/letsencrypt path.\n\n2) No subscription generator emits a ws-tls node. `grep -n \"vless-ws-tls\" web/server.js` -> lines 286 (filter passed to updateXrayConfig), 352-363 (inbound builder), 1076 (traffic-stats filter), 1846 (comment \"vless-ws-tls: 单独走 ws-tls inbound（不在此 4-URL 框架内）\"), 2014 (uuid-vs-password on edit). `/api/sub` (server.js:1848-1900) only branches on proto === fusion|vless-reality|hysteria2, so a ws-tls user gets an empty link list. `generateSingboxConfig` (server.js:650-) and `generateClashConfig` (server.js:850-) use the same proto/includeResi logic and contain no \"ws\" transport (`grep -n '\"ws\"\\|type=ws\\|transport' web/server.js` -> only line 368, inside the inbound builder). Only the browser-side web/app.js:317-324 `genUri()` fabricates a `vless://...type=ws&path=%2Fws` URI on :10003 (cfg.wsPort is never set by server.js: `grep -n wsPort web/server.js` -> no hits), so the panel shows a link for a server inbound that cannot be loaded.\n\n3) Runtime / live state. core.sh and update.sh never create a ws inbound (`grep -n \"vless-ws\\|10003\\|\\\"ws\\\"\" server/core.sh server/update.sh` -> no hits). Live: `ssh bwg-rick` and `ssh -J bwg-rick bwg-tizi` (both v3.6.3): xray-config.json inbounds are only api:10085, vless-direct:10001 reality, vless-residential:10002 reality; users.json protocols are fusion x4/x5 + hysteria2 x1 (zero vless-ws-tls users); `/etc/letsencrypt/live` does not exist on either host; config.yaml cert lines are /opt/b-ui/certs/fullchain.pem + privkey.pem on both.\n\n4) History. `git log --oneline -S'/etc/letsencrypt/live/' -- web/server.js` -> single commit 808f081 (2025-12-11, \"模块化重构，支持自动更新\"); the path was never touched after the Caddy->/opt/b-ui/certs move. The :10002->:10003 fix-up at server.js:353-356 is a v3.4->v3.5 migration guard only.",
  "deletable_under_v4": true,
  "correction": "Minor sharpening, not a refutation: the domain regex at server.js:360-361 cannot match core.sh's config.yaml, so a fresh install would write serverName \"localhost\" plus the wrong cert paths — the inbound builder is not merely mis-pathed, it is unreachable-in-practice/broken. Deleting it (server.js:349-384, the filter at :286, the :10003 migration guard at :353-356) changes nothing on a fresh install because no ws-tls user exists and core.sh never writes the inbound; the only observable \"change\" is that choosing the \"VLESS + WS + TLS (免流专用)\" option (web/index.html:199, app.js:157/317/391/542) would stop writing an unloadable xray config. If ws-tls is counted among the 8 kept capabilities it must be reimplemented against /opt/b-ui/certs and added to all three generators, not preserved as-is.",
  "high_impact": false
 },
 {
  "id": "web-C17",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static: server/core.sh:1727-1735 heredoc writes admin.env with exactly ADMIN_PORT/ADMIN_BIND/ADMIN_PASSWORD/HYSTERIA_CONFIG/USERS_FILE/XRAY_CONFIG/XRAY_KEYS — no JWT_SECRET. Same key set in the two other writers: server/update.sh:589-597 (migrate_admin_env) and server/b-ui-cli.sh:424-431 (password change fallback); web/server.js:2646-2670 password handler only rewrites the ADMIN_PASSWORD line. `grep -rn \"JWT_SECRET\\|jwtSecret\" server web install.sh b-ui-server.sh b-ui-client.sh` hits only web/server.js:41 (`jwtSecret: process.env.JWT_SECRET || crypto.randomBytes(32).toString(\"hex\")`), :222 (genToken HMAC-SHA256 with CONFIG.jwtSecret) and :228 (verifyToken compares HMAC with the same in-process secret). Nothing writes the secret to disk or to the unit file (core.sh:1738-1757 unit has only `EnvironmentFile=-${BASE_DIR}/admin.env`, no Environment=). Client side web/app.js:7/60 keeps the token in localStorage and app.js:47 calls logout() on any 401, so users are bounced to the login screen after a restart. Tokens themselves have a 24h exp (server.js:221), so only restart shortens them. Git: `git log --oneline -S JWT_SECRET -- web/server.js server/core.sh server/update.sh` → 808f081 (模块化重构) only; `git log -S jwtSecret` → 72eb7ee v1.0 … 523b18c v3.0.0 — the fallback has existed since v1.0 and was never given a persisted value. Live (read-only): bwg-rick `sed -n \"s/=.*//p\" /opt/b-ui/admin.env` → ADMIN_PORT ADMIN_BIND ADMIN_PASSWORD HYSTERIA_CONFIG USERS_FILE XRAY_CONFIG XRAY_KEYS; `grep -c \"^JWT_SECRET=\" /opt/b-ui/admin.env` → 0; `grep -n Environment /etc/systemd/system/b-ui-admin.service` → only line 7 EnvironmentFile=-/opt/b-ui/admin.env; `systemctl show b-ui-admin -p Environment` → empty; deployed /opt/b-ui/admin/server.js:41 identical fallback. bwg-tizi (via -J bwg-rick) identical: JWT_SECRET count 0, same 7 keys, same unit line, same server.js:41. Runtime reproduction (local scratch, `BASE_DIR=<scratch> ADMIN_PORT=18099 ADMIN_PASSWORD=test123 node server.js`): POST /api/login → token; GET /api/users with Bearer token in same process → 200; kill + restart node → same token → 401; repeat with JWT_SECRET=fixedsecret exported in both runs → 200 after restart. Restart triggers that exist in code: password change (server.js:2674 spawn systemctl restart b-ui-admin; b-ui-cli.sh), update.sh version updates touching web/* or version.json, migrate_admin_env (update.sh:610), Restart=always on crash.",
  "correction": "",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "web-C18",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static call graph (worktree HEAD 0b5b559, version.json 3.6.3):\n- web/server.js:492-504 `getEffectiveResidentialDomains()` → `spawnSync(CONFIG.residentialHelper, [\"domains\"], {timeout:3000})` (synchronous; CONFIG.residentialHelper = `${BASE_DIR}/residential-helper.sh`, line 46). No memoisation anywhere (`grep -n 'cache\\|Cache\\|memo' web/server.js` only hits _hostV4Cache/kernel version cache/clash cache_file, none wrap this).\n- web/server.js:508-524 `getResidentialConfig()`: line 520 `domains: !enabled ? [] : (Array.isArray(r.domains) && r.domains.length > 0) ? r.domains : getEffectiveResidentialDomains()` — i.e. spawns whenever enabled===true && urls.length>0 && domains is null/[] (follow-default state, documented at server.js:2163-2167, the claim's \"2170-2172\" is off by ~5 lines in this tree).\n- Callers: server.js:651 `generateSingboxConfig()` and :851 `generateClashConfig()` both start with `const resi = getResidentialConfig();`. Route dispatch: server.js:1783 `if (r.startsWith(\"subscription/\"))` → line 1792 `generateSingboxConfig(user, cfg, host)`; server.js:1921 `if (r.startsWith(\"clash/\"))` → `generateClashConfig(...)` 10 lines later. Both endpoints are unauthenticated, so each hit spawns bash+jq synchronously.\n- Nuance that strengthens the claim: the spawn also happens when `global:true`, even though resi.domains is then unused by both generators (server.js:776-782 `if (resi.global) {...} else if (resi.domains.length>0)`; :972 `!resi.global && resi.domains.length > 0`). /api/sub/ does NOT go through getResidentialConfig (only subscription/ and clash/), matching the claim's scope.\n- Helper side: server/residential-helper.sh:767-771 `domains)` → `get_domains` (:118-128 reads residential-proxy.json via jq, falls back to DEFAULT_DOMAINS) → jq -R | jq -sc unique.\n- Git: `git log -S getEffectiveResidentialDomains -- web/server.js` → introduced 42b1943 (helper `domains` subcommand, v3.6.0 R1), last touched 0b5b559 (v3.6.2 R12 follow-default = domains null).\nLive (bwg-rick, read-only): `jq -c '{enabled,global,domains,nurls:(.urls|length)}' /opt/b-ui/residential-proxy.json` → {\"enabled\":true,\"global\":true,\"domains\":null,\"nurls\":2}; deployed admin/server.js contains getEffectiveResidentialDomains (5 hits), version 3.6.3 — so the production server is exactly in the state where every /api/subscription and /api/clash request spawns the helper. `time /opt/b-ui/residential-helper.sh domains | jq length` → 67 domains, real 0m0.026s — the cost per request is ~26 ms of blocking the single-threaded event loop; correct but low impact (claim itself is marked high_impact:false).",
  "correction": "Minor: the follow-default handling is at web/server.js:2163-2167 (not 2170-2172), and the spawn also occurs in global mode where the result is discarded. Cost measured live is ~26 ms per request, not a serious hotspot.",
  "deletable_under_v4": false,
  "high_impact": false
 },
 {
  "id": "web-C2",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "CALL GRAPH (static):\n1. web/server.js:1149-1155 `checkUserLimits(u)` — grep -n \"checkUserLimits\" web/server.js web/app.js → definition at 1149 and exactly ONE caller at 2698, inside the `/auth/hysteria` POST handler (web/server.js:2691-2712). No eval/indirect/string invocation anywhere else.\n2. The only thing that can hit `/auth/hysteria` is Hysteria2 with `auth.type: http` (core.sh:560-563 / update.sh:457-459 / update.sh:1587-1589 templates; b-ui-server.sh:541-543 is the legacy v3.1 installer, not in version.json).\n3. Three independent mechanisms rewrite that auth block to `type: userpass`:\n   a. Install: install.sh:557 `source core.sh` → install.sh:569 `configure_hysteria` → core.sh:662 `apply_hy2_userpass_auth` (def core.sh:669; core.sh:672 bails only if jq missing; core.sh:682 `grep -qE '^  type: http$' || continue` guard; awk at core.sh:684 replaces the auth block). jq is a hard install dependency: install.sh:99 `\"jq:jq:jq\"`, install.sh:135-137 `print_error ... exit 1` if still missing after install → \"jq absent\" is not a surviving install state.\n   b. Every update (including \"already latest\"): update.sh:756 `apply_systemd_configs` called from update.sh:313, 2099, 2198, 2226; block D6 at update.sh:1089-1120 converts `^  type: http$` → userpass for config.yaml and config-residential.yaml, restarts the service.\n   c. Panel (no jq needed at all): web/server.js:276-291 `saveUsers()` → `updateHysteriaConfig(hy2Users)` (server.js:317-326) and `updateHysteriaResidentialConfig` (server.js:303-315) regex-replace `auth:[\\s\\S]*?` with `auth:\\n  type: userpass\\n  userpass:` on every user create/edit/delete (callers server.js:1198,1207,1221,1966,1973,2062). So the claim's \"where jq is present\" qualifier is actually weaker than reality: after the first panel user save the endpoint is dead even without jq.\n4. Enforcement of limits keys: grep -rn \"expiresAt\\|trafficLimit\\|monthlyLimit\\|speedLimit\" server/ b-ui-client.sh install.sh b-ui-server.sh → only server/core.sh:523 (seed users.json with speedLimit:100000000). In web/server.js the keys are only written (1193-1196, 1217-1219, 2027-2056), reported in Subscription-Userinfo headers (1915, 1937), or read by the dead checkUserLimits/auth handler (1151-1153, 2703-2707). app.js:150-151,234-246 only displays/edits them. Hysteria2 userpass mode has no per-user rx/tx, so speedLimit cannot be applied; there is no cron/timer that kicks expired or over-quota users (grep \"kick\" → server.js:2070 only proxies a manual /kick API call).\n5. Git: `git log -S\"apply_hy2_userpass_auth\" -- server/core.sh` → 5b6cde3 \"fix: v3.5.15 高并发硬化 — hy2 auth http→userpass\"; checkUserLimits dates from 808f081 (original modular refactor) and was never re-wired.\n\nLIVE HOSTS (read-only):\n- `ssh bwg-rick 'grep -nE \"^auth:|^  type: \" /opt/b-ui/config.yaml /opt/b-ui/config-residential.yaml; command -v jq; journalctl -u b-ui-admin --since \"3 days ago\" | grep -c AUTH'` → config.yaml:23-24 `auth:` / `type: userpass`; config-residential.yaml:23-24 same; /usr/bin/jq 1.8.1; 0 AUTH log lines in 3 days; version 3.6.3.\n- `ssh -J bwg-rick bwg-tizi '<same>'` → config.yaml:31-32 userpass; config-residential.yaml:16-17 userpass; jq 1.8.1; 0 AUTH log lines; version 3.6.3.\nBoth production servers run userpass; /auth/hysteria has received no requests (the handler logs every hit at server.js:2707/2709/2711 with tag AUTH).\n\nCOUNTER-CLAIM: none. also_raised_as hist-C5 does not exist as an id in the claims file (jq select returned nothing).",
    "deletable_under_v4": true,
    "correction": "Claim is accurate; only refinement: the \"where jq is present\" qualifier is unnecessarily narrow — jq is a hard install dependency (install.sh:99,135-137 exit 1), and even without jq web/server.js:276-326 saveUsers() rewrites both auth blocks to userpass on the first panel user save. On a v4 fresh install the following can be deleted with zero behaviour change: web/server.js:2690-2712 (/auth/hysteria handler), web/server.js:1149-1155 (checkUserLimits), the `auth: type: http` template lines in server/core.sh:560-563 and 618-621 (write userpass directly and drop apply_hy2_userpass_auth core.sh:667-696), update.sh D6 block 1089-1120 and the http-auth templates at update.sh:457-460 and 1587-1590. Note this leaves limits.expiresAt/trafficLimit/monthlyLimit/speedLimit as display-only fields; if the \"user limits\" capability is meant to be one of the 8 kept product capabilities, v4 needs a new enforcement path (e.g. a timer that removes expired/over-quota users from the userpass block, or Hysteria2 http auth restored), which is a separate design decision, not a redundancy cut."
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "CLAIM text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"/auth/hysteria (server.js:2683-2708) and checkUserLimits (1149-1155) unreachable on any install where jq is present ... limits enforced nowhere\". COUNTER: none. ALSO: hist-C5 — note `jq '.[]|select(.id==\"hist-C5\")'` returns NOTHING; that id does not exist in the claims file (dangling cross-reference). Other claims mentioning userpass/auth: ctrl-C1, eff-C7.\n\nCODE (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- web/server.js:1149-1155 `checkUserLimits` — grep shows exactly one caller: server.js:2698 inside the `/auth/hysteria` POST handler (server.js:2691-2712). No other file references the endpoint besides the YAML templates: `grep -rn auth/hysteria server web b-ui-client.sh install.sh version.json` → core.sh:563,621 (templates), core.sh:659 (comment), update.sh:459 (repair_hysteria_config template), :1090 (comment), :1589 (v3.5 A-block template), server.js:2691.\n- server/core.sh:555-563 and :613-621 write `auth: type: http` into config.yaml / config-residential.yaml, then core.sh:662 (inside the same generate function, right after chmod at :655) calls `apply_hy2_userpass_auth` (core.sh:669-693): requires jq (:672, warns and returns if absent), reads users.json, awk-replaces the auth block with `type: userpass`. Fresh install therefore never starts hysteria with http auth when jq exists.\n- install.sh:97-104 `deps_map` includes `\"jq:jq:jq\"` and install.sh:118-128 apt/yum/dnf-installs any missing one → jq is a hard dependency of every install.sh-driven install, so the \"where jq is present\" condition is effectively always true.\n- server/update.sh:1089-1120 block \"v3.5.15 D6\" in apply_systemd_configs (function starts :756) does the identical http→userpass rewrite (guard `grep -qE '^  type: http$'`, :1107) and restarts the instance. apply_systemd_configs is called on every update run including the \"already latest\" path: update.sh:313, :2099, :2226 (cron `update.sh auto` every 6h).\n- The two remaining `type: http` writers are neutralised in the same run: repair_hysteria_config (update.sh:418, template :456-460) is invoked at update.sh:859 and the v3.5 A-block writes config-residential.yaml with http at :1586-1590 — the A block lives later in the file but D6 at :1090 runs on the next apply (≤6h) and repair at :859 precedes D6 at :1090 in the same function, so http auth cannot persist.\n- Additionally, independent of jq: web/server.js:276-286 saveUsers → updateHysteriaConfig (:316-326) and updateHysteriaResidentialConfig (:303-314) regex-replace the whole `auth:` block with `type: userpass` on every user save. saveUsers passes ALL users (filter only by protocol/uuid, :280-282) — no expiresAt/trafficLimit/monthlyLimit filtering. No other enforcement: `grep -n \"expired|exceeded|kick\"` in server.js hits only checkUserLimits :1151-1153 and the manual admin `/kick` proxy :2070; b-ui-cli.sh and b-ui-client.sh have zero hits for limits/expires. speedLimit (rx/tx in the auth response, server.js:2703-2705) is thus also inert — config additionally sets ignoreClientBandwidth: true (update.sh:453, core.sh templates).\n\nHISTORY: `git log -S apply_hy2_userpass_auth -- server/core.sh server/update.sh` → 5b6cde3 \"fix: v3.5.15 高并发硬化 — hy2 auth http→userpass\" (also introduced D6). `git log -S checkUserLimits`/`-S /auth/hysteria -- web/server.js` → 808f081 (original modular refactor) — never touched since. version.json changelog 3.5.0 already states \"b-ui-admin /auth/hysteria 端点变 dead code\"; 3.5.15 states \"/auth/hysteria 本就是 dead code ... 无 per-user 限速(speedLimit 仅展示、ignoreClientBandwidth=true)\"; 3.6.0 added the `$` anchor to the D6 guard. The authors themselves documented the endpoint as dead.\n\nLIVE (read-only): ran on both hosts `awk '/^auth:/{p=1;print;next} p&&/^  type:/{print;p=0}'` on config.yaml and config-residential.yaml + `journalctl -u b-ui-admin | grep -c '\\[AUTH\\]'`:\n- bwg-rick (v3.6.3, jq=/usr/bin/jq, reinstalled 2026-06-03): both files `auth: type: userpass`, 0 lines matching `^  type: http$`; AUTH log lines in b-ui-admin journal (since Sep 04): 0; hysteria journals mention http auth: 0.\n- bwg-tizi via -J bwg-rick (v3.6.3, jq present, running since 2026-05-09): both files `type: userpass`; 0 http; AUTH log lines (journal since Aug 28): 0; hysteria journals: 0.\nThe `/auth/hysteria` handler logs `[AUTH]` on every hit (server.js:2707,2710,2714); zero hits on either production host confirms it is never called. Only backups from May 2026 (pre-3.5.15) contain `type: http`.\n\nCould not refute: every path that writes `type: http` is followed by a userpass rewrite (install-time, every-6h update, every panel user save), jq is a mandatory install dependency, and live journals show zero invocations.",
    "correction": "Minor precision only: (1) the claim's condition \"where jq is present\" is weaker than reality — install.sh:99 makes jq a mandatory dependency, and even without jq the panel's saveUsers (server.js:276-286, 303-326) flips both configs to userpass on the first user save, so the endpoint is dead on every install, not just jq-equipped ones. (2) Cross-reference `also_raised_as: hist-C5` points to an id that does not exist in the claims file. (3) Line numbers 2683-2708 are slightly off: the handler is server.js:2690-2715.",
    "deletable_under_v4": true
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "Repo: /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5\n\n1. Endpoint + limiter exist exactly where claimed. web/server.js:2691 `if (p === \"/auth/hysteria\" && req.method === \"POST\")` calls `checkUserLimits(user)` at :2698; `checkUserLimits` defined :1149-1155. `grep -n checkUserLimits web/server.js` -> only :1149 (def) and :2698 (inside /auth/hysteria). No other caller.\n\n2. Who could ever POST /auth/hysteria at runtime? `grep -rn 'auth/hysteria' . --include='*.sh' --include='*.js' --include='*.json' --include='*.html'` -> only YAML templates: server/core.sh:563, :621 (config.yaml / config-residential.yaml), server/update.sh:459 (repair_hysteria_config), :1589 (A-block migration), b-ui-server.sh:543 (legacy v3.1 installer not in version.json files[]). No cron line, systemd unit, ExecStartPre, watchdog, resi-health, b-ui-cli, client script or curl anywhere hits it (`grep -rn 'curl[^|]*8080\\|127.0.0.1:8080' server/ b-ui-client.sh install.sh | grep -v auth/hysteria` -> empty). The only possible caller is a hysteria instance whose auth block is `type: http`.\n\n3. Every runtime path that writes `type: http` is immediately followed by a converter to userpass:\n - Fresh install: core.sh:562/620 write `type: http`, then core.sh:662 `apply_hy2_userpass_auth` (def :669-692) rewrites both files to `type: userpass` from users.json (grep -qE '^  type: http$' guard :682; awk :683-687). jq guard :672 only returns early if jq missing — but install.sh:99 lists `jq:jq:jq` as required dep and install.sh:132-137 `exit 1` if still missing after install, so on any install.sh-driven install jq is present.\n - Every update (cron `0 */6 * * * /opt/b-ui/update.sh auto`, core.sh:1223 / update.sh:2064; also `check_and_update` runs apply_systemd_configs even when already latest, update.sh:2099/2222 comment): D6 block update.sh:1089-1120 converts `^  type: http$` -> userpass in both configs.\n - repair_hysteria_config (update.sh:418, template :457-459 writes type: http) is called at :859 — BEFORE D6 at :1089 in the same apply_systemd_configs pass, so D6 converts it in the same run.\n - Independent of jq: web/server.js saveUsers (:276-291) -> updateHysteriaConfig (:315-326) and updateHysteriaResidentialConfig (:302-313) regex-replace the whole `auth:` block with `type: userpass` on every user create/edit/delete via panel. So the claim's \"where jq is present\" qualifier is actually narrower than reality.\n\n4. Limits enforced nowhere: userpass writers (server.js:307, :320) `users.reduce(...)` include every hy2 user regardless of limits.expiresAt/trafficLimit/monthlyLimit; updateXrayConfig (:329-345) same. `grep -n 'expiresAt\\|trafficLimit\\|monthlyLimit\\|speedLimit' web/server.js` -> only setters (1193-1196, 1217-1219, 2027-2056), display in Subscription-Userinfo header (1915, 1937) and the dead /auth/hysteria path (2703-2707). Usage accounting (:1437-1446) only increments counters; /kick (:2070) is a manual admin action. Both hysteria templates set `ignoreClientBandwidth: true` (core.sh:551, :610; update.sh:453,481,1580) and userpass auth cannot carry per-user rx/tx, so speedLimit is unenforceable. `grep -rn 'speedLimit' server/ b-ui-client.sh` -> only core.sh:523 (initial users.json). version.json changelog 3.5.0 and 3.5.15 already call /auth/hysteria \"dead code\" and state \"无 per-user 限速(speedLimit 仅展示)\".\n\n5. Live hosts (read-only):\n ssh bwg-rick: jq=/usr/bin/jq; /opt/b-ui/config.yaml auth `type: userpass`, /opt/b-ui/config-residential.yaml auth `type: userpass`; grep -c auth/hysteria in both = 0; `journalctl -u b-ui-admin | grep -c 'AUTH\\] User\\|Auth failed for user'` = 0; hysteria journals mentioning http auth = 0; version 3.6.3; ExecStart uses /opt/b-ui/config.yaml and config-residential.yaml.\n ssh -J bwg-rick bwg-tizi: identical results (jq present, both configs userpass, 0 auth/hysteria refs, 0 AUTH journal lines since Aug 28, 0 hysteria http-auth lines, v3.6.3). users.json on both: every user has only `limits.speedLimit=100000000`; no expiresAt/trafficLimit/monthlyLimit set anywhere, and usage.total on several users far exceeds nothing (no limit) — consistent with nothing being enforced.\n\nOnly caveat (does not overturn the claim, and is migration-only): update.sh A-block (:1527-1590, inside apply_systemd_configs which really closes at :1789 — the `}` at :1384/:1448 are heredoc lines) creates config-residential.yaml with `type: http` AFTER D6 (:1089) has already run in that pass, so a v3.4->v3.5 migrating machine has hysteria-residential on http auth (and thus /auth/hysteria reachable) until the next cron pass ≤6h later or the next panel saveUsers. Both production hosts are past that window (verified above). Under the v4 clean-reinstall decision this path disappears.",
    "deletable_under_v4": true,
    "correction": "Minor sharpening only: the reachability does not even depend on jq — web/server.js:302-326 rewrites both auth blocks to userpass on the first panel user save regardless of jq, and install.sh:99/132-137 makes jq mandatory (exit 1) so \"where jq is present\" == every install.sh-driven install. Under v4 clean reinstall, /auth/hysteria (server.js:2690-2711) and checkUserLimits (:1149-1155) can be deleted with zero behaviour change, provided the v4 core.sh template writes `type: userpass` directly (or keeps apply_hy2_userpass_auth) instead of the `type: http` + URL stanza at core.sh:561-564 / :619-622, so no config ever points at the removed endpoint."
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": true,
  "high_impact": true
 },
 {
  "id": "web-C3",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "CLAIM (web-C3, also eff-C6 — eff-C6 has no text in the claims file, jq select returned empty): server.js:51 trafficPortResidential (9998) has no reader; fetchStats/postStats use only trafficPort 9999. No counter-claim.\n\nSTATIC (repo worktree):\n- `grep -rn \"trafficPortResidential\\|9998\" --include=*.js --include=*.sh --include=*.json .` → the only JS hit is the declaration web/server.js:51 `trafficPortResidential: 9998,`. No reader in web/server.js, web/app.js, server/*.sh, b-ui-client.sh. Other hits are only the config *writers*: server/core.sh:532 `local resi_traffic_port=\"9998\"` (config-residential.yaml trafficStats block) and server/update.sh:1592 `listen: 127.0.0.1:9998` (v3.5 migration template), plus HANDOVER.md:145 which already notes the same defect (\"流量口 9998 声明了但没人读\").\n- `grep -n \"trafficPort\" web/server.js` → 50 (decl 9999), 51 (decl 9998), 1038 (fetchStats → `port: CONFIG.trafficPort`), 1057 (postStats → `port: CONFIG.trafficPort`). Both HTTP helpers hard-wire 9999.\n- Consumers of those helpers: getMergedStats() 1134-1138 = fetchStats(\"/traffic\") + Xray gRPC stats; getMergedOnline() 1140-1144 = fetchStats(\"/online\") + Xray online; the 10 s accounting loop 1423-1460 uses getMergedStats() only; route handlers 2068 (/stats), 2069 (/online), 2070 (`postStats(\"/kick\", …)`). app.js:217-218 kick() → POST /kick. So HY2 residential (:40000, stats on 9998) traffic is never added to users.json usage, never shown online, never kicked. (VLESS residential :10002 IS covered because both Xray inbounds share the single Xray stats API 10085 — the claim correctly limits itself to hysteria-residential.)\n- Only indirect effect on the residential HY2 instance: /auth/hysteria (server.js:2691-2712) is shared by both instances and calls checkUserLimits(), so an over-quota user is refused on *reconnect* — but quota itself never grows from residential traffic, and existing sessions aren't kicked.\n- git: `git log -S trafficPortResidential -- web/server.js` → single commit 4328926 2026-05-14 \"feat(v3.5): … server.js sync 双 hy2/xray inbound\" (introduced, never touched since). `git log -S 9998 -- server/core.sh server/update.sh web/` → 73b20f2, 4328926, f751b0f (all 2026-05-14 v3.5); no later commit added a reader.\n\nLIVE (read-only, `ssh -o BatchMode=yes -o ConnectTimeout=25 bwg-rick '…'` and `… -J bwg-rick bwg-tizi '…'`; usernames masked to key length via jq):\n- Both hosts: deployed /opt/b-ui/admin/server.js line 51 is the only occurrence of 9998; `ss -lntp` shows hysteria pid listening on 127.0.0.1:9998 (residential) and :9999 (direct); `ss -tnp | grep :999[89]` shows node ESTAB sockets ONLY to :9999 (bwg-rick node pid 644440→9999; bwg-tizi node pid 1411497→9999 x2); nothing ever connects to 9998.\n- bwg-rick: `curl 127.0.0.1:9998/traffic` → 2 users with ~118 MB/136 MB and ~43 MB/3 MB rx/tx (unmetered); `9998/online` → [1] (one user online on the residential instance) while `9999/online` → [] — the panel would show that user as offline.\n- bwg-tizi: `9998/traffic` → 3 users incl. ~204 MB and ~165 MB/160 MB unmetered; `9998/online` → [].\n- journalctl b-ui-admin last 10 min: 0 lines mentioning 9998.\n\nConclusion: the audit's evidence reproduces exactly; the counter side does not exist. The residential HY2 instance's trafficStats API is generated by core.sh/update.sh and is live on both production hosts, but the panel never queries it.",
  "deletable_under_v4": true,
  "correction": "Not a correction, a scoping note: the constant `CONFIG.trafficPortResidential` (web/server.js:51) is dead and can be deleted with zero behaviour change, but the right v4 action is the opposite — fetchStats/postStats (web/server.js:1034-1066) should fan out to both 9999 and 9998 (sum /traffic per user, union /online, POST /kick to both) so the \"HY2住宅\" product capability gets real accounting/online/kick. The update.sh:1570-1615 v3.5 migration template that writes `trafficStats 127.0.0.1:9998` is migration-only and deletable under v4; core.sh:532 must stay.",
  "high_impact": false
 },
 {
  "id": "web-C4",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5):\n- web/server.js:1076 `const users = loadUsers().filter(u => u.protocol === \"vless-reality\" || u.protocol === \"vless-ws-tls\");` inside fetchXrayUserStats(); only these users are polled via `xray api stats -name \"user>>>${email}>>>traffic>>>uplink/downlink\"` (lines 1083, 1092). \"fusion\" and null-protocol users are excluded.\n- web/server.js:1134-1138 getMergedStats() = `{ ...hy2Stats(/traffic), ...fetchXrayUserStats() }`; web/server.js:1423-1456 the 10s traffic loop only accumulates usage.total/monthly from that merged object → for fusion users only the Hysteria2 (`hysteria-server` trafficPort) counter ever reaches usage. Also getMergedOnline() (1140-1144) → fusion users' Reality-only sessions never show online.\n- web/server.js:279-285 saveUsers(): `vlessUsers = u.filter(x => x.uuid)` → every user (incl. fusion) IS put in both Reality inbounds with `email: u.username` (line ~336), and server/core.sh:778-781 enables `stats` + `StatsService` + `statsUserUplink/Downlink`, so Xray DOES count their traffic; the panel just never reads it.\n- web/index.html:196 `<option value=\"fusion\">` is the first (default) option; server/core.sh:523 seed user `\"protocol\":\"fusion\"`.\n- git: `git log -S'u.protocol === \"vless-reality\" || u.protocol === \"vless-ws-tls\"' -- web/server.js` → single commit cdb5605 (2025-12-11, \"fix: 修复 VLESS/WS 流量统计和在线设备显示\"); the filter was written before fusion became default (0a063a1/02222e0 later) and never updated.\nLive (read-only):\n- `ssh bwg-rick 'grep -n ... /opt/b-ui/admin/server.js'` → line 1076 identical, version 3.6.3. users.json protocols: [null, fusion, fusion, hysteria2, fusion] (no vless-reality/vless-ws-tls user). `xray api statsquery --server=127.0.0.1:10085` joined against users.json by name (names not printed) → 4 non-zero `user>>>*>>>traffic>>>*` counters, ALL belonging to protocol \"fusion\" (values 35.4MB/60.8MB/1.6MB/36.1MB) — Xray counts them, the panel never polls them.\n- `ssh -J bwg-rick bwg-tizi` → 3.6.3, filter line present (grep -c = 1), protocols [null,fusion×4,hysteria2], 6 non-zero xray user counters, all \"fusion\" (up to ~630MB each) — uncounted.\nExtra nuance (not contradicting the claim): merely widening the filter to include fusion would NOT fix it, because getMergedStats spreads xrayStats OVER hy2Stats keyed by username (1137) — a fusion user's Hy2 counter would be replaced by the Xray counter instead of summed; the merge must sum per-user (and the null-protocol legacy users are only Hy2-counted).",
  "deletable_under_v4": false,
  "correction": "",
  "high_impact": false
 },
 {
  "id": "web-C5",
  "lenses": [
   {
    "lens": "callgraph",
    "verdict": "CONFIRMED",
    "evidence": "STATIC CALL GRAPH (web/server.js in worktree; verified line numbers): entry points of server.js are module top-level only (no importer: `grep -rn 'import.*server.js\\|require.*server.js' web server` → none). Top-level timers: 1418 setTimeout(syncKernels), 1419 setInterval(syncKernels), 1423 `setInterval(async () => {...}, 10000)` = the traffic sync loop, 2721 `server.listen(...)`. The loop at 1423 runs unconditionally every 10s from process start. Its call graph: 1426 `getMergedStats()` → 1134-1138 `fetchStats(\"/traffic\")` (1034-1050: plain GET http://127.0.0.1:${CONFIG.trafficPort=9999}/traffic, no query string) spread with `fetchXrayUserStats()` (1069-1111: `xray api stats --server=127.0.0.1:${xrayApiPort} -name \"user>>>…>>>traffic>>>uplink|downlink\"`, no `-reset`). 1422 `let lastTraffic = {};` 1439 `const last = lastTraffic[uName] || { tx: 0, rx: 0 };` 1440-1441 `deltaTx = (stat.tx < last.tx) ? stat.tx : (stat.tx - last.tx)` (same for rx) 1443-1447 adds deltaTx+deltaRx to `u.usage.total` and `u.usage.monthly[m]`, 1454 `fs.writeFileSync(CONFIG.usersFile, ...)`. So on the first tick after a b-ui-admin (re)start, last={0,0} and the full cumulative counter of both Hysteria2 and Xray is added to users.json. Nothing ever clears the kernel counters: `grep -rn 'clear=1\\|/traffic\\|-reset' --include='*.sh' --include='*.js' --include='*.json' .` (excluding docs/node_modules) hits only web/server.js:1135 and version.json changelog text; `grep -n 'clear\\|reset' web/server.js` hits only 2202/2204 (residential domains reset, unrelated). b-ui-admin.service unit (core.sh ~1742-1750) has only After=network.target / Restart=always — no PartOf/BindsTo/ExecStartPre that would restart hysteria/xray (and thus zero their counters) together with the panel; update.sh restarts b-ui-admin alone when web/* changes (CLAUDE.md convention, confirmed by the journal below).\n\nLIVE (bwg-rick, read-only): `systemctl show b-ui-admin hysteria-server xray -p ActiveEnterTimestamp` → hysteria-server & xray active since 2026-09-11 02:22:41 UTC, b-ui-admin since 05:07:44 UTC; journalctl -u b-ui-admin shows admin restarts at 03:04:48 (v3.6.1), 03:46:30 (v3.6.2), 05:07:44 (v3.6.3) — three panel restarts while hysteria/xray kept running. `grep -n \"clear=1\\|/traffic\\|lastTraffic = \" /opt/b-ui/admin/server.js` → only lines 1135 and 1422 (identical to repo). `curl -s 127.0.0.1:9999/traffic` twice, 12 s apart → identical non-zero counters (e.g. tx 73451371/rx 714199939, tx 279780696/rx 190271575, masked names): the endpoint is cumulative since hysteria start and is not being cleared by anyone, so each of the three restarts above re-added those cumulative bytes to users.json usage.total/monthly. users.json usage fields are the only accounting store (jq over /opt/b-ui/users.json, names masked) — no compensating logic exists. Counter-claim: none. Side note (not the claim): CONFIG.trafficPortResidential (9998, server.js:51) is never queried, so hysteria-residential traffic is not accounted at all.",
    "deletable_under_v4": false,
    "correction": ""
   },
   {
    "lens": "history-live",
    "verdict": "CONFIRMED",
    "evidence": "CODE (worktree, web/server.js @ 0b5b559 / version.json 3.6.3):\n- server.js:1422 `let lastTraffic = {};` (module-level, process memory only, never persisted).\n- server.js:1439-1441: `const last = lastTraffic[uName] || { tx: 0, rx: 0 }; const deltaTx = (stat.tx < last.tx) ? stat.tx : (stat.tx - last.tx);` → on the first 10 s tick after a b-ui-admin start, last={0,0} so delta = the kernel's full cumulative counter; server.js:1444-1446 add it to u.usage.total / usage.monthly and write users.json.\n- server.js:1134-1136 getMergedStats() = fetchStats(\"/traffic\") + fetchXrayUserStats(). fetchStats (1034-1050) does a plain GET to 127.0.0.1:<trafficPort>/traffic with no `?clear=1`; fetchXrayUserStats (1071-1110) runs `xray api stats ... -name \"user>>>X>>>traffic>>>uplink\"` with no `-reset`. Both sources are therefore cumulative-since-kernel-start. `grep -n \"clear=1\\|-reset\" web/server.js` → no hits (only unrelated `reset:true` at 2202 for residential domains). The \"counter went backwards\" guard (stat<last → delta=stat) only handles a kernel restart, not a panel restart.\n- HISTORY: `git log -S'lastTraffic' --oneline -- web/server.js` → single commit 808f081 (2025-12-11 \"feat: 模块化重构，支持自动更新\"); the block has been untouched since v3.x modularisation. core.sh:566 / :624 configure trafficStats listeners (9999 direct, 9998 residential) with no clear semantics; nothing in server/*.sh or update.sh compensates.\n\nLIVE (read-only, 2026-09-11):\ncmd: ssh bwg-rick 'systemctl show -p ActiveEnterTimestamp/NRestarts b-ui-admin hysteria-server xray; journalctl -u b-ui-admin --since <hysteria start> | grep -c Started; curl -s 127.0.0.1:9999/traffic | jq (values only); grep -c \"clear=1\\|-reset\" /opt/b-ui/admin/server.js'\n- bwg-rick: hysteria-server + xray ActiveEnter 02:22:41 UTC, b-ui-admin ActiveEnter 05:07:44 UTC (restarted independently, 3 admin starts logged since hysteria start); /traffic returns non-zero cumulative counters (e.g. one user tx=73,451,371 rx=714,199,939); deployed /opt/b-ui/admin/server.js has `let lastTraffic = {}` at line 1422 and 0 matches for clear=1/-reset. So at 05:07:44 the whole 02:22→05:07 cumulative was added a second time to users.json.\n- bwg-tizi (via -J bwg-rick): identical pattern — hysteria-server/xray ActiveEnter 02:26:36 UTC, b-ui-admin 04:34:57 UTC, 3 admin starts since hysteria start, non-zero cumulative /traffic (e.g. tx=149,892,984 rx=726,071,389) and xray stats (e.g. downlink 601,506,021), deployed server.js identical (line 1422, no clear/reset).\nPrecondition of the claim (panel restarted while kernels kept running with cumulative counters) is observed on both production hosts today; the code path then double-counts by construction. The exact re-added byte amount cannot be reconstructed from logs (no per-tick logging), but every element of the claim reproduces.\n\nSide notes (not part of claim): the same defect applies to the Xray path (no `-reset`), and the residential Hysteria2 instance's trafficStats on 127.0.0.1:9998 (core.sh:624-625) is never polled by server.js at all (only CONFIG.trafficPort), so HY2-住宅 traffic is not accounted.",
    "deletable_under_v4": false,
    "correction": ""
   },
   {
    "lens": "runtime",
    "verdict": "CONFIRMED",
    "evidence": "CODE (worktree /Users/woo/Desktop/b-ui/.claude/worktrees/bui-c-tun-mode-issue-df82e5/web/server.js):\n- :1422 `let lastTraffic = {};` (process-local, never persisted)\n- :1439-1441 `const last = lastTraffic[uName] || { tx: 0, rx: 0 }; const deltaTx = (stat.tx < last.tx) ? stat.tx : (stat.tx - last.tx);` -> on the first 10 s tick after process start, delta = full counter value.\n- :1135 `const hy2Stats = await fetchStats(\"/traffic\");` — only GET /traffic, no `?clear=1` (`grep -n 'clear=1' web/server.js` -> no hits; `git log -S'clear=1' -- web/server.js` -> empty; the loop has existed unchanged since 808f081). :1032-1046 fetchStats does a plain GET. Same applies to the Xray half (:1083/:1092 `xray api stats` without `-reset`, merged at :1137), so VLESS counters double-count too.\n- Runtime lens — restart paths that trigger the re-add: systemd unit `Restart=always` (core.sh:1749, live unit identical, plus drop-in 10-memory.conf MemoryMax=200M -> OOM-kill restarts); update.sh:247 `systemctl stop b-ui-admin` / :333 `start`, :610, :1084, :1134, :2208 (`web_changed` restart) — run by cron `0 */6 * * * /opt/b-ui/update.sh auto` (confirmed in live crontab); b-ui-cli.sh:458, :898, :1077, :1351. No ExecStartPre/timer clears counters; hysteria trafficStats has no `clear` in core.sh:566/624.\n\nLOCAL REPRODUCTION (ran): stub `http://127.0.0.1:9999/traffic` returning constant `{alice:{tx:1000,rx:2000}}`; started `BASE_DIR=<scratch> ADMIN_DIR=$PWD ADMIN_PORT=18080 ... node server.js` three times, 25 s each, `xray` stubbed on PATH. users.json usage after each run: `{\"total\":3000}` -> `{\"total\":6000}` -> `{\"total\":9000}` while the upstream counter never changed. Every process start re-added the full cumulative value.\n\nLIVE (bwg-rick, read-only): `curl -s 127.0.0.1:9999/traffic` at t0 and t0+12 s returned identical non-zero counters (e.g. tx 73451371 / rx 714199939, tx 279780696 / rx 190271575) -> endpoint is cumulative since hysteria start, not clear-on-read. `systemctl show`: hysteria-server ActiveEnter 02:22:41 UTC; b-ui-admin journal shows Stopped/Started at 03:04:48, 03:46:30, 05:07:44 the same day — three admin restarts after the hysteria counters started accumulating, each of which re-added the then-current cumulative totals to /opt/b-ui/users.json. Deployed /opt/b-ui/admin/server.js:1135 also has only `fetchStats(\"/traffic\")`, no clear=1. Note the over-count is bounded by hysteria/xray uptime since their last restart (a hysteria restart resets counters and the `stat.tx < last.tx` branch handles that correctly), not all-time totals — but each admin restart still inflates usage by whatever the kernels have accumulated since they last started.",
    "deletable_under_v4": false,
    "correction": "Claim is accurate as stated. Minor precision: the re-added amount is the kernel's cumulative counter since the last hysteria/xray (re)start, not the user's lifetime total; and the same defect also affects the Xray `api stats` path (no `-reset`), which the claim does not mention. This is a live bug in a kept capability (traffic accounting), not migration-only code — it must be fixed (e.g. `/traffic?clear=1` + `xray api stats -reset`, or persist lastTraffic), not deleted."
   }
  ],
  "verdict": "CONFIRMED",
  "deletable_under_v4": false,
  "high_impact": true
 },
 {
  "id": "web-C6",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "All four sub-claims reproduce.\n\n1) web/app.js:190 — `let url = \"/api/manage?key=\" + encodeURIComponent(cfg.adminPass || localStorage.getItem(\"ap\") || \"\") + \"&action=create&user=...\"` then `fetch(url)` (GET). Command: `sed -n '180,200p' web/app.js`.\n\n2) web/app.js:61 — inside `login()`: `localStorage.setItem(\"ap\", pw);` where `pw = $(\"#lp\").value` (the plaintext admin password). Command: `sed -n '50,70p' web/app.js`. `grep -n \"'ap'\\|\\\"ap\\\"\" web/app.js` → only lines 61 (set) and 190 (get); nothing else consumes it.\n\n3) server.js never returns adminPass: `grep -n adminPass web/server.js` → empty (only `adminPassword` at 40/1159/1699/2672, all internal CONFIG). `cfg` is populated solely from `api(\"/config\")` (app.js:105, :531) → server.js:2071 `if (r === \"config\") return sendJSON(res, getConfig());` and getConfig()'s return (server.js:467-476) is exactly `{domain, port, xrayPort, pubKey, shortId, sni, portHopping, obfs}`. So `cfg.adminPass` is always undefined and the localStorage fallback is the value actually sent. Server side: server.js:1705 `if (r === \"manage\") return handleManage(u.searchParams, res);` sits in the unauthenticated section (before any JWT check), and handleManage (server.js:1157-1159) does `const key = params.get(\"key\")...; if (key !== CONFIG.adminPassword) return sendJSON(res, {error:\"Invalid key\"}, 403);` — i.e. the query param IS the admin password, compared verbatim.\n\n4) Caddy logs the URI: server/core.sh:864-876 writes /etc/caddy/Caddyfile with `reverse_proxy 127.0.0.1:${ADMIN_PORT}` and `log { output file /var/log/caddy/b-ui-access.log { roll_size 10mb roll_keep 5 } }` (line numbers are 871-875 in this checkout, off-by-two from the claim's 869-874). Caddy's default JSON access log includes `request.uri` with the query string.\n\nLive confirmation (read-only, credentials redacted via sed before output):\n- bwg-rick: `grep -n log /etc/caddy/Caddyfile` → lines 7-8 identical to core.sh template. `zgrep -h \"api/manage\" /var/log/caddy/*.gz | jq -r '[.request.uri,.status,.request.method]|@tsv' | sed 's/key=[^&]*/key=REDACTED/…' | sort | uniq -c` →\n   50  /api/manage  403 GET   (external scanners probing the key-gated, JWT-free endpoint)\n    2  /api/manage?key=REDACTED&action=create&user=REDACTED&days=0&...&protocol=fusion&residential=true&sni=www.bing.com  200 GET\n    1  ...protocol=hysteria2&residential=false...  200 GET\n    1  ...protocol=hysteria2&residential=true...   200 GET\n  i.e. the admin password is physically present in /var/log/caddy/b-ui-access-2026-08-17T17-11-01.236-size.log.gz (4 successful user-creates). The log records query strings generally: 3331 logged URIs contain \"?\".\n- bwg-tizi (via -J bwg-rick): same Caddyfile log block (lines 7-8); 0 /api/manage hits in current + 5 rotated logs (no user created through the panel in the retained window), so not contradicting — the code path is identical.\n\nHistory: `git log -S'/api/manage?key=' -- web/app.js` → 808f081 2025-12-11 (模块化重构), untouched since; `git log -S'b-ui-access.log' -- server/core.sh` → 314c99b 2026-03-20 (Caddy replaces nginx), 72bf5bf 2026-04-02. Caller survey: `grep -rn \"api/manage\" web/ server/ install.sh b-ui-client.sh` → only app.js:190; no shell/cron/client consumer of this endpoint.\n\nNo counter-claim exists. Minor line-number drift only (core.sh 871-875 vs 869-874).",
  "deletable_under_v4": false,
  "correction": "Not a refutation, but precision: only the `cfg.adminPass ||` half of app.js:190 is dead code (getConfig never emits it) and could be deleted with zero behaviour change. The rest — localStorage \"ap\" (app.js:61), the `?key=` query parameter, and handleManage's key check (server.js:1157-1159, dispatched at :1705 outside the JWT gate) — is the ONLY auth path for panel user creation; deleting it without moving create-user to a JWT-authenticated POST would break the \"create user\" capability. Under v4 this is a rewrite target (POST + Bearer token; drop `ap` from localStorage), not a delete, and the Caddy log block at core.sh:871-875 is unrelated to migrations and stays.",
  "high_impact": false
 },
 {
  "id": "web-C7",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): \"handleManage actions delete/update/list (web/server.js:1198-1225) and POST /api/users (web/server.js:1958-1975) have no callers in web/app.js, web/index.html, b-ui-client.sh or server/*.sh.\" No counter-claim, no also_raised_as.\n\nStatic (repo worktree, v3.6.3):\n- web/server.js:1157 `function handleManage(params, res)`; the only dispatch is web/server.js:1705 `if (r === \"manage\") return handleManage(u.searchParams, res);` (unauthenticated, keyed by `key=` query param). Branches: create 1163-1201, delete 1203-1210, update 1212-1224, list 1226.\n- web/server.js:1958-1975 `if (r === \"users\") { GET -> loadUsers(); POST -> push {username,password,createdAt} }` (behind Bearer JWT). Note the POST path creates a user WITHOUT uuid/protocol/limits/usage — a legacy pre-fusion shape.\n- `grep -rn \"api/manage\\|action=\" web/app.js web/index.html b-ui-client.sh server/*.sh install.sh b-ui-server.sh` -> only web/app.js:190-191 builds `/api/manage?key=...&action=create&user=...`. No `action=delete|update|list` anywhere in the repo (only unrelated `local action=` shell vars in b-ui-client.sh:2773, server/b-ui-cli.sh:923,1000).\n- `grep -n '/users' web/app.js` -> app.js:114 `api(\"/users\")` (GET), app.js:212 `api(\"/users/\"+u, {method:\"DELETE\"})`, app.js:271-272 `api(\"/users/\"+origUsername, {method:\"PUT\", ...})`. No POST to /users. web/index.html has no fetch/api calls at all (`grep -n \"fetch(\\|api/\" web/index.html` -> empty). No curl to /api/manage or /api/users in server/*.sh, b-ui-client.sh, install.sh (only /api/kernel-versions at b-ui-client.sh:4958,5650).\n- README.md:225 still documents `/api/manage | 用户增删改查` — the only place that advertises delete/update/list, and it is documentation, not a caller.\n- git: `git log -L1198,1225:web/server.js` last touched 2e10db2 (2025-12-13, v2.9.1) / 808f081 (2025-12-11 modular refactor); `git log -L1958,1975` last touched 80bc56e (2026-05-10 v3.4.36 QA validation) / 808f081. `git log -S'action === \"delete\"' -- web/server.js` -> only 808f081 (introduced, never changed since).\n\nRuntime (live hosts, read-only, redacted):\n- ssh bwg-rick / ssh -J bwg-rick bwg-tizi: both VER=3.6.3; installed /opt/b-ui/admin/app.js has exactly one `/api/manage` reference and `grep -o \"action=[a-z]*\" | uniq -c` -> `1 action=create` only; users endpoints used with DELETE (line 212) and PUT.\n- No cron/cron.d entries mention manage or api/users; journalctl -u b-ui-admin (server.js has no request access log; only `new URL(req.url...)` at server.js:1464) shows nothing.\n- Caddy access logs (/var/log/caddy/b-ui-access.log + all rotated .gz since 2026-07): grep on method+uri for /api/(manage|users), key/user/pass redacted:\n  bwg-rick: 1444 GET /api/users, 50 GET /api/manage (no query -> \"Invalid key\" 403, scanner-style), 17 GET /api/users/U, 4 GET /api/manage?...action=create..., 2 DELETE /api/users/U. Zero action=delete/update/list, zero POST /api/users.\n  bwg-tizi: 1535 GET /api/users, 5 GET /api/users/U, 1 PUT /api/users/U. Zero /api/manage of any kind, zero POST /api/users.\nUpgrade paths: update.sh/core.sh never call these endpoints; migration blocks are shell-only and touch config files, not the HTTP API.",
  "deletable_under_v4": true,
  "correction": "Nothing to correct. Scope note: only the delete/update/list branches of handleManage (server.js:1203-1226) and the POST branch of `r === \"users\"` (server.js:1960-1975) are dead; `action=create` (server.js:1163-1201) is the live user-creation path used by web/app.js:190 and must stay, as must GET /api/users, DELETE /api/users/:u and PUT /api/users/:u. If deleted, also drop/reword README.md:225 (`/api/manage | 用户增删改查`) which is the only advertisement of the dead actions to third parties; live access logs since July show no third-party use.",
  "high_impact": false
 },
 {
  "id": "web-C8",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Static (worktree web/server.js, HEAD 2929947):\n- 1463-1464: single http.createServer handler; no auth middleware anywhere before the routes. `awk 'NR>=1380 && NR<=1770 && /verifyToken|authorization|Unauthorized/'` on server.js → zero hits, i.e. nothing between handler start and install-command checks a token.\n- 1769-1780: `// 安装命令 API (无需认证)` → `if (r === \"install-command\") { const key = getOrCreateInstallKey(); ... return sendJSON(res, { command, key, server: host, note: ... }) }` — returns the raw key to any caller.\n- 1942-1943 (claim says 1953; off by 11): `const auth = verifyToken((req.headers.authorization || \"\").replace(\"Bearer \", \"\")); if (!auth) return sendJSON(res, { error: \"Unauthorized\" }, 401);` — the only Bearer gate, and it is AFTER install-command.\n- 1616-1691 (claim says 1636-1691): `// --- 客户端安装包下载 (无需认证) ---`; `/packages/<file>` route at 1633; for `b-ui-client.sh` it prefers `${BASE_DIR}/b-ui-client.sh` (1643-1650) and serves it with only injectClientVersion() applied (1670-1672), no key check.\n- 1514-1520: `/install-client` is the ONLY consumer of verifyInstallKey (126-129); on a valid key it returns the same b-ui-client.sh wrapped in a bootstrap header (1590-1606) that sets BUI_SERVER/BUI_INSTALL_KEY and pulls kernels from `/packages/...` (167-178 of generateBootstrapScript, also keyless).\n- `grep -rn BUI_INSTALL_KEY b-ui-client.sh` → no hits: the client script never uses the key downstream, so the key gates nothing beyond the wrapper text.\n- web/app.js:78 `fetch(\"/api/install-command\")` — no Authorization header, so the panel itself relies on the endpoint being unauthenticated.\n- Only other key references: server/b-ui-cli.sh:1044/1068 (export-keys tarball includes install-key.txt), server/update.sh:1769-1771 (chmod 600 migration).\n- Caddy (server/core.sh:866-868): `${DOMAIN} { reverse_proxy 127.0.0.1:${ADMIN_PORT} }` — whole panel is publicly reverse-proxied, no path-level auth.\n\nGit: `git log -S verifyInstallKey -- web/server.js` → 5304348 (2025-12-11 \"feat: 从服务端一键安装客户端\"). `git log -S'// 安装命令 API (无需认证)'` → e668605 2025-12-11 \"fix: 修复安装命令API认证问题\" whose body says \"将 /api/install-command 移到 JWT 验证之前 (无需认证)\" — the unauthenticated placement was deliberate, not drift. Untouched since.\n\nLive (bwg-rick, read-only, key never printed):\n`curl -s -w http=%{http_code} https://$D/api/install-command` → http=200, jq `{has_command:true, has_key:true, keylen:32, server:\"<bwg-rick-domain>\"}`; comparing `.key` to /opt/b-ui/install-key.txt → MATCH.\n`curl https://$D/packages/b-ui-client.sh` → http=200 bytes=230427 (full client script, no auth).\n`curl https://$D/install-client` (no key) → http=403 — the gate exists but is trivially bypassed by the two public routes above.\n`ss -ltnp` → node on 127.0.0.1:8080 (only reachable through Caddy, which exposes everything).\n\nConclusion: an anonymous internet client can (a) read the install key from /api/install-command and then hit /install-client, or (b) skip the key entirely and fetch /packages/b-ui-client.sh. verifyInstallKey provides no access control. Claim fully reproduced; only the cited line numbers drift slightly (1953→1942, 1636→1616/1633).",
  "deletable_under_v4": false,
  "correction": "Line numbers only: the Bearer check is at web/server.js:1942-1943 (not 1953); the /packages block starts at 1616 (comment) / 1633 (route), not 1636. Substance of the claim is exact. On deletability: the key machinery (getOrCreateInstallKey/verifyInstallKey, install-key.txt, `key` in the install-command JSON, b-ui-cli export-keys including install-key.txt, update.sh:1769 chmod) is security-null and could be removed in v4, but removal is NOT zero-behaviour-change on a fresh install — /install-client without ?key would go 403→200, the panel's one-click command string would change, and export-keys tarball contents would change. It is a product-visible simplification, not migration-only dead code.",
  "high_impact": false
 },
 {
  "id": "web-C9",
  "lenses": [],
  "verdict": "CONFIRMED",
  "evidence": "Claim text (jq on docs/superpowers/audits/2026-09-11-claims.json): no counter-claim, no also_raised_as.\n\n1. Reachability of generateBootstrapScript — web/server.js:1530-1571 (grep -n 'generateBootstrapScript\\|b-ui-client.sh' web/server.js): the only call is line 1565, inside the `/install-client` handler (line 1514). Branch order: `if (fs.existsSync(localClientScript))` [BASE_DIR/b-ui-client.sh, 1534] → `else if (fs.existsSync(packagesClientScript))` [BASE_DIR/packages/b-ui-client.sh, 1537] → else https.get(\"https://raw.githubusercontent.com/Buxiulei/b-ui/main/b-ui-client.sh\") (1550); only when that yields non-200 or error (`resolve(null)`) does line 1565 call generateBootstrapScript. No other caller anywhere (grep -rn generateBootstrapScript --include='*.js' --include='*.sh' --include='*.html' . → only definition 132 and call 1565). So the claim's three-condition gate is exactly right.\n\n2. \"install.sh:246 always creates the first file\" — install.sh:246 lists `\"b-ui-client.sh:${BASE_DIR}/b-ui-client.sh\"` in download_all_files(); loop 256-265 counts failures and `return 1` at 267-270 if any file failed; download_all_files is called bare at install.sh:787 (upgrade) and :798 (fresh) with `set -e` re-enabled at :782, so a missing b-ui-client.sh aborts the whole install before core.sh ever runs. Hence on any install that completed, /opt/b-ui/b-ui-client.sh exists. version.json's files list also includes \"b-ui-client.sh\". update.sh:264 re-downloads it on every version update and :290 copies it to packages/; b-ui-cli.sh:559-561 refreshes it too.\n\n3. Minor overstatement in the claim: server/core.sh:1630-1642 is inside download_client_packages() (core.sh:1564), which is invoked only from install.sh:612-613 `if [[ \"$PREDOWNLOAD_PACKAGES\" =~ ^[yY]$ ]]` — i.e. NOT always, and its curl failure only print_error's (core.sh:1645) without exiting. But this does not weaken the claim, because install.sh:246 alone already guarantees the file. The core.sh comment at 1630-1631 itself states \"install.sh 已无条件下载到 BASE_DIR\".\n\n4. Live state: `ssh bwg-rick 'ls -la /opt/b-ui/b-ui-client.sh /opt/b-ui/packages/b-ui-client.sh; journalctl -u b-ui-admin | grep -c 本地脚本不存在'` → both files present (230428 bytes, Sep 11 05:07), 0 log hits for the fallback branch; same via `-J bwg-rick bwg-tizi` → both present (Sep 11 04:34), 0 hits. Deployed admin/server.js still contains generateBootstrapScript (grep -c → 2) but the branch has never fired.\n\n5. History: `git log -S generateBootstrapScript -- web/server.js` → introduced 5304348 (2025-12-11 \"feat: 从服务端一键安装客户端\"), fallback ordering changed in 52c9f4b (\"客户端安装 API 优先使用本地脚本\"); untouched since. Note the bootstrap script body (132-192) only downloads hysteria/sing-box/xray from `https://<domain>/packages/...` (which exist only if PREDOWNLOAD_PACKAGES=y) and never installs bui-c, so even when reached it does not produce a working client.",
  "deletable_under_v4": true,
  "correction": "Only nuance: core.sh:1630-1642 does not \"always\" run (gated by PREDOWNLOAD_PACKAGES at install.sh:612) and does not abort on failure; the guarantee comes from install.sh:246 + set -e (install.sh:782/787/798) alone. On a fresh v4 install the local file is guaranteed, so the GitHub fetch branch (server.js:1541-1570) and generateBootstrapScript (132-192) are unreachable and can be deleted; keep at most a plain 500 \"client script missing\" response for the existsSync-false case.",
  "high_impact": false
 }
]